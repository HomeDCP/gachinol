# WORKLOG

## 2026-08-29 — 기자 웹 실기 업로드 검증 (HANDOFF §0)

- **작업명**: 제온 실배포 기자 웹에서 촬영본 실기 업로드 한 바퀴(로그인→업로드→프리뷰) 검증.
  역할 분담: **사용자가 맥 브라우저로 직접 밟고**, 세션은 제온 무접근(LAN·Tailscale 타임아웃) 상태에서
  안내·진단(§0 실패 판별표: upload-url 400=sizeBytes 폴백 / PUT 부재=어댑터 스왑 / PUT 403=presign·CORS).
- **선행 관문 2건**: ① 접속 — nginx default_server 444 차단 대비 hosts 매핑(reporter.gachinol.local)·
  포트·제온 `DOMAIN` 실값 확인 ② 번들 API URL — Dockerfile.web 기본값 `http://localhost:4000` 대비
  Actions vars `WEB_EXPO_PUBLIC_API_URL` 실값·배포 번들 grep 확인. ③ 관문 결과로 nginx.conf 주석 ↔
  Dockerfile 기본값 불일치 판정(주석 stale이면 정정 PR).
- **순서 결정**: PR #77(HANDOFF §0 신설, docs-only)은 검증과 독립 → **검증 먼저, 결과 반영 PR은
  #77 머지 후**(같은 파일 충돌 방지). 머지는 사용자 실행(머지=제온 자동 배포 관례).
- **비고**: 실기 검증은 태스크 ID 없는 작업(코드 태스크 모수 무관). TDD 게이트는 코드 수정이
  발생하는 시점에 적용.
- **결과 (2026-08-30): 완주 — `published` + 공개 피드 노출 실증.** 전이 사슬 전 구간
  (draft→uploading→uploaded→processing→analyzing→preview_generating→기자승인→센터승인→publishing→
  published) + 자산 5종 ready(edited_master·preview 포함) + durationSec 95 기록. §0 체크포인트 ①②③
  전부 통과(진행률 실상승 = T-W2-02 XHR 어댑터 실동작 실증). **그 과정에서 배포·설정 결함 6건을
  실측으로 적발·해소**(전부 실기가 아니면 안 드러나는 층위 — 2026-08-15 실증의 교훈 재현):
  1. **제온 `inet filter forward`(policy drop)가 LAN→도커 발행 포트 전멸시킴** — nftables는 모든
     테이블의 forward 훅을 평가해 Docker `ip filter` accept가 무효였다(tcpdump+카운터로 드랍 지점 확정).
     `br-*` accept 2줄 런타임+`/etc/nftables.conf` 영속(백업·`nft -c`·제온 decisions.md 기록).
     파일의 "docker NAT와 무관" 주석은 오판이라 정정.
  2. **Actions vars `WEB_EXPO_PUBLIC_API_URL` 미설정** → 번들 API가 `http://localhost:4000`
     (3앱 grep 실측) = 로그인부터 불가·터널 구독자 웹도 API 불통. `/`(상대 경로) 설정 + 재배포
     (런 33221872913 deploy success)로 nginx 설계 전제와 정합 — **관문 ③의 nginx 주석은 이로써 참이
     되어 정정 PR 불요 판정**.
  3. **제온 `DOMAIN=localhost`** → vhost가 `reporter.localhost`인데 브라우저는 `*.localhost`를
     루프백 강제 해석(RFC 6761)이라 LAN 접근 구조적 불가 → `gachinol.local` 교정.
  4. **`WEB_ORIGINS` 미설정** → 웹 쿠키 로그인 CSRF 가드가 전면 차단(fail-closed 설계 기본값) →
     reporter·center 오리진 화이트리스트 배선. 양방향 검증(허용 401/비허용 403 유지).
  5. **`S3_PUBLIC_ENDPOINT=http://localhost:9000`** → presigned PUT이 사용자 자기 맥으로 나가
     즉사(Safari·Chrome 동일, 제온 9000 패킷 0으로 확정). LAN IP 교정. ⚠️ 중간에 세운 "Safari 로컬
     네트워크 정책" 가설은 Chrome A/B가 기각 — 원인은 DOMAIN과 같은 시기의 env 잔재였다.
  6. **media-worker 이미지 스테일(8/09, auto_edit 이전)** → `auto_edit` 잡 3회 거부로 `preview_failed`.
     GHCR 패키지 3종 공개 전환(사용자·리포가 PUBLIC이라 노출 증분 0) + pull·재생성(8/28 빌드)으로 해소.
  - **잠복 코드 결함 1건 적발(후속 수리 대상)**: `upload-complete` HEAD-미검출 복구가 비원자적
    (`assets.markFailed`→`workflow.failUpload` 2쓰기) — 사이에 프로세스가 죽으면 자산만 failed로 남고
    콘텐츠는 `uploading` 교착, `findOriginal`(failed 제외)로 **완료 경로까지 영구 차단**(실제 발생 →
    가드된 UPDATE 1행으로 수동 복원, 사용자 실행). 트랜잭션 묶음 수리 필요.
  - 후속 기록 대상: 스턱 콘텐츠 `01a04de0`(uploading) 정리 · api 이미지 스테일(로컬 8/21, 이제 pull
    가능) 풀스택 리프레시 · 주민 업로드가 터널 경유일 때 presigned LAN IP 도달 불가(구조) ·
    상대경로 전환 후 구독자·관제 socket.io `io('')` 원점 해석 확인 · vars
    `WEB_EXPO_PUBLIC_SUBSCRIBER_WEB_URL` placeholder 실값 교체.
- **상태**: 실기 검증 **완료**(실행 근거: 전이 로그·자산·피드 API 실측 + 사용자 브라우저 실기).
  HANDOFF §0 종결 반영은 PR #77 머지 후 별도 PR.

## 2026-08-28 — T-W2-02 기자 웹 업로더 어댑터 (오검출 정정 후속)

- **작업명**: 기자 웹 업로드를 실제로 가능하게 하는 XHR 어댑터 — 02 §E-7 "웹용 업로더 어댑터
  신규 작성(fetch/XHR `upload.onprogress`/`abort`) → 기존 `useUploadService()` DI 지점에 주입".
- **1순위 재판정 근거(사용자 확정 2026-08-28)**: HANDOFF 현행화 중 언급-오검출 전수 스윕으로
  T-W2-05·T-W2-02·T-W1-07a가 실은 미착수로 판명(진행률 26/46 정정, PR #74). 그중 T-W2-02는
  기존 `http-upload-service`가 expo-file-system(네이티브 전용) 단일 경로라 **기자 웹에서 촬영
  업로드 자체가 기능 부재** — W2 목표 한복판의 공백이 "완료"로 가려져 있던 것. 3파일 S·신규
  의존성 0·구독자 선례 재사용으로 후보 8건 중 최저 리스크·최고 실사용 가치. AskUserQuestion 확정.
- **범위**: `apps/reporter/src/upload/` — 신규 2(`xhr-upload-service.ts` 로직+DI ·
  `http-upload-service.web.ts` 웹 해석 진입) + 수정 1(`http-upload-service.ts` —
  `notifyUploadFailed`를 xhr 쪽으로 이동) + 신규 테스트 1스위트. **E2 문언과의 차이 2건(근거)**:
  ⓐ 수정 파일이 `use-upload-service.ts`가 아니라 `http-upload-service.ts`다 — DI 주입을 Metro
  플랫폼 해석(`.web.ts`, 구독자 `uploader.web.ts` 선례)으로 성립시켜 훅·화면 코드 무변경이 되고
  expo-file-system이 웹 번들에서 빠진다(총 3파일은 동일). ⓑ 이동은 웹 해석에서
  `'./http-upload-service'`가 `.web.ts` 자신이라 생기는 순환 회피 + 복구 의미론 사본 금지.
- **설계 고정**: ⓪ 본문(`resolveBody`)을 ①보다 **먼저** 읽는다 — 읽기 실패 시 서버 무접촉(draft
  유지. 네이티브는 전송 태스크가 읽기를 지연해 이 순서가 불가). `sizeBytes`는 실측 Blob 크기
  우선 — 웹 픽커가 fileSize를 안 줘 호출부가 0을 보내면(`asset.fileSize ?? 0`) 서버
  zod(positive)에서 ①부터 400이 나던 것을 어댑터가 흡수. 에러 메시지에 상태코드만(presigned
  서명 URL 비유출 — 구독자 dom-uploader 규칙 답습). 취소는 `UploadAbortedError`로 기존 화면
  분기(phase='aborted') 재사용.
- **검증 실측**: TDD 레드(모듈 부재) 확인 후 구현 → 신규 8 테스트 그린 · reporter 전체
  **299/299**(기준선 291+8) · typecheck 0 · lint 0 · web export 성공 + **번들 스왑 실증**(웹 번들
  `onprogress` 1파일·`createUploadTask` 0건 — 네이티브 업로드 경로가 웹 번들에서 소거) · E2 판정
  grep(`upload.onprogress|XMLHttpRequest`, spec 제외) **5**(등재 시 0). 콜드 인스톨은
  `REDISMS_DISABLE_POSTINSTALL=1` 선행(ci.yml env 주석의 함정이 로컬 인스톨에도 그대로 적용).
- **상태**: **완료 · 머지 반영**(2026-08-28) — 사용자 지시로 PR #75 머지(main `b6fcfb9`).
  머지분 **Deploy Web 런 #49의 deploy 잡 success 실측**(잡 단위: preflight → build web 2분 19초
  (코드 변경이라 캐시 미스) → deploy 제온 SSH `compose pull + up` 11:23:55 완료, purge만 의도된 skip)
  → 어댑터가 제온 web 컨테이너에 실제 반영. ⚠️ **실기 한 바퀴는 미확인**(HANDOFF §8 등재) —
  CI는 빌드·배포 통과까지만 증명한다.

## 2026-08-28 — T-W2-35 기자 웹 주민 링크 발급 UI (대장 #147)

- **작업명**: 지사 담당자가 주민 임시 업로드 링크를 발급·복사·공유하는 화면 — 03 §C-5 주민 공급
  경로의 마지막 실사용 공백(#147: 서버 `POST /v1/resident-links`·주민 소비 화면(T-W2-09)은 완비인데
  발급 화면이 0건이라 경로 전체가 실사용 불가).
- **1순위 재판정 근거**: HANDOFF §2 후보 7건 중 T-W1-07c는 실존하지 않는 태스크(E2 402행 "신설하지
  않음" 판정 종결 — HANDOFF 후보 목록이 stale). 잔여 6건 중 본 태스크가 ① 유일하게 "완성된
  서버+소비 화면을 실사용으로 전환" ② 사용자 채번 지시(#147) ③ 신규 의존성 0·CI 무접촉이라
  머지=배포 체제에서 최저 리스크. 차순위 T-W2-07(e2e)은 L 사이즈+신규 의존성+T-W2-17 CI 배선
  동반이라 독립 세션 감.
- **§1 실측에서 발견한 문서 어긋남 2건(별도 정정 대상)**: ⓐ 진행률 오검출이 §7-1 기지 1건이
  아니라 3건(T-W1-11b·T-W2-17·T-W4-02 — 전부 커밋 260d9f0의 언급, 산출물 미실재. 실제 29/46=63%)
  ⓑ #147 재현 명령이 주민 소비 측 POST(subscriber 3건)를 세어 착수 전인데 "해소≥1"로 읽힘
  (규율 17 위반 유형) → 본 작업에서 발급 본질을 재는 명령으로 정정.
- **범위**: `apps/reporter` — `src/api/resident-links.ts`(신규)·`src/features/resident-uploads/issue.ts`
  (신규)·`app/(app)/resident-uploads/issue.tsx`(신규)·`index.tsx`(진입점 수정) + 신규 공개 키
  `EXPO_PUBLIC_SUBSCRIBER_WEB_URL` 3점 배선(ⓐ `.env.example` ⓑ `infra/docker/Dockerfile.web`
  ⓒ `.github/workflows/deploy-web.yml` — Dockerfile.web 62행 명문 규칙·대장 #146. E2 예상 경로
  밖 파일 3건은 이 규칙이 강제하는 확장이며 미배선 시 프로덕션 번들에서 키가 영원히 빈다).
- **설계 고정**: 서버 제약치(72h·5건·500MB)는 화면에 박지 않고 발급 응답 값만 렌더(E2 명문).
  토큰 원문은 발급 응답 1회 노출(서버 해시 보관) → 화면에 "지금만 복사 가능" 경고 필수.
  공유 URL = `<구독자 오리진>/upload/<token>`, 오리진은 env 우선 → 웹 호스트
  `reporter.<rest>`→`watch.<rest>` 유도 폴백 → 둘 다 없으면 경로만 표시+설정 안내.
- **검증 실측**: TDD 레드 3스위트(모듈 부재로 전건 실패) 확인 후 구현 → 신규 11 테스트 그린 ·
  reporter 전체 **291/291**(기준선 280+11) · typecheck 0 · lint 0 · **web export 성공**
  (`/(app)/resident-uploads/issue` 라우트 산출물 포함) · AC5 grep(.env.example 1·Dockerfile.web 2·
  deploy-web.yml 2) · 대장 무결성(daejang-recheck 어긋남 0·행수 167·대기 42 불변) ·
  #147 신규 재현 명령 실측 2(≥1). 테스트 하니스 함정: 이 리포 @testing-library/react-native에선
  `render()`가 **await 대상**이다(안 기다리면 쿼리 없는 Promise — detail-screen.test.tsx가 선례).
- **상태**: **완료** — qa-verifier **AC 5/5 PASS**(독립 재실행: 3스위트 11테스트 전건·전체 291/291
  무회귀·typecheck 클린·하드코딩 리터럴 0건·3점 배선 1·2·2 정확·변경 범위 준수. 참고 관찰:
  로컬 `main` ref stale로 `git diff main...HEAD`가 머지된 T-W2-36 파일을 섞어 보임 — 오검출,
  실 diff는 `29985e2...HEAD`로 재야 한다). PR 준비 완료 — **머지=제온 자동 배포**이므로 머지는
  사용자 실행(HANDOFF §7-1 관례)

## 2026-08-28 — T-W2-36 동의서 판단 게이트 해체 (촬영자 책임 모델 전환)

- **작업명**: 미성년자 동의서 "판단" 장치 전면 제거 — 플래그(`hasMinorSubject`)는 리마인더로 유지.
- **근거**: 사용자 결정(2026-08-27) — *"동의서는 전부 촬영자가 책임지고 받도록 한다. 우리 앱에서는
  동의서를 받았는지에 대해서는 판단하지 않기로 한다."* 범위는 AskUserQuestion으로 확정:
  **판단만 제거, 플래그·체크박스 유지**. 수정 지도 보고 후 사용자 승인(2026-08-28 "지도대로 진행해").
- **목표(제거)**: policyGuard ④ 승인 차단 · minor-consent 확인/철회 엔드포인트 2종 · 재생성 확인
  무효화(#117) · fail-closed 확인기록 삭제 · DB 확인 컬럼 2개 drop · shared 술어/필터 ·
  관제 "동의 확인 대기" 뷰·확인/철회 UI. **유지**: 체크박스(문구를 촬영자 책임으로 교체) ·
  관제 "미성년 등장" 정보 표시. **정본 개정**: 07 §3-3 · 02 §E-20 · 03 §C-2-1 · 대장(#164 해소 ·
  #115 재판정 · 신규 행 등재). 참조 실측: 확인 개념 35파일.
- **채번 주의**: T-W2-36은 대장 행으로 등재하며 **E2 모수(코드 46) 재조정은 후속으로 분리**
  (E2는 평가 이력 앵커가 걸린 문서라 총계 연쇄 갱신이 필요 — 대장 #166 비고에 명시).
- **검증 실측**: TDD 레드 5건(api 2·관제 2·기자 1) 확인 후 구현. api 유닛 **901/901**(舊 942 — 판단
  스위트 제거분)·typecheck 0 · 기자 **280/280** · 관제 **282/282** · 구독자 무영향 · shared build ✓ ·
  lint 0(2건 수정) · **contents e2e 15/15**(신규 "판단 없음" 스위트 — 양 reviewPolicy 승인 통과 +
  舊 라우트 404 실증) · Prisma 마이그레이션 `20260827203549_drop_minor_consent_confirmation` 적용.
  ⚠️ e2e 전체에서 **analysis-pipeline 1건 실패 → 선존 결함으로 확정·수리(대장 #167)**:
  auto_edit(8/20) 이후 그 스펙만 s3rver 하드코딩이라 손상 함정을 밟았고(§9 함정), 이 맥에선
  redis-memory-server 빌드 실패로 **조용히 skip되던 것이 이번에 실행되며 드러났다**. media-pipeline과
  동형의 E2E_REDIS_URL·E2E_S3_ENDPOINT 주입 배선 이식 + MinIO 기존 버킷 무해화로 수리 → 통과.
- **최종 실측**: api 유닛 **901/901** · **e2e 86/86**(12스위트 전건 — MinIO 주입) · 기자 280/280 ·
  관제 282/282 · lint 0 · 3앱+api typecheck 0. AC 5개 전건 충족(레드→그린 실증).
- **상태**: **완료** — qa-verifier AC 5/5 PASS(관찰 stale 주석 2건 즉시 정정) · CI 전 워크플로 초록 ·
  사용자 머지(PR #71, main `d862a5f`, 2026-08-28)

- **작업명**: Deploy Web 실배포 게이트를 Tailscale 경로로 배선 + 노출 방식·도메인 결정 기록.
- **사용자 결정 (2026-08-27)**: ① 사용자 트래픽 = **Cloudflare Tunnel**(도메인 확정 후), 배포 SSH =
  **Tailscale**(지금 배선·도메인 후에도 유지 — 두 경로 독립). ② 도메인 = **보류**(추후 구매 시 vars만
  재정의하도록 설계·문서화). ③ ~~compose `image:` 1줄~~ → **실측 결과 이미 완료**(#143, 2026-08-16) —
  워크플로의 stale 주석이 지시의 근거였고, 주석을 정정했다.
- **변경**: deploy-web.yml — preflight에서 `WEB_DOMAIN`을 배포 필수에서 **퍼지 전용으로 강등**,
  Tailscale OAuth 시크릿 2종을 배포 요건에 추가, deploy 잡에 `tailscale/github-action@v3` 합류 스텝.
  CLAUDE.md §12 결정 기록(중복 행 정리 포함). 실가동은 사용자 시크릿 입력 후.
- **동의서 결정 (별도 태스크로 채번 예정)**: "앱은 동의서 수취를 판단하지 않는다 — 촬영자 책임"
  (범위: 판단만 제거, `hasMinorSubject` 플래그·체크박스는 리마인더로 유지). 수정 범위 지도 보고 후 착수.
- **상태**: 배선 코드 완료 — PR 대기. 실배포 검증은 시크릿 입력 후 `workflow_dispatch`로.

## 2026-08-26 — T-W2-14 기자 앱 미성년자 체크박스 (대장 #118 기한 이행)

- **작업명**: classify 폼에 `hasMinorSubject` 입력 배선 — "피촬영자 만 14세 미만 여부" 체크박스.
- **목표**: 법적 게이트(07 §3-3·02 §E-20)가 백엔드만 완성돼 실사용에서 무효인 상태를 닫는다.
  기자가 위저드 분류 단계와 초안 수정 화면에서 플래그를 켜고 끌 수 있게 하고,
  "예" 선택 시 법정대리인 동의서 안내를 노출한다(03 §C-2-1 — 업로드는 막지 않는다).
- **착수 전 확인 (HANDOFF §1·§6)**: 재현 4종 실측 — 대장 163행 ✓·대기 42 ✓·열린 PR 0 ✓·
  진행률 31/46(미착수 15 목록이 §2+§4와 전건 일치) ✓. main은 `64826a7`로 문서(`22ef8ad`)와
  다르나 HANDOFF 자신의 머지(PR #66)가 유일 차이. 규율 5: 전송 계약
  (`CreateContentDraftRequest.hasMinorSubject`·zod·영속·가드·센터 확인 UI·e2e) 전 구간 실구동 확인.
  규율 15: **main `Deploy Web` 3연속 실패 발견**(GHCR 이미지 참조 대소문자 — #161 동류, 별도 태스크 분리).
- **범위 제한**: 동의서 **사진 첨부 UI**(03 §C-2-1 문언)는 제외 — 백엔드에 첨부 계약·저장 필드가
  없고(최소수집 원칙, shared 주석) 계약 없는 입력 UI는 #96 계열 결함의 재생산이다. **대장 #164 등재**.
- **검증 (AC 5/5 PASS)**: TDD — 신규 11건 레드 확인 후 구현. reporter 단위 **281/281**(舊 267)·
  typecheck 클린. qa-verifier 독립 검증 전건 PASS + **뮤테이션 실행 증거**(전송 배선 1줄 제거 시
  6건 적색 → 원복·HEAD 불변). 체크박스는 ClassifyFields(위저드·수정 공용)에 배선, `hasMinorSubject`는
  **값과 무관하게 항상 명시 전송**(PATCH 키 생략='변경 없음' — 생략 시 fail-closed 무효화 도달 불가).
- **파생 핫픽스**: Deploy Web은 **도입(T-W1-11c) 이래 성공 0회**였다(대장 #165 등재) — 이미지 참조
  대소문자 + job outputs의 env 컨텍스트 미평가 2겹. PR #68로 수정, **build web 사상 첫 초록 실측**
  (번들 예산 게이트도 이때 처음 실행됐다).
- **상태**: **완료** — 사용자가 순서대로 머지(2026-08-26): #68 → update-branch → #67 (main `0bbce65`).
  ⭐ **#68 머지 커밋의 main Deploy Web 런이 도입 이래 첫 성공**(50s, 캐시) → **대장 #165 해소 실측 충족**.
  #67 머지분 런까지 완주 확인 후 #165 상태 갱신(별도 docs PR). 잔여: #164는 법률자문 대기(변동 없음)

- **작업명**: 대기 항목 재현 명령 일괄 대조 + 재검증 스크립트를 리포에 상설화.
- **목표**: "수동 판정 미완"이 쌓이는 구조 자체를 없앤다. #159(2026-08-22)가 같은 일을 했지만
  **스크립트를 커밋하지 않아** 매번 처음부터 다시 만들어야 했다.
- **산출물**: `infra/scripts/daejang-recheck.mjs` — 대장에서 대기 항의 재현 명령을 추출해
  **읽기 전용 화이트리스트**로만 실행하고, 기재값과 대조해 4분류(일치/어긋남/수동/오류)한다.
  다음 세션은 `node infra/scripts/daejang-recheck.mjs` 한 번으로 전건 대조한다.
- **실측 결과 (대기 43 → 42)**:
  - **#97 해소** — `pipeline.service.ts`의 `previewReviewTarget`이 구동부로 실재한다(정의 1 + 호출 1).
    주석이 "대장 #97"을 명시하고 있어 **해소 시점에 상태 갱신만 누락된 건**이다
    (규율 8 위반 3회차 — #98·#99에 이은 세 번째).
  - **#131·#134·#137 재현 명령 교체** — 셋 다 **명령이 오검출**하고 있었다(결함은 그대로).
  - **#88 수치 현행화** — resident-links 12→19파일. 해소가 아니라 **계획 산정 괴리의 심화**다.
- ★ **어긋난 6건 중 5건이 결함이 아니라 명령·도구의 오검출이었다** — 실제 상태 변화는 #97 하나뿐:
  1. **#134·#137 — `grep -c`가 주석을 센다**(어제 #114와 동형, 하루 만에 2건 더). `null 지우기 미지원`
     이라는 **주석 문구**가 세어져 "해소됨"으로 보였다. ⇒ 결함의 본질(타입 선언 `| null` 부재)을
     직접 재는 명령으로 교체.
  2. **#131 — 명령이 결함보다 넓다.** `grep -c "telemetry"`가 T-W2-11이 만든 링크아웃 계측까지 세어
     **이 항의 잔여인 소비 트랙 3종(여전히 0건)을 가렸다.**
  3. **#141 — 도구가 틀렸다.** 다중 파일 `grep -c`는 `파일:값` 여러 줄을 내는데 첫 줄만 봐서
     `package.json:0` ≠ `0`으로 오판했다. → 합계 환산으로 수정.
  4. **#88 — 두 명령이 병렬인데 마지막만 보면 앞 것의 어긋남이 가려진다.** → "교체:" 표지가
     있을 때만 이력으로 강등하도록 수정.
- ★ **총계가 우연히 맞고 있었다 — 두 층에서**:
  - **범위 제한 없음**: 대장은 §6-1~§6-11의 **여러 표**로 나뉘고 표마다 1번부터 시작하는데
    총계 명령에 범위 제한이 없다. 지금은 §6-11 밖에 대기 항이 없어 맞았을 뿐이다.
  - **표 자체가 깨져 있다**: 셀 안 이스케이프 안 된 파이프로 **칸이 밀린 행이 선존**(#71·#72).
    상태 칸을 뒤에서 두 번째로 읽는 총계 명령이 엉뚱한 칸을 볼 수 있다. 마침 전부 우연히
    맞았다. ⇒ #159는 "명령이 취약하다"며 명령을 고쳤지만 **데이터가 깨진 층은 점검하지 않았다.**
    스크립트에 **표 무결성 검사**를 넣어 매 실행마다 드러나게 했다(표별 최빈 칸수 기준).
- ⚠️ **작업 중 자초한 실수 2건(기록)**: ① 대장에 재현 명령을 쓰며 **파이프를 이스케이프하지 않아
  표를 3번 깨뜨렸다** — 그중 하나는 *"명령에 리터럴 파이프를 쓰지 말라"는 경고 문구 자신*이었다.
  ② #71·#72를 정규식 일괄 치환으로 고치려다 **이미 이스케이프된 것까지 재이스케이프**해 11→4칸으로
  파손했다(git에서 두 줄만 복원). ⇒ **대장 표는 일괄 치환 대상이 아니다.** #71·#72는 고치지 않고
  스크립트 경고로 남겼다(총계 영향 없음 확인).
- **상태**: **완료**

## 2026-08-23 — 미성년자 동의 게이트 잔여 사각 해소 (대장 #116·#117·#119·#114)

- **작업명**: 미성년자 동의 확인 게이트의 잔여 사각 4건 해소 — 확인·철회의 CAS/트랜잭션(#116),
  법적 게이트 e2e 루프 편입(#119), e2e `resetDb` TRUNCATE 목록 보강(#114), #117 대장 정합(코드는 이미 해소).
- **목표**: 07 §3-3이 최상위 블로커로 다루는 법적 게이트가 **경합에서 귀속이 흔들리지 않고**, **CI가 그것을
  지키도록** 만든다. 기능 추가가 아니라 이미 있는 게이트의 신뢰성 확보다.
- **범위 제한 (하지 않을 것)**:
  - **#115는 손대지 않는다** — `resident_link` 분은 07 §3-15(외부 법률자문 대기)가 선결이고
    EXEC-DECISIONS #30이 임의 구현을 금지했다. `live_vod` 분리 가능성은 이번 조사에서 확인했으나
    사용자 범위 결정(2026-08-23)에 따라 이번 슬라이스에서 제외한다.
  - `content-workflow.service.ts`의 `policyGuard ④`(게이트 자체)·shared 전이맵·`schema.prisma`는 무변경.
    이번 작업은 **게이트가 요구하는 사실을 기록하는 쓰기 경로**와 그 검증만 다룬다.
- **정본 대조 결과 (착수 전 수행)**:
  - **07 §3-3에는 재생성·세대에 관한 문언이 없다.** 따라서 #117의 승인된 완화안("화면 구성이 바뀐
    재생성만 무효화")을 지지할 정본 근거가 없고, 현행 **보수적 매번 무효화를 유지**하는 것이 맞다.
    코드 주석의 "정본 대조 전이라 좁히지 않는다" 단서를 대조 완료 결과로 갱신한다.
  - **07 §3-15는 주민 링크만 다룬다** — `live_vod`는 그 소관이 아니다(촬영 주체가 방송국이라 §3-3
    동의서 모델이 그대로 성립). #115의 차단 사유는 `resident_link`에만 걸린다는 사실을 기록해 둔다.
- **AC (Given-When-Then)**:
  1. **(#116 ⓐ 귀속)** Given `hasMinorSubject=true`이고 아직 미확인인 콘텐츠, When 센터 운영자 2인이
     동시에 `POST /v1/contents/:id/minor-consent`를 호출하면, Then **최초 확인자의 id·시각만 남고**
     두 번째 호출이 그것을 덮어쓰지 않는다(조건부 `updateMany` CAS — read-then-write 창 제거).
  2. **(#116 ⓑ 철회)** Given 아직 게이트를 통과하지 않은 콘텐츠의 철회 요청, When 게이트 로그 조회와
     쓰기 사이에 승인 전이가 끼어들면, Then 철회가 **409로 실패**한다 — "승인됐는데 철회 성공"이라는
     거짓 안심(D5가 막으려던 것)이 그 창에서 되살아나지 않는다.
  3. **(#119 CI)** Given 실 HTTP·실 DB e2e 하네스, When `contents.e2e-spec.ts`가 실행되면, Then
     「플래그 켬 → 승인 409 차단 → 센터 확인 → 승인 통과」 루프와 **RBAC 가드**(기자·타 지사의 확인 시도
     차단)가 라우트 배선·실 DB 제약을 통과해 검증된다(단위 스펙은 Prisma를 목으로 대체해 이를 못 덮는다).
  4. **(#114 격리)** Given e2e `resetDb`, When TRUNCATE가 실행되면, Then `resident_upload_links`·
     `resident_uploads`가 목록에 포함돼 주민 링크 e2e를 붙였을 때 이전 실행 잔여가 남지 않는다.
  5. **(#117 정합)** Given 코드에 이미 존재하는 해소(`content-workflow.service.ts` `regenerating` 전이의
     확인 기록 무효화), When 대장을 갱신하면, Then 상태가 수신 완료로 바뀌고 **재현 명령이 실제 구현을
     검출하는 명령으로 교체**된다 — 기존 명령은 대장 자신이 제시한 두 구현 선택지 중 한쪽만 검출했다.
- **회귀 기준선 (착수 시점 실측, 2026-08-23)**: api 유닛 **936 tests / 73 suites** 전건 통과.
  ⚠️ 최초 측정에서 5건이 실패했으나 원인은 **shared dist stale**이었고 `pnpm --filter @gachinol/shared build`로
  해소됐다(코드 결함 아님). CLAUDE.md §11의 기록치 923은 stale이었다 — 기록치는 출처가 아니라 검증 대상이다.
- **완료 (2026-08-23)**: AC 5개 전건 충족. **api 유닛 936→942(+6) · e2e 82→89(+7) · 회귀 0** ·
  lint·typecheck 통과 · **shared·schema.prisma·`policyGuard` 본체 무변경**(게이트 자체는 손대지 않았다).
  - **AC1·AC2 (#116)**: 확인은 `{hasMinorSubject:true, minorConsentConfirmedAt:null}` 조건부 `updateMany`로,
    철회는 `$transaction` + **`status` CAS**로 바꿨다. ⚠️ **트랜잭션은 이 경합을 혼자 막지 못한다** —
    Read Committed에서 타 트랜잭션의 로그 INSERT는 우리 SELECT에 안 보인다. 실제 방어는 where 절이며,
    그 사실을 코드 주석에 명시했다(트랜잭션을 방어로 착각하면 다음 사람이 CAS를 지운다).
  - **AC3 (#119)**: e2e 7건 편입. **reviewPolicy 두 갈래를 모두 밟는다** — 뮤테이션 M2(`reporter_only`
    갈래만 무력화)가 그 케이스 **하나에만** 걸렸다. 한쪽만 검증했으면 통과했을 뮤테이션이다.
  - **AC4 (#114)**: 두 테이블 명시 편입. ⚠️ **대장의 전제가 틀렸다** — 아래 참조.
  - **AC5 (#117)**: 코드는 이미 해소돼 있었고, **대장 재현 명령이 틀려서** 미해소로 보였다.
- **뮤테이션 검증 4종 전건 검출**(만든 자와 검증하는 자를 분리하는 대신, 테스트가 실제로 무엇을 지키는지
  실측했다): M1 게이트 무력화→e2e 3건 · M2 `reporter_only` 갈래만 제거→e2e 1건 · M3 확인 CAS 제거→유닛 1건 ·
  M4 철회 status CAS 제거→유닛 1건. ⚠️ **한계**: 유닛의 경합 시나리오는 목이 `count`를 직접 주므로
  where 절 변경에 반응하지 않는다 — 실제로 CAS를 지키는 것은 "where 절 형태" 테스트 2건이고,
  **동시성 자체는 유닛으로 실증되지 않는다**(실 DB 동시 요청 테스트는 이번 범위 밖).
- ★ **이번 작업의 주 발견은 코드가 아니라 대장이 부정확했다는 것이다 — 4건 중 3건**(#159의 재발):
  1. **#117 — 재현 명령이 자신이 제시한 두 구현 선택지 중 한쪽만 검출했다.** 칸3이 "재생성 시 무효화
     **또는** schema 컬럼"이라 해놓고, 명령은 후자만 셌다. 전자로 구현했더니 해소 후에도 영구히 0.
  2. **#116 — 결함 2개(ⓐ확인 ⓑ철회)를 한 명령으로 재려다 양쪽 다 못 잡았다.** `confirmMinorConsent`
     블록의 `transaction`을 셌는데, ⓐ의 해소는 단일 원자 연산이라 트랜잭션이 불필요하고 트랜잭션이
     필요한 쪽은 ⓑ다.
  3. **#114 — 전제 자체가 틀렸다.** "빠지면 잔여가 남는다"고 했으나 `station_id` FK 덕에
     `TRUNCATE … stations CASCADE`가 이미 두 테이블을 비우고 있었다(실증: NOTICE + before 1 → after 0).
     그래서 칸5의 기한("주민 검수 e2e 작성 전")이 **일주일 전에 이미 초과됐는데도**(`resident-review.e2e-spec.ts`,
     커밋 `a873cf7`) 아무 증상이 없었다. 수정의 실질 가치는 누수 차단이 아니라 **CASCADE 우연 의존 제거**다.
  ⇒ #159는 "재현 명령을 임의로 바꾸지 말라"는 규율을 세웠지만, **명령 자체가 타당한지는 검증하지 않았다.**
  등재 시점에 결함을 본 사람이 해소 방식까지 전제하고 명령을 쓰면, 다른 방식으로 고쳤을 때 영구 미해소로
  남는다. **재현 명령은 "무엇이 있어야 하는가"가 아니라 "무엇이 없으면 결함인가"로 써야 한다.**
- **범위 밖으로 남긴 것**: **#115**(사용자 범위 결정). 조사 중 확인한 사실 — 07 §3-15는 **주민 링크만**
  다루고 `live_vod`는 그 소관이 아니다(촬영 주체가 방송국이라 §3-3 동의서 모델이 그대로 성립하며,
  `live_vod`는 `awaiting_center_review` 직행이라 센터가 반드시 본다). 즉 **#115의 차단 사유는
  `resident_link`에만 걸리며**, `live_vod` 분은 법무 대기 없이 해소 가능하다. 대장 #115 행은 두 origin을
  한 줄로 묶고 있어 이 구분이 보이지 않는다 — 분리 등재 여부는 사용자 판단 대기.
- **상태**: **완료** — PR 대기

## 2026-08-04 — 웹 피벗 실행계획 수립

- **작업명**: 승인된 마스터플랜(W0~W4)의 실행계획 수립 — 부하 에이전트 편성·병렬 작업 순서·태스크 분해·
  서브에이전트 규모·토큰/모델/에포트 배분을 확정하고, 독립 루브릭 평가(전 영역 9.5+)를 통과시킨다.
- **AC (Given-When-Then)**:
  1. Given 승인된 docs/plan/*, When 실행계획(docs/plan/exec/)이 작성되면, Then 마스터플랜의 W0~W4 체크리스트·
     DoD·게이트에서 파생된 태스크만 담고(신규 범위 발명 금지) 각 태스크에 담당 에이전트·파일 소유권·검증 방법이 붙는다.
  2. Given 실행계획 v(n), When 독립 평가 에이전트(신규·이전 평가 미열람)가 EXEC-RUBRIC(0.1 단위)으로 평가하면,
     Then 결과가 exec/reviews/EXEC-EVAL-ROUND-n.md로 남고, 전 영역 9.5+ 도달까지 평가·수정(병렬 ≤5, 비동시)을 반복한다.
  3. Given 토큰·세션 한도 리스크, When 실행계획이 확정되면, Then 라운드당 동시 상한·모델/에포트 배분·한도 중단
     시 재개 프로토콜이 명시돼 실행 중단이 계획으로 관리된다.
  4. Given 전 영역 9.5+ 달성, When 보고하면, Then 자동진행 승인(2026-08-04)에 따라 실행(W0~) 착수로 이행하되
     사용자 결정 2건(도메인·제온 노출 방식)과 운전자금 게이트는 사용자 확인 전 착수 보류를 유지한다.
- **완료 (2026-08-08)**: 29라운드 평가–수정 반복 끝에 **EXEC-EVAL-ROUND-29에서 전 영역(8/8) 9.5+ 통과**
  (실산 평균 **9.60** — 영역1 9.7·2 9.6·3 9.5·4 9.7·5 9.6·6 9.6·7 9.6·8 9.5, 9.5 미만 0개, 중대·치명 이슈 0건).
  - **AC1 충족**: 69태스크(코드 46·트리거 3·코드외 20) 전건이 02 §E 28라벨 + 08 §E 13라벨에서 파생됐고
    누락·잉여 0(평가자 직접 실행). 각 태스크에 담당 역할·파일 소유권·DoD·검증자가 붙어 있다. 신규 범위 발명은
    E2 §0이 비범위 판정으로 차단.
  - **AC2 충족**: 라운드별 신규 평가자(이전 평가서 미열람)가 EXEC-RUBRIC 8영역을 0.1 단위로 채점,
    `docs/plan/exec/reviews/EXEC-EVAL-ROUND-1~29.md` + 결정문 `EXEC-ROUND-*-DECISIONS.md`로 잔존.
    수정은 파일 소유 배타 3팀(A=E1·E4 / B=E2·E3 / C=E5) 병렬, 평가·수정 비동시.
  - **AC3 충족**: 동시 상한 5(구현 4 + qa-verifier 1)·웨이브 상한 200만·모델/에포트 배분·재개 프로토콜이
    E4에 명시. 실제로 세션 한도 중단이 **5회** 발생했고 전건 재개 프로토콜로 복구(작업 손실 0) — 그 실증이
    §C-3에 기록돼 있다.
  - **정합 근거(스윕 실행 출력)**: 총 69 · SOLO 9 3자 일치 · 웨이브 23라벨·배정 46·고유 46 · E2↔E3 양방향
    차집합 공집합 · 구현 4건 웨이브 {2·4·5·9·12} · lockfile 8종 웨이브당 1건 이하(D6-1 위반 0) ·
    합계 1,944만·최대 Wave 5=166만·총예산 하한 ≥2,204만 · 발주–수신 대장 67+59=126행 · 앵커 ①~⑯ 전종 성립.
  - **AC4**: 자동진행 승인(2026-08-04)에 따라 병합까지 이행. **실행(W0~) 착수는 별도 세션에서 이어간다**
    (사용자 지시 2026-08-08). G9 사용자 게이트 3건(도메인·제온 노출 방식·05 §G 운전자금)은 확인 전 해당
    태스크만 보류하고, 무지출 로컬 검증 범위는 선행 가능(EXEC-DECISIONS #8).
- **상태**: **완료** — 계획 확정. 실행은 후속 작업.

## 2026-08-03 — 웹 피벗 마스터플랜 수립

- **작업명**: 네이티브 3앱 → 웹앱 3종 + 쉘(래퍼) 앱 전환 마스터플랜 재수립
- **목표**: 기존 계획(CLAUDE.md·docs/*) 전체를 정독한 뒤, 웹 피벗에 맞춘 수정 계획을 작성하고
  독립 평가 에이전트의 루브릭 평가(11개 영역, 1~10점·0.1 단위)에서 **전 영역 9.5점 이상**을
  달성할 때까지 평가→수정을 반복한다. 평가 결과는 매 라운드 마크다운으로 남긴다.
  법률·라이선스 검토는 전담 에이전트가 수행하며 계획에 필수 반영한다.
- **범위 제한**: 이 작업은 계획 문서만 산출한다(코드 변경 없음). 실행계획 수립은
  사용자 승인 후 별도 작업으로 진행한다.
- **완료 (2026-08-04)**: 19라운드 평가–수정 반복 끝에 **EVAL-ROUND-19에서 전 영역(11/11) 9.5+ 통과**(평균 9.55).
  산출물: `docs/plan/`(PIVOT-PLAN + 01~08 + reviews/ 평가 19본·결정 10본·RUBRIC). AC1~AC4 충족,
  AC5(승인 게이트)에 따라 사용자 승인 대기 — 승인 전 코드·기존 문서 무변경.
