# 세션 핸드오프 — 웹 피벗 실행계획 평가·수정 루프 재개 지점 (2026-08-07 11:35 KST 갱신)

> **새 세션용 재개 브리프.** 실행계획 수립은 완료됐고, 현재는 **독립 평가·수정 루프**(전 영역 9.5+ 게이트)를
> 반복 중이다. 이 파일 하나로 이어받을 수 있다. 루프 종료(전 영역 9.5+) 시 본 파일은 삭제한다.

## 0. 현재 위치 (2026-08-07 기준)

- 브랜치 `feat/web-pivot-exec-plan`, 워크트리 **`/Users/homedcp/Claude/Projects/gachinol`**(orca main 아님).
- **라운드 21까지 완료·커밋**(R21 = `f54e09f`). **라운드 22 평가 진행 중이었음** — 재개 시
  `docs/plan/exec/reviews/EXEC-EVAL-ROUND-22.md` 존재·완결 여부부터 확인:
  - 존재·완결(5부: 총평/점수표/영역별 상세 8건/이슈 목록/부록) → §3 수정 사이클로.
  - 부재·미완 → §2 평가자 템플릿으로 신규 기동(이슈 접두 `X2-n`).
- 확정 상태: **68태스크**(코드 45·TRIG 3·NC 20)·웨이브 라벨 22·SOLO 8·**준-공용 7종**·상한 200만
  (합계 1,892만·최대 Wave 5=166만)·총예산 **하한 전용 ≥2,152만**(구간 추정 폐기)·앵커 15종(④=5리터럴+2참조형).
- 대장: PIVOT-PLAN §6-11 **31행, 총 98행** 전건 수신 완료. EXEC-DECISIONS #1~#8.
- 점수 추이(실산): 8.44 → … → 9.26(R20) → **9.31(R21, 이슈 9건 사상 최소)**. 치명 0 지속.
- **수렴 조건**(E4 §A-8): 중대 이슈 0 + 신규 유형 0 + 8영역 전부 9.5.

## 1. 프로세스 (확립된 사이클 — 그대로 반복)

1. **평가**: 신규 opus 에이전트 1개(§2 템플릿). 이전 평가서 열람 금지, 라운드별 새 이슈 접두
   (사용 완료: X·Y·Z·V·U·W·Q·R·S·K·J·H·G·F·M·N·P·V2·R2·Z2·Y2·X2 — 다음은 W2·U2… 순으로 미사용 접두).
2. **선확정**: 이슈 중 문서 간 공유 판단·택일은 조율자가 `reviews/EXEC-ROUND-<n>-DECISIONS.md`로 선확정
   (경량 라운드는 팀 메시지 내 확정으로 대체 가능). 정본 이탈 해석은 `EXEC-DECISIONS.md` #n 누적.
3. **수정**: 3팀 병렬 — **A=E1·E4 / B=E2·E3 / C=E5** (파일 소유 배타 절대 불변). 각 팀 지시에 반드시 포함:
   ① 평가서 전문 정독 ② 최소 보장선(이슈별 배분) ③ **전수 대사 의무**(전 이슈 대비 자기 파일 — 해당 없음도
   이슈별 1줄) ④ **Q1 엄수**(모든 "확인"은 실행 grep 출력, 수정 후 증빙 동봉) ⑤ 완료 보고 고정 문구.
   **순서 규칙**: 태스크 수·사이징·웨이브가 변하면 **B 선행 → A 후행**(E4는 현행 E3 §C를 다시 읽고 재계산).
   아니면 3팀 병렬. 신규 위임은 완료 보고로 제출 → 조율자가 §6-11 등재(+총계 갱신).
4. **조율자 스윕**(팀 완료 후): E5 §D 앵커 관계식 재현(특히 ⑤ 양단 — E4 §A-8 총예산 ↔ §6-11 #9를 스윕이
   최종 동기화, D14-2)·팀 보고 교차 grep(보고-실체 괴리 이력 2건)·대기 위임 종결.
5. **커밋**: `docs(exec): 라운드 <n> 수정 — …` + Co-Authored-By/Claude-Session 푸터 → 차기 라운드.

※ 새 세션에서는 기존 서브에이전트(팀·평가자)를 재소환할 수 없다 — **팀도 평가자도 신규 기동**하면 된다
(문서가 전부 담고 있어 컨텍스트 승계 불요. 팀 신규 기동 시 역할 1문장 + 위 ③④⑤만 지키면 동형이다).

## 2. 평가자 프롬프트 템플릿 (라운드 n, 접두 <P>)

```
너는 실행계획을 공격적으로 검증하는 독립 평가 전문가다. 내부 맥락을 모른다고 가정, 문서·리포 실체만 근거.
# 작업 목표: docs/plan/exec/를 EXEC-RUBRIC 8영역으로 평가, reviews/EXEC-EVAL-ROUND-<n>.md 1파일만 생성.
# 독립성: 이전 EXEC-EVAL-ROUND-* 열람 금지. 이슈 번호 <P>-n. DECISIONS 문서(EXEC-DECISIONS #1~#8·
EXEC-ROUND-*-DECISIONS)는 열람 가능. 앵커 충족 영역에 9.5+는 정확한 채점(루브릭 원문 인용). 시점 명시
이력 서술·회고 인용은 stale 아님. 상태 칸 건수=시점 스냅숏. 앵커 절 기댓값 무박제=설계(D7-1).
평균은 8영역 실산(계산식 병기). 모든 사실 주장은 직접 실행 출력(Q1), 실측은 이 워크트리에서만. 자기 매칭 구분.
# 순서: EXEC-RUBRIC 정독 → EXEC-PLAN·E1~E5·EXEC-DECISIONS·EXEC-ROUND-*-DECISIONS 전문 → 상위 정본 대조
(PIVOT-PLAN §6·02 §E/§B/§C·08 §A~§E) — E2 전건 대사(68건)·E2↔E3 정합(22라벨·SOLO 8·준-공용 7종)·
E4 산술(1,892만/166만/하한 2,152만)·앵커 전종을 직접 실행으로 검증 → 리포 실체 표본 → 영역별 채점
(0.1 단위·감점은 문서·절 인용 강제·9.5 도달 수정 지시) → 5부 구성 기록.
# 완료 보고: "✅ EXEC-EVAL-ROUND-<n> 평가 완료: <8영역 점수+실산 평균(계산식)+9.5 미만 수+이슈 건수>"
```

## 3. 통과(8영역 전부 9.5+) 시

WORKLOG(리포 루트) 완료 기록(실행 근거로만) → **본 파일 삭제** → exec 전체 커밋 → PR 생성 → 자기머지
(로컬 `--no-ff`+main 푸시 — `gh pr merge` 막힘, memory `git-pr-workflow`) → 브랜치 정리 → 사용자 보고 →
**실행(W0~) 자동 이행**(승인·자동진행 기지시 — 단 G9 사용자 게이트 3건(도메인·제온 노출 방식(Tunnel 권장)·
05 §G 운전자금)이 걸린 태스크는 확인 전 보류, 나머지 로컬 검증 범위 병렬).

## 4. 축적 교훈 (재발 방지 규칙 — 위반 주의)

반증도 재검증 대상(#5) · 원시 grep 수치 주장 금지(D6-4) · 앵커 박제 금지(D7-1) · 계획 신설분 모수 자동
갱신(#4·#6) · 정본 결박 우선(#7) · 보고-실체 괴리는 스윕 교차 grep(#25) · 앵커 ⑤ 동기 순서(D14-2) ·
사용자 행위는 T-NC 분리(R2-3) · 하드코딩 대신 "그 시점 전수" 참조형(Z2-3) · "문자 승격" 선언은 리터럴
검증(Y2-3) · 대량 태스크 공유 파일은 이름 명시(Z2-11).
