# 세션 핸드오프 — 웹 피벗 실행계획 수립 재개 지점 (2026-08-04 11:03 KST)

> **새 세션용 압축 컨텍스트.** 긴 원 세션의 핵심만 담았다. 이 파일 + 아래 "필독 3개"만 읽으면 이어받을 수 있다.
> 실행계획 완성 후 본 파일은 삭제한다(운영 메모 — 계획 문서 아님).

## 0. 필독 (이 순서로)

1. `docs/plan/exec/EXEC-PLAN.md` — 실행계획 거버넌스 G1~G9 (조율자가 작성 완료)
2. `docs/plan/exec/reviews/EXEC-RUBRIC.md` — 평가 8영역·9.5 앵커 (작성 완료)
3. `docs/plan/PIVOT-PLAN.md` — 승인된 마스터플랜 인덱스(§6 대장·프로세스 규칙 포함)

## 1. 지금까지의 상태 (완료된 것)

- **마스터플랜**: 독립 루브릭 평가 19라운드 끝에 11개 영역 전부 9.5+(평균 9.55, EVAL-ROUND-19) 통과.
  **사용자 승인 + 자동진행 + 커밋/PR 지시 완료(2026-08-04)** → **PR #16 병합 완료**(main `179f65e`),
  CLAUDE.md §4·§5·§11·§12와 docs/ROADMAP.md에 피벗 반영 완료. 브랜치 정리 완료.
- **실행계획(현 단계)**: 브랜치 `feat/web-pivot-exec-plan`에서 진행 중. WORKLOG에 AC 4건 기록됨.
  - 작성 완료: `EXEC-PLAN.md`(조율자), `reviews/EXEC-RUBRIC.md`(조율자), `E5-quality-gates.md`(에이전트 — **단, 완료 보고를 받기 전에 세션 한도로 중단됐으므로 내용 검증 필요**)
  - **미작성**: `E1-agents-org.md`, `E2-work-breakdown.md`, `E3-parallel-schedule.md`, `E4-token-budget.md`
    (초안 에이전트 3팀이 2026-08-04 세션 한도(9:20am 리셋)로 전멸 — 리셋은 지났으므로 즉시 재가동 가능)

## 2. 재개 절차 (이 순서로 실행)

1. **E5 검증**: `E5-quality-gates.md`를 열어 EXEC-PLAN G6·G7·G8과 정합·완결성(리스크 3요소·체크리스트 존재) 확인.
   미완이면 아래 브리프 C로 멱등 보완("현재 상태 확인 후 미완만 반영").
2. **초안 재가동 2팀 병렬**(sonnet, 파일 소유권 배타): 아래 브리프 A(E1+E4)·브리프 B(E2+E3) — 멱등 문구 포함해 그대로 사용.
3. 전 문서 완성 후 → **독립 평가 라운드**: opus 신규 에이전트, `EXEC-EVAL-ROUND-<n>.md` 작성,
   "이전 EXEC-EVAL-* 미열람" 독립성 규칙. 9.5 미만 영역은 병렬 수정(동시 ≤5, 평가·수정 비동시) 후 재평가 —
   **8영역 전부 9.5+까지 반복**(마스터플랜과 동일 프로세스. 수렴 팁: 평가서의 감점 사유 전건 + 횡단 결정
   문서(EXEC-ROUND-n-DECISIONS.md) 패턴 + 수정 에이전트의 "신규 위임 목록" 보고 의무 + 조율자의 grep 실측 검증(Q1)).
4. 통과 시: WORKLOG 완료 기록(실제 근거로만) → exec/ 전체 커밋 → **PR 생성 → 자기머지**(로컬 `--no-ff`+main 푸시,
   `gh pr merge`는 막혀 있음 — memory `git-pr-workflow`) → 브랜치 정리 → 사용자에게 결과 보고.
5. 이후: **자동진행 지시가 이미 있으므로** 실행(W0~)에 착수하되, **G9 사용자 게이트 3건**(도메인·제온 노출
   방식(Tunnel 권장)·05 §G 운전자금 확인)이 걸린 태스크는 사용자 확인 전 보류(나머지는 로컬 검증 범위 내 병렬 진행).

## 3. 재가동 브리프 (그대로 서브에이전트 프롬프트로 사용)

### 브리프 A — E1·E4 (general-purpose, sonnet)

```
너는 멀티에이전트 오케스트레이션 전문 개발 매니저다. 이전 시도가 세션 한도로 중단됐다 — 파일이 일부/전부 없을 수 있으니 현재 상태 확인 후 미완만 작성하라(중복 금지).

# 작업 목표
웹 피벗 실행계획 중 2개 문서 작성. 이 2파일만 쓴다. 다른 파일 수정 금지, 코드 변경 금지.
- /Users/homedcp/Claude/Projects/gachinol/docs/plan/exec/E1-agents-org.md
- /Users/homedcp/Claude/Projects/gachinol/docs/plan/exec/E4-token-budget.md

# 작업 순서
1. 정독: docs/plan/exec/EXEC-PLAN.md(거버넌스 G1~G9 — 반드시 따른다), docs/plan/exec/reviews/EXEC-RUBRIC.md(영역 2·5·6의 9.5 앵커), docs/plan/PIVOT-PLAN.md §5·§6(승계 프로세스 자산), ~/.claude/CLAUDE.md의 "작업 지시 표준 템플릿"(위임 프롬프트 5요소).
2. E1 작성 — 역할군 7종(G1: 조율자 PM·FE 리드·BE 리드·인프라·QA 리드·PMO·법무/운영 지원 + 라운드별 독립 평가자): 각 역할의 책임 경계(소유/금지)·기동 방식(태스크 단위 서브에이전트)·산출물 형식·완료 보고 고정 문구. 위임 프롬프트 표준(5요소, 실제 템플릿 전문 1개 예시 — 파일 소유권 명시·"이 파일들만 수정"·신규 위임 보고 의무 포함). 검증 분리(G6): 구현↔검증 매핑 표(qa-verifier 활용). 리스크 테이블(3요소)+체크리스트.
3. E4 작성 — 동시 상한·예산(동시 ≤5, 태스크 유형별 토큰 예상 — 마스터플랜 수립 실측: 초안 ~10만·수정 ~15~25만·독립 평가 ~50~60만·PMO ~15~20만 서브에이전트 토큰, 웨이브당 상한과 초과 조치). 모델·에포트 배분(G5: 구현/문서=sonnet, 설계 판단/독립 평가/리뷰=opus, 조율=본체 — 근거·전환 규칙·과소/과대 감지 신호). 세션 한도 재개 프로토콜(G4, 실증 4회: 중단 통지→date로 리셋 대비 확인→리셋 전 대기/후 즉시 재가동→재가동 프롬프트는 멱등("현재 상태 확인 후 미완만 반영")→병렬 경합 의심 시 조율자 교차 grep — 표준 문구로 정식화). 리스크 테이블(3요소)+체크리스트.
4. 두 문서 정합(역할↔모델 배분) 자체 점검. 새 위임이 생기면 완료 보고에 "대장 등재 필요 신규 위임 목록" 제출.

# 완료 보고 (고정 문구)
"✅ E1·E4 작성 완료: <요약 3줄 + 신규 위임 목록(없으면 '없음')>"
```

### 브리프 B — E2·E3 (general-purpose, sonnet)

```
너는 대규모 마이그레이션 프로젝트의 딜리버리 매니저다. 이전 시도가 세션 한도로 중단됐다 — 파일이 일부/전부 없을 수 있으니 현재 상태 확인 후 미완만 작성하라(중복 금지).

# 작업 목표
웹 피벗 실행계획 중 2개 문서 작성. 이 2파일만 쓴다. 다른 파일 수정 금지, 코드 변경 금지.
- /Users/homedcp/Claude/Projects/gachinol/docs/plan/exec/E2-work-breakdown.md
- /Users/homedcp/Claude/Projects/gachinol/docs/plan/exec/E3-parallel-schedule.md

# 작업 순서
1. 정독: docs/plan/exec/EXEC-PLAN.md(G1~G9 — 반드시 따른다), docs/plan/exec/reviews/EXEC-RUBRIC.md(영역 1·3·4 앵커), 범위의 정본: docs/plan/02-web-architecture.md §E(체크리스트 0~24번 전건), docs/plan/08-rollout-transition.md §A(W0~W4·시간축 매핑·DoD)·§E, 필요 시 03 §C·04 §H·06 §F.
2. E2 작성 — 태스크 크기 기준(G2: 1태스크=서브에이전트 1회 기동으로 구현+테스트+자가 검증 완결, 수정 파일 ≤10 대략, 초과 징후 시 분할). W0~W4 태스크 표: 02 §E·08 §A/§E 체크리스트 전건 매핑(항목 번호 인용 — 신규 범위 발명 금지·누락 금지). 각 태스크: ID(T-W1-03 형식)·원천·담당 역할(E1 역할군)·파일 소유권(경로 집합)·산출물·DoD(정본 인용)·검증자(G6). 코드 외 태스크(법무·운영·사용자 결정 대기)는 별도 섹션 트리거 대기. 커버리지 선언("전건 매핑 — 누락 0건"은 실제 대사 후 기재). 리스크 테이블(3요소)+체크리스트.
3. E3 작성 — 의존 그래프(08 §A 선행조건·시간축 매핑 소비). 웨이브 편성(동시 ≤5·파일 소유권 배타 — 웨이브별 태스크·병렬 근거(파일 집합 비교집합)·예상 소요). 단독 슬롯 규칙(G3: packages/shared·Prisma 스키마·CI 설정·app.config.ts 공통 변경은 단독). 실패 태스크 재시도/이월·G9 게이트 보류 태스크 우회 경로. 리스크 테이블(3요소)+체크리스트.
4. E2↔E3 정합(전 태스크가 정확히 한 웨이브에 배정) 자체 검산. 새 위임이 생기면 완료 보고에 목록 제출.

# 완료 보고 (고정 문구)
"✅ E2·E3 작성 완료: <태스크 총수·웨이브 수·커버리지 선언 + 신규 위임 목록(없으면 '없음')>"
```

### 브리프 C — E5 검증·보완 (필요 시에만, general-purpose, sonnet)

```
너는 QA·릴리스 거버넌스 전문가다. E5 초안 에이전트가 파일을 쓴 뒤 완료 보고 전에 중단됐다. /Users/homedcp/Claude/Projects/gachinol/docs/plan/exec/E5-quality-gates.md 를 열어 EXEC-PLAN.md G6·G7·G8 및 EXEC-RUBRIC 영역 7·8 앵커와의 정합·완결성(4단 게이트·PR 흐름·W DoD 판정 절차·대장 연장 운영·진행 보고·리스크 3요소·체크리스트)을 점검하고, 미완·불일치만 보완하라(전면 재작성 금지). 이 1파일만 수정. 완료 보고: "✅ E5 검증·보완 완료: <이미 완결/보완한 것 구분>"
```

## 4. 프로세스 규칙 요약 (원 세션에서 확립·실증된 것)

- **독립 평가**: 매 라운드 신규 opus 에이전트, 이전 평가서 미열람, DECISIONS 문서는 열람 가능. 감점 사유는 문서·섹션 인용 강제.
- **병렬 수정**: 파일 소유권 배타·동시 ≤5·평가와 수정은 비동시. 횡단 모순은 조율자가 DECISIONS로 선확정 후 배분.
- **Q1 원칙**: "확인했다"는 실행한 grep 결과가 있을 때만 기재(허위 검증 1회 적발 이력 → 규칙화).
- **등재 책임 규칙**: 새 위임을 만든 에이전트가 완료 보고에 "대장 등재 필요 신규 위임 목록" 제출 → 조율자가 반영·등재.
- **병렬 경합 패턴**: 동시 편집 라운드에서 상대 문서 grep이 구판을 볼 수 있음 → "미수신" 판정은 조율자가 최종 grep으로 재확인.
- **git**: main 보호, feat/* → PR → 자기머지(--no-ff 로컬 병합+push, `gh pr merge` 막힘), Conventional Commits, 커밋 푸터에 Co-Authored-By + Claude-Session.
- **한도 재개**: date로 리셋 확인 → 지났으면 즉시, 멱등 브리프로 재가동. (메모리 `usage-limit-pause-resume` 참조)

## 5. 사용자 지시 원문 요지 (구속력 있는 것)

1. 실행계획도 마스터플랜과 **같은 방식**(독립 공격 평가·0.1 단위·전 영역 9.5+·마크다운 기록·평가/수정 비동시·병렬 수정·토큰 폭발 방지).
2. **승인·자동진행·커밋/PR 이미 지시됨**(2026-08-04) — 실행계획 통과 후 보고하고, 실행(W0~)로 자동 이행하되 G9 사용자 게이트만 보류.
3. 소통은 한국어. 완료 판정은 실행 근거 있을 때만.
