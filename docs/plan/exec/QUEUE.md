# QUEUE — 개발순서계획도 (순서의 단일 원천)

> **역할 분담**: 순서는 여기. **범위·DoD는 E2**, **결함 진단은 대장(PIVOT-PLAN §6-*)**,
> **세션 인계는 HANDOFF**, **왜 정체됐는가는 ROOT-CAUSE-2026-08.md**.
>
> ## ⚠️ 이 문서의 절대 규칙 — 가변 값을 적지 않는다
> 완료 여부·파일 수·계수·커밋 해시·테스트 수를 **여기 적지 않는다.** 적는 순간 썩기 시작한다.
> 필요하면 **재현 명령만** 적는다. 이 리포의 계획 문서가 전부 stale해진 원인이 값 박제였다
> (ROADMAP은 89행짜리 얇은 문서인데도 죽었다 — **얇음은 stale의 예방이 아니다**).
>
> **갱신 시점**: 슬라이스 종료 시. 커밋마다·매일이 아니다.
> **갱신 소유자**: `scribe`. 조율자가 직접 고치지 않는다(규율 8이 4회 깨진 자리가 여기다).

---

## 순서 판정 기준 — 사전식 사다리 (점수 합산 금지)

위에서부터 걸리면 그 자리에서 결정한다. **논쟁 여지를 없애는 것이 목적이다.**

| 단 | 질문 | 근거가 된 실패 |
|---|---|---|
| **L0** | 선행이 **실구동**으로 충족되는가? (타입·픽스처·테스트만 있는 건 구동 아님) | 후보 중 여럿이 착수 불가였다 |
| **L-obs** | **이것이 없으면 다른 모든 판정이 무의미한 계측기인가?** | 빌드 스탬프 0건이라 "배포된 것이 어느 커밋인가"를 물을 수 없었다 · 대장 판정 도구가 대장 아닌 행을 센다 |
| **L1** | **지금 사용자에게 도달하는 경로가 끊겨 있는가?** | 인터넷 502 · 배포 안 된 라우트 호출 · 번들 placeholder |
| **L2** | **실발생·재발 가능 결함인가?** (기능 부재와 구분) | #168 영구 교착 |
| **L3** | **MVP 한 바퀴의 미보유 조각인가?** (계획에 태스크조차 없는 것 포함) | 카카오 게시자산 0줄 |
| **L4** | 시니어 기자가 실제로 쓸 수 있게 하는가? | 사용자 정의 서비스 목적 |
| **L5** | 나머지 — **순서표가 아니라 백로그에 둔다** | |

같은 L 안에서는 **규모 작은 순**. 그 둘로 순서가 결정된다.

> ⚠️ **`L-obs`는 2026-08-31 독립 검증에서 신설됐다.** 원래는 빌드 스탬프(#186)를 L1로 적었는데,
> 검증자가 *"빌드 스탬프 부재는 도달 경로를 끊지 않는다 — ROOT-CAUSE 자신이 '수리가 아니라 계측기
> 복구'라고 적어 결함이 아님을 자인한다"*고 지적했다. **맨 앞에 두는 판단은 옳았고 L 라벨이 틀렸다.**
> ⭐ 더 중요한 지적: 검증 전 QUEUE는 **단계 번호와 L이 거의 완전히 일치**했다 —
> *"사다리로 단계를 정한 것이 아니라 단계로 L을 붙였다"*는 서명이다. **L 칸을 결과에 맞춰 채울 수
> 있으면 사다리의 목적(논쟁 여지 제거)이 무효화된다.** 아래 배치는 그 지적을 반영해 재정렬한 것이다.

**동반 의무 D1**(규율 23): 착수하는 결함이 *"검사 밖이라서 생긴 것"*이면 **그 검사 축을 넓히는
작업을 같은 슬라이스에 포함**한다. 순서에 넣으면 항상 뒤로 밀리지만 동반 의무면 반드시 같이 간다.

**두 부정 명제**: "미착수 = 곧 후보"가 아니다(L0 통과해야 후보) · "오검출 = 곧 후보"도 아니다.

⚠️ **선행 칸에는 "기능 의존"만 적는다.** 같은 파일 중복 편집을 피하려는 "순서 분리"는 **차단이 아니다**
(부록 A 참조). 이 구분을 놓쳐 검증 전 QUEUE가 선행 4건을 과잉 기재했다.

---

## ⭐ 사용자 실행 항목 — 언제 필요한가 (요청 시점 정본)

> **조율자의 의무**: 아래 "요청 시점"에 도달하면 **그날 일간보고서 ①에 올려 요청한다.**
> 미리 몰아서 묻지 않는다(결정 피로) · 닥쳐서 묻지 않는다(대기가 착수를 막는다).
> **리드타임이 있는 항목은 그것이 막는 작업보다 리드타임만큼 앞서 요청한다** — 이 표의 존재 이유다.

> **§A(착수 자체의 선행 — PR #80 머지)는 충족돼 절을 정리했다.** 재현: `git ls-tree -r --name-only
> origin/main | grep -c "^\.claude/agents/"` → **7**(≥6, 2026-08-31 재실측) — `gh pr view 80`도 머지
> 확인. 새 세션이 6롤을 못 부르던 문제(에이전트 정의는 세션 시작 시점 로드)는 이미 해소된 세션에서
> 대장 #186 슬라이스가 실제로 굴러갔다는 사실 자체가 증거다.

### B. 지금 — 리드타임 때문에 지금 시작해야 하는 것 (병렬, 코드와 무관)

| # | 항목 | 리드타임 | 이것이 늦으면 |
|---|---|---|---|
| **B-1** | ~~사업자등록~~ ✅ **충족(2026-08-31)** | — | 사용자가 개인사업자 등록증(일반과세자, 업종에 "영화·비디오물 및 방송 프로그램 제작 관련 서비스업" 포함) 제공(HANDOFF §0-A 참조) — 스토어 계정·카카오 비즈니스 인증 → 카카오 실 송출·YouTube 수익화·통신판매업·PG 등 **후속 신고·계약은 별개로 잔존**(07 §6 14항목 완료와도 무관) |
| **B-2** | **법률자문 계약**(체결 자체는 **사용자가 별도로 진행 중**, 2026-09-01 통보) **→ 07 §6 14항목**(scribe 재계수 2026-09-01: 舊 명령 `grep -c "^- \[ \]"`(미체크만 셈)은 항목 하나가 완료될 때마다 **분모 자체가 줄어드는 결함**이 있었다 — 정정 명령 `awk '/^### 첫 촬영 재개 전 \(최상위 블로커\)/{f=1;next}/^### /{if(f)exit}f' docs/plan/07-legal-license.md \| grep -c "^- \["`(체크 여부 무관, 총 항목수) → **14**(불변). 완료 현황은 별도로 `... \| grep -c "^- \[x\]"` → **1**(CLAUDE.md §12 "인터넷신문 등록 여부" 갱신, 2026-09-01 scribe)) | 미기재 | **지사 촬영 재개 자체.** 미계약이 지속되면 06 §E가 **촬영 보류 게이트**를 발동한다. ⚠️ **계약 체결(진행 중) ≠ 14항목 완료**(현재 1/14) |
| **B-3** | **웹접근성 법정의무 판정 의뢰** | **회신 2~4주** | 미회신 시 03 §A-1 접근성 토큰이 확정된 뒤 **소급 재작업**. **07 §3-11**이 지연 상한 2주 + 2단 분리 규칙을 준비해 뒀다(舊 "03이 준비" — scribe 정정, 2026-08-31: 원천은 07 §3-11, 03 §A-1 리스크 1은 **07 §3-11과 대칭**으로 이식된 사본이나 발동 트리거가 다르다 — 03은 어르신 패널 R1 완료율 <90%, 07은 접근성 법정 판정 회신 지연. `grep -n "07 §3-11과 대칭" docs/plan/03-accessibility-ux.md` → 1건 실재) |
| **B-4** | **스토어 개발자 계정** — ⚠️ **정정(2026-09-01, 규율 13 — 舊 "Apple 조직=D-U-N-S 선행" 표기 폐기)**: 사용자가 **개인사업자로 시작**하기로 결정 — Apple은 individual 계정(D-U-N-S 불요), 대신 Google Play 개인 계정은 **테스터 12명×14일** 클로즈드 테스트가 새로 필요(2023-11-13 이후 개인 계정 대상, 앱이 실재해야 시작). 근거: 07 §6 "스토어 계정 개설" 블록 | **舊 "W3 착수 8주 전" 폐기 → "W3 코드 완료 후 최소 14일"**(Google 개인 계정 트랙이 새 크리티컬 패스. Apple은 리드타임 소멸 — W3 착수 무렵이면 충분) | W3 전체 4건(T-W3-01·02·03·04)의 실질 차단자가 **Apple 조직 심사에서 Google 14일 트랙으로 이동**했다 — "코드가 끝나야 시계가 돈다"는 뜻이므로, **W3 코드 완료 직후 즉시 Google 클로즈드 테스트를 개시**하는 것이 새 요령이다 |

> **B는 코드 진행과 무관하게 병렬로 굴러간다.** 지금 착수해도 0~2단계 어느 행도 막지 않는다.
> 다만 **시작하지 않으면 4단계(MVP 카톡 송출)와 W3에서 그대로 벽이 된다.**

### C. 0단계 진행 중 — 해당 행 직전에 요청

| # | 항목 | 요청 시점 | 막는 행 | 선택지 |
|---|---|---|---|---|
| **C-1** | **Quick Tunnel 고정 방식** | ✅ **충족(2026-09-01)** — 사용자가 도메인 `bapfull.com` 구입 + named tunnel `bapfull-xeon` 개통(ⓐ 채택). scribe 재현: `curl -s -o /dev/null -w "%{http_code}" https://watch.bapfull.com` → 200 | 舊 **0-1**(해소·제거됨 — PIVOT-PLAN 대장 #179·#187 참조) | ⓐ named tunnel(무료·~~도메인 불요~~·고정 호스트, **권장**) ⓑ 로그에서 호스트를 읽어 재기입하는 스크립트 — ⚠️ **사실 정정(2026-09-01, 규율 13 — 원문 보존)**: "도메인 불요"는 틀렸다. named tunnel도 Public Hostname 라우팅에 **도메인이 필요**했고, 실제로 사용자가 `bapfull.com`을 구입해야 했다. **C-1과 D-3(도메인 확정)은 사실상 하나의 항목이었다** — named tunnel을 고르는 순간 도메인 확정이 동반 필수였다 |
| **C-2** | **Actions vars 교정** | ✅ **충족(2026-09-01)** — `gh variable list` 재확인: `WEB_EXPO_PUBLIC_SUBSCRIBER_WEB_URL` = `https://watch.bapfull.com`(placeholder 아닌 실값) | 舊 **0-2**(부분 해소 — PIVOT-PLAN 대장 #172 참조. 같은 행의 #189는 미해소 잔존, 0-1로 이관) | `WEB_EXPO_PUBLIC_SUBSCRIBER_WEB_URL` 실값 교체 **또는 삭제**(삭제 시 경로 표시로 정직 강등) — 실값 교체로 해소 |
| **C-3** | **제온 `DROP COLUMN` 마이그레이션 승인** | ✅ **충족(2026-09-02)** — 사용자가 승인했고, 조율자가 SSH로 `docker compose pull + up -d --no-build api ai-worker media-worker`를 실행하자 api 엔트리포인트의 `RUN_MIGRATIONS:-true`가 부팅 전 `prisma migrate deploy`를 자동 실행해 **적용까지 완료됐다**(PIVOT-PLAN 대장 #170 참조). 재현은 **ⓗ 명시적 수동**(제온 SSH 필요, scribe 재현 불가): `cd ~/gachinol/infra/docker && docker compose -f docker-compose.prod.yml -f docker-compose.xeon.yml pull api ai-worker media-worker && docker compose -f docker-compose.prod.yml -f docker-compose.xeon.yml up -d --no-build api ai-worker media-worker`. scribe 독립 재현(인터넷 경로, SSH 아님): `curl -s https://api.bapfull.com/v1/health/version` → SHA가 main HEAD와 일치(대장 #170 참조) | **0단계 완주**(舊 0-1 — 대장 #170이 이것으로 해소·행 제거됐다) | **적용됨.** `contents.minor_consent_confirmed_at`·`..._by_user_id` 두 컬럼 **drop 완료**(비가역). T-W2-36이 의도적으로 버린 데이터라 손실은 설계된 것이었고, 적용 전 실측으로 해당 두 컬럼 NOT NULL 손실 0건 확인 + 복원점(`gachinol-20260902-040924.sql.gz`) 확보 후 진행됐다 |

> **舊 0-1(#186)·舊 0-1(#182)은 사용자 실행이 필요 없었다** — A-1(머지)만 되면 바로 갔다.
> ⚠️ **이 문장은 2026-09-01부로 stale이다(규율 13, 이력 보존)**: 舊 0-1(#182, daejang-recheck
> 자기검사)이 해소·제거되면서 **지금의 0-1은 Quick Tunnel 고정**(舊 0-2)이 됐고, 이 항목은
> **C-1(사용자 실행)이 선행**이다 — 즉 지금의 0-1은 사용자 실행이 필요 없기는커녕 **그것을 요청하는
> 것 자체가 0-1의 착수 조건**이다(위 C-1 "도달함" 참조).
> ⚠️ **이 절 전체가 2026-09-01 재차 stale이다(규율 13, 이력 보존)**: 사용자가 C-1·C-2를 모두
> 실행해 舊 0-1(#179·#187)·舊 0-2 중 #172가 해소됐다. **C-1 선택지 칸의 "도메인 불요"는 사실
> 오류였다** — named tunnel도 Cloudflare Zero Trust Public Hostname 라우팅에 도메인이 필요했고,
> 사용자가 실제로 `bapfull.com`을 구입했다. **결과적으로 C-1(터널 방식 결정)과 D-3(도메인 확정,
> 아래)은 별개 항목이 아니라 사실상 하나였다** — ⓐ named tunnel을 채택하는 순간 도메인 확정이
> 자동으로 동반 필수가 됐다. 지금의 0-1(#189)·0-2(#170)는 이 절의 사용자 실행과 무관(코드/승인
> 게이트)하다 — 표는 위로 갱신 완료.

### D. 그 이후

| # | 항목 | 요청 시점 | 막는 행 |
|---|---|---|---|
| **D-1** | **GitHub Environment 승인 게이트 설정**(required reviewer) | ✅ **충족(2026-09-02)** — **사용자 실행 몫**: GitHub Environment `production` 생성(required reviewer `HomeDCP` 1명·배포 브랜치 `main` 한정) + "Allow administrators to bypass configured protection rules" 토글 **끔**. scribe 재현(읽기전용 API): `gh api repos/HomeDCP/gachinol/environments/production --jq '{can_admins_bypass,prevent_self_review,reviewers:[.protection_rules[].reviewers[]?.reviewer.login]}'` → `can_admins_bypass:`**`false`**(관리자 우회 차단 확정)·`prevent_self_review:false`(의도된 상태 — 켜면 혼자 쓰는 리포에서 아무도 배포 못 한다)·`reviewers:["HomeDCP"]` · `gh api repos/HomeDCP/gachinol/environments/production/deployment-branch-policies --jq '.branch_policies[].name'` → **`main`** 1건. **코드 몫**: `.github/workflows/deploy-web.yml`의 `deploy` 잡에 `environment: production` 배선 완료(조율자 PyYAML 파싱 인용: `deploy.environment='production'`·`preflight.if="github.event_name != 'pull_request'"`이라 PR마다 승인 요청이 쌓이지 않음·`purge.needs`에 `deploy` 포함이라 별도 게이트 불요). ⚠️ **이 배선은 아직 미커밋**이다(워크트리, 브랜치 `chore/deploy-approval-gate`) — scribe 재현 `git show origin/main:.github/workflows/deploy-web.yml \| grep -c "environment:"` → **0**(origin/main 기준, 2026-09-02, 커밋·PR·머지 대기). ⚠️ **승인 대기가 실제로 걸리는지는 다음 main 머지에서만 확인된다** — 이 문서는 그것을 검증된 것으로 적지 않는다 | **1-1**(api·워커 자동 배포) — ⚠️ **판단(2026-09-02 갱신)**: 사용자 실행·코드 배선 두 몫 모두 **내용상 완료**돼 **1-1 착수 차단은 해제**로 본다(설정·YAML 패턴 둘 다 확정됐고 남은 것은 커밋·머지라는 절차뿐, 설계·결정 공백이 아니다). 단 **권고 조건**: 1-1이 복제할 원본 패턴이 `chore/deploy-approval-gate`에 있으므로 **그 브랜치가 main에 병합된 뒤 1-1 PR을 열 것**(미병합 상태를 베끼면 다시 갈라진다) — 이 조건은 차단이 아니라 순서 권고다. ⚠️ **이 게이트 없이 api 자동 배포를 켜면 로컬 push와 프로덕션 사이에 아무 장치가 없는 상태로 사고 반경이 커진다**(DISCIPLINES §24-0). ⚠️ **후속(2026-09-03)**: 舊 1-1 자체가 PR #91·#92로 해소 완료됐다 — 이 차단 관계는 이제 이력이며, **지금의 1단계 1-1은 다른 태스크(배포 후 스모크)**를 가리킨다(1단계 표 참조). ⚠️ **재후속(2026-09-05, 브랜치 `feat/deploy-smoke-migration-gate` 커밋 `41d8fec`, main 미병합)**: 그 배포 후 스모크도 해소돼 행이 제거됐다 — **지금의 1단계 1-1은 가용성 감시 신설**을 가리킨다(1단계 표·PIVOT-PLAN 대장 #180 정정 항목 참조) |
| **D-2** | **카카오 실 송출 범위 결정** | **2단계 완료 시** | **3-1 → 4-1**. 게시자산 준비만 / YouTube 어댑터까지 / 전체 |
| **D-3** | **도메인 확정**(G9 ①) | ✅ **충족(2026-09-01)** — 사용자가 `bapfull.com` 구입(C-1과 사실상 동일 항목이었다 — 위 각주 참조) | Tunnel 정규화(해소·#179·#187 참조) · presign 도달성(#169, 미해소 잔존) · CF 퍼지(deploy-web.yml purge 잡 첫 성공 2026-08-31 21:50 확인) |

---

## 0단계 — 관측 복구 (A-1 머지 후)

지금 실측이 오염돼 있어 **무엇을 고쳐도 확인할 수 없다.** 수리가 아니라 계측기 복구다.

> **0-1(舊 대장 #186 — 빌드 스탬프 + 배포 후 SHA 대조)은 2026-08-31 해소돼 행을 제거했다**(커밋
> `e9f47f1`·PR #81, PIVOT-PLAN 대장 #186 참조). **舊 0-1(대장 #182 — daejang-recheck 자기검사·정정)도
> 2026-09-01 해소돼 행을 제거했다**(같은 슬라이스에서 대장 #191·#192 신규 채번·해소 동반 — PIVOT-PLAN
> 대장 #182·#191·#192 참조). **舊 0-1(대장 #179·#187 — 인터넷 경로 복구 + Quick Tunnel 재발 차단)도
> 2026-09-01 해소돼 행을 제거했다**(사용자가 도메인 `bapfull.com` 구입 + named tunnel `bapfull-xeon`
> 개통으로 ⓐ안 채택 — PIVOT-PLAN 대장 #179·#187 참조, scribe 인터넷 경유 재현: `curl -s -o /dev/null
> -w "%{http_code}" https://watch.bapfull.com` → 200). **舊 0-2(대장 #172·#189)의 #172(Actions vars
> placeholder)도 같은 날 해소돼**(`gh variable list` 재확인: `WEB_EXPO_PUBLIC_SUBSCRIBER_WEB_URL` =
> `https://watch.bapfull.com` 실값) 그 행에서 제목·차단자가 정리됐다(#189만 잔존). **舊 0-1(대장 #189 —
> URL 형태 검증)도 2026-09-02 해소돼 행을 제거했다**(PR #87 머지 — main 머지커밋 `3f8e873`, PIVOT-PLAN
> 대장 #189 참조). **그 뒤를 이은 0-1(대장 #170 — 제온 백엔드 풀스택 배포)도 같은 날(2026-09-02) 해소돼
> 행을 제거했다** — 사용자 승인(`DROP COLUMN` 마이그레이션 적용)을 받은 조율자가 SSH로 `docker compose
> pull + up -d --no-build api ai-worker media-worker`를 직접 실행(PIVOT-PLAN 대장 #170 참조). scribe
> 독립 재현(인터넷 경로, SSH 아님, 2026-09-02): `curl -s https://api.bapfull.com/v1/health/version` →
> `{"sha":"3f8e87316d991576c13e91570e90ffa9077eaa95"}`(`git log --oneline -1 origin/main` 최상단과 SHA
> 일치 — 배포본이 main HEAD와 같다).

### ⭐ 0단계 완주(2026-09-02)

**위 #170이 0단계에 마지막으로 남아 있던 행이었다 — 표가 이제 완전히 비었다.** 절 자체는 지우지
않는다(위 제거 이력이 "관측 계측기를 어떤 순서로 고쳤는가"의 근거이며, 규율 13대로 이력 보존).
**다음 착수 대상은 1단계 1-1(대장 #180 — api·워커 배포 경로 신설)이다.** 그 행의 차단자
"GitHub Environment 승인 게이트"는 **2026-09-02 해제됐다**(아래 §D-1 참조) — 사용자 실행 몫
(Environment `production` 생성·required reviewer·main 한정·admin bypass 끔)과 코드 몫(`deploy`
잡 `environment: production` 배선) 둘 다 내용상 완료됐다. **권고 조건(차단은 아니다)**: 배선이
아직 브랜치 `chore/deploy-approval-gate`에 미커밋 상태이므로, 1-1이 그 web 잡 패턴을 복제하려면
**그 브랜치가 main에 병합된 뒤 PR을 여는 편이 안전**하다(미병합 상태를 베끼면 다시 갈라진다) —
근거는 §D-1 "판단" 칸과 동일.

> **이력(0단계가 지나온 계측기 복구 순서, 규율 13)**: 舊 0-1(대장 #182 — daejang-recheck 자기검사)이
> 맨 앞에 있었던 이유는 *"대장 해소를 재는 계측기 자체가 고장나 있었다"*이고, 그게 고쳐진 뒤(#191·#192
> 동시 해소) 뒤이은 행들(#179·#187·#172·#189·#170)이 차례로 사용자 실행·코드 수리로 해소됐다.
> **#189의 소유 슬라이스가 0-1**이었다가(舊 라벨 기준 검증 전에는 0-2·0-4에 결박) PR #87 머지로
> 해소돼 **舊 0-2(#170)가 0-1로 당겨졌고**, 이번에 그마저 해소돼 **0단계 자체가 소진**됐다.

> **후속(2026-09-03)**: 위 문단이 가리켰던 1단계 1-1(대장 #180 — api·워커 배포 경로 신설)도 같은 날
> 해소됐다(PR #91·#92, 병합 커밋 `6651666` — 1단계 표 위 인용 블록·PIVOT-PLAN 대장 #180·#200 참조).
> **다음 착수 대상은 지금의 1단계 1-1(舊 1-2 — 배포 후 스모크)이다.** 위 §D-1 판단은 이제 전건
> 완료된 절차의 기록으로만 남는다.

> **후속(2026-09-05, 브랜치 `feat/deploy-smoke-migration-gate` 커밋 `41d8fec`)**: 위 문단이 가리켰던
> 그 1-1(배포 후 스모크)도 해소돼 행을 제거했다(1단계 표 위 인용 블록·PIVOT-PLAN 대장 #180 정정
> 항목 참조). **다음 착수 대상은 지금의 1단계 1-1(舊 1-2 — 가용성 감시 신설)이다.** ⚠️ 이 커밋은
> 아직 main 미병합 — 재현: `git log --oneline -1 origin/main`에 이 SHA 포함 여부.

## 1단계 — 도달 경로 자동화

0단계를 사람이 매번 하면 또 stale이 된다. **"고쳤는데 도달하지 않았다"가 하루에 3건 실측됐다.**

> **1-1(대장 #180 — api·워커 배포 경로 신설, web 잡 복제)은 2026-09-03 해소돼 행을 제거했다**
> (PR #91 경로 신설 + PR #92 상호 되돌림 수리, 병합 커밋 `6651666` — PIVOT-PLAN 대장 #180 참조).
> 최종 실배포 확인(scribe 재현): `curl -s https://api.bapfull.com/health/version` →
> `{"sha":"665166691ae0d998e82f841978f1f7cb186d60d2"}`. **완주 과정에서 신규 결함이 실발생·같은 날
> 해소됐다**(대장 #200 — 두 배포 워크플로가 서로의 배포를 되돌림, PR #92·커밋 `63b7a05`). 규율 2 검사
> (`infra/scripts/dockerfile-workspace-deps.mjs`, 동반 의무 D1)도 같은 슬라이스(PR #91)에서 완료됐다
> — 3단계 3-5의 "규율 2는 1-1에 동반으로 이미 편입" 문구가 그대로 실현됐다. **舊 1-2(배포 후
> 스모크)가 1-1로, 舊 1-3(가용성 감시)이 1-2로 당겨진다.**

> **후속(2026-09-05, 브랜치 `feat/deploy-smoke-migration-gate` 커밋 `41d8fec`) — 위 문단이 가리켰던
> 그 1-1(배포 후 스모크) 자신도 해소돼 행을 제거한다.** 처방 이행: `infra/scripts/deploy-smoke.mjs`
> (배포된 api에 미인증 요청을 던져 `≠404`를 실재 판정 기준으로 삼음 — **없는 경로 음성 대조가 판정의
> 전제**, catch-all 서버에서는 판정 불능 exit 1)를 `build-images.yml` deploy 잡에 배선 +
> `infra/scripts/check-destructive-migrations.mjs`(파괴적 구문 9종을 오프셋 기반 전역 스캔으로 탐지 →
> preflight red, 마이그레이션 디렉터리 안 승인 마커로 해제, 사용자 채택 ⓐ안 — *"red로 세우고 사람이
> 명시적으로 푼다"*)를 `build-images.yml` preflight·`ci.yml` PR 게이트에 배선(PIVOT-PLAN 대장 #180
> 정정 항목 참조). 재현: `node infra/scripts/check-destructive-migrations.mjs` → PASS exit 0 ·
> `pnpm run test:scripts` → 전건 pass(신설 스위트 2개 포함, 舊 121). ⚠️ **아직 main 미병합** — 병합
> 확인은 `git log --oneline -1 origin/main`에 이 커밋 SHA 포함 여부로 재현하고, 러너→제온 실도달·
> deploy 잡에서의 실제 스모크 실행·마이그레이션 게이트의 실전 red 발동은 그 머지 이후 실배포에서만
> 검증된다. **舊 1-2(가용성 감시)가 1-1로 당겨진다.** 아래 각주의 "1-1(배포 후 스모크)의 원리적
> 한계"는 이제 **완료·제거된 舊 1-1을 가리키는 이력**이지만, 그 한계가 지목한 사각(배포 직후 1회
> 판정은 그 이후의 되돌림을 원리적으로 못 잡는다)은 **여전히 유효**하며 소실시키지 않는다 —
> 지금의 1-1(가용성 감시)이 정확히 그 사각을 메우는 층이라는 근거로 아래 각주에 남긴다.

| 순 | ID | 제목 | L | 선행 | 동반 의무 | 차단자 |
|---|---|---|---|---|---|---|
| 1-1 | (미채번) | **가용성 감시 신설** — 인터넷 경로·배포 신선도·api 헬스. ⚠️ 대장 #72의 경보 7종과 **다른 것**이다. **舊 1-1(배포 후 스모크, 2026-09-05 해소·제거)의 사각을 지속 감시로 덮는 층(아래 각주)** | L1 | 없음(舊 0-1 대상 — Quick Tunnel 재발 차단 — 이 2026-09-01 해소로 충족. 이력: #186 제거로 0-2였다가 #182 제거로 0-1이 됐고, **2026-09-01 그 0-1 자체(대장 #179·#187)가 named tunnel 전환으로 해소·제거**돼 지금의 "0-1"은 물리적으로 다른 태스크(#189)를 가리킨다 — 그래서 번호 참조 대신 충족 여부로 기재한다) | 채번 선행(규율 24-1) | — |

> ⚠️ **검증이 잡은 오귀속**: 검증 전 지금의 1-1(番호 변경 이력: 舊 1-3 → 舊 1-2 → 지금 1-1)은
> *"대장 #72 모니터링 최소 구현"*이었다. 그런데 실측하니
> `infra/monitoring/uptime-kuma-alerts.json`의 경보 7종은 **전부 `security-*`(4)·`resource-*`(2)·
> `evidence-*`(1)**로 **가용성 감시가 하나도 없다.** 즉 **7종을 전부 구현해도 0단계의 502·스테일
> 이미지·placeholder 중 아무것도 알려주지 않는다.** ROOT-CAUSE가 "알려줄 주체가 없다"의 근거로
> 이 7종을 든 것은 잘못이었다(그 문서도 정정했다). #72(보안·자원 경보)는 **백로그**로 내린다.
>
> ⭐ **舊 1-1(배포 후 스모크, 2026-09-05 해소·제거)의 원리적 한계 — 2026-09-03 대장 #200 실측 근거**:
> 배포 경로를 켠 첫 실배포가 실물로 증명했다. `build-images.yml` 배포 잡의 SHA 대조는 `15:18:16`에
> **정확히 통과**했다(그 시점엔 api도 실제로 최신이었다) — 그런데 2분 뒤(`15:20:07`) 별개 워크플로
> (`deploy-web.yml`)가 `--no-deps` 없이 `up web`을 돌리며 api를 옛 `latest`로 되돌렸다. **"배포 직후"
> 검증은 그 순간의 참을 재는 것이지, 그 이후 다른 프로세스가 되돌리는 것을 원리적으로 못 잡는다** —
> 스모크를 아무리 정교하게 짜도 이 사각은 스모크 자체의 구조("배포 직후 1회 판정")에서 나온다.
> ⚠️ **이 한계는 舊 1-1 해소(2026-09-05)로도 해소되지 않는다** — 스모크 자체가 완성돼도 이 구조적
> 사각은 그대로 남고, 그래서 지속 감시(지금의 1-1)가 별도로 필요하다.
>
> ⭐ **지금의 1-1(가용성 감시)이 그 사각을 덮는 층인 이유**: 舊 1-1(스모크)은 배포 그 순간의 상태만
> 1회 재므로 원리적으로 "그 이후의 되돌림"을 못 잡는다(위 각주). **지속 감시**(지금의 1-1, 배포
> 신선도·api 헬스를 주기적으로 재확인하는 축)만이 배포 이후 벌어지는 이탈(대장 #200 같은 상호
> 되돌림·수동 되돌림·캐시 폴백 재발 등)을 잡을 수 있다 — 순간 검증(舊 1-1)과 지속 감시(지금의 1-1)는
> 서로를 대체하지 않는 **다른 층**이었고, 순간 검증 쪽이 먼저 완료된 지금도 그 관계는 변하지 않는다.
>
> ⚠️ **순서 판단 이력(조율자 판정, 2026-09-03) — 지금은 이미 지난 비교다**: 당시 舊 1-1(배포 후
> 스모크)·舊 1-2(가용성 감시 신설) 둘 다 L1이었고, 사다리상 "같은 L 안에서는 규모 작은 순"인데
> 스모크가 더 작아 순서를 유지했다("오늘 실측은 한계를 드러냈을 뿐 순서를 뒤집을 근거로는 부족하다").
> 舊 1-1이 2026-09-05 해소돼 지금은 1단계에 가용성 감시 하나만 남아 이 비교 자체가 이력이 됐다 —
> **다음 착수 여부·순서는 여전히 조율자 판단 몫**이며 scribe는 이 갱신에서 순서를 바꾸지 않았다.

## 2단계 — L2 결함 수리 + 검사 범위 확장

사다리상 **L2는 L3·L5보다 무조건 앞이다.** 그리고 각 결함이 "검사 밖이라서 생긴 것"이면
그 검사 축을 **같은 슬라이스에서** 넓힌다(D1).

> **舊 2-6(대장 #194 — web 이미지 폴백 정정)은 2026-09-02 해소돼 행을 제거했다**(커밋 `0fc1949`,
> PIVOT-PLAN 대장 #194 참조). ⚠️ **정정(규율 13, 원문 보존 — 아래는 舊 2-6 처방 칸에 있던 문구)**:
> 이 행이 제시한 처방 문언 *"`WEB_IMAGE:-gachinol-prod-web` → `${REGISTRY:-ghcr.io/homedcp}/gachinol-web:${IMAGE_TAG:-latest}`
> 형태"*(즉 `WEB_IMAGE` 참조 자체를 없애는 단순 대체안)는 **부정확했다** — 실제 구현은
> `${WEB_IMAGE:-${REGISTRY:-ghcr.io/homedcp}/gachinol-web:${IMAGE_TAG:-latest}}`(중첩 폴백, `WEB_IMAGE`
> 참조는 유지)였다. `deploy-web.yml:90`이 `WEB_IMAGE`에 **전체 이미지 참조**(정밀 SHA 태그)를 주입하므로
> 그 참조를 없애면 CI 배포가 상시 `latest`만 당기게 돼 SHA 대조(대장 #186)의 정밀성이 무너진다 —
> QUEUE가 "무엇을 고칠지"는 맞혔지만 "어떻게"는 틀렸던 사례.

| 순 | ID | 제목 | L | 선행 | 동반 의무 | 차단자 |
|---|---|---|---|---|---|---|
| 2-1 | 대장 #168 | **upload-complete 복구 원자성 + 교착 회귀 테스트** — 실기 2건 실발생. 수리 선례가 리포에 실재(`resident-reviews.service.ts:286`)해 저위험·최소 규모 | **L2** | 없음(舊 0-4는 *전달*의 선행이지 수리의 선행이 아니다 — **2026-09-01 정정**: 舊 0-1(#182) 제거로 그 태스크(제온 백엔드 풀스택 배포)는 지금 0-3이나, 이 행의 논지는 번호와 무관하다) | — | — |
| 2-2 | 대장 #181 | **지사 조회 2종 역할 게이트** — 같은 컨트롤러의 쓰기 3종엔 있고 읽기 2종만 없다(누락 명확). 데코레이터 2줄 | **L2** | 없음 | — | — |
| 2-3 | 대장 #173·#188 | **기자 웹 촬영(`<input capture>`) + `app/**` 로직 금지 린트** — D1 적용례. 지금 유령 영상을 만든다 | **L2** | 없음 | **린트 동반 필수** — 린트 없이 고치면 T-W2-02와 같은 재발 | — |
| 2-4 | 대장 #178 | **`.web.*` 로드 경로 확보** — ⚠️ 2-3의 린트로는 **원리적으로 못 닫는다**(#188 본문이 *"#178은 폐기하지 않는다"*고 명시). 해소 판정이 *"`.web.*` 최소 1개가 테스트에서 실제 로드됨"*이라 별도 행이다 | **L2** | 2-3 | — | — |
| 2-5 | 대장 #177 | **전이맵 `to`-상태 grep 테스트** — 추천 `publishing` + WS 6종을 동시에 | **L2** | 없음 | 계약-구동 게이트를 Content 전이맵 밖으로 (enum 8종·shared export는 **백로그**) | — |

## 3단계 — 정본 재정합

4단계에서 만들 것들이 정본에 **자리가 없다**. 규율 13 + 사용자 원칙("정본 순서부터 확인").

| 순 | ID | 제목 | L | 선행 | 차단자 |
|---|---|---|---|---|---|
| 3-1 | 대장 #184 | Distribute 실 송출 태스크 신설(E2) + `PIVOT-PLAN:18` "바뀌지 않는 것" 문언 정정 | L3 | — | **범위 결정 → 사용자** |
| 3-2 | 대장 #183 | **auto_edit Phase 2·3 범위 정의**를 정본에 등재(리포 밖 설계를 안으로) | L3 | — | — |
| 3-3 | (미채번) | STT 로컬 전환을 정본 6곳에 반영 — **07 국외이전 자문이 틀린 전제(RTZR)로 나갈 참**이다 | L3 | — | 채번 선행 |
| 3-4 | (미채번) | **완성 기준 3중 불일치 해소** — 기술 런칭·대외 런칭·제품 완성이 서로 다른 종착점인데 어느 것도 다른 것을 함의하지 않는다(ROOT-CAUSE §4-1) | L3 | — | 채번 선행 |
| 3-5 | — | **규율 기계화** — DISCIPLINES "검사 방법" 칸의 *"기계화 가능 — 미구현"* 잔여 4건(규율 1·6·11·16). 규율 2는 舊 1-1(대장 #180)에 동반 편입돼 **2026-09-03 구현 완료**(`infra/scripts/dockerfile-workspace-deps.mjs`, DISCIPLINES §규율2 참조) — 남은 대상에서 제외 | L3 | — | — |

> **3-4·3-5가 검증에서 추가됐다.** ROOT-CAUSE가 §4-1(완성 기준 불일치)과 §6(*"산문 규율은 지켜지지
> 않았다 — 이 문서가 하네스 개편을 요구하는 근거"*)을 **독립 절로 세워 놓고 QUEUE에는 한 줄도 없었다.**

## 4단계 — MVP 한 바퀴 실성립

0~2단계 없이 만들면 도달하지 않거나 곧 깨진다.

| 순 | ID | 제목 | L | 선행 | 동반 의무 | 차단자 |
|---|---|---|---|---|---|---|
| 4-1 | 대장 #175·#176 | **카카오 게시자산 준비 경로 + YouTube 어댑터** — MVP 정의의 가운데 토막 | L3 | 3-1(채번 — **기능 의존이 아니므로 같은 슬라이스로 병합 가능**) | #174(실 어댑터가 무조건 throw) | 범위 결정 |
| 4-2 | (미채번) | **시드 콘텐츠 6건 확보** — 08 앵커4가 대외 런칭의 조건으로 못박았다. 4단계가 MVP 바퀴를 만들면서 **그 바퀴를 6번 돌려 얻는 산출물**에 자리가 없었다 | L3 | 4-1 | 채번 선행 | 지사 촬영 재개(병렬 트랙) |

> **#136(주민 제보 제목·분류 수정)은 백로그로 내렸다** — MVP 정의(기자 업로드 → 카톡송출 → 구독자
> 시청)에 주민 제보는 없으므로 L3가 아니라 L5다. 대장 #136이 지정한 착수 트리거는
> *"주민 링크 실사용 시"*이며, ⚠️ **인터넷 경로 자체는 2026-09-01 복구됐다**(舊 0-1, 대장 #179·#187
> 해소 — named tunnel + `bapfull.com`). 다만 트리거는 "복구"가 아니라 "**실사용**"이라 경로 복구만으로
> 자동 도달하지 않는다 — 실사용 여부는 판정 대상(조율자)으로 남긴다.

## 백로그 (L5 — 순서표가 아니라 여기 둔다)

순서표는 **"지금 고를 수 있는 것"**만 담는다. L5가 섞이면 다음 한 줄을 고르기 어려워지고,
그때 순서표가 죽는다(ROADMAP이 89행으로 죽은 진짜 이유).

| ID | 항목 | 착수 트리거 |
|---|---|---|
| 대장 #72 | Uptime Kuma 보안·자원 경보 7종 구현 | 가용성 감시(지금의 1-1, 番호 변경 이력: 舊 1-3→舊 1-2→지금 1-1) 이후 |
| 대장 #136 | 주민 제보 제목·분류 수정 액터 | **주민 링크 실사용 시** |
| 대장 #171 | 상대경로 번들에서 `io('')` 해석 실측 | ⚠️ **트리거 도달(2026-09-01)** — 인터넷 경로(舊 0-1, 대장 #179·#187)가 named tunnel + `bapfull.com`으로 복구됐다. 백로그→순서표 편입 여부는 조율자 판정 대상(scribe는 등재만) |
| — | e2e를 CI에 편입 | — |
| — | E2 인벤토리 복구(신설분이 분모 밖) | — |
| — | 계약-구동 게이트를 enum 값·shared export까지 확장 | 2-5 이후 |
| 대장 #190 | `apps/reporter` 웹 번들의 jsDelivr 무조건 로드(QR 스캐너, env 게이트 없음) — **L2 아님으로 판정**(scribe, 아래 근거) | **07 §3-10 국외이전 해석 확정 + QR 스캔 기능 실사용 여부 확인 후**(택1: 인라인 번들·자체 프록시·기능 제거) |

> **#190 사다리 판정 근거(scribe, 2026-08-31)**: L1 아님(도달 경로가 끊기지 않는다 — CDN이 막혀도
> 앱 렌더링 자체는 죽지 않고, QR 미사용자는 영향을 못 느낀다). L2는 후보였으나 **기각** — L2 예시
> #168은 "특정 조건에서 발생하는 malfunction"인데 #190은 malfunction이 아니라 **매 로드마다 조용히
> 성공하는 정상 동작**(공급망·개인정보 **리스크**이지 사용자가 체감하는 오류가 아니다). L3(MVP 조각)도
> 아니다 — QR 스캔은 MVP 정의(기자 업로드→카톡송출→구독자 시청)에 없다. 오히려 **#72(보안 경보)·
> #136(주민 제보)와 동형** — 착수 조건이 **코드가 아니라 외부 판단**(07 국외이전 해석은 사용자 확인
> 대기 중, 기능 실사용 여부도 미확정)이라 지금 고를 수 있는 항목이 아니다 → **백로그**. ⚠️ 이 판정은
> scribe 개인 판단이며 최종 사다리 배치는 조율자 승인 사항이다(절대규칙 "판정하지 않는다"의 예외는
> 이 위임문이 명시적으로 부여한 것에 한정).

## 병렬 트랙 — 0단계부터 상시 (코드가 못 여는 축)

| ID | 항목 | 리드타임 | 막는 것 |
|---|---|---|---|
| 대장 #185 | **사업자등록** | 미기재 | 스토어 계정 · **카카오 비즈니스 인증 → 카카오 실 송출** · YouTube 수익화 · 통신판매업 · PG |
| 대장 #185 | **법률자문 계약** | 미기재 | 계약 자체는 전제일 뿐이다 |
| 대장 #185 | **07 §6 "첫 촬영 재개 전" 14항목 완료**(舊 "13항목" — scribe 정정, 2026-08-31: 07 체크박스 직접 재계수 **14**, 근거는 B-2 행 각주) | — | **지사 촬영 재개 자체.** ⚠️ **계약 체결 ≠ 14항목 완료** — 검증 지적대로 계약만 하고 "해소"로 넘어가면 규율 17이 경계한 *"너무 좁아 오해소"*의 변종이 된다 |
| — | **웹접근성 법정의무 판정**(장차법·KWCAG 적용 대상 여부) | **회신 2~4주** | 미회신 시 03 §A-1 토큰 **소급 재작업**. **07 §3-11**이 지연 상한 2주 + 2단 분리 규칙을 준비해 뒀다(舊 "03이 준비" — scribe 정정, 2026-08-31, 근거는 B-3 행 각주). ⚠️ 이 행은 ROOT-CAUSE §5 표(4행)를 QUEUE 병렬 트랙(4행)으로 옮기며 **행 수가 같아 눈에 띄지 않게 유실**됐다 — 검증에서 복원 |
| T-NC-08 | 스토어 계정(Apple 조직 = D-U-N-S 선행) — ⚠️ **정정(2026-09-01, 규율 13 — B-4 행과 동일 값)**: 사용자가 개인사업자로 시작 — Apple은 individual 계정(D-U-N-S 불요), 대신 Google Play 개인 계정은 테스터 12명×14일 클로즈드 테스트가 새로 필요. 근거: 07 §6 "스토어 계정 개설" 블록 | **舊 "W3 착수 8주 전" 폐기 → "W3 코드 완료 후 최소 14일"**(Google 개인 계정 트랙이 새 크리티컬 패스, Apple은 W3 착수 무렵이면 충분) | W3 전체 4건 |
| G9 ① | 도메인 확정 | — | Tunnel 정규화 · presign 도달성(#169) |

---

## 부록 A — 살아남은 의존 엣지 (E3에서 이식, 2026-08-31)

E3(`E3-parallel-schedule.md`)은 2026-08-31에 `archive/`로 이동했다. **웨이브 편성표는 폐기**하되
(웨이브 체계는 Wave 8a에서 실행상 이미 폐기됐고, 자기 검산 앵커가 46 vs 48로 깨진다),
**아직 유효한 의존 엣지만** 여기로 옮긴다. 완료된 태스크가 선행인 엣지는 제외했다.

| 엣지 | 성격 |
|---|---|
| `T-W1-01 ─ T-W2-05, T-W2-06` | 토큰 소비 — **선행 충족**(`packages/ui` 실구동 확인) |
| `T-W1-07a ─ T-W1-11a` | 구독자 E2E 4단계 마지막이 "자막 토글"이라 그 구현이 선행 |
| `T-W2-18 ─ T-W2-05` | 같은 `contents/new/scenes.tsx` 공유 — **순서 분리이지 기능 의존이 아니다** |
| `T-W1-07b ─ T-W2-05` | 같은 `contents/new/classify.tsx` 공유 — 순서 분리 |
| `T-W2-04, T-W2-16b ─ T-W2-06` | 같은 `live/[id].tsx` 공유 — **순서 분리.** 16b가 지출로 막혀 있어도 T-W2-06은 막히지 않는다(그 파일만 마지막에 편집) |
| `T-W2-01~16b(전건) ─ T-W2-07` | Playwright는 기능 완결 후 — **그 안에 착수 금지 4건이 있어 T-W2-07은 현재 착수 불가** |
| `T-W2-07 ─ T-W2-17` | CI 잡이 실행할 스펙이 먼저 있어야 한다 |
| `T-W1-04, T-W1-11a ─ T-W1-11b` | 번들예산·Lighthouse 게이트는 클라이언트측 완료 후 |
| `T-W3-01 ─ T-W3-02, T-W3-03, T-W3-04` | 쉘 토큰수신 API 선행 — **T-W3-01 자신이 T-NC-08(리드타임 8주)에 막혀 있다** |
| `T-W2-15 ─ T-W2-16a ─ T-W2-16b` | CF Stream 순차 — **T-NC-20(지출)에 막힘** |
| `T-NC-03(PoC) ─ T-W2-03` | 실기기 18시행, 사람 작업 |

⚠️ **엣지의 성격을 구분하라** — "기능 의존"(선행이 없으면 동작하지 않음)과 "순서 분리"(같은 파일
중복 편집 회피)는 다르다. **후자는 차단이 아니다.** 이 구분을 놓쳐 T-W2-06을 막힌 것으로 오판할 뻔했다.

## 부록 B — 모델·에포트 배분 (E4에서 이식)

| 작업 성격 | 모델 | 담당 롤 |
|---|---|---|
| 구현(코드)·문서 작성·정형 검증 | **sonnet** | `implementer`·`verifier`·`scribe`·`wiring-auditor`·`ops-prober` |
| 설계 판단(아키텍처 트레이드오프)·계획 | **opus** | `planner` |
| 조율·횡단 결정 | 조율자 본체(모델 무관) | — |

**상향 규칙**: 같은 태스크가 게이트②에서 **2회 연속 FAIL**하고 사유가 설계 오류면 opus로 재위임한다
(단순 오타·회귀 누락은 sonnet 유지 — 모델 문제가 아니다).
⚠️ 이 정책은 E4에 **이미 문서로 있었는데 배선이 0이었다**(에이전트 정의에 `model:`이 없어
전역 `opus[1m]`을 상속했다). **정본에 쓰는 것과 배선하는 것은 다르다** — 이번에 `.claude/agents/*.md`
프론트매터로 실제 배선했다.

---

## 현재 상태를 아는 법 (값을 적지 않는 이유)

```bash
git fetch origin main && git log --oneline -1 origin/main   # 기준 커밋
gh pr list --state open                                     # 열린 PR
node infra/scripts/daejang-recheck.mjs                      # 대장 대조(pending) — 상태 칸 "미수신"으로 시작하는 행만 대상
node infra/scripts/daejang-recheck.mjs --all                # 대장 대조(감사) — 상태 칸 보유 표 전건 대상
#  ⚠️ 대장 #182 해소(2026-09-01)로 "대장 아닌 표까지 함께 센다"는 舊 결함은 수리됐다 — **다만 "믿어도
#  된다"로 뒤집지 않는다.** pending 모드는 표 무결성·행 추출이 §6-1~§6-11 전 구간(259행, §6-11의 물리
#  분절 표 포함)을 정확히 잡지만, **판정 자체는 여전히 대장 원문의 재현 명령·기대값 정확성에 의존한다.**
#  `--all`(감사 모드, 상태 칸 무관 전건)은 **이미 해소된 행도 함께 재는데**, 그런 행은 등재 당시 기대값과
#  현재값이 다른 게 정상(해소했으니 값이 바뀌었다)이라 "어긋남"으로 뜰 수 있다 — 그건 이 도구의 결함이
#  아니라 감사 모드의 설계된 동작이다(해소 후 값 변화를 잡아내는 것이 감사의 목적). 요약: **행 추출·표
#  무결성은 신뢰 가능. 판정(일치/어긋남/수동)은 여전히 사람이 대장 맥락과 함께 읽어야 한다.**
pnpm --filter @gachinol/shared build && pnpm --filter @gachinol/api test   # api 기준선
```

**진행률 판정**은 구현 커밋(`feat|fix|refactor|perf`)에 태스크 ID가 등장하는지로 한다.
⚠️ 오검출을 뺀 뒤에 판정한다 — **원값 불변이 진척 없음을 뜻하지 않는다**(오검출로 이미 매치되던
태스크를 실제로 구현하면 원값이 안 움직인다).
