# E3. 병렬 스케줄 — 의존 그래프·웨이브 편성

> 근거: [EXEC-PLAN.md](EXEC-PLAN.md) G1~G9, [reviews/EXEC-RUBRIC.md](reviews/EXEC-RUBRIC.md) 영역 4,
> [08-rollout-transition.md](../08-rollout-transition.md) §A(선행조건·시간축 매핑 소비). **범위**: 본 문서는
> "언제·어떤 순서로·몇 개씩 동시에"만 정의한다 — 태스크 목록·크기·산출물·DoD는 [E2](E2-work-breakdown.md)를
> 재정의하지 않고 그대로 소비한다(신규 범위 발명 금지). 절대 시간(며칠·몇 주)은 다루지 않는다 — 08§A 자체가
> "기간 산정은 실행계획으로 이월하되 순서는 확정한다"고 선언했고, 절대 토큰·시간 예산은 [E4](E4-token-budget.md)
> 소유다. 본 문서는 **선후 관계·동시성 상한·파일 소유권 배타**만 소유한다.

## A. 의존 그래프 — 08§A 선행조건·시간축 매핑 소비

**W-단계 간 선후 관계(08§A "전환 단계" 표 그대로 인용)**:

- W1 선행조건: "W0 + `packages/ui` 디자인 토큰 승격 완료 ... + `<input capture>` 대용량 업로드 실기기 PoC 완료"
- W2 선행조건: **"W0 (W1과 병렬 가능)"** — 08§A "병렬성" 절 "W1과 W2는 서로 독립(구독자=무인증, 기자·관제=인증
  스택 공유). W2 내부에서 기자·관제도 병렬 가능(인증 어댑터를 공용 패키지로 먼저 추출)"을 그대로 반영해, 아래
  §C 웨이브 편성은 W1·W2 태스크를 **분리된 두 순차 블록이 아니라 상호 의존이 없는 것부터 같은 웨이브군에 교차
  배치**한다(단, `packages/ui` 스키마(T-W1-01)에 의존하는 W2 토큰 소비 태스크(T-W2-05·06)는 예외 — 아래 그래프 참조)
- W3 선행조건: "W1(구독자 웹 안정) + 스토어 개발자 계정 개설 완료(... W3 착수 8주 전 개시)"
- W4 선행조건: "W1~W3"

**W0 DoD의 범위 한정(웨이브 효율화 근거)**: 08§A W0 행의 DoD는 **"`https://api.<도메인>/health` 외부 도달 + 웹
오리진에서 로그인 왕복"**뿐이다 — 08§E-3(백업 파이프라인)·3-2(보안 모니터링)는 §E 실행체크리스트의 W0-시한
항목이지만 이 DoD 문장에는 포함되지 않는다. 또한 08§D 리스크 테이블 "방화벽/터널 승인 지연" 행이 "승인 전에는
로컬 LAN에서 W1 기술 검증 선행"을 완화책으로 명시한다 — 즉 **로컬/코드 준비 작업은 G9 사용자 확인(도메인·제온
노출방식·운전자금) 이전에도 진행 가능**하고, 그 확인이 막는 것은 **실제 대외 노출**(프로덕션 도메인 적용·방화벽
개방 실행·`T-NC-01` 외부 접속 실측)이다. 이 두 근거로 아래 §C는 T-W0-04(백업)·T-W0-06(보안 모니터링)을 W0 DoD의
엄격한 선행 경로 밖에 두고 웨이브 효율에 활용한다.

**세부 태스크 간 의존(E2 파일 소유권 대조로 도출, G3 파일 배타 원칙의 실행 형태)**:

```
T-W0-05[SOLO] ─┬─ T-W1-03(export 요구) ─ T-W1-11[SOLO]
               └─ T-W1-02(export 검증 요구)
T-W0-03 ─ T-W1-06(같은 nginx.conf, 순서 의존)
T-W0-04 ─ T-W0-06(같은 Uptime Kuma 설정)
T-W1-01[SOLO] ─┬─ T-W1-02 ─ T-W1-03 ─ T-W1-11[SOLO]
               └─ T-W2-05, T-W2-06(토큰 2단계 소비, W2 완료 시점까지)
T-W2-02 ─ T-W2-03(같은 업로드 플로우)
T-W2-08[SOLO] ─ T-W2-09(같은 주민링크 API 소비)
T-W2-13[SOLO] ─ T-W2-14(같은 미성년자 게이트 소비)
T-W2-03, T-W2-14 ─ T-W2-05(기자 앱 콘텐츠 등록 폼 파일 중복 가능성 — 순서 분리로 회피)
T-W2-15 ─ T-W2-16(CF Stream 발급 후에만 상태조회 연동 가능)
T-W2-04, T-W2-16 ─ T-W2-06(관제 앱 인증·CF Stream 작업 정착 후 토큰 소비)
T-W2-01~16(전건) ─ T-W2-07(Playwright 완성은 기자·관제 기능 완결 후)
T-W3-01[SOLO] ─ T-W3-02, T-W3-03, T-W3-04(쉘 토큰수신 API 선행)
T-W1~W3(전건) ─ T-W4-01, T-W4-02, T-W4-03
T-NC-03(PoC) ─ T-W2-02, T-W2-03(input capture 실사용 전 PoC 합격 필요, 02§E-0-1 "§E 7번의 착수 여부를 좌우한다")
T-NC-10(스토어 심사 통과) ─ T-W4-*(08§A W4 선행조건 "W1~W3" 전건)
```

**시간축 매핑표 소비(08§A 4개 앵커 인용)**: 앵커 2(런칭=W1 DoD 시점)는 §C Wave 10 종료 시점과 일치, 앵커 3(첫
실 방송=W2 DoD 이후+04§H-1 게이트)은 §C Wave 13 종료 이후(04 자체 게이트는 본 문서 비범위), 앵커 4(대외
런칭=시드 콘텐츠 6건)는 W축과 별도 캘린더라 웨이브에 대응시키지 않는다(08§A 원칙 그대로 인용 — "M축은 W단계
진행 속도와 무관"과 동형).

## B. 단독 슬롯 규칙 적용 (G3)

**정의 확정(G3 "병렬 금지"의 실행 해석)**: SOLO 태스크는 파일 집합이 겹치지 않는 다른 태스크와도 **같은 웨이브에
동시 배치하지 않는다** — 이유는 파일 diff 충돌 방지가 아니라 **공용 자산의 런타임 정합성**(예: Prisma 마이그레이션
진행 중 다른 서브에이전트가 동일 DB 스키마로 테스트를 실행하면 일시적 불일치 상태를 관측), CI 설정 변경 중 다른
태스크의 게이트①이 그 CI를 참조하는 경우의 경합을 막기 위함이다. 즉 SOLO 웨이브는 **시스템 전체가 그 1태스크만
실행**하는 웨이브다(다른 트랙도 일시 정지).

| 태스크 | G3 사유 | 웨이브 |
|---|---|---|
| T-W0-05 | `app.config.ts` 공통 변경(3앱) | Wave 1 |
| T-W1-01 | `packages/ui` 신설(공용 자산) | Wave 3 |
| T-W1-11 | CI 설정(`ci.yml`) 변경 | Wave 10 |
| T-W2-08 | Prisma 스키마 마이그레이션(`ContentOrigin` 확장) | Wave 6 |
| T-W2-13 | Prisma 스키마 마이그레이션(미성년자 필드) | Wave 7 |
| T-W3-01 | Prisma 스키마 마이그레이션(`PushSubscription`) | Wave 14 |
| T-W4-02 | CI 설정(`ci.yml`) 변경 | Wave 17 |

**Prisma 마이그레이션 순서 고정**: T-W2-08 → T-W2-13 → T-W3-01 (W축 순서와 자연히 일치하므로 별도 조정 불요,
E2 §G 리스크 표 인용).

## C. 웨이브 편성 (동시 ≤5)

**표기**: 병렬 근거 열은 "파일 소유권 비교집합"을 요약한다 — 상세 파일 목록은 E2 각 태스크 행을 인용(재정의 아님).
예상 소요는 **상대 크기**(S=1~3파일·M=4~6파일·L=7파일 이상, E2 §A 크기 기준의 파일 수에 대응)만 표기하며 절대
토큰·시간 예산은 E4 소관이다.

| 웨이브 | 태스크 | 병렬 근거(파일 집합) | 예상 소요 | 선행 웨이브 |
|---|---|---|---|---|
| **1** | T-W0-05 **[SOLO]** | 단독 — `app.config.ts`×3+`package.json`×3 | M | — |
| **2** | T-W0-01, T-W0-02, T-W0-03, T-W0-04 | `services/api/src/auth/*` vs `infra/docker-compose.yml`+R2스크립트 vs `infra/docker/docker-compose.xeon.yml`+`nginx.conf`(신규)+`Dockerfile.web`(신규) vs 백업스크립트+cron — 4개 워크스페이스/디렉터리 상호 배타 | M×4 | Wave 1 |
| **3** | T-W1-01 **[SOLO]** | 단독 — `packages/ui/*` 신설 | M | Wave 2 |
| **4** | T-W1-05, T-W1-06, T-W1-08, T-W0-06, T-W2-01 | `services/api/src/go-link/*` vs `infra/docker/nginx.conf`(T-W0-03 후속, 동일 파일이나 시점 분리) vs `services/api/src/telemetry/*` vs 모니터링 설정 vs `apps/reporter/src/auth/token-store.ts` — 5개 상호 배타 | M×5 | Wave 3(T-W1-06은 Wave2의 T-W0-03 완료 필요, T-W0-06은 Wave2의 T-W0-04 완료 필요) |
| **5** | T-W1-02, T-W1-07, T-W2-04, T-W2-02, T-W2-10 | `apps/subscriber` 테마소비(11파일) vs 3앱 신규 계측훅 vs `apps/control-center/src/auth/*` vs `apps/reporter/src/upload/*`(신규) vs `services/api/src/media/*`(공개렌디션, T-W1-05의 `go-link`와 다른 서브모듈) — 5개 상호 배타 | L(T-W1-02)+M×4 | Wave 3(T-W1-02는 T-W1-01 필요), Wave 4(T-W2-10 무관계지만 순서상 배치) |
| **6** | T-W2-08 **[SOLO]** | 단독 — Prisma 마이그레이션(`ContentOrigin`) | M | Wave 5 |
| **7** | T-W2-13 **[SOLO]** | 단독 — Prisma 마이그레이션(미성년자 필드) | S | Wave 6(동일 스키마 파일, 순차 필수) |
| **8** | T-W1-03, T-W1-04, T-W1-09, T-W1-10, T-W2-09 | `apps/subscriber` hls/OG(기존 route 수정, T-W1-02 이후라 안전) vs SW/workbox(신규) vs 문의하기(신규 route) vs 편성표(신규 route) vs 주민링크 프론트(신규, T-W2-08 소비) — 5개 상호 배타(모두 신규 파일이거나 T-W1-02가 이미 정리한 후의 기존 route) | M×5 | Wave 5(T-W1-03은 T-W1-02 후), Wave 6(T-W2-09는 T-W2-08 후) |
| **9** | T-W2-03, T-W2-11, T-W2-12, T-W2-15 | `apps/reporter` 촬영/업로드 화면(T-W2-02 후속, 동일 앱이나 시점 분리) vs 라이브 상품카드(신규) vs `services/api/src/telemetry/*`(T-W1-08 모듈 확장, 클릭 이벤트만 추가) vs `services/api/src/live/*`(CF Stream 연동 확장) — 4개 상호 배타 | M×4 | Wave 5(T-W2-03은 T-W2-02 후), T-NC-03 PoC 합격(아래 §F) |
| **10** | T-W1-11 **[SOLO]** | 단독 — `ci.yml`+`playwright.config.ts` 신설(CI 설정) | L | Wave 8(T-W1-03·04 완료 필요) — **W1 종료 웨이브** |
| **11** | T-W2-14, T-W2-16 | `apps/reporter` 콘텐츠 등록 폼(T-W2-13 소비, T-W2-03과 분리) vs `apps/control-center/src/live/*`+`services/api/src/live/*`(T-W2-15 후속) — 2개 상호 배타 | S+M | Wave 7(T-W2-14), Wave 9(T-W2-16) |
| **12** | T-W2-05, T-W2-06 | `apps/reporter` 테마소비(20파일, T-W2-03·14 정착 후) vs `apps/control-center` 테마소비(16파일, T-W2-04 정착 후) — 2개 앱 상호 배타 | L+L | Wave 11(T-W2-14), Wave 5(T-W2-04) |
| **13** | T-W2-07 | 단독(다른 W2 태스크 전건 완료가 전제라 병렬 대상 없음) | M | Wave 4~12 전건(관제·기자 기능 완결 필요) — **W2 종료 웨이브** |
| **14** | T-W3-01 **[SOLO]** | 단독 — Prisma 마이그레이션(`PushSubscription`) | M | Wave 10(W1 종료), T-NC-10 무관(스토어 심사는 T-W3-03·04만 필요) |
| **15** | T-W3-02, T-W3-03, T-W3-04 | 3앱 manifest(신규) vs Bubblewrap TWA 설정(별도 프로젝트) vs iOS WKWebView 프로젝트(별도 프로젝트) — 3개 상호 배타 | M+M+L | Wave 14(T-W3-03·04는 웹푸시 토큰수신 API 필요) |
| **16** | T-W4-01, T-W4-03 | `CLAUDE.md`(수정) vs 신규 실측 리포트 문서 — 2개 상호 배타 | S+S | Wave 13(W2 종료), Wave 15(W3 종료), **T-NC-10 스토어 심사 통과**(08§A W4 선행조건 "W1~W3") |
| **17** | T-W4-02 **[SOLO]** | 단독 — `ci.yml`(웹 E2E 필수 게이트 전환) | S | Wave 16 — **W4 종료 웨이브(전 태스크 마감)** |

**정상 웨이브 코드 태스크 합계 검증**: 1(1)+2(4)+3(1)+4(5)+5(5)+6(1)+7(1)+8(5)+9(4)+10(1)+11(2)+12(2)+13(1)+14(1)+15(3)+16(2)+17(1)
= **40건**(E2 §C W0~W4 코드 태스크 합계 6+11+16+4+3=40과 일치).

## D. 코드 외/트리거 태스크의 체크포인트 배치 (게이트 트랙)

E2 §D(트리거 대기 코드 3건)·§E(코드 외 12건)는 동시성 상한이 적용되는 서브에이전트 웨이브가 아니라, 위 웨이브
사이에 삽입되는 **체크포인트**(사람·외부기관·수치 트리거 대기)다. 각 체크포인트는 정확히 하나의 웨이브 구간에
배치해 "어느 웨이브 전후에 걸리는가"를 명확히 한다.

| 체크포인트 ID | 배치 위치 | 배치 근거 | 하류 영향(막는 것) |
|---|---|---|---|
| T-NC-02(사용자 결정 3건) | Wave 1 착수와 동시 접수 개시, **상시 대기**(확인 전까지 로컬 코드 웨이브는 계속 진행, §A "W0 DoD 범위 한정" 참조) | 08§A W0 선행조건 | 실제 대외 노출(T-NC-01 외부 접속 실측 포함)만 차단 — 나머지 전 웨이브는 비차단(EXEC-PLAN G9·08§D 리스크 완화책) |
| T-NC-01(W0 DoD 실측) | Wave 2 종료 직후 | 08§A W0 DoD | 형식상 W0 "완료" 선언(§F Wave 종료 보고), Wave 3 자체는 비차단(E5§C 판정 절차는 병렬 진행 가능) |
| T-NC-03(PoC) | Wave 1~2 구간과 병행 개시, **Wave 9 착수 전 필수 완료** | 02§E-0-1 "W1 착수 전, §E 7번 착수 여부를 좌우" | Wave 9의 T-W2-03(input capture 실사용) |
| T-NC-04(어르신 패널 1차) | Wave 3(토큰 게이트) ~ Wave 8(export 스모크) 구간 | 08§A 시간축 매핑표 "W1 진행 중(§E 1번 완료 직후 ~ 4번 사이)" | 없음(비차단, 정보 수집성) |
| T-NC-05(TTFF 실측) | Wave 8 종료 후 | 08§A W1 DoD① | Wave 10 종료 보고(W1 DoD 판정)의 근거 자료 — Wave 10 자체 착수는 비차단 |
| T-NC-06(go. 링크 캡처) | Wave 4(go. 라우트+nginx 동적라우트) 종료 후, Wave 8과 병행 가능 | 08§A W1 DoD② | Wave 10 종료 보고(W1 DoD 판정)의 근거 자료 |
| T-NC-07(W2 실기기 완주) | Wave 13 종료 후 | 08§A W2 DoD | 앵커 3(첫 실 방송, 04 소관·본 문서 비범위)의 전제 — Wave 14 착수는 비차단(W3는 "W1 안정"만 요구, 08§A) |
| T-NC-08(스토어 계정 개설) | Wave 1과 동시 개시(**Wave 15 착수 8주 전 완료 필요** — 08§E-1-1 리드타임) | 08§E-1-1 | Wave 15(T-W3-03·04 심사 대상 빌드) |
| T-NC-09(Meta App Review) | 사업자등록 완료 직후(외부 이벤트, 본 웨이브 그래프 밖) | 08§E-10 | 04§H-1 R4(첫 실 방송 게이트, 본 문서 비범위) — W0~W4 어느 웨이브도 차단하지 않음 |
| T-NC-10(스토어 심사 통과) | Wave 15 종료 후, **Wave 16 착수 전 필수** | 08§A W4 선행조건 "W1~W3" | Wave 16·17(W4 전체) |
| T-NC-11(클라우드 트리거 대시보드 리뷰) | Wave 13 종료 후 상시(월 1회, 종료 없음) | 08§E-8 | 없음(비차단 상시 운영) |
| T-NC-12(커머스 2단계 리뷰) | Wave 13 종료 후 상시(월 1회, 종료 없음) | 08§E-9 | 없음(비차단 상시 운영) |
| T-TRIG-01(멀티파트 승격) | 트리거(D-T4 문장) 충족 시 — 그 시점 이후 첫 여유 웨이브(≤5 미달 웨이브)에 편입 | 02§E-12 | 없음(선택적 승격) |
| T-TRIG-02(B2B 워터마크) | B2B 착수 결정 시 — 동일 방식 편입 | 02§E-15 | 없음 |
| T-TRIG-03(쉘 UA 감지) | 05§A-4 결정 시(전체) 또는 Wave 15 이전 아무 여유 웨이브(감지 로직 선구축분만) | 02§E-11 | 없음(전체 기능은 05§A-4 미결정 시 대상 UI 자체가 없어 비차단) |

## E. 실패 태스크 재시도·이월 규칙

1. **게이트① 자가검증 실패(레드/미실행)**: E5§A 게이트① 조치를 그대로 적용 — 해당 웨이브에서 그 태스크만
   "미완료" 상태로 유지, **같은 웨이브의 나머지 태스크는 영향받지 않고 계속 진행**(파일 소유권이 배타적이므로
   실패가 전파되지 않음 — 이것이 G3 파일 배타 병렬의 핵심 이점).
2. **게이트② qa-verifier FAIL**: 동일 웨이브 내 재기동(같은 서브에이전트 또는 재위임) 1회 허용. 재기동도 실패하면
   해당 태스크를 **다음 웨이브로 이월**(carry-over)하고, 원래 웨이브의 나머지 태스크는 계획대로 다음 단계 진행.
   단, 이월된 태스크에 **의존하는 하류 태스크는 이월된 태스크 완료까지 자동 보류**(예: T-W2-08 실패 시 T-W2-09는
   T-W2-08 완료 후 웨이브로 재배치).
3. **SOLO 태스크 실패**: 시스템 전체가 그 태스크만 기다리는 구조이므로, 실패는 **후속 전 웨이브를 지연**시킨다.
   SOLO 실패가 확인되면 조율자는 즉시 원인 분류(설계 문제/구현 실수) 후 24시간(또는 다음 조율자 점검 시점) 내
   재기동 여부를 판단 — 무기한 방치 금지.
4. **이월 한도**: 동일 태스크가 **2개 웨이브 연속 이월**되면(재기동 2회 실패), E5§A "동일 유형 위반 2회 이상"
   조항과 동일 기준으로 조율자가 태스크 자체를 재설계(범위 축소/분할)한다 — E2로 피드백해 태스크 정의를 갱신한다.
5. **웨이브 부분 실패의 재계산**: 한 웨이브에서 5개 중 일부가 이월되면, 그 웨이브의 "완료"는 **성공한 태스크만
   반영**하고 이월분은 다음 웨이브 번호에 재편입한다(웨이브 번호 자체를 재부여하지 않음 — 예: Wave 8에서 1개
   이월되면 Wave 9는 원래 계획대로 진행하고 이월분은 Wave 9 이후 여유 슬롯에 삽입).

## F. G9 게이트 보류 태스크 우회 경로

**원칙**(EXEC-PLAN G9·HANDOFF 승계): "미확인 항목은 해당 게이트에 걸리는 태스크만 보류, 나머지는 로컬 검증 범위
내 병렬 진행."

| G9 항목 | 직접 차단 대상 | 우회 경로(차단 전 진행 가능한 것) |
|---|---|---|
| ① 도메인 미확정 | T-NC-01(외부 `https://api.<도메인>/health` 실측), T-W1-05·06의 실제 배포 검증(임시 도메인 대체 가능, 08§D 리스크 "도메인·브랜드 미결정으로 W0 지연" 완화책 인용: "임시 도메인으로 W0~W2 기술 검증 진행") | Wave 1~13 전건은 임시 도메인 또는 로컬 LAN으로 코드·통합 검증 진행 |
| ② 제온 노출 방식(Tunnel/포트개방) 미확정 | T-W0-03의 실제 Cloudflare 존 연결(설정 코드 자체는 작성 가능) | Wave 2(T-W0-03) 코드 작성은 진행, 실 연결만 보류. **08§D 데드라인**: "W0 착수 + 4주 초과 시 승인 대기를 종료하고 Tunnel 단독안으로 자동 확정" — 4주 경과 시 조율자가 자동 확정 후 진행 |
| ③ 05§G 운전자금 미확인 | Wave 1(T-W0-05) 자체는 순수 코드라 비차단. 08§A는 "W0 착수 보류"라 명시하나, 08§D "로컬 LAN 기술 검증 선행" 완화책을 적용해 **코드 웨이브는 진행하되 실 배포·비용 발생 단계(Cloudflare 유료 티어 전환·서버 실사용 등)만 보류** | Wave 1~9(코드 작성)까지 로컬 검증 범위 내 진행, 실비용 발생 지점(도메인 구매·Cloudflare Stream 실계정 — T-W2-15) 도달 전 재확인 필수 |

**우회 경로의 한계**: 위 표는 "코드를 미리 짜 둔다"는 뜻이지 "확인 없이 실행한다"는 뜻이 아니다 — 비용이 실제로
발생하는 지점(도메인 구매·Cloudflare 유료 전환·CF Stream 실계정 개설=T-W2-15)에 도달하기 **직전**, 조율자가
G9 확인 상태를 재점검한다. 미확인 상태로 이 경계를 넘는 것은 E5§A "테스트 미실행 완료 선언 금지"와 동급의
절차 위반으로 취급한다.

## G. E2 ↔ E3 정합 자체 검산

**검산 방법**: E2의 전 태스크 ID(55건: 정상 웨이브 코드 40 + 트리거 대기 3 + 코드 외 12)를 위 §C 웨이브 표 +
§D 체크포인트 표와 대조해, 각 ID가 **정확히 1곳**에만 배정됐는지 확인한다.

| 구간 | E2 태스크 수 | E3 배정 위치 | 중복/누락 |
|---|---|---|---|
| W0 코드(T-W0-01~06) | 6 | Wave 1(1)+Wave 2(4)+Wave 4(1, T-W0-06) | 0/0 |
| W1 코드(T-W1-01~11) | 11 | Wave 3(1)+Wave 4(2, 05·08)+Wave 5(2, 02·07)+Wave 8(4, 03·04·09·10)+Wave 10(1, 11)+Wave 6 문맥의 T-W1-06(Wave4)... | 재확인: T-W1-01(3)·02(5)·03(8)·04(8)·05(4)·06(4)·07(5)·08(4)·09(8)·10(8)·11(10) — **11건 전건 배정, 중복 0** |
| W2 코드(T-W2-01~16) | 16 | Wave4(01)·Wave5(02·04·10)·Wave6(08)·Wave7(13)·Wave8(09)·Wave9(03·11·12·15)·Wave11(14·16)·Wave12(05·06)·Wave13(07) | **16건 전건 배정, 중복 0** |
| W3 코드(T-W3-01~04) | 4 | Wave14(01)·Wave15(02·03·04) | 0/0 |
| W4 코드(T-W4-01~03) | 3 | Wave16(01·03)·Wave17(02) | 0/0 |
| 트리거 대기(T-TRIG-01~03) | 3 | §D 표(트리거 발동 시 여유 웨이브 편입 — 고정 웨이브 미배정은 설계 의도, "정확히 한 웨이브"가 아니라 "정확히 한 체크포인트 규칙"에 배정) | 0/0(트랙 배정 완료) |
| 코드 외(T-NC-01~12) | 12 | §D 체크포인트 표 12행 전건 | 0/0 |
| **합계** | **55** | **40(웨이브)+3(트리거 규칙)+12(체크포인트)=55** | **전건 배정 — 누락 0건, 중복 0건** |

## H. 리스크 테이블

| 리스크 | 완화책 | 담당 | 발동 트리거 |
|---|---|---|---|
| 계획 시점 파일 소유권 근사치가 실제와 달라 같은 웨이브 내 태스크가 충돌(특히 Wave 4·5·8·9의 5개 동시 배치) | 웨이브 착수 직전 조율자가 각 태스크의 실제 대상 디렉터리를 `git status`/`grep` 재확인 후 배치 확정(E2§G 리스크 표와 동일 원칙) — 충돌 발견 시 그 웨이브 내에서 즉시 재배치(다음 웨이브로 순연) | 조율자 | 매 웨이브 착수 직전 상시 |
| SOLO 웨이브(7건)가 전체 파이프라인을 순차 정지시켜 총 웨이브 수가 불필요하게 늘어남(동시성 손실) | SOLO 웨이브는 최대한 작게 설계(단일 관심사 1태스크)해 정지 시간을 최소화 — 이미 §C에서 T-W1-11에 CI 관련 항목을 병합해 SOLO 웨이브 수를 8→7로 축소한 사례 적용. 추가 병합 여지는 웨이브 착수 전 조율자가 재검토 | 조율자 | 웨이브 편성 확정 전 상시 |
| W1·W2 교차 배치(Wave 4·5·8·9)에서 실제로는 08§A "서로 독립" 전제가 깨지는 숨은 의존(예: 인증 스택 공유— "W2 내부에서 기자·관제도 병렬 가능(인증 어댑터를 공용 패키지로 먼저 추출)"의 "공용 패키지 추출"이 본 계획에는 별도 태스크로 없음 — E2가 발주하지 않은 전제) | E2에 "인증 어댑터 공용 패키지 추출" 태스크가 없다는 사실을 확인 — 현재 T-W2-01(기자)·T-W2-04(관제)는 각자 독립 구현으로 설계돼 있어 08§A의 "공용 패키지 추출" 전제와 다르다. 08§A 원문은 병렬화 아이디어(선택적 최적화)이지 필수 선행조건이 아니므로, 본 계획은 공용 패키지 추출 없이 두 태스크를 독립 구현으로 진행(더 단순·더 안전) — 아래 신규 위임 목록에 이 판단을 기록 | 조율자 | Wave 5 착수 전 확인 완료(본 문서 작성 시점에 이미 반영) |
| 이월(carry-over) 누적으로 하류 웨이브가 연쇄 지연 | §E 이월 규칙의 "2웨이브 연속 이월 시 재설계" 상한 적용 + 웨이브 종료 보고(E5§E 형식)에 이월 현황 상시 표기 | 조율자 | 동일 태스크 2웨이브 연속 이월 |

## I. 실행 체크리스트

- [ ] 웨이브 착수 전 파일 소유권 실측 재확인(§H 리스크 1행)
- [ ] SOLO 웨이브 7건이 전부 단독으로(다른 트랙 정지 포함) 실행됐는지 확인
- [ ] Prisma 마이그레이션 순서(T-W2-08→T-W2-13→T-W3-01) 위반 없음 확인
- [ ] 코드 외 체크포인트(§D 12건)가 해당 웨이브 전후에 실제로 실행됐는지 웨이브 종료 보고에서 확인
- [ ] G9 3항목 상태를 매 웨이브 종료 보고에 표기(§F 우회 경로가 실비용 발생 지점을 넘지 않았는지)
- [ ] 이월 발생 시 §E 규칙에 따라 하류 태스크 자동 보류 반영
- [ ] §G 정합 표를 새 태스크(E2 갱신) 발생 시 즉시 재검산

## 신규 위임 목록 (등재 책임 규칙에 따라 제출)

| # | 발주처 | 수신처 | 요청 내용 | 처리 상태 |
|---|---|---|---|---|
| 1 | E3 §C(예상 소요 열) | **E4**(토큰·시간 예산) | 웨이브 17개의 상대 크기(S/M/L) 표를 절대 토큰 예산 산정의 입력으로 사용 요청 — 특히 SOLO 웨이브 7건은 병렬 슬롯 손실이 있으므로 토큰 예산에 "정지 비용"을 반영 요청 | 대장 등재 필요 |
| 2 | E3 §H(리스크 3행) | **PIVOT-PLAN §6**(대장 소유자) | "인증 어댑터 공용 패키지 추출 없이 기자·관제 독립 구현" 판단(08§A "선택적 최적화" 문구에 대한 실행계획 단계 해석)을 EXEC-DECISIONS에 1건 기록 요청(E5§D 절차 2 "코드가 계획과 달라지는 결정" 동형 — 본 건은 08 자체를 변경하지 않고 실행계획 해석만 확정하는 것이라 08 갱신은 불요하나 결정 기록은 필요) | 대장 등재 필요 |
| 3 | E3 §D(T-NC 체크포인트 12행) | **E5**(§C DoD 판정 절차 표) | E2 신규 위임 목록 #3(PoC·패널 증적 경로)과 동일 건 — E3의 체크포인트 배치가 그 경로 확정에 의존하므로 중복 발주하지 않고 E2#3에 합류 | E2#3과 통합(중복 등재 아님) |
