# EXEC-ROUND-5-DECISIONS — 라운드 5 횡단 결정 (조율자 선확정)

> [EXEC-EVAL-ROUND-5](EXEC-EVAL-ROUND-5.md) 이슈 15건(U-1~U-15)의 공유 문안·배분을 확정한다.
> **프로세스 정정(평가 진단 수용)**: 15건 중 10건이 "확정의 소비처 미전파" 유형이며 원인은 배분표가 지적을
> 한쪽 팀에만 배정한 것이다. 본 라운드부터 ① 각 팀은 **이슈 전 목록을 읽고 자기 소유 파일에 걸리는 것을
> 전수 처리**하며(배분표는 최소 보장선일 뿐), ② 3팀 완료 후 **조율자 최종 정합 스윕**(E5 §D 앵커 전종 +
> 자체 점검 인용 재실행 + DECISIONS 확정 문안의 소비처 grep)을 라운드 6 평가 전에 수행한다. (2026-08-04)

## D5-1. T-W4-01 기준선 재도출 — U-1 (EXEC-DECISIONS #5로 기록)

**확정.** 라운드 4에서 E2 T-W4-01에 부착된 "Q1 재검증 각주"는 **이중 오류**였다(인용 좌표 293 ≠ 실측 302,
`grep -c "reporter 102|media-worker 24"` 0건 주장 ≠ 실측 1건(CLAUDE.md:202) — 그 1건을 만든 커밋
`037bdf3`(마스터플랜 승인 반영)은 실행계획 착수 **전**에 이미 존재). 각주를 삭제하고, T-W4-01의 산출물을
"**현행 CLAUDE.md 재실측 기준** 문서 정리"로 재정의한다: §9 Expo Go 운용 절차의 웹 절차 격하 + §11 네이티브
잔존 수치·서술 정리(구체 항목은 착수 시점 재실측으로 확정 — 지금 열거한 고정 좌표를 쓰지 않는다).
경위·교훈("반증 각주도 앵커 ⑨ 재검증 대상")은 조율자가 **EXEC-DECISIONS #5**로 기록.

## D5-2. W2 Playwright 판정 조합 = 8 — U-2

**확정 문안**(E5 §C W2 행): "핵심 시나리오 3종 × 브라우저 프로필 — **관제·기자 = 3종(데스크톱 Chrome 포함),
구독자 = 2종(02 §E-9 ② 단서: 데스크톱 프로필은 관제·기자 시나리오 한정) = 합계 8조합 전건**". E2 T-W1-11a와
동일 계수(태스크·판정 일치).

## D5-3. W0 DoD 판정 기점 = Wave 2 완료 후 — U-4

**확정.** E3 §C의 이원 선언(코드 종료=Wave 4 / DoD 판정 기점=Wave 2 완료 후)이 정본이다. E2 §E T-NC-01
게이트를 "**T-W0-01~04 완료 후(Wave 2 종료 직후)**"로 정정한다 — health 외부 도달·로그인 왕복은
T-W0-01(인증)·T-W0-02(R2 CORS)·T-W0-03(nginx)·T-W0-04(백업·모니터링 기반)까지로 충족되며, T-W0-05(Expo Web
활성화)·T-W0-06(모니터링 확장)은 이 DoD의 입력이 아니다. E5 §D 앵커 ⑥의 소재 인용 오기(§C 표)도 정정.

## D5-4. 실측 토큰 회수 경로 — U-5

**확정 문안**: ① E5 §A 게이트① 산출물/증적 칸에 "**+ 소비 토큰(대략치— 세션 표시값 기준)**" 1항 추가.
② E5 §E 진행 보고 2항에 "**웨이브 토큰 실측 합계(게이트① 신고치 합산) vs E4 §A-6 편성치**" 추가.
③ E4 §A-2·§A-4의 잠정치 교체·초과 감지 절차가 이 두 곳을 **명시적 입력**으로 인용(수집 주체=조율자,
웨이브 종료 시). 세 문서(E4·E5) 동일 문안 — 본 절이 정본.

## D5-5. 템플릿 시연 태스크의 모델 정합 — U-6

**확정.** E1 §B-1 표준 템플릿의 시연 태스크 표기는 **E4 DDD6 선배정 목록과 정합해야 한다**. T-W1-05를
시연으로 유지하려면 "모델: opus(근거: E4 §B-1 초기 보정 — 최초 api 신규 모듈 선배정)"로 고치고, sonnet
시연을 원하면 선배정 대상이 아닌 태스크로 교체한다(형식은 E1 재량, 정합은 필수). E4는 §B-4 분류표에
선배정 여부가 드러나도록 비고 열(또는 표기)을 보강한다.

## D5-6. 물리 자원 리스크 트리거 전진 — U-7

**확정 문안**(E1 §D 물리 자원 행 트리거): "**T-NC-03(PoC) 개시 시점(Wave 1~2 병행 개시) 직전 상시**" —
舊 "W1 Wave 3 착수 직전"은 최초 물리 자원 수요(PoC 실기기, Wave 1~2 병행)보다 늦다(E3 §D와 모순).
근거 서술도 E3 §D 인용으로 교체.

## 배분 (최소 보장선 — 각 팀은 이슈 전 목록 대비 자기 파일 전수 처리 의무)

- **B팀(E2·E3)**: U-1(E2 — #5 인용)·U-4(E2 게이트)·U-9(T-W2-03·T-W1-03·07a·07b 예상 경로 명시)·
  U-10(E2 T-NC-06 게이트에 T-W1-03 선행 추가 + E3 §A 엣지·§D 배치 "Wave 8 종료 후"로)·U-11(E3 Wave 8
  배타 근거에 `app/_layout.tsx` 명시 대사)·U-12(E3 Wave 17a 셀 파일 수 5로 정정)·U-14(E2 T-W2-09 담당 —
  02 §E-13 정본 담당이 BE 리드이므로 **BE 리드로 정정**하거나 유지 시 근거 1줄 기록)·U-15(E3 Wave 4 셀에
  02 §E-6 "§E 4번과 동일 시점" 대비 선행 배치의 안전 방향 차이 1줄 대사).
- **C팀(E5)**: U-2(D5-2 문안)·U-3(앵커 ②③ 정규식을 E3 §G 확장형 `[0-9]+[ab]?`로 + ≤4 검사에 17a/17b 포함)·
  U-4(앵커 ⑥ 좌표 정정)·U-5(D5-4 ①②)·U-13(앵커 ⑤ — 총예산 대조를 "E4 §A-3·§A-8 ↔ PIVOT §6-11 #9" 2자로
  수정(EXEC-DECISIONS는 상한만 소유), 앵커 ⑧ — 비교 대상 추출 방식을 실행 가능한 형태로 수리).
- **A팀(E1·E4)**: U-5(E4 몫 — D5-4 ③)·U-6(D5-5 — E1 §B-1 + E4 §B-4 비고)·U-7(D5-6)·U-8(E1 §E 인용 출력
  재실행 교체 — 2행 전건 인용).
- **3팀 병렬 가능**(웨이브 재편 없음 — E3 수정은 셀 단위, 사이징 라벨 불변이라 E4 §A-6 불변. 단 E3가 §D
  T-NC-06 배치를 옮기며 웨이브 구성이 변하면 즉시 조율자에게 보고 — 그 경우에만 E4 후속).
- **완료 후**: 조율자 최종 정합 스윕 → 잔여 미세 정정 → 커밋 → 라운드 6.
