# EXEC-ROUND-2-DECISIONS — 라운드 2 횡단 결정 (조율자 선확정)

> [EXEC-EVAL-ROUND-2](EXEC-EVAL-ROUND-2.md)의 이슈 20건(Y-1~Y-20) 중 판단이 필요한 4건(Y-5·Y-12·Y-13·Y-14)과
> 문서 간 공유 수치를 선확정한다. 수정 에이전트는 확정 문안을 **인용**하며 재해석하지 않는다. (2026-08-04)

## DD1. T-W1-07(계측 훅) 앱 경계 분할 — Y-13

**확정: 앱 경계 분할 + 호출 지점 배선을 소유에 포함.**
- `T-W1-07a`(구독자): 훅/유틸 신규 + **재생 시작·진행률·큰 자막 토글 호출 지점 배선**(재생 컴포넌트 등 호출
  파일을 소유권 열에 명시). **T-W1-03 완료 후 웨이브에 배치**(Wave 8에서 제거 — 재생 컴포넌트 충돌 해소).
- `T-W1-07b`(기자): 훅/유틸 신규 + 업로드 위저드·모드 선택 호출 지점 배선(호출 파일 명시).
- 관제 몫(`T-W1-07c`)은 **02 §E 16번에 관제 이벤트가 명시된 경우에만 신설**한다 — E2가 02 §E 16번을 재확인해
  판정하고 결과(신설 여부·근거 인용)를 완료 보고에 명시한다. 없으면 07a·07b 2분할로 종결.
- 각 태스크 AC에 "훅 호출이 실제 발생함을 단위 테스트로 확인(모의 로거 호출 1회 이상)" 추가. E2 §F-1 16번
  매핑·E3 §C·§G 검산 동반 갱신.

## DD2. `playwright.config.ts` 위치 = 앱별 — Y-14

**확정: 앱별 설정**(`apps/<app>/playwright.config.ts`). 근거: E5 §B 명령 형식(`pnpm --filter @gachinol/<app>
exec playwright test`)과 정합하고, 루트(공용 자산)로 두면 G3상 SOLO 승격이 강제돼 웨이브 손실이 생긴다.
- T-W1-11a 소유권 = `apps/subscriber/playwright.config.ts`(신규) + 구독자 4단계 시나리오.
- T-W2-07 소유권에 `apps/{reporter,control-center}/playwright.config.ts`(신규 2) 명시.
- E5 §B 웹 E2E 행 비고에 "설정은 앱별 — 루트 단일 설정 아님" 1줄 추가.

## DD3. W4 DoD 판정 명령 재설계 — Y-5 (E5 §C W4 행 · E2 T-NC-13 **동일 문안** — 본 절이 정본)

**확정 문안**(두 문서가 그대로 인용):
- **명령 ① (문자열 잔재)**: `grep -rn "expo-env\.d\.ts\|EAS Build" CLAUDE.md docs/ --exclude-dir=exec`
  — 통과 기준 **0건**. 단 `CLAUDE.md`의 개발 편의 안내 문맥(예: §9 "Expo Go로 충분·EAS Build 불요")의 히트는
  잔재가 아니므로 제외 가능 — **제외 시 판정자가 히트별 문맥 1줄을 증적에 기재**한다(무설명 제외 금지).
  W4에서 CLAUDE.md §9가 웹 절차로 대체되면 제외 사유 자체가 소멸함을 부기.
- **명령 ② (배포 경로 잔재)**: `eas.json` 부재 확인(`ls eas.json` → 없음) +
  `grep -rn "eas build\|eas submit\|expo build" .github/workflows/ apps/*/package.json` → **0건**.
- 증적: 두 명령의 실행 전문 + CI yaml에 웹 E2E 필수 게이트 존재 인용. 저장 위치 `reviews/dod-evidence/w4/` 불변.

## DD4. T-NC-08·09 실행 주체 통일 — Y-12

**확정 문안**(E1 §A 금지 칸이 이미 확정한 것을 전파): T-NC-08·T-NC-09의 담당 =
"**사용자**(법무/운영 지원: 절차 정리·문서 준비·상태 추적)". 적용: E2 §E 담당 열 2건, E5 §C 비-DoD 표
실행 담당 2건, E5 §E 사용자 개입 목록에 "8. Meta App Review + Business Verification 신청(T-NC-09 —
사업자등록 완료 직후, 리드타임 2~4주)" 추가.

## DD5. 공유 수치 확정 (문서 간 단일 원천)

- **`app.module.ts` 모듈 수 = 17**(평가자 실측 §5 A-4 — 재현 명령 포함). "18모듈" 표기 4개소(D4 확정 문안
  포함) 전건 정정. EXEC-ROUND-1-DECISIONS D4는 조율자가 정정, E2·E3는 인용만.
- **T-NC = 15건**(기존 13 + 신설 `T-NC-14`(방송별 URL 게시 절차, Y-15)·`T-NC-15`(복구 리허설 분기 1회, Y-16))
  → 코드 외 트랙 예산 = **5만 × 15 = 75만**(D8 단가 불변). E2 §E·E3 §D·E4 §A-3·§A-8·E5 §C가 전부 이 값 사용.
  E5 §C 하단 문구 = "**15건 중 DoD 계열 8건 + 비-DoD 7건**(T-NC-02·08·09·11·12·14·15)".
- **E4 재계산 기준 = B팀 수정 완료 후의 E3 §C 현행 편성**(웨이브 수·구성·사이징을 E3에서 다시 읽어 재계산 —
  평가자 재계산표(§5 B-4, 합계 1,874만)는 검산 참고용이며 **자체 재계산 의무**). 이를 위해 **E4 수정은 E2·E3
  수정 완료 후에 착수**한다(라운드 1에서 E4가 구판 E3 위에서 계산한 실패 유형의 구조적 재발 방지 — 순서 강제).

## 부기 — 조율자 직접 수정분 (본 라운드)

Y-1(EXEC-ROUND-1-DECISIONS D4 "18모듈"→17 정정), Y-10(PIVOT §6-11 #9 상태 칸의 폐기 수치 인용 갱신 +
6-11 서두에 "EXEC 단계 수치 변경 시 상태 칸 인용 전건 재검증(§6-5 원칙 연장)" 명시). 반영 후 grep 재검증.
