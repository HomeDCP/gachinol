# Gachinol 웹 피벗 마스터플랜 (인덱스)

> 2026-08-03 수립. 네이티브 3앱(Expo RN) 체제를 **웹앱 3종 + 스토어 쉘 앱** 체제로 전환하는 계획의 최상위 문서.
> 본 계획은 독립 평가 에이전트의 루브릭 평가([reviews/RUBRIC.md](reviews/RUBRIC.md), 11개 영역 전부 9.5/10 이상)를
> 통과해야 확정되며, 확정·사용자 승인 후 실행계획을 별도 수립한다. **승인 전 코드 변경 없음.**

## 1. 전환 선언

| | 기존 | 전환 후 |
|---|---|---|
| 클라이언트 | 네이티브 앱 3종 (Expo RN, iOS/Android) | **웹앱 3종**(기자·관제·구독자, Expo Web으로 기존 코드 재사용) |
| 스토어 존재 | 앱 3종 각각 배포(예정이었음) | **껍데기 쉘 앱**(웹앱을 여는 래퍼) — 통합 1개 + PWA 병행 |
| 배포·업데이트 | 스토어 심사·버전 관리 | **웹 배포 즉시 반영**(심사 없음) — 유지보수 획기적 절감 |
| 접근·공유 | 설치 필요 | **URL만으로 접근·공유** — 카톡 채널 유입과 직결 |
| 인프라 | (동일) | **제온 서버 우선**, 클라우드 전환은 수치 트리거 기반(급격히 하지 않음) |

**바뀌지 않는 것**: 백엔드 전체(api·media-worker·ai-worker·shared 계약·DB), 제품 정의(12지사+센터, 콘텐츠 6종,
카카오 반자동 게시, 라이브 YT+FB), 편성 원칙, DCP 공존 규칙. 피벗은 **배포 형태의 전환**이지 제품의 전환이 아니다.

**전환의 이유** (사용자 결정, 2026-08-03):
1. 기개발 기능·로직을 그대로 웹에서 동작시켜 **앱 유지보수 업무를 획기적으로 축소**한다.
2. URL 기반 접근·공유 = "누구나 쉽게 찍어 올리고, AI가 편집해 방송국이 송출"하는 과업과 정합.
3. 제온 서버로 우선 운영하고, 비용·수익 관계가 정리된 뒤 클라우드 전환을 검토한다.

## 2. 확정 결정 로그 (Decision Log)

| ID | 결정 | 근거 문서 |
|---|---|---|
| D1 | 웹 프레임워크 = **Expo Web(react-native-web)** — 재작성 금지. 재사용률 실측 정본은 **02 §B**(수치는 라운드마다 정밀화되므로 여기 사본을 두지 않는다 — 라운드 18 기준 3앱 51.8%·전체 66.4%·백엔드 86.0%, 舊 "~90%"는 초기 추정치라 폐기. 이후 갱신은 02 §B만 보라) | [02](02-web-architecture.md) §A D-T1·§B |
| D2 | 쉘 = **통합 1개**(TWA + iOS WKWebView+푸시·딥링크) + PWA 병행. 기자·관제는 PWA만 | [02](02-web-architecture.md) §A D-T2 |
| D3 | 웹 인증 = refresh **HttpOnly 쿠키** + access 메모리, api 병행 경로(하위호환) | [02](02-web-architecture.md) §A D-T3 |
| D4 | 업로드 = presigned 유지 → 실측 트리거로 **멀티파트 승격** | [02](02-web-architecture.md) §A D-T4 |
| D5 | 호스팅 = 제온 nginx + **Cloudflare 프록시/Tunnel**(권장) | [02](02-web-architecture.md) §A D-T5 · [08](08-rollout-transition.md) §B |
| D6 | 공유 = OG 정적 메타 + `go.` 단축 링크(필요 시 OG만 SSR) — 카톡 미리보기 최적화 | [02](02-web-architecture.md) §A D-T6 |
| D7 | 알림 주 경로 = **카카오톡 채널**, 웹푸시·쉘 푸시는 보조 | [02](02-web-architecture.md) §A D-T7 |
| D8 | 전환 순서 = W0 기반 → **W1 구독자 웹 최우선** → W2 기자·관제 → W3 쉘 → W4 정리. 네이티브는 승인 즉시 동결 | [08](08-rollout-transition.md) §A |
| D9 | 클라우드 전환 = **수치 트리거** 기반. 재무·전면 전환 트리거의 유일 원천은 05 §C, 08은 컴포넌트 단위 기술 트리거만 | [05](05-monetization.md) §C · [08](08-rollout-transition.md) §C |
| D10 | published 콘텐츠는 **공개 렌디션 버킷 + CF 캐시 서빙**으로 전환(W2) — api 다운 시에도 VOD 재생 생존. 그 전까지 "제온 다운=VOD도 중단"으로 정직 기재 | [reviews/ROUND-1-DECISIONS.md](reviews/ROUND-1-DECISIONS.md) X1 |
| D11 | 쉘 앱 내 **디지털 재화(후원·멤버십) 결제 동선 비노출**(리더 앱 모델) — IAP 리젝 리스크 구조 해소. 실물 커머스는 노출 유지 | 동 X3 |
| D12 | TTFF 단일 정의 = 카톡 탭→첫 프레임 **p75 ≤4초**(LTE·카카오 인앱), 정본은 02 §C. 라이브 지연 지표는 04 소유 별개 지표 | 동 X4 |
| D13 | 주민 제보 = **지사 발급 임시 업로드 링크**(로그인 없는 간단 모드 → 기자 검수 편입), W2 구현 | 동 X5 |
| D14 | 멀티파트 승격 트리거 = "주간 실패율 5% 초과 또는 평균 원본 300MB 초과" 단일 문장(02 소유) | 동 X6 |
| D15 | **실 STT·비전 주입·auto_edit는 웹 피벗 범위 밖** — 별도 트랙 T-AI로 분리, W 게이트 비의존. ai-worker는 현재 결정적 스텁임을 전 문서 정직 기재, "AI 자동편집" 단정 표현 절제 | [reviews/ROUND-2-DECISIONS.md](reviews/ROUND-2-DECISIONS.md) Y1 |
| D16 | 전화 CS = 평일 코어타임 주 25h 즉시 응대 + 그 외 콜백·카톡 유도(0.5인 인건비와 정합) | 동 Y3 |
| D17 | 예산·인력 수치의 단일 원천 = 05 §B, 수요 KPI 궤적·동시시청 환산 계수의 단일 원천 = 01 (타 문서는 인용만) | 동 Y6·Y7 · [R3](reviews/ROUND-3-DECISIONS.md) Z3 |
| D18 | 커머스 = **단계형**: MVP는 링크아웃(상품 카드·외부 링크·클릭 계측만 구현), 자체 결제는 GMV 월 300만원×3개월 또는 전환 손실 입증 시 착수(PG·에스크로·판매자 약관 일괄) | [reviews/ROUND-3-DECISIONS.md](reviews/ROUND-3-DECISIONS.md) Z1 |
| D19 | 후원·멤버십 = **범위 이월**(익명 구독자 체계와 충돌). 착수 트리거 = WAU 3,000명 + 커머스 2단계 이후. MVP 수익 축 = 링크아웃 커머스·B2B·광고협찬 3축 | 동 Z2 |
| D20 | 지사 담당자 직통 전화를 06 CS 채널로 정식 편입(03 발주 수신) | 동 Z4 |

## 3. 문서 맵 (평가 대상 전체)

| 문서 | 내용 | 주 평가 영역 |
|---|---|---|
| [01-product-strategy.md](01-product-strategy.md) | 서비스 성립성·레거시 미디어 흡수·콘텐츠 확보 | 2·4·5 |
| [02-web-architecture.md](02-web-architecture.md) | 웹 전환 기술 설계·재사용 판정·성능 예산 | 1·2 |
| [03-accessibility-ux.md](03-accessibility-ux.md) | 고령층 사용성·검증 방법·공급자 UX | 5·6 |
| [04-live-stability.md](04-live-stability.md) | 라이브 SLO·장애 매트릭스·리허설·Runbook | 7 |
| [05-monetization.md](05-monetization.md) | 수익 4축·손익 시나리오·클라우드 재무 트리거 | 3 |
| [06-cs-voc.md](06-cs-voc.md) | CS 채널·소통창구 라우팅·클레임 SLA | 8·9·10 |
| [07-legal-license.md](07-legal-license.md) | 스토어 정책·오픈소스·국내 규제·외부 약관 | 10·11 |
| [08-rollout-transition.md](08-rollout-transition.md) | 전환 단계·제온 운영·클라우드 트리거 | 1 |
| [reviews/](reviews/) | 루브릭 기준·라운드별 독립 평가 기록 | (프로세스) |

## 4. 기존 문서와의 관계

- CLAUDE.md·docs/architecture.md·docs/infrastructure.md·docs/ROADMAP.md는 **승인 전까지 무변경**(현행 체제의 사실 기록).
- 계획 승인 시: CLAUDE.md §4(앱 3종)·§5(스택)·§11(로드맵)·**§12(미정/결정 대기 — 본 계획이 해소한 항목 반영, 07 §3-5 발주 수신)**을 본 계획 기준으로 갱신하고(§11의 테스트 건수 stale 정정 74→102·13→24 포함, 02 §E 24번), ROADMAP에 W0~W4를 편입한다.
- infrastructure.md의 확정 사항(R2·CF Stream·RTZR·반자동 카카오·YT+FB)은 본 계획이 **전부 승계**한다.

## 5. 평가·수정 프로세스 (본 계획의 품질 게이트)

1. 초안 완성 → **독립 평가**(신규 에이전트, 내부 맥락 차단, RUBRIC.md 기준, 공격적) → `reviews/EVAL-ROUND-n.md`
2. 9.5 미만 영역 존재 시: 영역별 수정을 병렬 서브에이전트로 진행(동시 ≤5, 평가와 수정은 비동시)
3. 전 영역 9.5+ 도달 시 사용자 보고 → **승인 후** 실행계획 수립(동일 품질 게이트 적용)

## 6. 발주–수신 대장 (문서 간 위임 추적)

> **이후 모든 수정 라운드는 이 대장 갱신을 의무로 한다**([ROUND-4-DECISIONS.md](reviews/ROUND-4-DECISIONS.md) V5).

01~08 문서 중 한 문서(발주처)가 다른 문서(수신처)에 실행·등재를 요구한 항목을 전건 추적한다. **확인은 항목
번호가 아니라 제목·핵심어 grep**으로 한다(번호는 상대 문서의 재배열에 취약 — 07 §6이 세운 원칙을 그대로
따른다). 아래 상태는 **라운드 4 수정이 모두 끝난 현재 시점(2026-08-03)**을 기준으로 grep 재현 검증한
결과이며, 라운드 4 이전의 미수신 이력은 기록하지 않는다(과거 상태 아님 — 현재 수신 여부만).

**총 150행(마스터플랜 67행[선언치 — 실측 73행과 어긋나며 **#83**으로 등재, 정의 확정 전까지 선언치 유지]
+ EXEC 단계 6-11 83행) — 수신 완료 133건 / 트리거 대기 7건 / 미수신 10건.**
(**Wave 5 스윕에서 6-11에 3행 추가(#78~#80) + 기존 2행 수신 완료 전환(#67·#71)**. 검산: 6-11 80행 = 66 수신 +
5 트리거 + 9 미수신 / 마스터플랜 67행 = 65 + 2 + 0 → 합계 131 + 7 + 9 = 147. **미수신 총수는 9로 불변**이다 —
두 건이 닫히고 두 건이 새로 열렸다. Wave 4가 "실행이 깊어질수록 미수신이 4→9로 늘어난다"를 관측한 뒤 처음으로
**증가가 멈춘 웨이브**이며, 신규 2건(#78 제온 방화벽·#79 계측 공개 표면)은 **코드 결함이 아니라 배포 환경·
운영 표면**이라는 점에서 성격이 다르다. #80은 게이트②가 적발하고 **같은 웨이브 안에서 닫은 3번째 사례**다
(#60·#65·#61에 이어 — 다만 앞의 셋과 달리 **재기동 1회를 거친 첫 사례**이며, 그 발견이 **동형 태스크
2건의 교차검증**에서 나왔다는 점이 새롭다).)
(**Wave 4 스윕에서 6-11에 8행 추가(#70~#77)** — 실행 웨이브 단일 스윕 최대치이며 **8건 중 6건의 발주처가
게이트② qa-verifier**다. 그중 2건은 "**기능이 실제로는 닿지 않는다**"를 실측한 것이다: #70(`go.` 링크가
api에 미도달 — 격리 컨테이너로 업스트림 수신 경로 확인) · #71(기자 앱 웹 인증 배선 부재 — 유효한 쿠키
세션이 있어도 무조건 로그아웃). **#70은 같은 웨이브 안에서 닫았고**(T-W1-06 범위 확장, EXEC-DECISIONS #21 ⑤ⓐ)
#71은 **신규 태스크 신설 필요**로 판정하되 채번·배치는 Wave 5 편성 시점으로 미뤘다(⑤ⓑ). 舊 선언
"총 136행 … 수신 완료 127 / 트리거 대기 5 / 미수신 4". **미수신이 4 → 9로 늘었다** — 실행이 깊어질수록
계획이 상정하지 않은 통합 지점이 드러나는 국면이며, 그 적발 주체가 대부분 게이트②라는 점이 G6
("만든 자 ≠ 검증하는 자")의 실효를 보여준다.)
(**Wave 3 종료 시 #61 수신 완료** — 기한 "W1 착수 전"이 도래했고 EXEC-DECISIONS #19로 **웨이브 사이 단독
슬롯**에 배치해 같은 날 이행했다. 舊 선언 "수신 완료 126건 … 미수신 5건". 실행 웨이브 발생 위임(#60~#69)의
수신 완료는 #60·#65에 이어 **3건째**이며, **기한 도래를 조율자가 스스로 적발해 배치·이행까지 같은 웨이브
안에서 닫은 첫 사례**다.)
(**Wave 3 스윕에서 6-11에 3행 추가(#67~#69)**: T-W1-01 구현 에이전트 2건(#67 `global.css` 미배선 — 게이트②
qa-verifier가 독자 재현으로 사실 확정 / #69 `packages/ui` 컴포넌트 승격 시 `react` 피어 해시 정렬)과
**조율자 자신의 실측 발견 1건**(#68 `lineHeight` 하드코딩 23건 — 게이트④ 직전 산출물 검토 중). #67·#68은
미수신, #69는 트리거 대기다. 舊 선언 "총 133행 … 수신 완료 126건 / 트리거 대기 4건 / 미수신 3건" —
**미수신이 3 → 5로 늘었다**. #67은 기한이 W1 종료 전이라 이번 W1 구간 안에서 배치가 확정돼야 하고,
#68은 소비 전환 3태스크의 사이징 전제를 건드리므로 T-W2-05·06 착수 전 조율자 재판정 대상이다.
**조율자 자신이 발주처인 실행 웨이브 위임은 #68이 첫 사례**다.
**Wave 2 종료 스윕에서 6-11에 7행 추가(#60~#66 — 실행 웨이브 발생 신규 위임의 최초 등재분)**: 발주처가
계획 수립 라운드가 아니라 **구현·검증 에이전트**인 첫 사례다(#60~#64 = T-W0-01 구현 에이전트 5건,
#65~#66 = T-W0-01 게이트② qa-verifier 2건). 그중 2건(#60 env 스키마·#65 02 §D-T3 문안)은 같은 웨이브
안에서 조율자가 반영해 수신 완료, 3건(#61 웹 쿠키 e2e·#63 socket.io CORS 축소·#66 주석 부채)은 미수신,
2건(#62 FE 클라이언트 계약·#64 도메인 확정 시 재방문)은 트리거 대기다. 舊 선언 "총 126행 … 수신 완료
124건 / 트리거 대기 2건 / 미수신 0건" — **미수신이 0이 아닌 상태는 실행 단계 진입 후 처음**이며,
각 행의 "미수신 시 조치" 칸이 재상신 시점을 지정한다. 라운드 28 스윕에서 6-11에 1행 추가(#59 — 동일 문안 선언 3계열 정합·앵커 ⑯ 정의역 통일. 팀 발주 0건이라 이슈 자체를 등재 원천으로 삼았다), 舊 선언 "총 125행 … 수신 완료 123". 라운드 27 스윕에서 6-11에 4행 추가(#55~#58 — H2-1 적발 누락 위임 정식 등재·앵커 6건 확장·A팀 자기 적발 미등재 발주·위상 단정 금지 확장), 舊 선언 "총 121행 … 수신 완료 119". 라운드 26 스윕에서 6-11에 3행 추가(#52~#54 — 앵커 ⑨ 3축 확장·실행 컨텍스트 존량 종결·대장 #9 재개 하한 참조형 전환)하고 발주 문서 상태 칸을 행 단위로 전환(D23-6 5회차), 舊 선언 "총 118행 … 수신 완료 116". 라운드 25 스윕에서 6-11에 3행 추가(#49~#51 — 앵커 ⑫·⑬ 신설 1행·K2-1 실질 결함 해소 1행·조율자 파손 수리 1행)하고 발주 문서 상태 칸을 **행 단위 확인으로** 전환(D23-6 4회차·D25-0 재발 방지 첫 적용), 舊 선언 "총 115행 … 수신 완료 113". 라운드 24 스윕에서 6-11에 3행 추가(#46~#48 — 앵커 신설·개정 3건 묶음 1행·E4 §G #11 1행·EXEC-PLAN 개시 라벨 정정 1행)하고 같은 스윕에서 발주 문서 상태 칸을 전환(D23-6 2회차), 舊 선언 "총 112행 … 수신 완료 110". 라운드 23 스윕에서 6-11에 4행 추가(#42~#45 — 라운드 22 미등재 잔여 2건·라운드 23 신설 2건)하고 #40을 수신 완료로 종결, 舊 선언 "총 108행 … 수신 완료 105". **D23-6 스윕 양단 동기 규칙 첫 적용**: #42~#45 등재와 같은 스윕에서 발주 문서(E5 §신규 위임 #12·#16·#17·#18) 상태 칸을 `→ 6-11 #n 참조`로 전환했다. 라운드 22 스윕에서 6-11에 10행 추가(#32~#41 — D22-1 T-W1-11c 신설 파급 4건·D22-8 1건·E5 제출 3건·E4 제출 1건·EXEC-DECISIONS #9 신설 1건), 舊 선언 "총 98행 … 수신 완료 96".
EXEC 단계 편입: EXEC-EVAL-ROUND-1 X-1 정정, 2026-08-04 — 6-11 행 추가 시 본 총계도 함께 갱신한다.
舊 선언 "총 67행 — 수신 완료 65 / 트리거 대기 2 / 미수신 0"은 마스터플랜 시점 기준으로 정확했다.) (라운드 16~18에서 6-10에 총 6행 추가 — 등재
책임 규칙(P-4)이 세 라운드 연속 작동. 라운드 18에는 게이트·상한 신값(세무·회계 비용 반영)의 08 재전파,
D1 재사용률 재갱신(51.8%/66.4% — D1은 이후 사본을 두지 않고 02 §B만 정본), 6-5 행 4 갱신도 조율자가 수행.)
**KPI 측정-판정 명시 규칙(라운드 18 신설 — EVAL-ROUND-18 S-2, "분모 밖 위임" 4회차 대응)**: 각 문서가 KPI의
측정 방법을 확정할 때, **그 측정이 타 문서의 판정(법률·기술·재무)을 요구하면 KPI 행에 "요구 판정: <문서 §절>"을
명시**한다 — 측정 방법을 쓰는 순간이 위임이 생기는 순간이며, 자각 없는 위임은 등재 책임 규칙으로도 잡히지 않는다. (라운드 13에서 6-9에 M5 행 1건 추가. 라운드
14에서 6-10 신설 3행 — 카카오 게시 역할(O1)·망법 §50(O2)·연령대 지표(O5). 라운드 15에서 6-10에 2행 추가 —
EVAL-ROUND-15가 적발한 "분모 밖 위임" 2건(P-1 웹푸시 단서·P-2 §12 편입/센터 게시 부하)을 조율자가 즉시
반영·등재해 같은 라운드에 마감. 총부하 SSOT 신값(37~47h) 전파는 라운드 14에 완료 — 6-5 행 3 참조.)
**등재 책임 규칙(라운드 15 신설 — EVAL-ROUND-15 P-4, "분모 밖 위임" 3회 재발 대응)**: 새 위임을 만드는 문서
소유자(수정 에이전트)는 **자기 문서에 위임을 쓰는 것과 동시에, 완료 보고에 "대장 등재 필요 신규 위임 목록"을
명시적으로 제출**해야 한다 — 조율자·PMO가 문서를 다시 뒤져 위임을 발굴하는 단독 구조가 단일 실패점이었다. (라운드 10에서 6-8 신설 1행(대외 런칭 게이트,
양측 동시 신설·조율자 grep 검증) 추가. 라운드 9 PMO 실사에서 적발된 미수신 1건(6-2
22번 장비 조달)은 조율자가 즉시 05에 반영·grep 재검증해 마감했다. 대장 최초 작성 시점의 미수신 7건은 라운드 4
마감 수정에서 전건 반영·재검증 완료. 라운드 6에서 신규 위임 2행(6-2의 19·20) 추가. 라운드 7에서 04 발주
트리거성 위임 2행(6-6) 추가 — 실측·견적 확보 후에만 발동하는 위임이라 "트리거 대기"가 정상 상태다. **라운드
8에서 6-4에 3행 신규 등재**(07이 라운드 6·7에 신설한 05·06·08향 발주 3건 — [EVAL-ROUND-8](reviews/EVAL-ROUND-8.md)
G-4·G-5·G-6, 종합 지적 G-8: 이 3건이 대장에 미등재였던 탓에 舊 "총 44행·미수신 0건" 선언이 분모 밖의 위임을
깔고 있었다. 舊 44행 계수 자체(16+19+4+3+2)는 정확했다 — 누락은 계수 오류가 아니라 분모가 3건 작았던
것이다. 3건 전부 이번 라운드 grep 재확인 결과 수신 완료다 — 아래 6-4 참고, 확인 근거는 각 행의 "확인
방법" 칸.) **라운드 9에서 신규 위임 6행 등재**(6-2에 2행(21·22, 01 §D절 "지사 확장 실행 조건" 신설이
자신의 부록 표에 새로 얹은 05향 위임) + 6-7 신설 4행(EVAL-ROUND-9 ④ 정합성 이슈·[ROUND-9-DECISIONS](reviews/ROUND-9-DECISIONS.md)
P1·P4·P5·P6이 만든 문서 간 위임) — 전건 grep 재현 검증 결과 5건 수신 완료·1건 미수신(6-2 신규 22번)을
적발했고, **적발된 1건은 조율자가 즉시 05 §B-1에 "신규 지사 장비 조달" 문단으로 반영·재검증해 같은 라운드
안에 마감했다**(최종: 6건 전건 수신 완료). 경위는 아래 6-2 22번 행 참고.) **라운드 12에서 6-9 신설 1행 등재**
([EVAL-ROUND-12](reviews/EVAL-ROUND-12.md) L-1·영역 1 감점 1·영역 11 감점 1 — 07 §1-4가 선언한 X3 예외(쉘
내 디지털 재화 비노출)가 03에 전달되지 않아 이 대장의 분모 밖에 있던 위임쌍이었다. 舊 "총 54행·미수신 0건"
선언은 이 위임쌍이 등재되기 전 분모 기준으로는 정확했다 — 라운드 8 G-8과 동일한 "분모 밖 위임" 유형의
재발이며, 이번에 반영해 재발 자체를 닫는다). 07 §1-4 액션 ④가 같은 라운드 안에 03향 발주를 신설했고 조율자가
직접 grep으로 즉시 수신을 확인해 같은 라운드 안에 마감했다(미수신 0건 유지). 라운드 12에서 6-2·6-4·6-7의
`파일:줄번호` 수신 증거 9개소(대장 자신이 세운 "번호가 아니라 제목·핵심어 grep" 원칙과 자기모순이던 stale
좌표, EVAL-ROUND-12 L-4)와 6-7 4번의 확인 방법(04에서 재현 실패하던 `go\. 가용성`, L-3)도 함께 정정했다 —
모두 "문서 §섹션 + 확인 문자열" 방식(6-5 방식)으로 교체.
**이 대장 자체는 원 문서를 수정하지 않는다** — 이후 라운드에서 새 위임이 생기면 행을 추가하고 상태를 갱신한다.
**신규 결정(횡단 DECISIONS)의 인용처는 발주–수신 1쌍이 아니라 인용처 전건으로 등재한다**(EVAL-ROUND-11 K-10 —
한 결정을 3개 이상 문서가 인용할 때 쌍 단위 추적은 나머지 인용처의 드리프트를 놓친다. 6-8이 실제 사례).
**6-5 인용 대조는 값(수치·정의)뿐 아니라 기산점(기준 시점)의 변경도 대조 대상으로 한다**(EVAL-ROUND-13 영역 2 —
같은 "1개월"이라도 기산점이 "MVP 착수"에서 "대외 런칭 게이트"로 재정박되면 인용처 라벨 전건을 함께 갱신해야 한다).

### 6-1. EVAL-ROUND-4 ④ "문서 간 정합성 이슈" 중 위임 관련 전건

> D-1~D-18 중 **D-9**(코드 실측 대 문서 서술의 사실 오류 — 문서 간 위임이 아님)와 **D-17**(03 문서 내부
> 절 간 모호성 — 문서 간이 아님)은 정의상 이 대장의 대상이 아니라 제외한다. 나머지 16건 중 **D-3·D-4·D-5·
> D-6**은 07 §6 "발주 수신 확인 추적"이 자체 추적하는 4건과 **동일한 위임쌍**이다 — 07이 스스로 grep
> 원칙을 재적용해 2건의 오기재(D-5·D-6)를 정정하고 4건 전건을 "수신 완료"로 종결 처리했음을 본 대장이
> 재확인했다(아래 표에 반영).

| # | 발주처 | 수신처 | 확인 방법(grep) | 기한 | 상태 | 미수신 시 조치 |
|---|---|---|---|---|---|---|
| D-1 | 01 §C-5(횟수)·05 §B-1(회당 1시간) | 04 §A·§E(방송시간 모수 재계산) | 04 §A에서 `05 §B-1`·`01 §C-5 캘린더` 인용 + M3 21.3h/M6 25.3h/M12 37.3h 표 | 즉시(R4) | **수신 완료** | — |
| D-2 | 01 §B-3·§B-7(크레딧) + 06 §C①·07 §5-2(회수·방영중단) | 05 §A-2 B2B 계약서 표준안 | 05 grep `크레딧`·`회수`·`방영중단` | 첫 B2B 계약 전 | **수신 완료** | — |
| D-3 | 07 §3-14 액션2 | 05 §A-3 실행조건6 | 05 grep `집계 통계` | 첫 협찬 계약 체결 전 | **수신 완료**(07 §6 추적항목1과 동일쌍) | — |
| D-4 | 07 §3-3 액션2 | 02 §E 20번 + 03 §C-2-1 | 02·03 grep `미성년자`·`법정대리인` | 첫 촬영 재개 전 | **수신 완료**(07 §6 추적항목4와 동일쌍) | — |
| D-5 | 07 §1-2 | 02 §E 18번(installability CI) | 02 grep `installability` | TWA 패키징(02 §E 10번) 착수 전 | **수신 완료**(舊 "미수신" 오기재를 07 §6이 자체 정정) | — |
| D-6 | 07 §3-14 액션4 | 03 §A-2(쿠키 잠정원칙 단서) | 03 grep `쿠키 동의 전체화면 금지의 법적 지위` | — | **수신 완료**(오기재 정정) | — |
| D-7 | 02 §D-T8(공개 렌디션 버킷+CF 캐시 신설) | 06 §C①·§E, 07 §5-2 | 06·07 grep `공개 렌디션`·`캐시 퍼지` | D-T8 가동 전(W2) | **수신 완료** | — |
| D-8 | 05 §A-1/§E·04 §E(M6 월 4회 램프) | 01 §C-5·§C-7(총부하 재계산) | 01 grep `M6부터` | 즉시(R4) | **수신 완료**(M6 이후 24~34/31~41h 재계산 + 상시초과 신규 리스크 §C-7 등재) | — |
| D-10 | 07 §1-3(최대 리스크 선검증) | 02 §E 0-1번(W0~W1 PoC), 08 W1 선행조건 | 02 grep `0-1`·`W0~W1 병행 PoC`, 08 grep `V3` | W1 착수 전 | **수신 완료** | — |
| D-11 | 04 §B④(라이브 신규진입 완화책 전제) | 02 §B/§E 21번(정적 편성표 페이지), 08 W1 | 02·08 grep `편성표` | W1 | **수신 완료** | — |
| D-12 | 04 §H 13번 | 08 §E 10번(Meta App Review) | 08 grep `Meta App Review` | 사업자등록 완료 직후 | **수신 완료** | — |
| D-13 | 04 §H-1(舊 요구, R10 게이트) | 08 §A W2 DoD | 08 §A grep `R10` | 첫 실 방송 전 | **수신 완료**(04 §H-1 서두의 "미반영" 오기재를 이번 라운드 정정) | — |
| D-14 | 01 §C-6(항목번호 확정 시 갱신 약속) | 02 §E 16번 | 01 grep `02 §E 16번` | 02 §E 16 확정 즉시 | **수신 완료**(갱신됨) | — |
| D-15 | 05 §H(Z1 단계정의 소유) | 06 §C KPI(커머스 분쟁률·청약철회) | 06 §C grep `Z1 단계 분리` | — | **수신 완료**(1단계/2단계 분리 표기) | — |
| D-16 | 02 §E 18번(기준시점 소유) | 07 §6·§1-2(자기 시점 표현 정정) | 07 grep `완료 기한은 02 §E 18번과 동일` | — | **수신 완료** | — |
| D-18 | 06 §A(전담 CS 인건비 150~200만원 편입 요청) | 05 §B-3 인건비 표 | 05 §B-3 grep `CS` 인건비 라인 | 조직·인건비 계획 확정 시(05 §H 트리거) | **수신 완료(라운드 4 마감)** — 05 §B-3 표에 "트리거 발동 시 증분" 행으로 편입(스냅샷 비용과 분리, 트리거 미도달 시 0원 명시) | — |

### 6-2. 01 부록 "교차 문서 의존성 요약" 전건

> 01 §부록 15행(B-3·B-7 크레딧 표기 의무 → 05 §A-2)은 6-1의 **D-2**와 동일 위임쌍이라 별도 행을 만들지
> 않고 D-2로 흡수한다(중복 기재 방지). 아래는 나머지 21행(라운드 6 신설 19·20번, 라운드 9 신설 21·22번 포함 —
> EVAL-ROUND-11 K-5 정정: 舊 "17행" 표기는 신설 행 반영 전 stale 계수였다).

| # | 발주처 | 수신처 | 확인 방법(grep) | 기한 | 상태 | 미수신 시 조치 |
|---|---|---|---|---|---|---|
| 1 | 01 §A-3-① | 02 §D-T4·08(presigned 파트 분할 설계) | 02 grep `멀티파트 승격` | — | **수신 완료** | — |
| 2 | 01 §A-3-①·§A-5 | 02 §D-T2(쉘 앱 기술스택) | 02 §D-T2 본문 | — | **수신 완료·확정**(더 이상 위임 대기 아님) | — |
| 3 | 01 §A-3-④·§A-6 | 03(홈 화면에 추가 온보딩 UX) | 03 grep `홈화면 추가` | — | **수신 완료**(§A-5) | — |
| 4 | 01 §A-6 | 03 §C-3-1(업로드 이탈 방지 배너/다이얼로그 UX) | 03 §C grep `이탈 방지` | W1 착수 전 | **수신 완료(라운드 4 마감)** — 03 §C-3-1 신설(`beforeunload` 다이얼로그 + 페이지 내 배너, §C-3 복구와 구분) | — |
| 5 | 01 §B-3·§B-7 | 07 §3-9-1(B2B 시연 샘플 이용범위 법무 검토) | 07 grep `시연`·`워터마크` | B2B 착수 전 | **수신 완료(라운드 4 마감)** — 07 §3-9-1 신설(이용범위 고지 문구·재배포 금지·워터마크 표시 원문, 02 §E 15번 인용 대상 실체화) | — |
| 6 | 01 §B-3·§B-7 | 02 §E 15번(B2B 워터마크, 기술 구현) | 02 grep `워터마크` | B2B 착수 전 | **수신 완료** | — |
| 7 | 01 §B-4·§B-7 | 05 §B-3(객원 인력 위촉계약 손익검증) | 05 grep `01 §B-4가 위임한` | — | **수신 완료** | — |
| 8 | 01 §B-4·§B-7 | 07 §5-5(객원 인력 위촉계약 필수 조항) | 07 grep `경업`·`전속계약` | 후보자 컨택 전 | **수신 완료(라운드 4 마감)** — 07 §5-5 신설(경업·겸직 허용 범위, 저작권 귀속, 초상 이용 3개 필수 조항 + §6 "객원 인력 첫 컨택 전" 체크리스트) | — |
| 9 | 01 §C-1 | 03 §C-2-2(카테고리별 Scene 순서 체크리스트 UI) | 03 grep `Scene 순서` | MVP 착수 전 | **수신 완료(라운드 4 마감)** — 03 §C-2-2 신설(2종 즉시 반영, 잔여 4종은 01 §C-8 원안 산출 대기를 명시) | — |
| 10 | 01 §C-2·§C-7 | 02 §D-T9·08(주민 제보 임시 업로드 링크) | 02 grep `임시 업로드 링크` | W2 | **수신 완료**(X5 확정) | — |
| 11 | 01 §C-3·§C-7 | 07(날씨예보 면책 고지 문구 법무 검토) | 07 grep `날씨예보` | 날씨예보 콘텐츠 최초 제작 전 | **수신 완료**(§3-12, "이번 문서로 수신 완료" 명기) | — |
| 12 | 01 §C-4 | 05 §D-4(라이브커머스 이벤트 운영방안·비용) | 05 grep `라이브커머스 이벤트 운영` | — | **수신 완료** | — |
| 13 | 01 §C-5 | 05 §B-3(날씨예보 갱신빈도 확대의 인력 손익 정합) | 05 grep `예보` | — | **수신 완료(라운드 4 마감)** — 05 §B-3에 확대 트리거(B-4 인력 확충 완료 시점)·증분 공수(01 §C-7 촬영 건당 ≈4h 기준) 항목 신설 | — |
| 14 | 01 §0-1·§A-1·§A-5 | 08(T-AI 트랙 등재·일정) | 08 grep `T-AI` | — | **수신 완료** | — |
| 16 | 01 §B-4·§B-7 | 05 §B-3(객원 활동비 시장가 근거) | 05 grep `01 §B-7 위임 수신` | MVP 착수 전 | **수신 완료** | — |
| 17 | 01 §C-7·§C-8 | 05 §B-3/G절(지사 담당자 겸임 총부하표) | 05 grep `01 §C-7` | MVP 착수 전 | **수신 완료(라운드 4 마감)** — 05 §B-3 "정합성 한계" 문단과 §G 겸임 매핑표 체크리스트가 01 §C-7 총부하표(M6 이후 당번 주 최대 ≈41h)를 명시 인용(3개소). 매핑표 산출물 자체는 조직계획 확정 트리거 대기(정상) | — |
| 18 | 01 §C-6·§C-8 | 02 §E 16번(콘텐츠 소비 계측 이벤트 파이프라인) | 02 grep `01 §C-8` | W1 | **수신 완료** | — |
| 19 | 01 §A-2·§A-6 (라운드 6 신설) | 02 §E 22번(유지보수 공수 절감·상계 실측, W4) | 02 grep `유지보수 공수 절감·상계 실측` | W4 | **수신 완료(라운드 6)** — 01의 수신 확인 문구가 02 실제 항목명과 어긋났던 것을 조율자가 정정(01 §A-2·§A-6 2곳) | — |
| 20 | 01 §C-7 택일 (a) 증원 인건비 (라운드 6 신설) | 05 §B-3① "트리거 발동 시 증분" 행(+150만원/월, 트리거=M5 말 (a) 선택) | 05 grep `지사 담당자 증원 인건비` | M5 말 택일 시 | **수신 완료(라운드 6)** | — |
| 21 | 01 §D-1(지사 확장 실행 조건, 라운드 9 신설) | 05 §B-3①(지사 확장 게이트 미충족·미실현 시 "확장 미실현 시나리오" 손익 재계산) | 05 grep `확장 미실현` | M6 말 D-1 게이트 판정과 동시 | **수신 완료(라운드 9)** — 05-monetization.md §B-3에서 "확장 미실현(2지사 고착) 시나리오" 신설 확인(확인 문자열 `확장 미실현`), "지사 확장의 실행 조건은 05가 아니라 01이 소유한다"고 명시 인용 확인 | — |
| 22 | 01 §D-2(지사 확장 실행 조건, 라운드 9 신설) | 05 §B-1(신규 지사 확장 시 장비(촬영 단말·업로드용 PC) 조달 비용 편입) | 05 grep `장비 조달` | MVP 착수 전(가정) | **수신 완료(라운드 9 마감, 조율자 반영)** — 05 §B-1 "신규 지사 장비 조달" 문단 신설: MVP(기존 2지사)=0원 가정(기보유 기기), 신규 지사 확장 확정 시 1회성 약 150~200만원/지사(가정), 트리거성 지출로 계상(D-18 패턴). PMO 최초 적발 → 조율자 직접 마감 | — |

### 6-3. 03 발주 → 02·06·08 수신

| # | 발주처 | 수신처 | 확인 방법(grep) | 기한 | 상태 | 미수신 시 조치 |
|---|---|---|---|---|---|---|
| 1 | 03 §A-1(타이포/터치타깃 확정 수치) | 02 §E 1번(`packages/ui` 디자인 토큰 승격) | 02 grep `03 §A-1` | W1 착수 전(미완료 시 W1 차단) | **수신 완료** | — |
| 2 | 03 §B-2(재생 도달률 계측 인프라) | 02 §E 16번(콘텐츠 소비 계측 파이프라인, 01 §C-8과 공동 발주) | 02 grep `03 §B-2` | W1 | **수신 완료** | — |
| 3 | 03 §A-6·§B-3(지사 담당자 직통 전화) | 06 §B(CS 채널 정식 편입, Z4) | 06 grep `03 §A-6` | — | **수신 완료** | — |
| 4 | 03 §A-1·리스크8 게이트(디자인 토큰 승격 완료) | 08 W1 선행조건 | 08 §A grep `03 §A-1·리스크 8 게이트` | W1 착수 전 | **수신 완료** | — |

### 6-4. 06 ↔ 07 상호 발주 + 07 라운드 6·7 신설 발주(→05·06·08, 라운드 8 확장)

> **라운드 8 확장(4~6행 신규)**: 07이 라운드 6·7에 새로 만든 05·06·08향 발주 3건이 이 대장에 등재되지 않은
> 채로 남아 있던 것을 [EVAL-ROUND-8](reviews/EVAL-ROUND-8.md) G-4·G-5·G-6(개별 이슈)·G-8("미수신 0건" 선언
> 불성립의 근거)이 지적했다. [ROUND-8-DECISIONS.md](reviews/ROUND-8-DECISIONS.md) Q3에 따라 3건을 신규 행으로
> 등재하고, 등재와 동시에 grep을 실제 실행해 수신 여부를 판정한다(미수신이면 미수신으로 정직 기재 — 실행하지
> 않은 검증을 "확인했다"고 쓰지 않는다는 Q1 원칙을 그대로 적용).

| # | 발주처 | 수신처 | 확인 방법(grep) | 기한 | 상태 | 미수신 시 조치 |
|---|---|---|---|---|---|---|
| 1 | 06 §A(지사 담당자 개인 회선 동의·번호 승계 정책, V6) | 07 §5-4(개인정보처리방침 반영 요건) | 07 grep `운영 정책 원천은 06 §A` | 이용약관·개인정보처리방침 최초 초안 발주 시 | **수신 완료** | — |
| 2 | 07 §5-1(정보통신망법 임시조치 법적 근거) | 06 §C①(삭제 절차 운영 원천) | 06 grep `07 §5-1` | — | **수신 완료**(상호 인용 구조 — 06이 절차를 소유하고 07이 법적 근거를 공급) | — |
| 3 | 06 §C①·§C③(법률자문 계약 의존) | 07 §6(법률자문 계약 체결 체크리스트) | 07 §6 grep `06 §C①` | 첫 촬영 재개 전 | **수신 완료** | — |
| 4 | 07 §5-6 액션2(라운드 6 신설) | 05 §B-3① 인건비 표 단가 셀 각주(고용형태 판정 전 잠정치) | 05 grep `고용형태 판정 전 잠정치` | 첫 채용·위촉 계약 체결 전 | **수신 완료(라운드 8)** — grep 실행 결과 05-monetization.md §B-3① 각주("¹ 각주 — 상시 인력 단가는 고용형태 판정 전 잠정치…")로 실재 확인(확인 문자열 `고용형태 판정 전 잠정치`, EVAL-ROUND-8 G-4 해소) | — |
| 5 | 07 §5-7 액션2(라운드 7 신설) | 08 §B "보안사고 대응"(침해 감지·로그 보존) | 08 grep `침해 감지` | 개인정보처리방침 최초 공개 전 | **수신 완료(라운드 8)** — grep 실행 결과 08-rollout-transition.md §B 서술·§E 3-2번 체크리스트 2곳 실재 확인(확인 문자열 `침해 감지`, EVAL-ROUND-8 G-6 해소) | — |
| 6 | 07 §5-7 액션3(라운드 7 신설) | 06 §C⑧ "개인정보 유출·침해사고 제보" 유형 | 06 grep `유출·침해사고` | 개인정보처리방침 최초 공개 전 | **수신 완료(라운드 8)** — grep 실행 결과 06-cs-voc.md §C 클레임 유형 표 ⑧행에서 실재 확인(확인 문자열 `유출·침해사고`, EVAL-ROUND-8 G-5 해소) | — |

### 6-5. 단일 원천 값 인용 대조 (인용 갱신 유형)

> 이 소절은 [ROUND-5-DECISIONS U5](reviews/ROUND-5-DECISIONS.md)에 따라 신설 — 원천 값이 바뀌는 라운드는
> 해당 행의 인용처 전건 대조가 의무다. 6-1~6-4가 "실행·등재 위임"을 추적한다면, 이 소절은 "숫자·정의 자체"의
> SSOT(단일 원천) 6종과 그 값을 그대로 재사용하는 모든 지점을 추적한다 — 원천이 갱신되고 인용처가 갱신되지
> 않으면 문서 간 수치가 어긋난다(舊 05 §A-1/§B-1 독자 정의 대 01 §C-5/§C-6 이관 이력이 실제 발생 사례). 아래
> 표는 2026-08-03(라운드 5 마감) 시점 grep 재현 검증 결과이며, 인용처 목록은 문서·섹션 단위로 집계한다(줄
> 번호 아님 — 07 §6과 동일 원칙).

> **Q1 재발 방지 규칙(라운드 8 신설 — [ROUND-8-DECISIONS.md](reviews/ROUND-8-DECISIONS.md) Q1)**: 이 소절의
> "확인했다"·"일치"·"불일치" 서술은 **그 라운드에 조율자가 실제로 실행한 grep 결과가 있을 때만** 기재한다.
> 원천 값이 변경된 라운드에서는 조율자가 해당 행의 인용처 전건에 대해 grep을 실제로 실행하고 결과(일치/불일치
> 목록)를 이 소절에 기록하는 것이 의무다 — grep을 실행하지 않고 "확인했다"고 서술하는 것은 라운드 7에서
> 실제로 발생한 허위 검증([EVAL-ROUND-8](reviews/EVAL-ROUND-8.md) G-3 — 아래 舊"라운드 7 갱신" 문단이
> grep 미실행 상태로 "확인했다"고 기재했다가 05·06에 구값이 잔존한 채로 적발됨)이며, 이 문서에서는 재발을
> 금지한다.

| # | 원천(문서 §섹션 · 값 요약) | 인용처 목록(문서 §섹션) | 대조 방법(grep) | 갱신 규칙 |
|---|---|---|---|---|
| 1 | **01 §C-5**(콘텐츠 캘린더 — 방송 슬롯·정기 라이브 주 2회×2h≈월17.3h·라이브커머스 회당 1h·빈도 램프 M3 월2회→M6 월4회) | 01 §C-7(총부하표 리스크 행, 내부 인용) · 04 §A(SLO#1 모수 주석)·§E(월간 총 방송시간표) · 05 서두(변경이력)·§A-1(GMV표)·§B-1(라이브 비용표)·§B-3(날씨예보 갱신빈도 공수 증분) | `01 §C-5` | 값(횟수·회당 시간·램프 시점) 변경 시 04 §A·§E, 05 §A-1·§B-1·§B-3 전건 재계산 후 본 행 인용처 목록 갱신. 불일치 발견 시 ⚠ 표기 |
| 2 | **01 §C-6**(WAU 궤적 M1≈40·M3≈400·M6 애월분≈600+ · 동시시청 환산 계수 WAU×5~15%) | 03 §C-5(주민 제보 비중 KPI 시점) · 04 §E(동시시청·CF Stream 비용) · 05 서두(변경이력)·§A-1(회당 동시시청표)·§A-3(최소 시청 지표·CPM 검증)·§A-4(후원·멤버십 착수 트리거)·§B-1(라이브 비용)·§B-3(날씨예보 갱신빈도)·§E(KPI 대시보드)·§H(문서 범위 경계) · 06 §A(MVP 예상 문의량 가정) | `01 §C-6` | WAU 궤적 또는 계수 구간 변경 시 04 §E·05 전 절(§A-1·§A-3·§A-4·§B-1·§B-3·§E·§H)·06 §A 전건 재계산 후 본 행 인용처 목록 갱신. 불일치 발견 시 ⚠ 표기 |
| 3 | **01 §C-7**(지사 담당자 주간 겸임 총부하 — W2 가동 전/후 × 당번·커머스 조합별 구간, **최대 약 37~47h**(당번·커머스 겹침 주 — 라운드 14 재계산: 카카오 채널 게시 행(≈10분/건) 신설, 舊 36~46h 폐기. 이력: 라운드 7 QR 부스·간단 모드 자막 반영으로 31~41h→36~46h. **라운드 14 전파(조율자 grep)**: 05 4곳·06 1곳 신값 갱신 완료, 이력 서술 내 舊값만 잔존) | 05 §B-3(겸임 매핑표 입력값)·§G(체크리스트) · 06 §A(CS 1선·전화 응대 겸임 전제)·§F(실행 체크리스트 모니터링 항목) | `01 §C-7` | 총부하 상한·시간 구간 변경 시 05 §B-3·§G, 06 §A·§F 전건 재계산 후 본 행 인용처 목록 갱신. 불일치 발견 시 ⚠ 표기 |
| 4 | **05 §B-3/§G**(인건비·예산 — 기자 150만원/인~센터 운영 200만원/인 단가, 누적 순현금유출 상한 가정 **1억 5,000만원**(라운드 18 상향 — 세무·회계·법인 부대비용 신설로 보수 상단 12,423.9만원×1.2 재산정, 舊 1억 4,700(R17)·1억 4,500(R13)·1억 3,800(R7) 폐기), §G MVP 착수 게이트 금액 **기본 약 2,018.2~2,344.5만원 / 보수 약 2,056.8~2,383.4만원**(보수 상단 약 2,383만원 하한 권장 — 라운드 18 신값. **라운드 18 전파(조율자)**: 08 §A W0·§E 1번 인용 2곳 신값 갱신·grep 검증), 트리거 발동 시 증분 행) | 01 §B-4(객원 인력 처우 기대공수)·§C-7(리스크 완화책 인용)·§C-8(체크리스트)·부록(위임 매핑 행) · 03 §B-1(어르신 패널 운영비 반영 원천) · 06 §A(코어타임 산술 정합·CS 인건비 환산·헬프데스크 도구비)·§B(지사 확장 지역 매니저 인건비·B2B 사업개발 담당) · 07 §5-5(객원 인력 처우·수익쉐어 금액 단일 원천) · **08 §A W0·§E 1번(MVP 착수 게이트 금액 — 라운드 8 신규 등재, [EVAL-ROUND-8](reviews/EVAL-ROUND-8.md) 영역1 수정지시2 — 舊 목록에 08이 없어 08의 게이트 금액 불일치[G-1]가 이 소절로 구조적으로 검출되지 않았다. **라운드 9: 이 행이 다시 구값 잔존을 검출 — 아래 라운드 9 갱신 문단 참고**)** | `05 §B-3` 및 `05 §G` | 인건비 단가·현금 상한·MVP 착수 게이트 금액 등 값 변경 시 01 §B-4·§C-7·§C-8·부록, 03 §B-1, 06 §A·§B, 07 §5-5, **08 §A·§E** 전건 재계산 후 본 행 인용처 목록 갱신. 불일치 발견 시 ⚠ 표기 |
| 5 | **05 §C**(클라우드 전환 트리거 — 전면 전환 ①~⑤ 중 2개 이상 동시 충족, 컴포넌트 조기 이관 ②·⑤ 단독 충족) | 04 §A(08 §C 경유 간접 인용)·§G(SLO-트리거 계측 단절 해소) · 08 §A(W1 DoD 판정 불가 시 05 §C 트리거 점검 상정)·§C(전면 전환 ①~⑤ 재인용, 컴포넌트 조기 이관 ②·⑤ 수치 인용)·§E(대시보드 체크리스트) | `05 §C` | 트리거 번호·임계값 변경 시 04 §A·§G, 08 §A·§C·§E 전건 재계산 후 본 행 인용처 목록 갱신. 불일치 발견 시 ⚠ 표기 |
| 6 | **02 §C**(성능 예산 — TTFF 정본: 카톡 링크 탭→VOD 첫 프레임, p75 ≤4초, LTE·카카오 인앱 브라우저 기준) | 01 §A-3(지원 브라우저 범위) · 03 §0(문서 존재 이유 서두)·§B-1(패널 과업1 소요시간 상한, 정본과 다른 지표로 구분 명시)·§B-2(정량 지표)·KPI 요약 · 04 §0(라이브 지연 지표와의 구분 선언)·§B⑨(카카오 인앱 웹뷰 검증) · 07 §1-2(Lighthouse CI 게이트 동일 잡)·§1-3(`<input capture>` 카카오 인앱 검증)·발주 수신 확인 추적(02 §E 18번 반영 확인) · 08 §A(W1 측정 프로토콜 p75 판정 기준)·§E(CI installability 확인) | `02 §C` 및 `TTFF` | 정의·목표치(p75·4초·측정 조건) 변경 시 01 §A-3, 03 §0·§B-1·§B-2·KPI요약, 04 §0·§B⑨, 07 §1-2·§1-3·발주수신확인추적, 08 §A·§E 전건 재계산 후 본 행 인용처 목록 갱신. **Lighthouse·installability 행의 적용 범위(구독자 앱=게이트/기자·관제=참고, 라운드 16 확정)도 TTFF와 나란히 대조 대상**(02 §C↔07 §1-2·§6↔02 §E 18번). 불일치 발견 시 ⚠ 표기 |

**대조 결과(2026-08-03, 라운드 5 마감 시점)**: 6행 전건 grep 재현 완료, 표본 검증(01 §C-5 램프값 대 04 §E·05 §B-1
수치 일치, 01 §C-7 총부하 구간값 대 06 §A 인용치 일치, 05 §C 트리거 ①~⑤ 대 08 §C 재인용치 일치) 결과
**불일치 없음**. 이 대조는 표본 검증이며 grep 재현이 확인한 범위 내 결과다 — 전건 수치 재계산 검증은 아니다.

**라운드 8 갱신(2026-08-03) — 실제 grep 대조 결과(Q1 규칙 적용, 舊 "라운드 7 갱신" 문단 대체)**: 舊 문단은
"06 §A는 이미 갱신 완료, 05 §B-3·§G와 06 §F도 신값 기준으로 재계산됨을 확인했다"고 기재했으나 grep을
실행하지 않은 상태의 선언이었고, [EVAL-ROUND-8](reviews/EVAL-ROUND-8.md) G-2·G-3이 05 §B-3·§G(3곳)·06 §A에
舊값(41시간·28~36/31~41시간)이 실제로 잔존한 것을 적발했다(허위 검증). 이번 라운드에 조율자가 직접 실행한
grep 결과는 다음과 같다.

- **행 3(01 §C-7 총부하, 최대 약 36~46h) 인용처**: `grep -n "41시간" 05-monetization.md` → 3곳(§B-3①
  증원 인건비 주석·§B-3 정합성 한계 문단·§G 겸임 매핑표 체크리스트) 전부 "약 36~46시간(…정정 전 표기 약
  41시간)" 형태로 신값이 본문에, 舊값은 폐기 표시로만 남아 있음을 확인 — **일치**. `grep -n "41시간"
  06-cs-voc.md` → §A 1곳, "M3~M5 당번·커머스 있음 주 약 36~46시간, M6 이후 당번 주 약 36~46시간"으로
  신값이 본문에 있고 舊 "28~36/31~41시간"은 "01 §C-7이 라운드 7에 이미 폐기 선언한 舊값이 06에 미전파된
  것이었다(EVAL-ROUND-8 G-2)"는 정정 주석으로만 남아 있음을 확인 — **일치**. 06 §F(14번)는 구간값을 직접
  인용하지 않고 01 §C-7 트리거(상한의 90% 2주 연속 초과)만 참조하므로 대조 대상 값 자체가 없음 — **해당
  없음(값 미인용, 불일치 여지 없음)**.
- **행 4(05 §G MVP 착수 게이트 금액) 인용처 — 이번 라운드 신규 등재된 08 §A W0·§E 1번**: `grep -n
  "1,789\|1,935\|1,827\|1,974\|1,975" 08-rollout-transition.md` → §A W0·§E 1번 모두 "05 §G 정본을 그대로
  인용": 기본 약 1,789.0~1,935.7만원 / 보수 약 1,827.6~1,974.6만원(상단 약 1,975만원 하한 권장)으로,
  05 §G "MVP 착수 게이트" 원문(확인 문자열 `MVP 착수 게이트`)의 실제 값과 대조 결과 **일치**. 舊 라운드6 잔존값(기본
  1,537.0~1,539.7만원/보수 1,575.6~1,578.6만원, 05 정본과 약 400만원 괴리 — EVAL-ROUND-8 G-1)은 08 본문에
  "舊 수치" 각주로만 남아 현재 게이트 판정에는 쓰이지 않음을 확인.

**결과: 행 3·행 4 모두 일치, 불일치 0건** — 표본 범위는 이번 라운드 값 변경이 없는 행 1·2·5·6을 제외한 이
두 SSOT 행에 한정한다(1·2·5·6은 이번 라운드 원천 값 변경이 없어 재검증 대상이 아니다).

**라운드 9 갱신(2026-08-03) — 실제 grep 대조 결과(Q1 규칙 적용)**: 이번 라운드 원천 값이 바뀐 곳은 행 4
(05 §G MVP 착수 게이트 금액, 기타 상시비 반영 재계산) 하나뿐이다 — 행 1·2·3·5·6은 아래 확인대로 무변경이다.

- **행 4(05 §G MVP 착수 게이트 금액) 인용처 대조**: 05 §G "MVP 착수 게이트" 원문(확인 문자열
  `MVP 착수 게이트`)이 기본
  1,789.0~1,935.7→**1,821.0~1,982.7만원**, 보수 1,827.6~1,974.6→**1,859.6~2,021.6만원**(상단 약
  2,022만원 하한 권장)으로 재계산된 것을 확인했다. PMO 대조 시점 grep에서 **08 §A W0·§E 1-1번이 舊
  라운드7 값을 인용 중인 ⚠ 불일치가 적발**됐고, **같은 라운드 안에 조율자가 08 두 곳을 신값으로 직접
  갱신·재grep 검증해 해소했다**(현재 `grep "1,821.0~1,982.7" 08-rollout-transition.md` 2건 일치, 舊값은
  교정 이력 서술 내에만 잔존). 이 대장 자체는 기록만 하고 08 반영은 조율자(문서 간 정합 책임)가 수행 —
  **최종: 일치**.
- **행 1(01 §C-5 콘텐츠 캘린더)·행 2(01 §C-6 WAU 궤적)·행 3(01 §C-7 총부하)**: `grep -n "라운드 9\|
  EVAL-ROUND-9" 01-product-strategy.md`로 C-5·C-6·C-7 인근에 라운드 9 변경 마커가 없음을 확인 — 값
  무변경.
- **행 5(05 §C 클라우드 전환 트리거)**: 05 §C 절(510~533행) 구간에 라운드 9 마커 없음 — 값 무변경.
- **행 6(02 §C 성능예산/TTFF)**: 02 §C 절에 라운드 9 마커가 있으나(EVAL-ROUND-9 영역1 감점2/H-2, "지원
  브라우저 — 관제·기자(데스크톱)" 행 신설) 이는 TTFF 정의·목표치(p75 ≤ 4초, LTE·카카오 인앱 브라우저
  기준)와 무관한 별도 행 추가다 — TTFF 값 자체는 무변경, 본 행 인용처 갱신 대상 아님.

**결과(라운드 9): 행 4 불일치 1건(08 §A·§E 구값 잔존), 나머지 5행(1·2·3·5·6) 무변경 확인** — 불일치는
위 미수신 행(6-2 22번)과 별개로 발생한 **인용 갱신형** 결함이며, 조치는 08 문서 소유자가 다음 라운드에
반영한다.

### 6-6. 04 발주 → 05 (트리거성 위임 — 라운드 7 등재, ROUND-7-DECISIONS S6)

> 아래 2건은 실측·견적이라는 **선행 사건이 발생해야 비로소 05 갱신 의무가 생기는** 위임이다. 따라서 현재
> "트리거 대기"가 정상이며, 트리거 도달 후 다음 라운드(또는 운영 중 갱신)에서 수신 확인으로 전환한다.

| # | 발주처 | 수신처 | 확인 방법(grep) | 트리거 | 상태 | 트리거 도달 시 조치 |
|---|---|---|---|---|---|---|
| 1 | 04 §H 19번(라이브커머스 회당 실소요 실측) | 05 §A-1·§B-1(회당 시간 갱신 — 01 §C-5 원천 경유) | 05 grep `회당 실측` 갱신 여부 | 첫 3회 방송 완료 + 확정치가 1시간과 괴리 시 | **트리거 대기(정상)** | 01 §C-5 캘린더 값 갱신 → 6-5 인용 대조 절차로 04·05 연쇄 갱신 |
| 2 | 04 §H 20번(Tier-1 이중 인코더 정식 견적) | 05 §B-1("Tier-1 이중 인코더(도입 시)" 행) | 05 §B-1 grep `Tier-1` | R2 게이트 판정 2주 전 견적 확보 시 | **트리거 대기(정상)** | 04 §B② 정식 수치 갱신 + 05 §B-1 행 신설 |

### 6-7. 라운드 9 신규 위임 (EVAL-ROUND-9 ④ 문서 간 정합성 이슈 대응, ROUND-9-DECISIONS P1·P4·P5·P6)

> [EVAL-ROUND-9](reviews/EVAL-ROUND-9.md) ④ 정합성 이슈 목록과 [ROUND-9-DECISIONS.md](reviews/ROUND-9-DECISIONS.md)가
> 이번 라운드에 신설한 문서 간 위임 중, 01 부록 표 확장(6-2 21·22번)으로 흡수되지 않는 나머지를 여기 모은다.
> 01 §D 관련 2건(D-1·D-2)은 01 자신의 "교차 문서 의존성 요약" 부록 표에 실린 항목이라 6-2에 편입했고,
> 아래 4건은 그 부록 표에 없는(발주처가 01이 아닌) 신규 위임쌍이다. 전부 이번 라운드에 조율자가 직접 실행한
> grep으로 수신 여부를 판정했다(Q1 원칙).

| # | 발주처 | 수신처 | 확인 방법(grep) | 기한 | 상태 | 미수신 시 조치 |
|---|---|---|---|---|---|---|
| 1 | 03 §B-4(공급자 사용성 검증, 라운드 9 신설 — H-6/P4) | 06 §F 4번(FAQ "기자·지사 담당자 대상 절차 이해도" 확인 원천 실체화, 舊 "03 패널에서 확인"의 참조 실체) | 06 grep `03 §B-4` | 개인정보처리방침/FAQ 분기 갱신(최초 W1) | **수신 완료(라운드 9)** — 06-cs-voc.md §F 4번(FAQ 체크리스트)에서 "기자·지사 담당자 대상 절차 이해도는 03 §B-4 공급자 사용성 검증으로 별도 확인" 문구로 실재 확인(확인 문자열 `03 §B-4`). (03 리스크 9의 동일 참조는 03 문서 **내부** 참조라 대장 대상에서 제외) | — |
| 2 | 05 §B-3(사업개발 담당 MVP 0.3인 겸임 배분, 라운드 9 이전부터 확정치) | 06 §B(광고주·협찬사 창구 블록 신설 — 1차 접수=사업개발 담당, 라운드 9 신설 P5/H-7) | 06 grep `05 §B-3 0\.3인` | 첫 광고·협찬 계약 접수 전 | **수신 완료(라운드 9)** — 06-cs-voc.md §B에서 "사업개발 담당(1차, 05 §B-3 0.3인 겸임)" 문구로 실재 확인(확인 문자열 `05 §B-3 0.3인`), 05 §B-3① 배분치("사업개발 0.3인")와 정합 확인 | — |
| 3 | 07 §6(스토어 계정 개설 블록 — Apple Developer 조직 계정 D-U-N-S·Google Play, 라운드 9 신설 P6/H-8) | 08 §A W3 선행조건·§E 1-1번(동일 항목 등재, 오너 병기) | 08 grep `스토어 개발자 계정 개설` | W3 착수 8주 전 | **수신 완료(라운드 9)** — `08-rollout-transition.md:15`(§A W3 선행조건)·`:149`(§E 1-1번) 2곳 모두 "07 §6 신설 블록" 인용 + 동일 리드타임("W3 착수 8주 전") 명시 확인. 07 §6 자신은 아직 "확인 대기"로 적어뒀으나(`07-legal-license.md:1229`) 이 대장의 독자 grep으로 수신을 확정한다 | — |
| 4 | 02 §D-T6·§D-T8(`go.` 라우트 가용성 조치 — Cloudflare 캐시+`watch.` 병행 발급, 라운드 9 신설 P1/H-1) | 01 §A-5·04 §B④·08 §B(보강 후 "VOD 신규 진입 ✅ 생존" 셀에 `go.` 경유 한정 단서 부기) | 01·04·08 grep `캐시된 링크`(라운드 12 정정 — EVAL-ROUND-12 L-3: 舊 확인 방법 `go\. 가용성`은 04에서 0건이라 재현 실패) | W2(D-T8과 동시) | **수신 완료(라운드 9, 확인 방법 라운드 12 정정)** — 01-product-strategy.md·04-live-stability.md·08-rollout-transition.md 3개 문서 전부에서 "캐시된 링크"(01 §A-5·04 §B④ 표, 08은 §B 보강 후 표) 문구로 실재 확인. (H-1 원 지적은 04·08 2곳만 지목했으나, 01 §A-5도 동일 원인(영역2 감점1)으로 독자 단서를 신설해 grep 결과 3개 문서 전부에서 확인됨 — 정직 기재) | — |

### 6-8. 라운드 10 신규 위임 (N1 — 대외 런칭 게이트)

| # | 발주처 | 수신처 | 확인 방법(grep) | 기한 | 상태 | 미수신 시 조치 |
|---|---|---|---|---|---|---|
| 1 | ROUND-10-DECISIONS N1(기술/대외 런칭 분리) | 01 §A-7(전환 기간 서비스 상태 — 시청 경험 서술 소유) + 08 §A 앵커 4(대외 런칭 게이트 수치 소유 — 시드 콘텐츠 6건) + **03 §B-3(시연용 콘텐츠 출처 — 라운드 11 인용처 추가, EVAL-ROUND-11 K-2·K-10 해소)** | 01 grep `### A-7` · 08 grep `시드 콘텐츠`(3건) · 01 grep `시드 콘텐츠`(9건) · **03 grep `앵커 4`(3건 — 라운드 11 오조준 정정 후 확인)** — 전부 조율자 직접 grep(2026-08-03) | W1 판정과 동시 | **수신 완료(라운드 10 신설 → 라운드 11에 03 §B-3 인용처 편입·오조준 정정 완료)** | — |

### 6-9. 라운드 12 신규 위임 (L-1 — X3 예외의 03 명문화, EVAL-ROUND-12 영역 1 감점 1·영역 11 감점 1)

> [EVAL-ROUND-12](reviews/EVAL-ROUND-12.md) L-1이 적발한 "분모 밖 위임"(07 §1-4가 선언한 X3 예외가 03에
> 전달되지 않아 최상위 리젝 리스크 완화책이 03의 무예외 "기능 분기 없음" 코드 리뷰 기준에 문언상 막히는 상태)을
> 여기 신규 등재한다. 07 §1-4 액션 ④가 같은 라운드 안에 03향 발주를 신설했고, 03·07 동시 수정으로 같은
> 라운드 안에 종결됐다(07 §6 자체 추적 블록도 동일 결론 — "수신 확인 완료(라운드 12)", 라운드 8 "동시 추가"
> 규칙·라운드 10 이월 금지 단서 준수). 이 대장의 舊 "총 54행·미수신 0건" 선언은 이 위임쌍이 분모 밖에 있었다는
> 뜻이라 L-1이 지적한 대로 정확했다 — 아래 신설로 분모에 편입한다.

| # | 발주처 | 수신처 | 확인 방법(grep) | 기한 | 상태 | 미수신 시 조치 |
|---|---|---|---|---|---|---|
| 1 | 07 §1-4 액션 ④(+원천 ROUND-1-DECISIONS X3·02 §D-T2) | 03 §D "원칙 — 설치 없이도 전 기능 동작"·실행 체크리스트(기능 분기 없음 코드 리뷰 기준에 X3 예외 명문화) | 03 grep `후원`·`멤버십`·`쉘 UA`·`X3` | 쉘 심사 제출 전 | **수신 완료(라운드 12, 2026-08-03 조율자 직접 grep)** — `grep "후원\|멤버십\|쉘 UA\|X3" 03-accessibility-ux.md`(EVAL-ROUND-12가 0건으로 지적한 바로 그 grep) 재실행 결과 §D 원칙 불릿("단 하나의 의도된 예외 — 쉘 UA/런치 파라미터 감지 시 디지털 재화(후원·멤버십) 결제 동선 비노출은 이 원칙의 유일한 예외로 허용", ROUND-1-DECISIONS X3·02 §D-T2 인용)·실행 체크리스트("기능 분기 없음을 코드 리뷰 기준에 명시" 항목에 동일 예외 + 코드 리뷰 체크 문구 부기) 2곳 모두 실재 확인. 같은 라운드 03·07 동시 수정으로 해소(07 §6 자체 추적도 "수신 확인 완료"로 동일 종결) | — |
| 2 | ROUND-13-DECISIONS M5(X3 게이트 발동 조건 — 05 §A-4 이월로 현재 비노출 대상 UI 부재) | 02 §E 11번 · 03 §D · 07 §1-4(3개 문서 대칭 단서: "A-4 착수(후원·멤버십 트랙 재개) 시 발동하는 트리거 대기 게이트, 그 전까지 구현 대상 없음") | 02·03·07 grep `A-4 착수` | — (트리거성 표기) | **수신 완료(라운드 13, 3개 문서 동시 반영 — 각 소유자 grep 자체 검증 + 07이 02·03 반영을 교차 grep 확인). 게이트 실체는 05 §A-4 재개 시 발동하는 트리거 대기 상태(6-6과 동일 유형이나 표기 자체는 완료라 "수신 완료"로 계상)** | — |

### 6-10. 라운드 14 신규 위임 (O1·O2·O5 — 카카오 게시 역할·망법 §50·연령대 지표)

| # | 발주처 | 수신처 | 확인 방법(grep) | 기한 | 상태 | 미수신 시 조치 |
|---|---|---|---|---|---|---|
| 1 | ROUND-14-DECISIONS O1(카카오 반자동 게시 담당·공수 확정) | 01 §C-7(게시 행 ≈10분/건·합계 재계산, 상한 36~46→**37~47h**) + 06 §A/§B(게시 담당 역할 신설 — 02 §D-T6·04 §D 호출 실체) + 05 §B-3①(부하 정합 각주 — 신규 채용 아님) | 01·05·06 grep `카카오 채널 게시` | W2(반자동 게시 가동)와 동시 | **수신 완료(라운드 14, 3개 문서 동시 신설 + 조율자가 총부하 신값 37~47h를 05 4곳·06 1곳에 전파·grep 검증)** | — |
| 2 | ROUND-14-DECISIONS O2(정보통신망법 제50조 — 광고성 정보 전송) | 07 §3-17(판정 절 신설) + 06(공지 발송 전 광고성/정보성 구분 단계) + 01 §B-6/§B-7(대리 등록 동의 리스크·폼 증빙 필드) | 07 grep `제50조` · 06 grep `광고성/정보성` · 01 grep `동의 증빙` | 첫 채널 공지(대외 런칭) 발송 전 | **수신 완료(라운드 14 — 07 §6 자체 추적도 양방향 종결, 07이 01·06 반영을 교차 grep 확인)** | — |
| 3 | 03 §B-2(연령대 추정 CS 문의 비율 — 담당·임계·사용처 부여) | 06 §D(월간 VOC 리포트 고정 항목 등재 — 측정치 제공, 임계·사용처는 03 소관 명시) | 06 grep `연령대 추정` | 최초 월간 VOC 리포트 | **수신 완료(라운드 14, 03·06 동시 반영)** | — |
| 4 | 01 §A-4/§A-5(웹푸시 폐기 사정거리 단서 — 쉘 네이티브 푸시는 별개 축, 라운드 14 신설 발주) | 02 §E 23번 + 07 §1-1 완화책 1번(동일 단서 대칭 부기) | 02·07 grep `별개 축`(01의 舊 확인 문자열 "쉘 네이티브 푸시"는 오탐 통과라 교체 — EVAL-ROUND-15 P-1) | M6 진입 전 | **수신 완료(라운드 15, 조율자 직접 부기·grep 검증 각 1건)** | — |
| 5 | 07 §3-5(CLAUDE.md §12 승인 후 갱신 편입 요청) + O1 파생(06 센터 운영자 게시 부하 계상 — EVAL-ROUND-15 P-2·영역 8) | PIVOT-PLAN §4(갱신 목록에 §12 편입) + 06 §A 산술 정합 문단(센터 몫 게시 부하 월 1.3~2h 행 신설, "통화 처리시간뿐" 한정 해제) | PIVOT-PLAN grep `§12` · 06 grep `카카오 채널 게시(센터 운영자 몫)` | 승인 시 / 상시 | **수신 완료(라운드 15, 조율자 직접 반영·grep 검증)** | — |
| 6 | 05 §H 라운드 16 위임 노트(개발 인력 전환 민감도의 08 인용 — 등재 책임 규칙에 따라 05가 완료 보고에 제출) | 08 §D "개발 인력 미확보·이탈" 행 완화책(05 §B-3③ 신설 민감도 인용) | 08 grep `개발 인력 외주/채용 전환 시 현금 민감도` | 상시 | **수신 완료(라운드 16, 조율자 직접 반영·grep 검증)** | — |
| 7 | 02 라운드 16 완료 보고(등재 책임 규칙 제출분 — `go.` 리다이렉트 판정 항목) | 08 §A W1 DoD ②(미리보기 렌더에 더해 "리다이렉트 후 상세 화면 렌더 확인" 판정 추가 — 02 §D-T5 동적 라우트 서빙과 연동) | 08 grep `상세 화면이 실제로 렌더` | W1 | **수신 완료(라운드 16, 조율자 직접 반영·grep 검증)** | — |
| 8 | 01 §C-8(주민 제보 인센티브 운영안 — 라운드 17 신설, 등재 책임 규칙 제출분) | 05 §B-1 "오프라인 물적비"(인센티브 월 1만~2만원 예산 행 — 01은 예산을 05 인용) | 05 grep `주민 제보 인센티브` · 01 grep `05 §B-1` | W2 착수 전 | **수신 완료(라운드 17, 01·05 동시 반영)** | — |
| 9 | 05 §H 라운드 17 위임 노트(게이트·상한 신값의 08 인용 갱신 — 등재 책임 규칙 제출분) | 08 §A W0·§E 1번(라운드 17 신값 → **라운드 18에 재차 갱신**: 기본 2,018.2~2,344.5/보수 2,056.8~2,383.4만원·상단 약 2,383만원 — 05의 세무·회계 비용 신설분 반영) | 08 grep `2,018.2~2,344.5` | 즉시 | **수신 완료(라운드 17·18 각각 조율자 직접 갱신·grep 검증 2곳)** | — |
| 10 | 04 R4 종료 조건(YouTube 단독 진행 구간 — 라운드 18 신설, 등재 책임 규칙 제출분) | 06 §F 22번(단독 구간 시청자 고지 절차 수신) | 06 grep `단독` | 첫 방송 전 | **수신 완료(라운드 18, 04·06 동시 반영)** | — |
| 11 | 07 §3-18(재설문 목적 외 이용 판정 — 라운드 18 신설) ↔ 01 §B-5·§B-7(동의 필드·보류 단서) / 07 §5-2·06 §C①(Facebook 아카이브 대칭) | 01·06·07 3개 문서 동시 반영(07 §6 자체 추적도 같은 라운드 등재·종결) | 01 grep `07 §3-18` · 06 grep `Facebook` · 07 grep `§3-18` | 첫 촬영 재개 전 | **수신 완료(라운드 18, 병렬 동시 반영 — 07이 교차 grep 확인)** | — |

### 6-11. EXEC 단계 신규 위임 (실행계획 수립·실행 웨이브 — E5 §D 절차 4에 따라 신설)

> 실행계획([exec/EXEC-PLAN.md](exec/EXEC-PLAN.md)) 수립과 이후 실행 웨이브에서 발생하는 신규 위임을 여기
> 누적한다(새 대장을 만들지 않는다 — E5 §D "단일 원천 유지"). 등재 책임 규칙·Q1 원칙(확인=실행한 grep 결과만)은
> §6 본문과 동일하게 적용하며, 웨이브 종료 시 PMO가 등재 여부를 grep 재검증한다(E5 §D 절차 1).
> **EXEC 단계 수치(웨이브 상한·총예산·태스크 수 등)가 변경되면 §6-5 인용 대조 원칙을 연장 적용해 본 소절
> 상태 칸의 수치 인용도 전건 재검증한다**(EXEC-EVAL-ROUND-2 Y-10 — 상태 칸이 폐기 수치를 근거로 남는 SSOT
> 오염 재발 방지).
> **상태 칸의 건수·좌표는 확인 시점 스냅숏**(시점 명기 의무 — 이후 문서 진화로 값이 변해도 시점이 명시돼
> 있으면 stale이 아니다). E5 §D 앵커 ⑬은 확인 grep의 **존재 재현(1건 이상)만** 판정한다(라운드 11 N-8 —
> 건수 일치 요구는 D7-1 박제 금지와 충돌해 폐기).

| # | 발주처 | 수신처 | 확인 방법(grep) | 기한 | 상태 | 미수신 시 조치 |
|---|---|---|---|---|---|---|
| 1 | E5 §A 게이트②(qa-verifier 독립 검증 — E5 완료 보고 제출분) | E1(qa-verifier 위임 프롬프트 표준에 "자가검증 로그 재신뢰 금지·AC 최소 1개 독자 재현" 문구) | E1 grep `독자 재실행\|독자 재현` | E1 작성 완료 시 | **수신 완료(2026-08-04 조율자 직접 grep — E1 §B-1 대장표·§C-2 필수 삽입문 2곳 실재 확인)** | — |
| 2 | E5 §A 게이트①(자가 검증 로그 동봉 — E5 완료 보고 제출분) | E1(구현 에이전트 위임 프롬프트 표준에 "완료 선언 시 실행 로그 동봉 의무" 문구) | E1 grep `실행 로그.*동봉\|로그.*동봉 의무` | E1 작성 완료 시 | **수신 완료(2026-08-04 조율자 직접 grep — E1 §B-1 템플릿·§C-2 필수 삽입문·체크리스트 3곳 실재 확인)** | — |
| 3 | E5 §C(W단계 DoD 판정 절차 — E5 완료 보고 제출분) | E2(W1·W2 DoD 실측 태스크의 산출물란에 실측 담당=QA 리드·증적 경로 `reviews/dod-evidence/` 명시) | E2 grep `dod-evidence` | E2 작성 완료 시 | **수신 완료(2026-08-04 조율자 직접 grep — E2 내 8건 실재, T-NC-01·05·06·07 산출물란 확인)** | — |
| 4 | E5 §D 절차 4(실행 단계 위임 편입 소절 신설 요청) | PIVOT-PLAN §6(본 6-11 소절) | PIVOT-PLAN grep `6-11` | 즉시 | **수신 완료(본 소절 신설로 종결 — 조율자 직접 반영)** | — |
| 5 | E5 §D 절차 2(EXEC-DECISIONS·dod-evidence 관행 등재 요청) | EXEC-PLAN.md §2 문서 맵·§4(EXEC-DECISIONS.md·`reviews/dod-evidence/` 등재) | EXEC-PLAN grep `EXEC-DECISIONS` | 즉시 | **수신 완료(조율자 직접 반영 — 반영 후 grep 검증)** | — |
| 6 | E4 §G(재개 프로토콜 실증 횟수 갱신 — E4 완료 보고 제출분) | EXEC-PLAN.md §1 G4 행("3회 실증"→"4회 실증 = 마스터플랜 3회 + 실행계획 수립 자체 1회(E4 §C-3 #4)") | EXEC-PLAN grep `4회 실증` | 즉시 | **수신 완료(2026-08-04 조율자 직접 반영·grep 검증)** | — |
| 7 | E2 §B(역할 매핑 표 — E2 완료 보고 제출분) | E1(마스터플랜 문서들의 임시 호칭 "테크리드/PO/사업총괄/센터운영" → E1 역할군 매핑 표의 공식 채택 또는 기각 판정) | E1 grep `공식 채택` | E1 갱신 시 | **수신 완료(2026-08-04 조율자 직접 grep — E1 §A-1 신설 "판정: 공식 채택" 실재 확인. 채택 과정에서 CI 설정 소유권 서술의 E2 실배정과의 충돌 1건도 정정됨 — 다중 소유·SOLO 시점 배타로)** | — |
| 8 | E2/E3 완료 보고(08 §A W1 내용물 대사에서 적발) | E5 §C(DoD 증적 표에 `w1-poc/`(02 §E 0-1 PoC)·`w1-panel-r1/`(03 §B-1 패널 R1) 2행 추가) | E5 grep `w1-poc` · `w1-panel-r1` | 즉시 | **수신 완료(2026-08-04 조율자 직접 반영·grep 검증 — E5 소유 에이전트 부재로 조율자가 §C 표 2행 신설)** | — |
| 9 | E3 §C·§H(웨이브 사이징 — E3 완료 보고 제출분) | E4(웨이브 S/M/L 사이징·단독 슬롯 7웨이브의 병렬 손실 비용을 토큰/시간 예산 모델에 반영) | E4 grep `SOLO` | E4 갱신 시 | **수신 완료(2026-08-04 초회 확인: E4 §A-5·§A-6·§A-7 실재. 수치 이력: 초회 인용 "17웨이브·143만≤145만"→EXEC-DECISIONS #3으로 폐기·상한 200만(라운드 2 Y-10 재확인)→라운드 3 실산 1,618만→라운드 4: 20라벨·1,638만→라운드 6: 21라벨·1,696만→라운드 7: 21라벨·1,724만→라운드 8: 22라벨(T-W2-17 [SOLO], #7)·1,766만→**라운드 12 최종(K-2 갱신): 코드 44건·SOLO 8·합계 1,804만(M-6 T-W3-02 L·M-7 T-W2-09 M 사이징 반영)·최대 Wave 5=166만 ≤ 상한 200만·T-NC 19건=95만·프로그램 총예산 이력 2,059~2,219만(R13)→**라운드 14 최종(D14-2 스윕 동기화): 총 67태스크(코드 45 — T-W2-18 신설, J-6)·합계 1,892만·최대 166만 ≤ 200만·총예산 이력 2,307~2,467만(R14)→**라운드 19 최종(D14-2 스윕 동기화): 총 68태스크(코드 45·TRIG 3·NC 20 — T-NC-20 분리, R2-3)·T-NC 100만·총예산 이력 2,312~2,472만(R19)→**라운드 21 최종(D14-2 스윕 동기화): 잔여 구간 추정 폐기(백테스트 — 7라운드 연속 초과)·수렴 조건 서술 전환(중대 이슈 0+신규 유형 0+8영역 9.5), 총예산 하한 전용 ≥2,152만(E4 §A-8 스윕 시점 현행 — 상한 표기 폐기)→**라운드 22 최종(D14-2 스윕 동기화, 2026-08-07): D22-1로 신규 태스크 T-W1-11c [SOLO]·웨이브 라벨 `8c` 신설 → 총 69태스크(코드 46·TRIG 3·NC 20)·웨이브 라벨 23·SOLO 9·합계 1,934만(舊 1,892만, +42만)·**최대 부하 Wave 5 = 166만 불변**(≤ 상한 200만, 여유 34만)·총예산 하한 전용 **≥2,194만**(舊 ≥2,152만 — ① 1,934 + ② T-NC 100 = ③ 2,034 + 잔여 최소 1라운드 160 = ⑤ 2,194)→**라운드 25 최종**(D14-2 스윕 동기화, 2026-08-08): K2-1(`packages/ui` 소비에 필요한 `apps/*/package.json` 워크스페이스 의존성 선언 무소유 → lockfile 집합 과소 → D6-1 위반) 해소로 T-W1-01이 3앱 선언을 일괄 소유(3→6파일, **S→M**) → 합계 **1,944만**(舊 1,934만, +10만 = 구현 단가 차분 1건. 라벨 순증·재배치 0건) · **최대 부하 Wave 5 = 166만 불변** · 총예산 하한 **≥2,204만**(① 1,944 + ② 100 = ③ 2,044 + 160) · 재개 하한 = **E4 §A-8 ⑥ 산식의 그 시점 출력**(라운드26 J2-1/D26-1로 §A-8 ④·⑥의 라운드 수 리터럴이 전면 폐지돼 정본이 부착 명령의 출력으로 바뀌었다 — 대장에 절사값·분모를 사본으로 두면 라운드가 늘 때마다 stale되므로 값을 적지 않는다. 舊 표기 "≥2.7회(분모 19+24=43)"는 라운드25 시점 스냅숏이었다. **앵커 ⑤ 총예산 축은 무영향** — 라운드26은 모수 무변동이라 하한 2,204만 유지, D14-2 "#9 동기화 필요" 미발동) — 태스크 69·라벨 23·SOLO 9·앵커 ⑮ {2·4·5·9·12} 전부 불변** — 수치 변경 시 본 칸 재검증 규칙(6-11 서두) 이행)** | — |
| 10 | E3 §H 리스크 3행(인증 어댑터 공용 패키지 추출 없이 독립 구현 판단 — E3 완료 보고 제출분) | EXEC-DECISIONS.md #1(실행계획 단계 해석 확정 기록 — 08 자체는 무변경) | EXEC-DECISIONS grep `독립 구현` | 즉시 | **수신 완료(2026-08-04 조율자가 EXEC-DECISIONS.md 생성·#1 기록·grep 검증)** | — |
| 11 | E2 신규 위임 목록 #4(T-W3-04 iOS 쉘 파일 수 가정치 — E2 §C 제출분. 조율자 전달 목록(2건)에서 누락됐으나 E1·E4 작성 에이전트가 E2 본문 대사로 직접 발견·처리 — "분모 밖 위임" 재발 방지 패턴 작동 사례) | E4(가정치 3파일의 확장 가능성을 토큰 예산에 반영) | E4 grep `T-W3-04` | E4 갱신 시 | **수신 완료(2026-08-04 — E4 §A-5 "T-W3-04 특이사항"(L 상한 35만 기본 편성+§A-4 모니터링)·§F-1 수신 확인표 실재, 조율자 grep 재검증)** | — |
| 12 | E5 신규 위임 #6(라운드 1 수정 제출분 — §A 게이트③ 주체 택일 규칙 확정) | E1 §A(QA 리드 행)·E2 §C(검증자 열) — "개별 회귀=QA 리드 / 공용 자산 루트 회귀=QA 리드 실행+조율자 수신" 표기 통일 | E1 grep `조율자가 결과 수신`(1곳) · E2 grep `조율자 결과 수신`(9곳 — 라운드 10 P-6 정정: 초회 문구가 이후 표기 정비로 변형돼 舊 grep이 0건이 됐던 것을 현행 재현형으로 갱신, 앵커 ⑬ 신설 계기) | 라운드 1 수정 완료 시 | **수신 완료(2026-08-04 초회 확인·2026-08-06 재검증 — E1 1곳+E2 9곳 현행 재현)** | — |
| 13 | E4 §G #3(라운드 1 수정 제출분 — 웨이브 상한 145만→200만 상향의 정식 기록 요청, E4는 EXEC-DECISIONS 직접 수정 불가) | EXEC-DECISIONS.md #3(qa-verifier 누락 정정+XL 반영 근거로 상한 200만 확정 — §A-4 "근거 기록 후에만" 조항 이행) | EXEC-DECISIONS grep `200만` | 즉시 | **수신 완료(2026-08-04 조율자가 #3 기록·grep 검증)** | — |
| 14 | E1 §신규 위임 목록 ①(라운드 2 신설 절 제출분 — E1 §A-1 확정 문안의 E2 §B 전파) | E2 §B "테크리드" 행(구 "잠정 처리" 문구를 "E1 §A-1 공식 채택 완료·CI 다중 소유·SOLO 시점 배타" 확정 인용으로 갱신) | E2 grep `§A-1 공식 채택` | 라운드 2 마감 전 | **수신 완료(2026-08-04 조율자 직접 grep — `§A-1 공식 채택` 1건 실재·"잠정 처리" 0건 확인)** | — |
| 15 | E1 §신규 위임 목록 ②(라운드 2 신설 절 제출분 — E1 §A 법무/운영 지원 금지 칸 확정의 전파) | E2 §E 담당 열·E5 §C 비-DoD 표·§E(T-NC-08·09 실행 주체 = 사용자, 법무/운영 지원은 절차 지원까지) | E2·E5 grep `사용자(법무/운영 지원` | 라운드 2 | **수신 완료(2026-08-04 — DD4 확정 문안으로 B·C팀이 동시 전파, E1이 grep 3개소 실재 확인·조율자 등재)** | — |
| 16 | EXEC-DECISIONS #2(PoC 게이트 차단 범위 축소 해석 — 라운드 1 D2·라운드 4 D4-1로 영향 문서 확장) | E3 §A·§C·§D, E2 §C W1 선행 문단, E5 §C "W1 선행 PoC" 행 | E2·E3·E5 grep `EXEC-DECISIONS #2` | 즉시 | **수신 완료(라운드 7 Q-10 소급 등재 — 3문서 전건 #2 인용 실재, 조율자 grep 재검증)** | — |
| 17 | EXEC-DECISIONS #4(라우트 스모크 모수 = 판정 시점 전수 — 라운드 4 D4-2) | E2 T-W2-07·T-NC-07, E5 §C W2 행·§D 앵커 ⑩ | E2·E5 grep `EXEC-DECISIONS #4` | 즉시 | **수신 완료(라운드 7 Q-10 소급 등재 — 양 문서 인용 실재, 조율자 grep 재검증)** | — |
| 18 | EXEC-DECISIONS #5(T-W4-01 기준선 재도출 — 라운드 5 D5-1) | E2 §C T-W4-01(각주 삭제·산출물 재정의) | E2 grep `#5` | 즉시 | **수신 완료(라운드 7 Q-10 소급 등재 — E2 반영 실재, 조율자 grep 재검증)** | — |
| 19 | EXEC-DECISIONS #6(동적 라우트 = 폴백·모수 동반 원칙 — 라운드 6 D6-2) | E2 T-W1-06·T-W2-09, E5 §D 앵커 ⑪ | E2·E5 grep `#6\|앵커 ⑪` | 즉시 | **수신 완료(라운드 7 Q-10 소급 등재 — 양 문서 반영 실재, 조율자 grep 재검증)** | — |
| 20 | EXEC-DECISIONS #7(웹 E2E CI 자동 실행 태스크 결박 — 라운드 8 D8-1, T-W2-17 신설) | E2 §C·§F-1, E3 §C·§G, E4 §A-6, E5 §B·§C W2 | E2·E3·E5 grep `T-W2-17` | 라운드 8 수정 완료 시 | **수신 완료(2026-08-06 조율자 스윕 — T-W2-17이 4문서 실재·66태스크·22라벨·SOLO 8 재검산. 정직 정정(라운드 9 S-16): 이 스윕 선언은 E3 §C 내장 재검산 블록의 stale(43/21 잔존)을 누락한 채 '전건'을 칭했다 — 라운드 9에서 E3 블록 재작성으로 종결, 스윕 절차에 '내장 검산 블록도 앵커 ⑨ 대상' 교훈 반영)** | — |
| 21 | EXEC-DECISIONS #8(운전자금 게이트 무지출 로컬 선행 해석 — 라운드 8 D8-2) | E3 §F ③행(#8 인용 교체) | E3 grep `#8` | 라운드 8 수정 완료 시 | **수신 완료(2026-08-06 조율자 스윕 — E3 §F ③행 #8 인용 실재)** | — |
| 22 | E4 §A-7(게이트② 대기의 국면 구분 지정 — 라운드 8 작성분, 라운드 9 S-6이 분모 밖 위임으로 적발) | E5 §F 리스크1 비고(국면 구분: 구현 진행 중 대기=정상 / 전건 완료 후 지속=이상 — D9-4 문안) | E5 grep `국면 구분` | 라운드 9 수정 완료 시 | **수신 완료(2026-08-06 조율자 스윕 — E5 §F 리스크1 국면 구분 문안 실재·E4 §G #4 소급 기재 확인)** | — |
| 23 | D6-6(라운드 6 확정 — E4 §A-8의 앵커 ⑨ 대상 편입 지시. 라운드 11 N-6이 분모 밖 위임 2회차로 적발 — §6-11·E4 §G 양쪽 미등재였고 그 공백에서 §A-8 미갱신(N-5) 손해 실현) | E5 §D 앵커 ⑨ 열거(+§A-8)·E4 §G 소급 기재 | E5 grep `§A-8` · E4 §G grep `6-11 #23` | 라운드 11 수정 완료 시 | **수신 완료(2026-08-06 조율자 스윕 — E5 앵커 ⑨ 열거 8개(§A-8 포함)·E4 §G row5 소급 기재 실재)** | — |
| 24 | E1 §A-1(스토어 심사 제출 매핑 — 라운드 11 N-2 적발: "제출→조율자" 표기가 DDD4 실배정(사용자+법무/운영 지원, 조율자는 승인만)과 충돌) | E1 §A-1 보완 조건·E2 §B 동일 문구(DDD4 정합 정정) | E1·E2 grep `DDD4` | 라운드 11 수정 완료 시 | **수신 완료(2026-08-06 조율자 스윕 — E1 §A-1 보완 조건 ②·E2 §B 동일 문구 DDD4 정합 실재)** | — |
| 25 | E4 §G #6(라운드 12 재계산 중 발견 제출분 — B팀 완료 보고가 "E3 Wave 17a 셀 처리"를 칭했으나 실체 미반영(M(5파일) 잔존), 보고-실체 괴리 2번째 사례(라운드 4 반증 각주와 동류) — A팀의 정본 재독 원칙이 적발) | E3 §C Wave 17a 셀(D12-2 확정 L(9파일) 반영 — sw.js·호출 지점 4파일 편입) | E3 grep `L(9파일` | 즉시 | **수신 완료(2026-08-06 — B팀 정정·Q1 증빙(수정 후 234행 grep 전문) 동봉, 조율자 재확인 grep 1건 실재. 교훈: 팀 완료 보고도 조율자 스윕의 교차 grep 대상(기확립 병렬 경합 패턴의 적용 범위 확대))** | — |
| 26 | E5 §D 절차 7·앵커 ⑨·⑬(라운드 17 F-8 적발 — PMO 앵커 재현 책임이 소유·템플릿에 미결박, "분모 밖 위임" 3회차) | E1 §A PMO 행·§B-4 템플릿("웨이브 종료 검증 시 앵커 ①~⑮ 전종 재현·불성립 즉시 보고" 명시 — D17-3) | E1 grep `앵커 ①~⑮` | 라운드 17 수정 완료 시 | **수신 완료(2026-08-07 조율자 스윕 — E1 §A PMO 행·§B-4 템플릿에 앵커 ①~⑮ 재현 책임 실재(grep 2건+), E5 §신규위임 #7 제출 정합)** | — |
| 27 | E4 §G #7(라운드 17 재계산 중 발견 제출분 — E3 §C 검산 블록이 D17-1 재배치 전 舊 편성(`10(4)+11(1)+13(2)`)을 인용 잔존) | E3 §C 검산 블록(현행 `10(3)+13a(3)+13b(1)`·Wave 11 폐지 반영 재실행 출력으로 재작성) | E3 grep `13a(3)` | 즉시 | **수신 완료(2026-08-07 조율자 스윕 — E3 검산 블록 신 편성 `10(3)+13a(3)+13b(1)` 실재·합계 45 불변 재검산)** | — |
| 28 | E5 §신규 위임 #8(라운드 18 V2-11 제출분 — D17-2 인프라 게이트① 실행 축의 위임 표준 미결박, "분모 밖 위임" 4회차) | E1 §C-2(인프라 결박 행 보강)·§B-1(작업 순서 일반화)·§F(체크 1항) | E1 grep `compose config` | 라운드 18 수정 완료 시 | **수신 완료(2026-08-07 조율자 스윕 — E1 §C-2 인프라 결박 행·compose config 2건 실재)** | — |
| 29 | E4 §G #8(라운드 18 제출분 — E4 §G "재현:" 주장을 앵커 ⑨ 9번째 검산 절로 편입 요청. 라운드 19 R2-11이 대장 공백 적발 — 수신처(E5 앵커 ⑨ 9개 확장)는 라운드 18 C팀이 기이행) | E5 §D 앵커 ⑨ 열거(E4 §G 편입 — 9개) | E5 grep `9개로 명시 열거` | 라운드 18 | **수신 완료(2026-08-07 소급 등재 — E5 앵커 ⑨ "9개로 명시 열거" 실재(라운드 18 기이행), E4 §G #8 상태 칸은 A팀이 D7 형식 전환 중)** | — |
| 30 | E5 §신규 위임 #9(라운드 19 R2-9 제출분 — CI 설정 태스크 게이트① 실행 축(신설 행)의 위임 표준 결박) | E1 §C-2 8행째(CI 설정 태스크 결박 행)·§F 1항 | E1 grep `YAML 구문` | 라운드 19 수정 완료 시 | **수신 완료(2026-08-07 조율자 스윕 — E1 §C-2 CI 설정 결박 행·YAML 구문 실재)** | — |
| 31 | E5 §신규 위임 #10(라운드 20 Z2-2 제출분 — 문서 산출물 태스크 게이트①·③ 축(신설 행)의 위임 표준 결박, 동형 4회차 종결) | E1 §C-2 9행째(문서 산출물 결박 행)·§F 1항 | E1 grep `문서 산출물 태스크` | 라운드 20 수정 완료 시 | **수신 완료(2026-08-07 조율자 스윕 — E1 §C-2 9행째 실재(477행 인근, E5 §B 문안 문자 승격 확인). 확인어를 실재 문구로 조정 — 舊 예정어 '자가 대조'는 E5측 표현이었다)** | — |
| 32 | **조율자 D22-1**(라운드 22 X2-1(1차) — 02 §B 명시 신설 산출물 `.github/workflows/deploy-web.yml`이 exec 전 문서 0회 등장하던 무기록 1건. 02 §B 대사 분모가 정본 총계(신규 `25+20+5+3`)보다 8 적었던 것이 구조적 원인) | E2 §C 신규 태스크 `T-W1-11c [SOLO]`(인프라 담당·1파일·S)·§0 분모 참조형 정정+차집합 8건 3단 판정표·§F-1 라벨 4-1 3분할 / E3 신규 SOLO 웨이브 라벨 `8c` | E3 grep `^\| \*\*8c\*\*` · E2 grep `T-W1-11c` | 라운드 22 수정 완료 시 | **수신 완료(2026-08-07 조율자 스윕 — E3 §C `8c` 행 1건 실재·E2 `T-W1-11c` 15건. 모수 파급 전건 반영 확인: 코드 45→46·총 68→69·웨이브 라벨 22→23·SOLO 8→9·CI 설정 태스크 3→4건, E2↔E3 차집합 빈 출력·구현 4건 웨이브 집합 {2·4·5·9·12} 불변)** | — |
| 33 | E5 §신규 위임 #11(라운드 22 X2-4(1차) 제출분 — "계획이 신설하는 CI 게이트의 required 여부는 그 잡을 신설하는 태스크가 산출물에 명시한다"는 라운드 21 신설 규칙이 현행 유일 대상 T-W1-11b에 미적용이던 비대칭) | E2 §C T-W1-11b 산출물 열(게이트별 required 표기 = required 분류의 **정본**, E5 §A 게이트④·§G는 인용만) | E2 grep `required 표기` | 라운드 22 수정 완료 시 | **수신 완료(2026-08-07 조율자 스윕 — E2 `required 표기` 1건 실재. 번들 예산·Lighthouse = required(02 §C "병합 자동 차단" 실측 2건) / installability = 비필수·T-W3-03·04 착수 선행조건 / CF 캐시 퍼지 = 게이트 아님(배포 스텝, #32 T-W1-11c로 소유 이관, CI 게이트 4종→3종))** | — |
| 34 | E5 §신규 위임 #13(라운드 22 22B X2-7 제출분 — E5 §B "인프라 태스크" 행 8건 열거 중 T-W3-03·04(`infra/shell/*`)에 그 행이 지정한 게이트① 정적 축(compose·nginx·4항)의 적용 대상 자체가 없어 "통과 불가 또는 공허한 그린" 양자택일이던 구조. D13-1→D17-2→R2-9→Z2-2에 이은 동형 5회차) | E5 §B "쉘 프로젝트 골격 태스크" 행 신설(인프라 행 8→6건 분리) → E1 §C-2 10행째 리터럴 승격·§F 1항 | E1 grep `패키지·도메인 식별자 정합` | 라운드 22 수정 완료 시 | **수신 완료(2026-08-07 조율자 스윕 — 리터럴 양방향 재현: E1 3건 / E5 2건, 양쪽 ≥1(Y2-3 검증 형식). E1 §C-2 필수 삽입문 9유형→10유형)** | — |
| 35 | E5 §신규 위임 #14(라운드 22 영역 8-b 제출분 — 앵커 ⑯ 신설에 따른 PMO 임무 범위 확장) | E1 §A PMO 행·§B-4("앵커 ①~⑮ 전종 재현" → **①~⑯**, ⑯ 표본 3건 재실행 임무 병기) | E1 grep `①~⑯` | 라운드 22 수정 완료 시 | **수신 완료(2026-08-07 조율자 스윕 — E1 `①~⑯` 5건 실재. 라운드 17 근거 서술의 회고 인용 1건은 시점 명시 이력이라 보존)** | — |
| 36 | E5 §신규 위임 #15(라운드 22 D22-1 파급 — #32의 신설 라벨 `8c`·ID `T-W1-11c`가 舊 정규식 `[ab]?`에 계수되지 않아 **앵커 ⑨ 재실행이 상시 불일치**하던 실패 모드. 실증: 舊 명령 = 웨이브 22·SOLO 8 / 신 명령 = 23·9, `T-W1-11c`→`T-W1-11` 오추출) | E4 §F-1 검산 절 인용 명령 2줄(`[ab]?`→`[abc]?`) | E4 grep `\[abc\]?` | 라운드 22 수정 완료 시 | **수신 완료(2026-08-07 조율자 스윕 — E4 `[abc]?` 7건 실재, 신정규식 재실행이 웨이브 23·코드 46·총 69·SOLO 9로 정상 계수. E5 §D "접미 문자 클래스 확장 규칙"(고정 클래스 무박제) 인용 형태)** | — |
| 37 | E2 §신규 위임 #7(라운드 22 D22-1 파급 제출분 — 앵커 재현 명령 전건의 태스크 ID·웨이브 라벨 정규식 확장) | E5 §D 앵커 ①·②·③·⑭·⑮ 재현 명령(`[ab]?`→`[abc]?`) + **접미 문자 클래스 확장 규칙 신설**(문자가 `d` 이상으로 늘면 그 시점 실측으로 확장, 고정 클래스 박제 금지 — D7-1 동형) | E5 grep `\[abc\]?` | 라운드 22 수정 완료 시 | **수신 완료(2026-08-07 조율자 스윕 — E5 `[abc]?` 14개소 실재. 갱신 명령 재실행이 B팀 최종 실측(웨이브 23·코드 46·총 69·SOLO 9·4건 웨이브 집합 불변)과 전건 일치)** | — |
| 38 | E3 §신규 위임 #1(라운드 22 #32 파급 제출분 — 신설 태스크의 예산 편입) | E4 §A-6 웨이브 예산 표 행 신설(22→23행)·§A-1·§A-3·§A-8 모수·§B-4 에포트 분류표 +1행 | E4 grep `8c` | 라운드 22 수정 완료 시 | **수신 완료(2026-08-07 조율자 스윕 — E4 `8c` 28건 실재. Python 재계산 실행 출력: 합계 1,892만→**1,934만**(+42만 = S 12 + qa-verifier 10 + PMO 20)·labels 23·tasks 46·**최대 부하 Wave 5 = 166만 불변**(여유 34만 ≤ 상한 200만)·§B-4 합계 46건(낮음 36 / opus 5·sonnet 38·본체 3))** | — |
| 39 | **조율자 D22-8**(라운드 22 22B X2-8 — E1 FE 리드 금지 칸 "배포 설정(infra)"의 예외가 `nginx.conf` 1줄뿐인데 E2는 FE 리드가 `infra/shell/*` 신규 5파일(T-W3-03·04)을 소유한다고 확정하고 E5는 같은 2건을 분류. E1 전체에 `infra/shell` 0건·`T-W3-04` 0건으로 **판정 자체가 부재**. §A-1 보완 조건 ①→(T-W2-16 분할)→③에 이은 동형 4회차이자 최대 규모) | E1 §A-1 **보완 조건 ④ 신설**(`infra/shell/*` 다중 소유 채택·시점 배타 Wave 17b)·§A 금지 칸 예외 1구 | E1 grep `보완 조건 ④` · `infra/shell` | 라운드 22 수정 완료 시 | **수신 완료(2026-08-07 조율자 스윕 — E1 `보완 조건 ④` 7건·`infra/shell` 17건·`T-W3-04` 9건 실재(舊 0/0). 성격 판정은 #34가 E5 §B에 신설한 "쉘 프로젝트 골격 태스크" 분류를 인용 → E1·E2·E5 3자 일치)** | — |
| 40 | E4 §G #10(라운드 22 제출분 — 라운드 22 수정 사이클에서 **실제 발생한 세션 한도 중단·재개 사건**을 재개 프로토콜 실증에 편입할지 판정 요청. 기존 4회와 형태가 다르다: 중단 주체가 조율자 본체가 아니라 **서브에이전트**이고, 복구가 **조율자의 실행 grep 진단 → 미완분만 특정한 멱등 재개 지시**였다) | **판정: 편입 확정**(조율자, 2026-08-07) → EXEC-PLAN §1 G4 "4회 실증"→**5회**(조율자 직접 갱신) + E4 §C-3 실증 #5 신설·§A-8 ⑥ 재개 횟수 하한 분자 4→5 재산정(A팀) | EXEC-PLAN grep `5회 실증` · E4 grep `#5` | 라운드 22 스윕 시 | **수신 완료(2026-08-07 조율자 스윕 종결 — 양단 이행 실측 확인: EXEC-PLAN G4 `grep -c "5회 실증"` → 1, E4 §C-3 실증 #5 신설. 舊 상태 "판정 완료·이행 중". 판정 경위: 2026-08-07 — 사건 경위: B팀(E2·E3)이 E3 §G 재검산 블록 갱신 직전 세션 한도로 종료 → 리셋 후 조율자가 실행 grep으로 중단 지점 진단(본체 수정은 디스크 반영·정합 성립, 미완은 §G·§H·§I 서술 수치뿐임을 특정) → 미완분만 특정한 멱등 재개 지시 → 트랜스크립트 컨텍스트로 재개·완주, **작업 손실 0**)** | — |
| 41 | **EXEC-DECISIONS #9**(계획 문서의 출처 인용은 git blob 참조형만 허용 — 라운드 22 X2-3(1차)·22B X2-4 · D22-3. #5 "고정 좌표 금지"의 해석 확장: 워크트리 경로+행번호 금지 / git 이력에 있으면 커밋 blob 참조형(불변 → 파일 수정·삭제 후에도 재현 가능) / 없으면 본문 자기 완결 서술로 흡수 / 출처 인용 칸은 앵커 ⑨ 재검증 대상) | E4 §A-2·§C-2·§C-3·§G #9 / E5 §D 앵커 ⑨(판정 기준 서술 전환) | E4 grep `git show` · E5 grep `커버리지 대사표` | 라운드 22 수정 완료 시 | **수신 완료(2026-08-07 조율자 스윕 — E4 `git show` 5건·`D22-3` 8건 실재, E5 앵커 ⑨ 판정 기준 서술 2건 실재. 현행 워크트리 `HANDOFF-SESSION.md`에서는 인용 핵심어 0건이나 커밋 `fabe649` blob에서 전건 재현 — 그 파일이 루프 종료 시 삭제돼도 dangling 0)** | — |
| 42 | E5 §신규 위임 #12(라운드 22 D22-3·D22-4 제출분 — 앵커 ⑨ 판정 기준 전환·앵커 ⑯ 신설의 대장 편입 요청. 라운드 22 스윕이 개별 파급 행(#32~#41)은 등재하면서 **앵커 규칙 자체의 편입 행**을 누락했다) | PIVOT-PLAN §6-11(본 행) — 앵커 ⑨ "검산 절 = 자기 문서 안에서 타 문서·타 파일 상태를 근거로 제시하는 모든 절"(성격 열거 → 판정 기준 서술, 신규 절 자동 편입) · 앵커 ⑯ "배타·전수 주장은 재현 명령 동반" | E5 grep `커버리지 대사표` · `앵커 ⑯` | 라운드 23 스윕 | **수신 완료(2026-08-07 조율자 스윕 — 본 행 신설로 종결. E5 §D 앵커 ⑨ 판정 기준 서술 2건·앵커 ⑯ 정의 실재)** | — |
| 43 | E5 §신규 위임 #16(라운드 22 제출분 — D22-1 모수 파급으로 §E 2항 예시 수치("Wave 5 편성치 166만 중 91만 = 55%")가 stale될 수 있어 A팀 확정값 수신 후 판정 요청) | 조율자 스윕(판정) | E4 grep `MAX` 재실행 · E5 §E 2항 예시 대조 | 라운드 23 스윕 | **수신 완료·종결(2026-08-07 조율자 판정 — A팀 재계산 결과 최대 부하 웨이브가 Wave 5 = 166만으로 **불변**이므로 §E 2항 예시는 stale 아님. 재현: `python3` §A-6 블록 재실행 → `MAX ('5', 166)`. 갱신 불요)** | — |
| 44 | E5 §신규 위임 #17(라운드 23 D23-1 제출분 — 앵커 ⑯ 존량 전환 규칙·"동일 문안 선언" 대상 어휘 확장이 PMO 작업 순서 밖에 남던 "분모 밖 위임" 6회차) | E1 §A PMO 행(소유·금지·산출물 형식 3열)·§B-4 PMO 위임 템플릿 — ⑯ 임무를 3축(표본 3건 재실행 / 존량 잔여 `grep -c` 실측·§E 7항 기록·웨이브 감소 / 동일 문안 양단 리터럴 재현)으로 확장 + 표본 편중 금지 + **존량 3분기 종결 판정은 조율자 전속** | E1 grep `존량 잔여` · `표본 편중 금지` · `동일 문안 선언` | 라운드 23 수정 완료 시 | **수신 완료(2026-08-07 조율자 스윕 — 양방향 재현 3쌍 전건 양쪽 ≥1: `존량 잔여` E1 4/E5 6 · `표본 편중 금지` E1 2/E5 2 · `동일 문안 선언` E1 4/E5 5)** | — |
| 45 | E5 §신규 위임 #18(라운드 23 신설 3건의 대장 편입 요청 — 앵커 ⑥ 단일 선언 정상 조항(D23-4)·앵커 ⑯ 존량 전환 규칙(D23-1)·§D 절차 6 **스윕 양단 동기 규칙**(D23-6)) | PIVOT-PLAN §6-11(본 행) + **같은 스윕 안에서 발주 문서 상태 칸 전환**(D23-6 자신이 규정한 절차의 첫 적용) + §6 본문 총계 동기(앵커 ⑫) | E5 grep `단일 선언이 정상` · `존량` · `같은 스윕 안에서` | 라운드 23 스윕 | **수신 완료(2026-08-07 조율자 스윕 — 3건 실재 확인 후 본 행 신설. 스윕 양단 동기 규칙은 조율자 라운드 마감 체크리스트에 편입했고, 본 라운드 등재분 #42~#45 전건에 대해 발주 문서 상태 칸을 같은 스윕에서 전환했다)** | — |
| 46 | E5 §신규 위임 #19(라운드 24 신설·개정 3건 — 앵커 ② **오분류 대조**(D24-6: `[SOLO]` 표기끼리만 보던 3자 일치가 표기 누락·오표기를 검출하지 못하던 사각)·앵커 ⑥ **개시 라벨 대조**(D24-2: 종료 웨이브만 대조해 개시 라벨이 사각지대였다)·앵커 ⑨ **§A-8 재현 대상 재정의**(D24-1 파급, E4 §G #11 수신분)) | PIVOT-PLAN §6-11(본 행) + 같은 스윕 내 E5 표 상태 칸 전환(D23-6) | E5 grep `단독 배치이나 SOLO` · `개시 라벨` · `스냅샷 값이 명령 출력보다 작으면` | 라운드 24 스윕 | **수신 완료(2026-08-07 조율자 스윕 — 3건 실재 확인 후 본 행 신설. 앵커 ② 양단 재현 완주: 단독 배치 ∖ SOLO 차집합 = `8b·14·15a·17a` = E3 §B 부기 목록 일치, 역차집합 빈 출력. 앵커 ⑥ 좌항(E3 §C 첫 행) = 우항(EXEC-PLAN §3-3 참조형) 일치, 舊 "웨이브 0" 라벨 실재 0건)** | — |
| 47 | E4 §G #11(라운드 24 제출분 — D24-1의 형태 전환(라운드 수 리터럴 → 재현 명령)이 앵커 ⑨ 舊 문안("독립 평가 라운드 수와 일치")과 어긋나 **형태 전환이 성공한 바로 그 이유로 다음 라운드에 오탐**이 나던 구조. 세는 대상이 다른 후보가 공존한다 — 실측 25 / 24 / 23) | E5 §D 앵커 ⑨ §A-8 절(재현 대상 재정의 — 스냅샷과 명령 출력의 차이가 "평가만 완료되고 수정이 진행 중인 라운드 수" 범위인지로 판정, 어느 명령의 출력인지까지 대조) | E4·E5 grep `스냅샷 값이 명령 출력보다 작으면`(양단 ≥1) | 라운드 24 수정 완료 시 | **수신 완료(2026-08-07 조율자 스윕 — 양방향 리터럴 재현 E4 2 / E5 2, 양쪽 ≥1. 재정의 앵커로 실판정: E4 스냅샷 R=23 vs 명령 출력 23 → 차이 0, 허용 범위 내 성립. 舊 문안이었다면 24≠23으로 D6-6 위반 오탐 1건이 났을 자리)** | — |
| 48 | **조율자 D24-2 전단**(라운드 24 U2-2 — EXEC-PLAN §3-3이 "착수 순서는 E3의 **웨이브 0**부터"라 적었으나 E3에 그 라벨이 실재하지 않았다. 실행 첫 지시가 존재하지 않는 대상을 가리키던 상태) | EXEC-PLAN §3-3(참조형 교체 — "E3 §C 웨이브 표의 첫 라벨"+재현 명령) | EXEC-PLAN grep `E3 §C 웨이브 표의 첫 라벨` | 라운드 24 스윕 | **수신 완료(2026-08-07 조율자 직접 수행 — 신 문안 1건 실재, 舊 "웨이브 0부터" 0건. 재현 명령 실행 결과 첫 라벨 `**1**`. 구조적 원인(앵커 ⑥ 개시 라벨 사각)은 #46이 닫았다)** | — |
| 49 | E5 §신규 위임 #20(라운드 25 D25-3 신설 2축 — 앵커 ⑫ **번호 정합 대조**(발주 문서가 표기한 `→ 6-11 #n`과 실제 등재 행의 양방향 일치)·앵커 ⑬ **표 구조 무결성**(표 행 파이프 수 = 헤더, 코드스팬 마스킹 후 볼드 폐쇄). 라운드 24 스윕 산출물의 물리적 파손이 앵커 어느 것에도 걸리지 않던 공백을 닫는다) | PIVOT-PLAN §6-11(본 행) + 같은 스윕 내 발주 문서 상태 칸 전환(D23-6) | E5 grep `번호 정합 대조` · `표 구조 무결성` | 라운드 25 스윕 | **수신 완료(2026-08-08 조율자 스윕 — 2축 실재 확인 후 본 행 신설. 신설 직후 그 앵커가 즉시 4건을 적발했다: E5 자기 문서 §A 게이트④ 행의 6셀/7헤더 결함(16종 앵커가 전부 놓친 건)·E1 §A "FE 리드" 행 볼드 미폐쇄·E2 §C T-W1-07a 행 볼드 미폐쇄·E3 §신규 위임 #1 대장 번호 병기 누락. 4건 전건 수리 후 앵커 ⑬ 재실행 결과 exec 전 문서 히트 0. 마커 검사는 오탐 방지를 위해 표 행 한정 + 백틱 코드스팬 마스킹으로 정의했고 대조군(마스킹 없음) 대비 허위 12건 제거를 실증했다)** | — |
| 50 | **EXEC-EVAL-ROUND-25 K2-1**(중 — 계획이 신설하는 `packages/ui`를 3앱이 소비하려면 각 앱 `package.json`에 `workspace:*` 선언이 필요한데 어느 태스크도 그 파일을 소유하지 않아, lockfile 편집 태스크 집합이 실체보다 작게 잡히고 **Wave 13a에 lockfile 2건 동시 배치 = D6-1 위반**이 성립하던 결함) | E2 §A·§C·§0·§G·§H(소유 확정·판정표) / E3 §B·§C·§I(lockfile 집합·검증 블록) / E4 §A-6·§A-3·§A-8(사이징 재계산) / E5 §B·§A(정의역 확장 인용) | E3 §B python 블록 재실행 → `lockfile 2건 이상 웨이브: []` | 라운드 25 수정 완료 시 | **수신 완료(2026-08-08 조율자 스윕 — B팀이 두 대안을 비교해 **T-W1-01 일괄 소유**(ⓑ)를 채택하고 근거 5건을 E2 §A에 기록. T-W1-01이 SOLO 웨이브라 D6-1을 정의상 충족해 **재배치 0건으로 위반 해소**(대안 ⓐ는 라벨 순증 23→24를 강제했다). 근본 원인인 대상 어휘를 "신규 npm 패키지 도입"에서 **"`pnpm-lock.yaml`을 갱신하는 모든 태스크"**로 확장해 발생 경로 자체를 차단했고, 그 정의로 재점검해 **동형 잔여 T-W3-02**(Workbox 이식인데 두 앱 `package.json` 미소유)도 편입했다. 조율자 독립 재실행으로 lockfile 8종 전건 웨이브당 1건 이하 확인)** | — |
| 51 | **조율자 D25-0**(라운드 25 K2-2 — 라운드 24 스윕에서 조율자가 상태 칸 전환을 **python 정규식 일괄 치환**으로 처리하다 요청 칸 안의 **인용된 문자열**까지 치환에 걸려 E5 §신규 위임 #19 셀에 문자열 중복·인용 미폐쇄를, E4 §G #11에 닫는 볼드 소거를 만들었다. 대장 번호도 실제 등재 2행(#46·#47)과 어긋났다) | E5 §신규 위임 #19 · E4 §G #11(원문 복원·번호 정정) | 앵커 ⑬ 재실행(표 행 파이프 수·마커 폐쇄) | 라운드 25 스윕 | **수신 완료(2026-08-08 조율자 직접 수리 — 파이프 수 E5 #19 = 6 · E4 #11 = 8로 헤더와 동일, 마커 폐쇄 정상. **재발 방지**: 본 라운드부터 상태 칸 전환은 **행 단위 확인으로 수행하고 일괄 정규식 치환을 쓰지 않으며**, 전환 직후 앵커 ⑬ 스크립트를 1회 실행한다(#49가 그 검사를 담당). 본 스윕이 그 절차의 첫 적용이다)** | — |
| 52 | E5 §신규 위임 #25(라운드 26 D26-1·D26-3·D26-4 — 앵커 ⑨ **3축 확장**: ⓐ §A-8 재현 대상을 "스냅숏 값과 명령 출력의 차이" 판정에서 **"④·⑥이 산식 입력으로 쓴 값이 부착 명령의 그 시점 출력과 일치하는지"**로 재정의 + 라운드 수 리터럴 재등장 금지(단 정당한 고정 상수·동명이수는 명시 제외) ⓑ 검산 절 정의역에 **자기 파일의 문자열·항목 수를 근거로 제시하는 절** 추가(자기 파일 계수는 존재 재현 형식만 허용) ⓒ **재현 명령의 실행 컨텍스트(cwd) 명시 여부** 판정 항목 신설) | PIVOT-PLAN §6-11(본 행) + 같은 스윕 내 발주 문서 상태 칸 전환(D23-6) | E4·E5 grep `부착된 명령의 그 시점 출력`(양단 ≥1) | 라운드 26 스윕 | **수신 완료(2026-08-08 조율자 스윕 — 3축 실재 확인. 양단 대조 어휘가 舊 "스냅샷 값이 명령 출력보다 작으면"에서 신 리터럴로 교체됐고 E4 3 / E5 3으로 양쪽 ≥1 성립(舊 어휘 잔존은 양쪽 각 1건, 전부 과거형 회고). ⓒ 신설 판정 항목을 전 문서에 적용한 결과 지정 범위(E3 §G·E4 §G #7) **밖에서 동형 존량이 추가 검출**됐고(E1·E4는 A팀, E3는 B팀이 같은 라운드에 해소) 최종 탐지 명령 히트 0)** | — |
| 53 | E5 §신규 위임 #26(라운드 26 D26-4 존량 규칙 최초 등재 — 실행 컨텍스트 미명시 존량. 앵커 ⑯ D23-1 동형으로 최초 1회 등재 후 해소까지 무보고) | E3 §C 재검산 블록(B팀) + 지정 범위 밖 추가분(E1 §E·§B-4·§C-2 · E4 §F·§F-1·§A-8 ④ = A팀 · E3 §B 인라인 1개소 = B팀) | 탐지 명령 `grep -nE '^\$ .*[[:space:]"(<][A-Za-z0-9_.-]+\.md' docs/plan/exec/E[1-5]-*.md` → 히트 소멸 | 라운드 26 수정 완료 시 | **수신 완료·종결(2026-08-08 조율자 스윕 — 존량 잔여 0. C팀 제출 시점 계수(E1 15줄·E3 5줄·E4 7줄)는 `$` 블록 줄만 센 값이었고 B팀이 산문 인라인 1개소를 추가 발견해 E3 실제 잔여는 6개소였다. A·B 양 팀이 같은 라운드에 전건 해소해 조율자 재실행 결과 히트 0)** | — |
| 54 | E4 §G #12(라운드 26 제출분 — §A-8 ⑥ 라운드 수 값 폐지로 **대장 §6-11 #9의 재개 하한 사본**(절사값·분모)만 남으면 R 상승마다 stale된다. 앵커 ⑤ 총예산 축과 분리 판단 요청) | PIVOT-PLAN §6-11 #9 상태 칸(조율자 소유) | PIVOT grep `E4 §A-8 ⑥ 산식의 그 시점 출력` | 라운드 26 스윕 | **수신 완료(2026-08-08 조율자 직접 수행 — #9의 재개 하한을 값 없는 참조형으로 전환(舊 "≥2.7회(분모 19+24=43)"은 라운드25 시점 스냅숏임을 병기). **앵커 ⑤ 총예산 축은 무영향** — 라운드26은 모수 무변동이라 하한 2,204만 유지, D14-2 "#9 동기화 필요" 미발동. A팀이 두 축을 분리 기재해 발주한 판단이 정확했다)** | — |
| 55 | E3 §신규 위임 #5(라운드 25 K2-1/D25-1 제출분 — E5 §B "의존성 추가 태스크" 행 열거를 6종 → 8종으로 갱신하고 대상 어휘를 "신규 npm 패키지 도입"에서 **"`pnpm-lock.yaml`을 갱신하는 모든 태스크"**(워크스페이스 `workspace:*` 선언 포함)로 확장 요청. **라운드 27 H2-1이 적발한 진짜 미등재** — 두 스윕(25·26)을 생존했다) | E5 §B "의존성 추가 태스크" 행 · §A 게이트③ 인용부 | E5 grep `pnpm-lock.yaml`을 갱신하는 모든 태스크 | 라운드 25 수정 완료 시 | **수신 완료(2026-08-08 조율자 스윕 — 수신처 이행은 라운드 25에 이미 완료돼 있었고(C팀이 정의역 확장을 인용형으로 반영) **등재만 누락**됐다. 누락 경로: 발주 문서 상태 칸이 D7 두 허용 형식(`→ 6-11 #n 참조` / `등재 필요(6-11 편입 대기)`) 밖의 제3 문구(`미등재 — 조율자 스윕 등재 요청`)였고, PMO 미등재 탐지 grep과 앵커 ⑫ 후보 형성이 모두 그 두 형식을 분모로 삼아 **탐지 분모가 비었다**. "분모 밖 위임" 7회차이며 이번에는 위임이 아니라 **상태 칸 문구의 형식 이탈**이 원인이다. 부수 원인: 조율자 스윕이 "그 라운드 제출분만" 처리하던 암묵 전제가 이전 라운드 잔여를 영구히 놓치는 경로였다 — 재발 방지는 #56)** | — |
| 56 | E5 §신규 위임 #27(라운드 27 신설·개정 6건 — **상태 칸 형식 배타 규정**(D27-1①: 두 형식만 허용, 그 외는 값의 정오와 무관하게 위반. 해악은 값이 아니라 **탐지 분모를 비우는 것**이라 정확한 이탈일수록 오래 생존한다)·**스윕 대상 범위 정정**(D27-1③: 대상 원천은 팀 제출분이 아니라 탐지 명령의 그 시점 출력 전수)·앵커 ⑫ **형식 배타 축**·앵커 ⑨ **정의역 exec 전 문서 확장 + 인라인 판정 ⓑ 신설**·앵커 ⑯ **④ 줄바꿈 정규화 판정**·앵커 ⑨ **ⓑ축을 "리터럴 + 특정 라운드 지목 위상 단정"으로 확장**(A팀 §G #13 수신)) | PIVOT-PLAN §6-11(본 행) + 같은 스윕 내 상태 칸 전환 + 전환 직후 앵커 ⑫ 형식 배타 축 1회 실행 | E5 §D 내장 스크립트를 리포 루트에서 원문 실행 | 라운드 27 스윕 | **수신 완료(2026-08-08 조율자 스윕 — 6건 실재 확인. 조율자가 C팀 내장 스크립트를 원문 그대로 추출·실행: 검사 행 E1 2·E2 7·E3 6·E4 15·E5 27 = 57행 **형식 위반 0**, 검사 행 0인 문서 없음. cwd 탐지 ⓐ exec 전 문서 히트 0. 설계 중 실측으로 **분모 구멍 2개**를 닫았다 — ① 평가서 검증 스크립트가 하드코딩 화이트리스트로 1건을 제외해 평가에서도 미검출이었다 ② E1은 표가 아니라 산문 `상태:` 형식이라 표 기반 탐지의 검사 행이 0이었다)** | — |
| 57 | E4 §G #2(D3 편성 규칙 발주 — 舊 "선확정이라 등재 불요"로 대장 미등재였던 발주. 라운드 27 형식 배타 규정 적용 중 A팀이 자기 파일에서 적발) | PIVOT-PLAN §6-11(본 행, 정식 등재 — 수신처 이행은 이미 완료) | E4 §G #2 상태 칸 형식 | 라운드 27 스윕 | **수신 완료(2026-08-08 조율자 스윕 — 이행 완료·등재만 누락됐던 #55와 동형 사례. A팀이 두 허용 형식으로 전환해 PMO 탐지 분모에 진입시켰다)** | — |
| 58 | E4 §G #13(라운드 27 H2-3 제출분 — 라운드 26에 라운드 수 **리터럴**을 전면 제거했으나 그 자리에 넣은 **특정 라운드 지목 단정**("라운드26은 R에 포함되지 않는다")이 해당 커밋 기록과 동시에 거짓이 됐다. **값을 없애도 단정을 넣으면 같은 방식으로 뒤집힌다**) | E5 §D 앵커 ⑨ ⓑ축(금지 대상 확장) + E4 §A-8 서두(일반 규정 신설) | E4 grep `라운드 지목 단정` · E5 앵커 ⑨ ⓑ 문안 | 라운드 27 수정 완료 시 | **수신 완료(2026-08-08 조율자 스윕 — A팀이 시점 명시형으로 교체하고 **미래 검증**까지 수행(이번 수정 커밋이 기록돼도 문안이 거짓이 되지 않음을 실행으로 확인). C팀이 ⓑ축을 확장하며 **시점 명시 회고·이력 제외의 판별 표지**를 명문화하고 오탐 대조로 라이브 위상 단정 0·신규 상시 위반 0을 실증했다)** | — |
| 59 | **EXEC-EVAL-ROUND-28 G2-1·G2-2**(라운드 28 — "동일 문안 / 문자 그대로 동일" 선언 3계열이 리터럴 대조로 반증되고 앵커 ⑯의 양단 재현을 미동반. 그중 1건은 ⑯ 신설과 같은 커밋에서 수정돼 **존량 면책 밖**이었다. 아울러 앵커 ⑯ 정의문("exec 전 문서")과 ②·④ 명령 glob(`E[1-5]-*.md`)이 불일치해 `EXEC-PLAN.md`·`EXEC-DECISIONS.md`가 분모 밖이었다 — D27-2가 앵커 ⑨·⑬에 적용한 정의역 통일의 ⑯ 미전파) | E2 §E·§0·W3 선행 문단 · E3 §C Wave 16·§D · E5 §C W2/W4 행·§D 앵커 ⑯ ②④ · E1 §C-2 6행 | 양단 `grep -c` 3계열 + ⑯ 확장 glob 실행 | 라운드 28 수정 완료 시 | **수신 완료(2026-08-08 조율자 스윕 — 3계열 양단 재현 전건 성립(조율자 독립 실행: (a) 1/1·3/2/1 · (b) 1/1 · (c) 2/1). **세 계열을 각각 다르게 처리한 판단이 정확하다** — (a)는 E2 통일 + E3는 **셀 축약형이라 리터럴 일치가 원리적으로 불가**해 선언 정정, (b)는 E5 정본 통일(결과가 상위 정본 02 §E-9 열거 순서와도 일치), (c)는 통일 + **인용 어절이 줄바꿈에 쪼개져 재현 명령이 원리적으로 볼 수 없던 구조**를 한 줄로 재작성. ⑯ glob 확장 후 분모 297→314·잔여 198→212(조율자 독립 재현 일치)이며 증가 8건 중 6건은 면제(시점 명시 이력·舊 정본 인용), 2건은 최종 수정 커밋이 ⑯ 신설보다 앞서 **존량 면책 → 즉시 위반 0**. 대조군 `B∖A = 0` 유지로 새 오탐 경로 없음)** | — |
| 60 | **T-W0-01 구현 에이전트**(게이트① 완료 보고 #1, 우선순위 높음 — `WEB_ORIGINS`가 zod Env 스키마 밖이라 `@nestjs/config`가 validate 통과분만 주입하는 구조상 파일 경유 설정이 strip돼 도달하지 않는다. 부팅은 정상이고 CORS·쿠키 경로만 조용히 닫혀 원인 추적이 어렵다) | `services/api/src/config/env.schema.ts`(조율자 직접 반영 — 소유권 밖 필수 1줄, **EXEC-DECISIONS #13 ①b**) | `grep -c "WEB_ORIGINS" services/api/src/config/env.schema.ts` → ≥1 | Wave 2 커밋 전 | **수신 완료(2026-08-08 조율자 반영 `2b2c457` — 실효 확인: `envSchema.parse({WEB_ORIGINS:…, SOME_UNKNOWN_KEY:…})`가 WEB_ORIGINS는 보존·미지 키는 strip(게이트② 독립 실증), api typecheck exit 0·유닛 56/485 그린)** | — |
| 61 | **T-W0-01 구현 에이전트**(게이트① 완료 보고 #2 — 웹 쿠키 세션 e2e 신설. `test/**`가 이 태스크 소유권 밖이었다(조율자 위임의 한계). Set-Cookie 왕복·CSRF 403·회전 후 쿠키 교체를 실 HTTP로 실증) | `services/api/test/auth-web.e2e-spec.ts`(**배치 확정 — EXEC-DECISIONS #19**: Wave 3 종료 ~ Wave 4 착수 사이 **웨이브 사이 단독 슬롯**. 웨이브 라벨 신설 없음 — 태스크 ID 없는 대장 이행 항목이라 T-NC 트랙과 동형으로 웨이브 밖 처리, E2 코드 46건·E3 웨이브 23·SOLO 9 전부 불변) | `ls services/api/test/auth-web.e2e-spec.ts` | W1 착수 전 | **수신 완료(2026-08-08 — `791a8c8`)** — 존치 사유였던 "일회성 실측"이 해소됐다. T-W0-01 게이트②가 실기동 HTTP 왕복으로 동등 범위를 외부 관측했으나(9스위트/52건 + 블랙박스 14종) **회귀 스위트로 고정되지 않았던** 것이 이 행의 존치 사유였고, 본 이행이 **11테스트/계약 13종**으로 고정했다. **2026-08-08 Wave 3 종료 시 기한 도래 확인**(W1은 Wave 3에서 착수됨 — 기한 칸 "W1 착수 전"과 조치 칸 "W1 종료 보고 재상신"이 서로 다른 시점을 가리켰고, 더 이른 기한 칸을 따랐다) → EXEC-DECISIONS #19로 배치 확정 → 웨이브 사이 단독 슬롯에서 이행. **게이트①②③ 통과**(`services/api/src/**` 무변경 — 소스를 고쳐 통과시키지 않았다. 허위 통과 방지를 양측이 각각 실증: 구현자 브레이크 4건 + 게이트② 독자 브레이크 3건[AC2·AC3·AC4 세 축 분산], 전건 red 확인 후 바이트 단위 원복. 유닛 56/485 before=after, 전체 e2e 10파일/63테스트 단일 워커 무회귀) | — |
| 62 | **T-W0-01 구현 에이전트**(게이트① 완료 보고 #3 — FE 클라이언트 계약 인계: `fetch(…, { credentials: 'include' })` + `X-Requested-With: XMLHttpRequest` 필수, access는 메모리 보관, **refresh single-flight 필수**. 웹 멀티탭이 쿠키를 공유해 동시 회전 시 CAS 패자가 family 폐기를 유발 → 전 기기 로그아웃) | W1/W2 웹 전환 FE 태스크(T-W2-01·T-W2-04 등 — 인증 클라이언트 소유 태스크) | `grep -rn "credentials: 'include'\|X-Requested-With" apps/` → 해당 태스크 완료 후 ≥1 | 해당 FE 태스크 착수 시 | **트리거 대기(2026-08-08 등재)** — 관제 앱의 `getFreshAccessToken` single-flight 패턴이 이미 있어 동형 이식이 경로다 | 해당 태스크 위임 프롬프트에 본 행을 인용 |
| 63 | **T-W0-01 구현 에이전트**(게이트① 완료 보고 #4 — `services/api/src/live/live.gateway.ts`가 `cors: { origin: true, credentials: true }`로 전면 허용. WS는 쿠키를 쓰지 않아 즉시 위험은 아니나 `WEB_ORIGINS` 화이트리스트로 좁히는 것이 맞다. 소유권 밖이라 미수행) | `services/api/src/live/live.gateway.ts`(후속 태스크 — 배치는 조율자 판정) | `grep -n "origin: true" services/api/src/live/live.gateway.ts` → 해소 후 0 | W2 종료 전 | **미수신(2026-08-08 등재)** | 미해소 시 W2 종료 보고에서 재상신 |
| 64 | **T-W0-01 구현 에이전트**(게이트① 완료 보고 #5 — `api.<도메인>`이 웹 3종과 **같은 등록가능 도메인**이어야 `SameSite=Lax`·`__Host-`·first-party 전제가 성립한다. 별도 도메인이면 `SameSite=None` 재설계 필요) | T-W0-01(도메인 확정 시 재방문) · T-W0-03(vhost 구성) · G9 ① 확정 절차 | 도메인 확정 시 `reviews/dod-evidence/w0/T-NC-02-g9-receipt.md`에 등록가능 도메인 동일 여부 기재 | G9 ① 확정 시 | **트리거 대기(2026-08-08 등재)** — 현재 임시 도메인 선행 중이라 미발동 | G9 ① 접수 시 조율자가 본 행을 먼저 확인 |
| 65 | **T-W0-01 게이트② qa-verifier**(정본↔코드 경로 수준 불일치 — 02 §D-T3 원문 "`POST /v1/auth/refresh` 쿠키 수용"(기존 라우트 확장) vs 구현 `POST /v1/auth/web/{login,refresh,logout}`(분리 3종)) | `docs/plan/02-web-architecture.md` §A D-T3(**EXEC-DECISIONS #14 ①** — 코드가 옳고 문서를 고친 사례) | `grep -c "auth/web/{login,refresh,logout}" docs/plan/02-web-architecture.md` → ≥1 | Wave 2 종료 시 | **수신 완료(2026-08-08 조율자 반영 — 분리안이 AC 2·3을 구조적으로 더 강하게 만족한다는 게이트② 판정 근거를 문안에 병기)** | — |
| 66 | **T-W0-01 게이트② qa-verifier**(주석 부채 — `auth.controller.ts:68-71`·`main.ts`의 "`WEB_ORIGINS`는 env.schema 밖" 주석이 조율자 조치 `2b2c457` 이후 사실과 다름. 겸해 `requestOrigin`의 "불투명 오리진 불가" 주석이 Referer 폴백 실동작과 불일치(보안 결함 아님, 주석-동작 불일치)) | `services/api/src/auth/{auth.controller.ts,auth.service.ts}` · `src/main.ts`(후속 정리 — 배치는 조율자 판정) | `grep -c "env.schema.ts(Env) \*\*밖\*\*\|Env) 밖" services/api/src/auth/auth.controller.ts services/api/src/main.ts` → 해소 후 0 | W1 종료 전 | **미수신(2026-08-08 등재)** — 동작 영향 0(P3) | 미해소 시 W1 종료 보고에서 재상신 |
| 67 | **T-W1-01 구현 에이전트**(게이트① 완료 보고 신규 위임 #1) + **T-W1-01 게이트② qa-verifier**(독자 재현으로 사실 확정) — 3앱 `src/global.css`가 **어떤 JS 엔트리에서도 import되지 않는다**. T-W0-05가 파일만 신설하고 배선은 남겼다(각 파일에 "현재는 배선하지 않는다" 주석 실재). 이 상태로는 T-W1-01이 노출한 `packages/ui/src/tokens.css`를 `@import`해도 CSS 커스텀 프로퍼티가 브라우저에 **도달하지 못해** 03 §A-1의 `rem` 실현 경로가 성립하지 않는다 | 후속 소비 전환 태스크(T-W1-02 구독자 · T-W2-05 기자 · T-W2-06 관제) 중 하나가 흡수하거나 별도 항목화 — **배치는 조율자 판정** | `grep -rln "global.css" apps/reporter apps/control-center apps/subscriber` → 해소 후 ≥1(2026-08-08 실측 **0건**, exit 1) | **W1 종료 전**(구독자 웹이 W1 산출물 — 그 안에 배선되지 않으면 W1 DoD의 접근성 수치가 웹에서 미적용인 채 닫힌다) | **수신 완료(2026-08-09, Wave 5 — subscriber분)** — 등재 시점 게이트②가 독자 재현해 사실로 확정했고(구현자 주장을 베끼지 않음), 해소도 게이트②가 웹 빌드 산출물로 실증했다 | **T-W1-02가 흡수**(EXEC-DECISIONS #22 ③) — `app/_layout.tsx` JS import + `src/global.css`의 `@import tokens.css` 2단 배선(소유권 13→14파일 확장 승인). 게이트② 실증: `expo export -p web` 산출물에 `_expo/static/css/tokens-*.css`가 생성되고 `index.html`이 이를 `<link>`로 로드하며, 렌더된 HTML에 `font-size:var(--gachinol-font-body)`가 직접 박힌다. 네이티브는 `@expo/metro-config`의 `transformCss()`가 `platform!=='web'`에서 빈 모듈로 치환해 무해(`expo export -p android` 완주로 실증). **reporter·control-center 2건은 T-W2-05·06 소관으로 남는다** — 그 2건이 닫히기 전까지 본 행의 재현 명령(3앱 대상)은 여전히 부분 충족이다 |
| 68 | **조율자**(Wave 3 게이트④ 직전 산출물 검토 중 실측 발견) — 3앱에 `lineHeight: <숫자>` **하드코딩 23건/11파일**(reporter 11/6 · control-center 11/4 · subscriber 1/1). `fontSize`만 `rem` 전환되면 행간이 고정돼 **본문 18px에서 행간 비율 1.0으로 붕괴**하고, 큰 자막 모드(22~24px)에서는 **행간이 폰트보다 작아져 글자가 겹친다**. 03 §A-1 1.4.12(줄간격 1.5배)·1.4.4(200% 확대까지 콘텐츠 손실 없음)에 직접 걸린다. 겸해 E2가 소비 전환 3태스크를 "import 교체 기계적 반복"(분할 예외②)으로 사이징한 **전제를 흔든다** | T-W1-02(subscriber 1건) · T-W2-05(reporter 11건) · T-W2-06(control-center 11건) — 각 소비 전환 태스크 | `grep -rn "lineHeight:" apps/*/src apps/*/app \| grep -cE "lineHeight: *[0-9]"` → 해소 후 0(2026-08-08 실측 **23**) | 각 소비 전환 태스크 착수 시(최초 = T-W1-02, Wave 5) | **미수신(2026-08-08 등재)** | 각 소비 전환 태스크 위임에 본 행 인용. subscriber 1건은 T-W1-02가 흡수해도 **사이징 불변**(해당 파일이 이미 그 태스크의 12파일 소유 집합 안), reporter·control-center 22건은 **T-W2-05·06 착수 전 조율자가 사이징 영향 재판정** |
| 69 | **T-W1-01 구현 에이전트**(게이트① 완료 보고 신규 위임 #3) — `packages/ui`가 실제 컴포넌트(JSX)를 갖게 되면 `react`도 3앱과 **정확히 같은 버전으로 정렬**해야 한다. T-W1-01에서 `@types/react` 미정렬만으로 pnpm이 **다른 해시의 `react-native` 인스턴스**를 별도 설치해 jest-expo의 Platform 목을 타지 못하는 **실 런타임 실패**(`__fbBatchedBridgeConfig is not set`)가 발생했고 `"@types/react": "~19.1.0"` 추가로 해소했다 | 향후 `packages/ui` 컴포넌트 승격 태스크(현행 계획에 미존재 — 발생 시 자동 수신) | 해당 태스크 발생 시 `python3 -c "import json;print(json.load(open('packages/ui/package.json')).get('devDependencies'))"`가 3앱과 동일 버전대로 정렬됐는지 확인 | 해당 태스크 착수 시 | **트리거 대기(2026-08-08 등재)** — 현행 계획에 컴포넌트 승격 태스크가 없어 미발동 | 해당 태스크 위임 프롬프트에 본 행 인용 |
| 70 | **T-W1-05 게이트② qa-verifier**(격리 컨테이너 실측) — **`go.<도메인>/c/:id` 공개 링크가 api에 도달하지 않는다.** 업스트림이 받는 경로가 `/c/<uuid>` 원경로 그대로(rewrite 없음)인데 api는 `setGlobalPrefix('v1')`이라 `/v1/go/c/:id`만 서빙한다 → 현재 커밋 그대로 배포하면 **404, 카톡 미리보기 전체 사망**. 계획이 이 매핑을 **어느 태스크에도 배정하지 않은 범위 공백**(T-W1-05 산출물·T-W1-06 DoD 어디에도 없다) | `infra/docker/nginx.conf`의 `go.` vhost(**T-W1-06 범위 확장으로 같은 웨이브에서 처리** — EXEC-DECISIONS #21 ⑤ⓐ) | `curl -H "Host: go.<도메인>" .../c/<uuid>` 시 업스트림 수신 경로가 `/v1/go/c/<uuid>`인지(가짜 업스트림 격리 재현) | **Wave 4 내**(이월 시 "기능이 있으나 닿지 않는" 상태가 main에 남는다) | **수신 완료(2026-08-08 — T-W1-06 범위 확장으로 같은 웨이브에서 이행)** — `rewrite ^/c/(.*)$ /v1/go/c/$1 break;` 추가. 격리 컨테이너 실측으로 업스트림 수신 경로가 `/v1/go/c/<uuid>`임을 확인(`/c/:id/thumb`·쿼리스트링 보존 포함), 기존 SPA 폴백·no-cache·해시자산 캐시·444 격리·`api.` 프록시 전건 무회귀. **구현 중 함정 1건 재현·수정**: `rewrite … break;`를 `set $upstream_api` **앞**에 두면 `break`가 이후 `set`까지 건너뛰어 500(`using uninitialized "upstream_api" variable`) — `set`을 먼저 두는 것으로 해소하고 주석으로 남겼다 | — |
| 71 | **T-W2-01 게이트② qa-verifier**(코드 추적) — **기자 앱 웹 인증 배선이 통째로 없다.** `auth.ts`가 `/auth/login`(바디)을 호출하고, `client.ts`의 `doRefresh()`는 `getRefreshToken()` falsy면 **네트워크 호출조차 안 하며**(웹에서 항상 null), `doFetch`에 `credentials:'include'`·`X-Requested-With` 부재, `auth-context.tsx`의 `bootstrap()`은 **유효한 HttpOnly 쿠키 세션이 있어도 무조건 signedOut** 판정 → 웹에서 access 만료·새로고침 후 **복구 경로 없음**. T-W0-01이 서버측을 완성했으나 **클라이언트 소비가 계획에서 누락**됐다(02 §E-7 원문·E2 전체에 `client.ts` 언급 0건) | `apps/reporter/src/api/{client.ts,auth.ts}` · `apps/reporter/src/auth/auth-context.tsx` — **신규 태스크 신설 필요**(EXEC-DECISIONS #21 ⑤ⓑ. 지금 채번하지 않는다: E2 태스크 수·E3 편성·E4 예산 동시 파급 + Wave 5는 이미 G4 상한) | `grep -rn "credentials: 'include'\|X-Requested-With\|auth/web/login" apps/reporter/src/` → 해소 후 ≥1(현재 0) | **W2 착수 전**(기자 웹앱 자체가 W2 산출물이라 그 전까지는 미배포 — 이월 가능) | **수신 완료(2026-08-09, Wave 5)** — 대장 #62(FE 클라이언트 계약)가 지목한 4축 중 3축의 실제 수신처가 여기다 | **T-W2-19로 채번·구현·게이트② PASS**(EXEC-DECISIONS #22 ②). 판정 명령 재현 결과 **20건 히트**(등재 시점 0). 해소 형태: `doRefresh`가 웹에서 로컬 판정 없이 `POST /v1/auth/web/refresh`를 시도하고 그 HTTP 결과(`'ok'|'signed-out'|'error'` 3치)로만 세션을 판정 · `doFetch`가 웹 요청에 `credentials:'include'` 부착 · CSRF 헤더는 서버 `WebCsrfGuard`의 실제 키(`x-requested-with`)를 코드에서 확인해 사용 · `bootstrapSession()`이 네이티브만 조기 signedOut. 서버(`services/api`)는 무변경 — T-W0-01 산출물을 소비만 했다 |
| 72 | **T-W0-06 구현 에이전트**(게이트① 완료 보고 · 게이트②가 사실 확인) — `uptime-kuma-alerts.json`은 **desired-state SSOT**이고 `implemented` 7건 전부 `false`다. push 스크립트 실체(`.sh` 7종)·crontab 등록·Kuma Push 모니터 실제 생성이 없다(실측: `find infra -iname "*.sh"` → `pg-dump-to-r2.sh` 1건뿐) | `infra/monitoring/*.sh`(신규) + `infra/backup/crontab` 확장 — T-W0-04의 "uptime-kuma-api 스크립트" 후속과 동일 성격 | `find infra -iname "*.sh" | wc -l` → 해소 후 ≥2 | **제온 실배포 전**(경보가 실제로 울려야 의미가 있다) | **미수신(2026-08-08 등재)** | 제온 배포 태스크 편성 시 동반 |
| 73 | **T-W0-06 구현 에이전트**(게이트②가 사실 확인) — `users.role` 변경 감시가 현재 `updatedAt` **폴링 diff**로 대체돼 있어 **변경 이력이 남지 않는다**(누가·언제·무엇을 바꿨는지 소실). 전용 감사 테이블 부재(실측: `schema.prisma`에 `audit` 매치 0) | `services/api/prisma/schema.prisma`(신규 모델) + 기록 지점 — **Prisma 스키마 = 공용 자산이라 SOLO 필요** | `grep -c "model.*[Aa]udit" services/api/prisma/schema.prisma` → 해소 후 ≥1 | W2 종료 전 | **미수신(2026-08-08 등재)** — 보안사고 대응(08 §B)의 실효성에 직결 | SOLO 웨이브 편성 시 후보 |
| 74 | **T-W0-06 구현 에이전트**(게이트②가 사실 확인) — 증거 보전 버킷의 **R2 Object Lock(WORM) 미설정**. 실측 `ObjectLock`/`COMPLIANCE`/`GOVERNANCE` 전체 매치 0 → **"로그 삭제·덮어쓰기 금지"를 강제하는 것이 현재 규율뿐**이며 코드·인프라 어디에도 강제 장치가 없다 | R2 콘솔 설정(코드 아님 — 운영자 결정·실행) | R2 계정 확보 후 버킷 설정 스크린샷 또는 API 응답을 `reviews/dod-evidence/w0/`에 기록 | **R2 실계정 확보 시**(G9 ③ 운전자금 확인 이후 — EXEC-DECISIONS #12·#8) | **트리거 대기(2026-08-08 등재)** — 지출 발생 항목이라 무지출 선행 경계 밖 | G9 ③ 접수 시 조율자가 본 행을 함께 확인 |
| 75 | **T-W1-05 구현 에이전트**(게이트②가 사실 확인) — CF 존 설정(Cache Everything·Edge TTL "Respect origin"·stale 서빙·퍼지)의 **실행 가능한 런북 슬롯 부재**. 실측: `docs/infrastructure.md`에 "Cache Everything"·"Page Rule"·"Cache Rule"·"CF 존" 매치 0(계획서 프로즈 체크리스트만 존재) | `docs/infrastructure.md` 또는 별도 런북 — E2 T-W1-05 행이 지시한 "CF 설정으로 T-W1-06과 함께 문서화"의 미이행분 | `grep -c "Cache Everything" docs/infrastructure.md` → 해소 후 ≥1 | **CF 계정 확보 시**(현재 미보유 — #12) | **트리거 대기(2026-08-08 등재)** | CF 계정 접수 시 조율자가 본 행을 먼저 확인 |
| 76 | **T-W1-05 구현 에이전트**(게이트②가 사실 확인) — `buildShareLinks`(단축링크+직접링크 병행 발급, D-T6 조치2)가 **소비자 0**이다. 실측: `grep -rn "buildShareLinks"` → go-link 모듈 자신뿐, `distribution/*` 카카오 게시자산 경로가 소비하지 않는다 → 조치2가 코드에만 있고 실제 게시물에 반영되지 않는다 | `services/api/src/distribution/adapters/*` — 착수점 B(`KakaoManualPublishAdapter`)와 동일 슬롯 권장 | `grep -rn "buildShareLinks" services/api/src/distribution/` → 해소 후 ≥1 | 카카오 반자동 게시 어댑터 재구현 시(착수점 B) | **미수신(2026-08-08 등재)** | 착수점 B 편성 시 동반 |
| 77 | **T-W1-05 게이트② qa-verifier**(실측, 저심각도) — `deriveSelfUrl`의 `X-Forwarded-Host` 검증이 **형식 검증**(문자셋)일 뿐 **신원 검증**이 아니라, 문법상 유효한 임의 호스트명(`evil.attacker.example`)이 `og:url` 메타에 그대로 반영된다. **리다이렉트 목적지·XSS는 무영향**(env 기반 고정·이스케이프 확인)이고 `GO_LINK_BASE_URL` 정상 설정 시 이 경로 자체가 동작하지 않아 **저하 모드로 국한**되나, 코드 주석이 암시하는 보증보다 실제 방어 범위가 좁다 | `services/api/src/go-link/go-link.service.ts` — 신뢰 오리진 화이트리스트 검증으로 강화 검토 | `grep -c "X-Forwarded-Host" services/api/src/go-link/go-link.service.ts` 주변 로직 재확인 | 우선순위 낮음 — W2 종료 전 검토 | **미수신(2026-08-08 등재)** — P3(보안 침해 아님, 문서-실체 괴리). **동일 함수 2번째 발견 병합(2026-08-08, T-W1-06 추가분 구현자 보고)**: #70의 `go.` rewrite가 들어가면서 저하 경로의 `req.path`가 `/v1/go/c/:id`가 돼 **`selfUrl`에 내부 프리픽스 `v1`이 노출**될 수 있다 — 코드 주석이 "v1 지식이 모듈에 새지 않는다"고 전제하는 부분과 어긋난다. 둘 다 `GO_LINK_BASE_URL` 미설정 저하 모드 한정이고 정상 운영에서는 무영향 | 미해소 시 W2 종료 보고에서 재상신 |
| 78 | **조율자**(Wave 5 제온 실배포 중 실측 발견) — **제온의 nftables `forward` 체인이 `policy drop`이고 허용 규칙이 Incus 브리지(`incusbr0`)에만 있어 Docker 컨테이너의 아웃바운드가 전면 드롭된다.** NAT 마스커레이드 규칙은 Docker가 넣어 뒀으나 카운터가 0이다(FORWARD에서 먼저 막혀 도달하지 못함). 가동 중인 DCP가 멀쩡한 이유는 **DCP 컨테이너가 외부 인터넷을 필요로 하지 않기** 때문(이미지 pull은 데몬이 호스트 네트워크로 수행). 부수로 호스트 `/etc/resolv.conf`가 Tailscale MagicDNS(100.100.100.100)로 덮여 있어 컨테이너 DNS도 성립하지 않는다 | `infra/docker/docker-compose.xeon.yml`(cloudflared 서비스 블록의 전제) · 제온 호스트 nftables(우리 리포 소관 밖) | 컨테이너에서 `node -e "fetch('https://registry.npmjs.org/...')"` → 현재 `fetch failed` / `sudo nft list ruleset`의 `chain forward`에 docker 브리지 허용 규칙 0건 | **제온을 상시 운영 백엔드로 승격하기 전**(현재는 우회로 무영향) | **미수신(2026-08-09 등재)** — 우회 채택으로 **실행은 비차단**: ⓐ 빌드는 `docker build --network=host` ⓑ 런타임은 스택이 내부 통신만 써서 무영향 ⓒ **cloudflared는 컨테이너가 아니라 호스트 데몬으로 설치**(호스트 네트워크라 forward 불요). 근거·되돌리는 조건은 EXEC-DECISIONS #22 ⑦ | 방화벽 정비(DCP 공유 호스트라 **사용자 승인 사항**) 시 compose의 cloudflared 서비스로 복귀. 미해소 시 W2 종료 보고에서 재상신 |
| 79 | **T-W1-08 게이트② qa-verifier**(실측, 소유권 밖 발견 3건 병합) — `POST /v1/telemetry/events`가 **`@Public`이라 인증 없이 호출 가능한데 요청 빈도 제한이 없다**(리포 전체에 `@nestjs/throttler` 미설치, 유사 사례는 소켓 채팅 전용 `live/chat-rate-limiter.ts`뿐이며 telemetry는 미재사용). ② 개별 이벤트 **페이로드 크기 상한 부재**(`payload: z.record(z.string(), z.unknown())`, 배치 "건수" 상한 100만 존재). ③ **인메모리 롤업이 무한 증가**한다 — 특히 `latestCaptionStateBySession`은 sessionId 없는 익명 토글마다 `__anon_${counter++}`로 **매번 새 키를 발급**해 요청 1건당 1엔트리씩 영구 누적된다(①과 결합 시 메모리 고갈 벡터). 서비스 주석은 "재시작 시 롤업 소실 허용"만 명시하고 **재시작 없는 장기 가동의 증가 경계는 문서화돼 있지 않다** | `services/api/src/telemetry/telemetry.service.ts`(롤업 컬렉션 3종·익명 카운터) · 레이트리밋 도입 시 `services/api` 전역 | `grep -rln "Throttler\|rate-limit\|RateLimit" services/api/src/` → 현재 0건 / `telemetry.service.ts`의 `__anon_` 카운터 실재 | **외부 공개(Cloudflare Tunnel 연결) 전 필수** — SSH 터널·LAN 한정 구간에서는 노출 표면이 없어 비차단 | **수신 완료(2026-08-15, Quick Tunnel 개통 직전)** — 게이트②는 **PASS**다(AC 전항목 충족). 본 행은 AC 밖 아키텍처 리스크의 사실 기술이며, EXEC-DECISIONS #22 ⑤가 명시한 "한계"의 구체화다 | 조치 4종 완료(EXEC-DECISIONS #24 ⑥): ① 익명 키 발급 **원천 제거**(세션 없는 토글은 Map 미투입, 스칼라 카운터로만 집계 — 요청을 아무리 보내도 Map 크기 불변) ② 롤업 5개 컬렉션에 **5,000키 상한 + `capacityDrops` 응답 노출**(관측 불완전을 숨기지 않음, 과소추정만 가능) ③ payload **4KB 상한**(UTF-8 바이트 기준) ④ **IP 토큰버킷 레이트리밋 429**(리미터 자신도 IP 1만 상한 + 유휴 청소). 부수로 **KPI 정확도 상승** — 익명 토글이 `__anon_N` 가짜 세션으로 섞여 큰 자막 활성 비율을 왜곡하던 것이 사라졌다. **신뢰 IP는 `CF-Connecting-IP` 우선**(엣지가 재작성해 위조 불가) → `X-Forwarded-For` → 소켓 순 폴백이며, **CF 뒤가 아닌 경로에서는 위조 우회가 가능하다는 한계를 코드 주석에 존치**한다. 게이트①: api 585→616 그린(회귀 0) |
| 80 | **T-W2-04 게이트② qa-verifier**(독자 재현으로 결함 확정 → **같은 웨이브에서 해소**) — 관제 앱 웹 부트스트랩에서 **refresh 5xx(일시 서버 장애)가 항상 `signedOut`으로 오판정**됐다. 원인은 `doRefresh()`가 boolean만 반환하고 실패 원인(401 vs 5xx)을 `tokenStore.getRefreshToken()`으로 되짚는 구조인데, **웹에서는 그 값이 성공·401·5xx 무관하게 항상 null**이라 재판정이 무너진 것. 코드 자신의 주석("5xx 등: 세션 판정 보류 — 토큰 보존")이 선언한 의도와 실제 동작이 어긋났다. 영향: 쿠키 세션이 유효한데도 API 일시 장애 시 사용자가 로그인 화면으로 강제 이동 | `apps/control-center/src/api/client.ts` · `apps/control-center/src/auth/auth-context.tsx` | 검증자 독자 재현(실제 `client.ts`를 import해 5xx 응답 주입) → 수정 전 `bootstrap 최종 판정=signedOut`, 수정 후 `error` | **해소 완료(2026-08-09, 같은 웨이브)** | **수신 완료(2026-08-09)** — 게이트② **FAIL** 판정 → E3 §E 2항 재기동 1회 → `RefreshOutcome` 3치 승격으로 해소(테스트 166→169) | **교차검증이 잡아낸 건**: 같은 계약을 배선한 T-W2-19(기자)는 처음부터 3치 반환이라 이 함정을 피했고, 두 구현을 나란히 놓고 본 게이트②만이 이 비대칭을 발견했다. **동형 태스크가 다른 웨이브·다른 에이전트로 갈릴 때 교차검증을 붙이는 근거 사례**로 남긴다 |
| 81 | **조율자**(Wave 5 머지 후 제온 실배포에서 발견) — **`infra/docker/Dockerfile.web`이 `packages/ui`를 복사하지 않아 web 이미지 빌드가 깨진다.** T-W1-01(Wave 3)이 패키지를 신설하고 3앱이 `workspace:*`로 의존 선언을 했지만, **실제 import가 0이던 구간에는 빌드가 통과**했다 — T-W1-02(Wave 5)가 subscriber에서 처음 소비를 시작하자 `--frozen-lockfile` 설치 단계부터 실패한다. **4단 게이트 전 구간(①②③④)과 CI를 모두 통과한 뒤 실배포에서만 드러났다**: 로컬·CI는 워크스페이스 전체가 있으므로 재현되지 않는다 | `infra/docker/Dockerfile.web` — 매니페스트 복사(`packages/ui/package.json`)와 소스 복사(`packages/ui/`) 2줄 | 수정 전 제온 빌드 실패 재현 / 수정 후 `docker build -f infra/docker/Dockerfile.web` 성공 | **즉시**(실배포 차단 결함) | **수신 완료(2026-08-09, hotfix)** | `hotfix/dockerfile-web-packages-ui`로 해소. 소비 0 상태의 의존 선언은 **빌드 컨텍스트 누락을 숨긴다** — 공용 패키지 신설 태스크는 그 패키지를 **실제로 import하는 첫 태스크**에서 컨테이너 빌드 경로를 함께 확인해야 한다 |
| 82 | **조율자**(#81의 근본 원인 분리) — **CI가 web 이미지를 빌드하지 않는다.** `build-images.yml`의 matrix는 api·media-worker·ai-worker 3종뿐이라(재현: `grep -c "Dockerfile.web" .github/workflows/build-images.yml` → **0**), #81 계열(웹 빌드 컨텍스트 결함)을 **구조적으로 검출할 수 없다**. #81은 사람이 실배포를 시도했기 때문에 발견됐을 뿐, 게이트가 잡은 것이 아니다 | `.github/workflows/build-images.yml`(matrix에 web 추가) 또는 `T-W1-11c`(`deploy-web.yml`, Wave 8c — 웹 배포 워크플로 소유) | `grep -c "Dockerfile.web" .github/workflows/*.yml` → 해소 후 ≥1(2026-08-09 실측 **0**) | **T-W1-11c 착수 시**(Wave 8c — 그 태스크가 웹 export 빌드를 CI에 처음 들이는 지점이라 자연 귀속) | **수신 완료(2026-08-16 — T-W1-11c, Wave 8c)** — `deploy-web.yml`이 `Dockerfile.web`을 빌드한다(`grep -c "Dockerfile.web" .github/workflows/deploy-web.yml` → **10**, 등재 시 0). **`build-images.yml`은 무변경** — 정적 산출물은 `Dockerfile.web`이 만드는 nginx 이미지 안에 `COPY`되므로 "정적 배포"와 "web 이미지 빌드"가 대립하지 않는다는 것을 구현자가 확인했다(그 Dockerfile 헤더 주석이 T-W0-03 시점에 이미 "이미지 자체가 배포 단위"라고 확정해 뒀다). `pull_request` 트리거가 **푸시 없이** 이 이미지를 빌드하므로 #81 계열 빌드 컨텍스트 결함이 실배포 전 PR에서 red가 된다 = 본 항이 지적한 "구조적 검출 불가"가 닫혔다 | T-W1-11c 위임에 본 행 인용. 그 태스크가 흡수하지 않으면 `ci.yml` 소유 태스크(T-W2-17·T-W4-02)로 재배치 |
| 83 | **조율자**(Wave 5 종료 앵커 재검산에서 발견) — **대장 총계 선언과 실측이 어긋난다.** 본 §6 서두는 마스터플랜 몫을 **67행**이라 선언하는데, 실측은 **73행**이다(숫자 번호 57 + `D-n` 번호 16). 원인 후보: ① 마스터플랜 대장이 **6-1~6-10 열 개 하위 표로 쪼개져 각 표가 자체 번호(1부터)를 쓰고** `D-n` 계열이 별도 번호 체계라 단순 카운트로 검산되지 않았다 ② 과거 라운드가 67을 확정할 때 쓴 "대장 행" 정의가 현재 실측 범위와 다르다. **6-11 몫은 정확하다**(번호 1~83 연속, 끊김 0으로 검증) | PIVOT-PLAN §6 서두 총계 선언 — **정의 확정이 선행**이며 값 갱신은 그 뒤다 | 마스터플랜 몫 `awk 'NR>=76 && NR<380' docs/plan/PIVOT-PLAN.md \| grep -cE "^\\\| (D-)?[0-9]+ \\\|"` → **73**(선언 67) / 6-11 몫 `awk 'NR>=380' … \| grep -cE "^\\\| [0-9]+ \\\|"` → 실측치 | **다음 웨이브 종료 전** | **미수신(2026-08-09 등재)** — 선재(先在) 불일치이며 Wave 5가 만든 것이 아니다. 앵커가 6-11만 재현하고 마스터플랜 몫은 재현 대상이 아니었던 **사각**이다 | 다음 웨이브 조율자가 "대장 행"의 정의(하위 표 자체 번호·`D-n` 계열 계상 여부)를 확정한 뒤 총계를 갱신하고, 그 재현 명령을 앵커에 편입한다. **정의 확정 전에는 값을 임의로 바꾸지 않는다** |
| 90 | **조율자**(영상 파이프라인 실증 중 실측 발견, 2026-08-15) — **제온에서 미디어 큐가 전역 정지해 있었다.** api의 `DcpArbiterService`가 `DCP_ARBITER_URL=http://host.docker.internal:8080`으로 DCP 상태를 조회하는데, DCP arbiter는 **컨테이너가 아니라 호스트 위 uvicorn 프로세스**로 `127.0.0.1:8080`에만 바인딩돼 있어 컨테이너에서 도달 불가다. 조회가 전부 타임아웃 → fail-mode 기본값 `hold` 발동 → BullMQ 미디어 큐 `pause()` 유지. 업로드는 성공하지만 트랜스코딩 잡이 영원히 소비되지 않는다(**arbiter는 설계대로 안전하게 동작한 것**이고 틀린 것은 도달 경로 가정이다). 제온에서 미디어 잡을 한 번도 돌린 적이 없어 실배포 검증 4단을 전부 통과한 채 잠복했다 | `services/api/src/arbiter/*`(코드 무변경) + 제온 호스트 구성 | 컨테이너에서 `fetch($DCP_ARBITER_URL/api/arbiter/state)` 성공 여부 / `ss -ltn \| grep 8080`이 `172.17.0.1` 포함 | **해소 완료** | **수신 완료(2026-08-15)** — 호스트 systemd 릴레이(`gachinol-dcp-relay.socket`, `172.17.0.1:8080 → 127.0.0.1:8080`) + nftables input 규칙 1줄(`ip saddr 172.19.0.0/16 ip daddr 172.17.0.1 tcp dport 8080 accept`, 출발지·목적지·포트 3중 제한이라 LAN·인터넷 노출 아님). **#78의 "방화벽 무변경" 원칙에서 처음 벗어난 건이며 사용자 승인을 받았다**(릴레이만으로는 input drop을 못 넘는다는 사실을 확인한 뒤 재질의) | — |
| 91 | **조율자**(영상 파이프라인 실증 중 실측 발견, 2026-08-15) — **서브도메인 vhost 3종(`watch.`/`reporter.`/`center.`)에 API 프록시가 없어 앱 3종이 전부 데이터를 받지 못했다.** 웹 번들은 `EXPO_PUBLIC_API_URL=/`(상대 경로)로 빌드되므로 앱은 자기가 서빙된 호스트로 `/v1/...`을 호출하는데, 정적만 서빙하는 vhost에서 그 요청이 SPA 폴백에 걸려 `index.html`(text/html)로 돌아왔다. 증상은 기자·관제 "서버에 연결할 수 없습니다", 구독자 스켈레톤 무한. `api.${DOMAIN}` vhost가 따로 있는 것은 무관하다 — **상대 경로 번들이라 그 호스트를 아무도 호출하지 않는다**. **실배포 검증이 전부 터널 URL(단일 호스트 경로 분리라 `/v1/` 프록시 보유)로만 이뤄져** 서브도메인 경로가 한 번도 검증되지 않았다 | `infra/docker/nginx.conf`(T-W0-03·T-W1-06 준-공용 자산) | `curl -H "Host: reporter.${DOMAIN}" .../v1/feed`의 `content_type`이 `application/json` | **해소 완료** | **수신 완료(2026-08-15, 커밋 `1610b02`)** — vhost 3종에 터널 vhost와 동형 `/v1/`·`/socket.io/` 추가(`/c/`는 `go.` vhost 소관이라 제외). 4 vhost 전건 JSON 반환·정적 서빙 회귀 0 확인 | — |
| 92 | **조율자**(영상 파이프라인 실증 중 실측 발견, 2026-08-15) — **react-native-web의 `Alert`가 빈 함수라 웹에서 확인 다이얼로그와 토스트가 통째로 무력화됐다**(`class Alert { static alert() {} }`, react-native-web@0.21 `dist/exports/Alert/index.js`). 예외를 던지지 않아 ① 확인 콜백이 영원히 실행되지 않고(요청 자체가 안 나가 DB·api 로그에 흔적 0) ② `Alert.alert(message)` 폴백을 쓰던 토스트가 전부 사라져 **성공·실패 어느 피드백도 표시되지 않는다**. 기자 승인이 막히면 센터 승인·송출·구독자 재생까지 전 경로가 함께 막힌다. **웹 피벗 계획의 사각지대**다 — 02·E2 어디에도 `Alert`의 웹 미동작을 다루는 태스크가 없다(재현: `grep -rn "Alert" docs/plan/02-web-architecture.md docs/plan/exec/E2-work-breakdown.md` → 0) | `apps/{reporter,control-center}/src/ui/*` + 화면 8곳 | `grep -rn "Alert.alert" apps/*/app` → 0(네이티브 위임용 `feedback.tsx` 내부만 허용) | **해소 완료** | **수신 완료(2026-08-15, 커밋 `c83a604`)** — 앱별 `src/ui/feedback.tsx` 신설(`showToast` 시그니처 유지로 호출부 무변경 + `confirmDialog(): Promise<boolean>` + `FeedbackHost` 웹 전용 렌더), 호출부 8곳 교체, 테스트 +8×2앱(회귀 0). **웹 미마운트 시 `false`(취소)로 결정**해 확인 없는 파괴적 동작을 구조적으로 막았다 | — |
| 93 | **조율자**(영상 파이프라인 실증 중 실측 발견, 2026-08-15) — **react-native-web의 `ScrollView`가 `flexGrow:1·flexShrink:1`을 기본으로 갖는데 필터 칩 줄들이 `style` prop 없이 `contentContainerStyle`만 지정해, ScrollView 자신의 세로 크기가 부모 flex에 휘둘렸다.** 증상은 정반대 두 갈래였다: 구독자 웹은 넓은 화면에서 카드 썸네일이 공간을 밀어내 칩이 **눌려 잘렸고**, 관제 웹은 형제가 공간을 다 쓰지 않아 칩이 **세로 600px까지 늘어났다**. 동반 발견으로 3앱 모두 콘텐츠가 화면 전폭으로 늘어나 초광폭에서 읽기 나빴다 | `apps/{subscriber,control-center}/app/*`(칩 6곳) + 3앱 `src/ui/screen.tsx` | `grep -c "chipScroll" apps/subscriber/app/\(tabs\)/index.tsx apps/control-center/app/\(app\)/\(tabs\)/*.tsx` → 6 / 브라우저 실측 `flexGrow=0·flexShrink=0`·칩 높이=scrollHeight | **해소 완료** | **수신 완료(2026-08-15, 커밋 `cb7a9f2`·`1d6d4fd`)** — `chipScroll({flexGrow:0,flexShrink:0})` 6곳 전건(`flexBasis`는 RNW 기본 auto라 콘텐츠 높이만큼) + Screen 폭 상한(구독자 720=720p 렌디션 원폭 · 기자 960 · 관제 1440=W2 DoD 확인 폭). 3앱 전 화면이 Screen을 경유함을 확인(누락 0)해 앱당 1파일로 일괄 적용 | — |
| 94 | **조율자**(영상 파이프라인 실증 중 실측 발견, 2026-08-15) — **관제 웹에 송출 지시가 미배선이라 센터 승인 후 `center_approved`에서 멈췄다.** 승인과 송출은 설계상 분리돼 있고(`reporter_only`는 기자 승인이 `publishing`으로 자동 연쇄하지만 `reporter_then_center`는 사람이 송출을 지시한다 — shared `afterReporterApproval`), api는 엔드포인트 4종을 이미 갖고 있는데 관제 앱에 호출부가 없었다. 관제 코드 주석 자체가 "송출은 Distribute 몫"이라 명시하고 있었으므로 **의도된 미완이 검증 없이 방치된 상태**였다 | `apps/control-center/src/api/contents.ts`·`features/contents/*`·`app/(app)/contents/[id].tsx` | `grep -c "distributeContent" apps/control-center/src/api/contents.ts` → 1 / 배포 번들 `grep -c canDistribute` → 2 | **해소 완료** | **수신 완료(2026-08-15, 커밋 `d3e3545`, shared·api 무변경)** — api 클라이언트 4종 + mutations 3종 + `usePublications`(진행 중이면 5s 폴링) + `centerActionsFor.canDistribute` + 액션바 [송출] + 채널별 결과 섹션(failed→재시도·published→회수, 노출 조건은 shared `PUBLICATION_STATUS_TRANSITIONS`와 동일·사본 0). 테스트 +2(전 상태 순회·세 액션 상호배타) | — |
| 95 | **조율자**(영상 파이프라인 실증 검산에서 발견, 2026-08-15) — **업로드한 영상의 길이가 기자·관제 목록과 구독자 피드에서 전부 `0:00`으로 표시됐다.** `media_assets.duration_sec`에는 실측값(63·117초)이 있는데 `contents.duration_sec`이 NULL이다 — shared 계약이 이 필드를 "편집 완료 후 확정"으로 정의하는데 **자동편집(`auto_edit`)이 미구현이라 아무도 채우지 않았다**. 읽기 계층도 어긋나 있었다: 재생정보는 렌디션 폴백을 갖는 반면(`feed.service` `row.durationSec ?? rendition.durationSec ?? 0`) 목록은 폴백이 없다(`feed.mapper` `row.durationSec ?? 0`) | `services/api/src/pipeline/pipeline.service.ts` | `grep -c syncContentDuration services/api/src/pipeline/pipeline.service.ts` → 2 / 실배포 4번째 업로드가 업로드 직후부터 63초 기록 | **해소 완료** | **수신 완료(2026-08-15, 커밋 `9f606da`, 스키마·shared 무변경)** — 폴백을 화면마다 복사하는 대신 **기록 시점에 확정**(실측 원천이 `media_assets` 하나뿐이므로). transcode 완료 자산 upsert 직후 기존 `findDurationSec` 재사용. 길이 부재 시 미기록(기존 확정값 보존)·실패 시 throw 금지(`processing` 고착 방지). api 664→667 | — |


| 84 | **조율자**(Wave 6 착수 전 정본 대조에서 발견) — **07 §3-15의 착수 게이트가 실행계획에 결박돼 있지 않다.** 07 §3-15(무인증 주민 업로드의 법적 통제)는 "02 §E 13번 구현 **착수 전 게이트**"를 명시하고 02 §D-T9도 이를 승계하는데, exec 문서 전체에 이 게이트가 0건이다(E2 T-W2-08 행의 원천 인용은 02§E-13·02§D-T9·03§C-5뿐, 07 미인용. T-NC 20건에도 소유자 없음) | `docs/plan/exec/E2-work-breakdown.md` T-W2-08·T-W2-09 행(원천 열에 07 §3-15 추가) + T-NC 신설 여부(외부 법률자문 = 사용자·법무 운영 지원 몫, G9 ③ 지출 게이트 대상) | **확인 명령 정정(2026-08-15 조율자 자기 적발)**: `grep -c "3-15" docs/plan/exec/E2-work-breakdown.md` → 등재 시점 **0건**, 해소 후 ≥1. **舊 명령 `grep -rn "3-15" docs/plan/exec/`는 부실했다** — 조율자가 EXEC-DECISIONS #25에 이 게이트를 *서술*한 것만으로 히트가 발생해(실측 11건, 전부 `EXEC-DECISIONS.md`) **수신처에 아무것도 반영되지 않았는데 형식 충족**된다. 대장 확인 명령은 **수신처 파일을 특정**해야 한다는 규율의 실증 사례다(Q1 원칙 "확인=실행한 grep 결과만"이 명령의 *조준*까지 요구한다는 점이 舊 명령의 사각이었다) | **T-W2-09(Wave 9) 착수 전** — 업로드 링크 페이지 문구가 그 태스크 산출물이라 자연 귀속 | **미수신(2026-08-15 등재 · 2026-08-15 확인 명령 정정 후 재판정 = 여전히 미수신)** | **EXEC-DECISIONS #25 ②로 차단 범위 축소 해석**(차단 지점 = 실배포·외부 공개, 서버 구현 아님 — 선례 #2·#8 동형) 후 T-W2-08 진행. 게이트 자체는 유효하며 T-W2-09·실배포에서 재판정한다. 07 §3-15 데이터 모델 2항(연락처·이용허락 동의)은 #25 ③으로 이번 마이그레이션에 선반영(SOLO 웨이브 추가 방지) |
| 85 | **조율자**(Wave 6 착수 전 기준선 실측에서 발견) — **api 유닛 테스트에 플레이키 1건.** `TelemetryController (HTTP) — IP 레이트리밋 429 실측` 스위트가 **전체 스위트 병렬 부하에서만** 간헐 실패한다(`Parse Error: Expected HTTP/, RTSP/ or ICE/` — supertest 임시 서버가 연타 요청 중 응답을 파싱하지 못함). 단독 실행 3/3 그린, 전체 실행 3회 중 1회 레드. **게이트③의 before→after 회귀 판정을 오염**시킨다(무관한 태스크가 회귀로 오판정될 수 있음) **범위 정정(2026-08-15, T-W2-13 구현자 관측 + 조율자 모집단 특정)**: 이것은 telemetry 스위트 고유의 결함이 **아니다**. api 유닛에서 **supertest로 실 HTTP 서버를 띄우는 스펙은 정확히 2건**(`go-link.controller.spec.ts`·`telemetry.controller.spec.ts` — 재현: `grep -rln "supertest\|request(app" services/api/src --include='*.spec.ts'`)이고, **그 2건 전부에서 같은 클래스가 관측됐다**(telemetry는 Wave 6 조율자 실측, go-link는 Wave 7 구현자 반복 실행 8회 중 1회). 즉 원인은 특정 테스트 케이스가 아니라 **인프로세스 HTTP 서버 + jest 병렬 워커 부하**라는 구조이며, 모집단은 "supertest 사용 스펙 전수"다(향후 신설되는 supertest 스펙도 자동 편입 — 닫힌 목록 아님) | 단독 `jest telemetry` → 3/3 그린 · 단독 `jest go-link` → **4/4 그린 61건**(2026-08-15 조율자 실측) / 전체 `pnpm --filter @gachinol/api test` → 부하 시 간헐 `1 failed`(Wave 6 3회 중 1회 telemetry · Wave 7 8회 중 각 1회 telemetry·go-link). **재현율 주의**: 같은 Wave 7의 게이트② qa-verifier는 전체 실행 **4회에서 2건 모두 미재현**했다 — 발생률이 낮아 **미재현은 반증이 아니며 소수 회차 그린을 "해소"로 읽으면 안 된다**. 모집단 논거(supertest 스펙 전수)는 관측 빈도가 아니라 **구조**에서 나온 것이라 재현율과 독립적으로 유효하다 | **supertest 스펙을 소유하는 다음 태스크**(T-W2-12/Wave 10이 telemetry 확장) 또는 **한 웨이브에서 재발 2회 시 즉시** | **미수신(2026-08-15 등재 · 2026-08-15 범위 정정)** | 게이트③에서 **supertest 스펙 2건 중 하나가 레드면 단독 재실행으로 판별**한 뒤 플레이키로 기록한다(태스크 회귀로 계상하지 않는다). 위임 프롬프트에 기지 플레이키임을 명시하되 **모집단을 "supertest 스펙 전수"로 적는다**(舊 "telemetry 1건" 표기는 go-link 실패를 신규 회귀로 오판정하게 만들 뻔했다 — Wave 7 구현자가 "내 변경 탓이 아니다"를 스스로 판별해 보고한 덕에 걸러졌고, 그 보고가 없었다면 조율자가 회귀로 계상했을 경로였다). 근본 해소(직렬 실행 강제 또는 요청 직렬화)는 소유 태스크 착수 시 |
| 86 | **T-W2-08 구현자 신고 + 게이트② qa-verifier 독자 재현 + 조율자 재현**(2026-08-15) — **지사 담당자 검수 승인/반려 경로가 통째로 없다.** `resident_uploads.status`를 `approved`/`rejected`로 **기록하는 코드가 0건**이고 `reviewed_by_user_id`·`reviewed_at`을 채우는 코드도 0건이라, 주민 업로드물은 `awaiting_branch_review`에 **영구 정지**한다. 03 §C-5 "지사 담당자 승인 **필수**"의 승인 행위 자체를 소유한 태스크가 계획에 없다(T-W2-08은 4엔드포인트 = 발급·조회·presign·완료통지만, T-W2-09는 주민측 무인증 화면만). **부수**: T-W2-08이 만든 가드 `assertPipelineEntryAllowed`의 **호출 지점도 0건**이라, 현재 강제는 "모듈이 QueueModule을 import하지 않는다"는 구조적 사실에만 의존한다 — 하류가 가드를 배선하지 않고 파이프라인에 연결하면 게이트가 조용히 무력화된다 | **신규 태스크 신설 필요**(BE: 검수 대기열 조회 + 승인/반려 엔드포인트 + `assertPipelineEntryAllowed` 배선 + 승인 시 미디어 큐 인큐 / FE: 지사 담당자 검수 화면 — 소유 앱 미정, `apps/reporter`(지사 소속=reporter role) 유력) | 조율자 실측 2026-08-15: `grep -rn "reviewedByUserId\|reviewed_by_user_id" services/api/src --include='*.ts' \| grep -v '\.spec\.' \| wc -l` → **0**. 해소 후 ≥1 | **Wave 9 편성 시**(T-W2-09가 주민측 화면을 세우는 시점 — 그때까지 승인 경로가 없으면 W2 DoD "주민 링크 업로드 1건 완주"가 구조적으로 불가) | **수신 완료(2026-08-16 — BE=T-W2-24(Wave 7e) + FE=T-W2-25b(Wave 7h))** — 조율자 직접 재현: `grep -rn "reviewedByUserId\|reviewed_by_user_id" services/api/src --include='*.ts' \| grep -v '\.spec\.' \| wc -l` → **≥1**(등재 시 0), 기자 앱 `app/(app)/resident-uploads/{index,[id]}.tsx` 신설. 게이트② 양쪽 PASS | BE·FE 두 몫으로 나눠 채번했다(#96의 ⓑ 판정과 동형). FE는 계약 위치 문제로 **T-W2-25a(SOLO, `ResidentUploadStatus` shared 승격)** 를 선행 분리했다 — T-W2-08이 "소비자는 후속 FE 태스크 하나뿐"이라며 모듈 내부에 둔 계약이, 바로 그 소비자가 등장하며 전제가 깨진 건이다. **잔여는 #120~#122로 분리 등재** |
| 87 | **T-W2-08 구현자 신고 + 게이트② 독자 재현 + 조율자 재현**(2026-08-15) — **`policyGuard`가 `resident_link`를 몰라 승인 후에도 교착한다.** `preview_generating`의 두 출구가 각각 `reporter_upload` 전용(`→awaiting_reporter_review`)·`live_vod` 전용(`→awaiting_center_review`)이라, 검수 승인·인큐가 붙더라도 주민 콘텐츠는 `preview_generating`에서 **어느 쪽으로도 나갈 수 없다**. `ContentOrigin`이 2값에서 3값이 되며 생긴 **분기 사각**이다(전이 맵 자체는 무변경이라 shared 계약으로는 검출되지 않는다) | `services/api/src/contents/content-workflow.service.ts` `policyGuard`(342~359행) — **귀속 후보 ①** T-W2-13(Wave 7, `contents/*` 전이 가드를 소유한 유일 SOLO 태스크, 동일 파일) **②** #86의 신설 태스크와 묶음 | 조율자 실측 2026-08-15: `sed -n '342,359p' services/api/src/contents/content-workflow.service.ts` → 두 분기 모두 `resident_link` 미포함. 해소 후 `grep -c "resident_link" services/api/src/contents/content-workflow.service.ts` ≥1 | **#86과 동시**(승인 경로 없이는 이 교착이 관측되지 않으므로 단독 선행은 무의미) | **미수신(2026-08-15 등재)** | Wave 7 착수 시 T-W2-13 위임에 본 행을 인용해 **귀속 여부를 먼저 판정**한다(동일 파일이라 편입이 자연스러우나 T-W2-13의 산출물 정의는 미성년자 가드다 — 임의 확장이 아니라 조율자 판정으로 편입하거나 #86 묶음으로 미룬다) |
| 88 | **게이트② qa-verifier 관찰**(2026-08-15) — **E2의 api 모듈 파일 산정 관례가 실제와 어긋난다.** E2 T-W2-08은 `resident-links/*`를 "컨트롤러 1 + 서비스 1 = 2파일"(+ 스펙 1)로 산정해 총 6파일·버킷 M으로 편성했으나 실제 산출은 **12파일**(소스 7 + 스펙 5)이다. 인용된 자매 모듈도 동일 — `go-link`+`telemetry` 실측 **12파일**. 즉 개별 태스크의 오류가 아니라 **산정 관례 자체**가 모듈 파일(module.ts·constants·schemas·순수 유틸)을 계상하지 않는 구조다 | `docs/plan/exec/E2-work-breakdown.md`(api 신규 모듈 태스크의 파일 산정 관례) + `docs/plan/exec/E4-token-budget.md` §A-6(버킷→예산 매핑의 입력) | 조율자 실측 2026-08-15: `find services/api/src/resident-links -type f \| wc -l` → **19**(2026-08-23 현행. 등재 당시 12·스펙 5) / `find services/api/src/go-link services/api/src/telemetry -type f \| wc -l` → **12**. ⚠️ **2026-08-23 재실측: resident-links가 19파일로 늘었다**(12→19, T-W2-24·30·31·#120 단건조회 누적). **결함이 해소된 게 아니라 심화**했다 — 이 항은 "E2 산정이 실제와 어긋난다"는 계획 정합성 이슈이므로 **괴리가 커진 것**이다. 두 명령은 교체가 아니라 **병렬 지표**다(각각 다른 모듈군을 잰다) | **다음 api 신규 모듈 태스크 편성 전**(#86 신설 태스크가 그 지점) | **미수신(2026-08-15 등재)** | **버킷은 결과적으로 타당했다**(M 예산 52만 대비 실측 구현 약 150k·qa 약 264k) — 즉 산정 관례는 "파일 수"가 아니라 "모듈 1개"를 세는 것이었고 표기만 파일 수 형식이었다. 정의를 그렇게 명시하거나 계상 방식을 실제에 맞추거나 **택일**하되, 값 박제 없이(D7-1) 재현형으로 적는다 |
| 89 | **게이트② qa-verifier 관찰**(2026-08-15) — **429 응답의 에러 `code`가 `"internal"`로 나간다.** `common/filters`의 `CODE_BY_STATUS`에 429 매핑이 없어 레이트리밋 차단이 `{"code":"internal", …}`로 응답된다(HTTP 상태 코드는 429로 정확). **선재 부채**이며 T-W2-08이 만든 회귀가 아니다 — 선례인 `TelemetryController`(대장 #79 조치④)도 동일 경로다. 클라이언트가 `code`로 분기하면 레이트리밋과 서버 오류를 구분하지 못한다 | `services/api/src/common/filters/*`(`CODE_BY_STATUS`에 429 매핑 추가) — 준-공용 자산, 소유 태스크 미정 | 해소 후 `grep -rn "429" services/api/src/common/filters/` ≥1 | **레이트리밋 응답을 클라이언트가 분기 처리하는 첫 태스크 착수 전**(현재 소비자 0이라 비차단) | **미수신(2026-08-15 등재)** | 1줄 수정이라 다음 api 태스크에 부수로 붙일 수 있다. 단 `common/filters`는 여러 모듈이 공유하므로 편집 태스크의 회귀 범위는 api 전체 |
| 96 | **T-W2-13 게이트② qa-verifier 관찰 + 조율자 재현**(2026-08-15) — **미성년자 플래그·동의 확인에 쓰기 경로가 0건이라 신설 게이트가 발화 불가능하다.** `hasMinorSubject`·`minorConsentConfirmed*` 참조는 가드·스펙·픽스처·스키마뿐이고 **생성/수정 DTO·엔드포인트·mapper 노출·앱 참조가 전부 0**이다. 결과: ⓐ 프로덕션에서 플래그를 켤 수단이 없어 게이트가 **무해하나 무효** ⓑ 반대로 DB로 직접 켜면 **해제 수단이 없어 승인이 영구 차단**된다(반려·취소만 가능). T-W2-14(Wave 12)는 `apps/reporter/.../classify.tsx` **프론트만** 소유하므로, shared DTO 확장·api 영속·"동의서 확인" 기록 엔드포인트를 소유한 태스크가 계획에 **없다** — 대장 #86("승인 행위의 소유자 부재")과 **동형의 사각**이며, 웹 피벗 계획이 백엔드 가드와 프론트 입력만 나누고 **그 사이 배관**을 아무에게도 주지 않은 3회차다 | `packages/shared/src/content/content.ts`(`CreateContentRequest` 등 DTO에 플래그 추가) + `services/api/src/contents/contents.service.ts`(생성·수정 시 영속) + 동의서 확인 기록 엔드포인트(센터 role, `minorConsentConfirmedBy/At` 기입) + 관제·기자 화면 노출 — **신규 태스크 신설 또는 T-W2-14 범위 확장 판정 필요** | 조율자 실측 2026-08-15: `grep -rn "hasMinorSubject" packages/shared/src services/api/src/contents/contents.service.ts \| wc -l` → **0**. 해소 후 ≥1 | **T-W2-14(Wave 12) 착수와 동시** — 그렇지 않으면 그 태스크가 만드는 체크박스가 **서버에 도달하지 못한다**(입력 UI만 있고 전송 계약이 없는 상태) | **수신 완료(2026-08-16 — T-W2-23 [SOLO], Wave 7f)** — 조율자 직접 재현: `grep -rn "hasMinorSubject" packages/shared/src services/api/src/contents/contents.service.ts \| wc -l` → **10**(등재 시 0). 게이트② AC 7항 전건 PASS·뮤테이션 6건 전건 검출, api 756→784/70 회귀 0. EXEC-DECISIONS #30 | 판정 결과 **ⓑ 확정**(ⓐ는 E1 §A 금지 칸 위반이라 불가) — T-W2-23으로 채번해 Wave 7f 단독 배치. **잔여 사각 5건은 #115~#119로 분리 등재**했고, 그중 **#118(센터의 "확인 대기" 발견 수단 부재 → `reporter_only` 교착)은 이 태스크가 만든 신규 사각**이라 T-W2-14 착수와 동시가 기한이다 |
| 97 | **T-W2-13 게이트② qa-verifier 관찰 + 조율자 재현**(2026-08-15) — **`resident_link`의 preview 출구가 가드에서만 열렸고 구동부가 없다.** T-W2-13이 `policyGuard`에 `preview_generating→awaiting_center_review` 출구를 신설했으나(대장 #87 해소), 그 전이를 **자동으로 실행하는** `pipeline.service.ts`의 `case 'preview'`는 origin과 무관하게 `awaiting_reporter_review`를 **하드코딩**한다 → resident_link 콘텐츠가 실제로 preview에 도달하면 가드 ①에서 throw. **#87은 "가드 차원에서만" 해소된 상태**다 | `services/api/src/pipeline/pipeline.service.ts` `case 'preview'`(origin 분기 추가 — `live_vod`가 이미 같은 분기를 필요로 하는지도 함께 확인할 것) | 조율자 실측 2026-08-15: `grep -c "awaiting_center_review" services/api/src/pipeline/pipeline.service.ts` → **0**. 해소 후 ≥1. ⚠️ 이 명령은 **문자열 존재만** 재므로 주석에도 반응한다 — 구동부 실재는 `grep -c "previewReviewTarget" services/api/src/pipeline/pipeline.service.ts` → **2**(정의 1 + 호출 1)로 확인한다 | **대장 #86 신설 태스크와 묶음** — 주민 업로드가 승인·인큐를 얻어 파이프라인에 진입하는 시점에야 이 경로가 실제로 밟힌다(단독 선행은 관측 불가) | **수신 완료(2026-08-23 재검증 — 코드에서 이미 해소돼 있었다)** — `pipeline.service.ts`의 `previewReviewTarget(origin)`이 `reporter_upload`면 `awaiting_reporter_review`, 그 외(`live_vod`·`resident_link`)는 `awaiting_center_review`를 고른다(정의 1 + 호출 1 = 2). 주석이 "대장 #97"을 명시하고 있어 **해소 시점에 상태 갱신만 누락된 건**이다(규율 8 위반 3회차 — #98·#99에 이은 세 번째). 권위는 `policyGuard`이고 여기는 검증되는 파생이라 어긋나면 invalid_transition으로 잡힌다(fail-loud) | #86·#97을 한 태스크로 묶으면 "승인 → 인큐 → preview → 센터 검토"가 한 번에 닫힌다. **`live_vod`도 같은 하드코딩에 걸려 있는지 착수 시 먼저 실측**할 것(걸려 있다면 선재 결함이 하나 더 있는 것이고, 아니라면 다른 경로로 상태를 세팅한다는 뜻이라 그 경로를 resident_link도 쓸 수 있는지 판정한다) |
| 98 | **배관 공백 전수조사 B축 + 조율자 재현**(2026-08-15) — **★수정요청이 콘텐츠를 영구 정지시킨다. UI가 약속한 동작이 서버에 없다.** `requestRevision()`이 콘텐츠를 `revision_requested`로 보내지만 그다음 전진 경로(`regenerating`)를 밟는 코드가 **0건**이고 `auto_edit` 인큐도 **0건**이다. 탈출구는 관리자가 `POST /:id/transitions`를 curl로 때리는 것뿐인데 그 엔드포인트를 호출하는 앱 코드도 0건이다. 추천 도메인엔 있는 고착 복구 타이머(`RECOMMENDATION_STUCK_MS`)가 콘텐츠엔 없다. **잠복이 아니라 이미 노출된 함정**: 관제 앱에 `POST /contents/:id/request-revision` 클라이언트가 실재하고("승인·수정요청·반려를 결정하세요"), 기자 앱은 `revision_requested`에 **`needsMyAction: true`** 배지와 **"초안을 수정하면 반영 후 재생성됩니다"** 안내를 띄운다 — 사용자에게 명시적으로 약속한 동작이 구현돼 있지 않다. **영상 파이프라인 실증(#26)이 "한 바퀴 완주"라 한 것은 정확히 행복 경로였고 이 분기는 밟지 않았다** | `services/api/src/contents/content-workflow.service.ts`(`revision_requested→regenerating` 전이 트리거) + `services/api/src/queue/queue-producer.service.ts`(`enqueueAutoEdit`) + `services/media-worker/src/worker.ts`(`MediaJobType`이 타입 수준에서 `auto_edit` 배제, `switch`에 case 없음) | 조율자 실측 2026-08-15: `grep -rn "'regenerating'" services/api/src --include='*.ts' \| grep -v '\.spec\.' \| grep -v STATUS_TRANSITIONS` → 콘텐츠 도메인 히트는 `content-workflow.service.ts:443` **1건뿐이고 그것은 세대+1 부수효과이지 트리거가 아니다**(대조: 추천 도메인은 `recommendation-workflow.service.ts:66`에 `const to: RecommendationStatus = 'regenerating'` 실재). `grep -rn "auto_edit\|AutoEdit" services/api/src services/media-worker/src --include='*.ts' \| grep -v '\.spec\.'` → **주석 1건뿐** | **즉시 판단 대상**(실사용자 노출 상태) — 구현이 로드맵(CLAUDE.md §11 "다음 후보" 2번 `auto_edit`) 몫이라면 **최소한 UI 약속을 걷어내는 것**(수정요청 버튼 비활성 또는 "재생성은 준비 중" 명시)이 선행돼야 한다 | **해소 확인(2026-08-22 재검증)** — PR #54 `auto_edit` Phase 1이 닫았으나 **상태 칸이 갱신되지 않은 채였다**(규율 8 위반 — 해소하고도 대기 목록에 남아 있었다). 실측: `queue-producer.service.ts:61`에 `auto_edit` 인큐 구동 · `'regenerating'` 참조 14건(spec·전이맵 제외) | 두 갈래 택일: ⓐ `auto_edit` 워커까지 구현해 루프를 닫는다(로드맵 2번 착수 — 규모 큼) ⓑ **UI 약속을 사실에 맞춘다**(수정요청 시 "센터/관리자 수동 진행 필요" 명시 + 관제에 수동 전이 버튼 노출). ⓑ가 즉시 가능하고 ⓐ의 선행을 막지 않는다. **로드맵에 "다음 후보"로 적혀 있다는 사실이 "이미 노출된 버튼이 사용자를 막다른 길로 보낸다"를 면책하지 않는다** — 로드맵 문구는 엔지니어링 순서만 말하고 이 사용자 영향은 어디에도 경고돼 있지 않았다 |
| 99 | **배관 공백 전수조사 A축 + 조율자 재현**(2026-08-15) — **반려된 콘텐츠를 재작업할 경로가 제품에 없다.** `rejected`는 종결 상태(`CONTENT_STATUS_TRANSITIONS.rejected: []`)이고 코드 주석 **3곳**(`content-workflow.service.ts:134`·`contents.controller.ts:123`·shared `workflow.ts:44`)이 유일한 복구 경로로 "재작업은 새 콘텐츠 + `remakeOfContentId`"를 명시하는데, **그 값을 쓰는 코드가 0건**이고 `CreateContentDraftRequest`/`UpdateContentDraftRequest` 둘 다 필드 자체가 없다. 매퍼는 읽어서 응답에 싣지만 영원히 `undefined`다. #98과 합치면 **정상 승인 외의 모든 분기(수정요청·반려)가 막다른 길**이다 | `services/api/src/contents/schemas/content.schemas.ts`(생성 DTO에 `remakeOfContentId` 추가) + `services/api/src/contents/contents.service.ts`(createDraft 영속) + shared DTO(`packages/shared/src/content/dto.ts`) | 조율자 실측 2026-08-15: `grep -rn "remakeOfContentId" services/api/src packages/shared/src --include='*.ts' \| grep -v '\.spec\.'` → 매퍼 읽기 1 · 주석 3 · 픽스처 1 · 타입 선언 1 = **쓰기 0건** | **#98과 동시 판단** — 둘 다 "콘텐츠가 비-행복 경로에서 빠져나오지 못한다"는 한 문제의 두 면이다 | **해소 확인(2026-08-22 재검증)** — 상태 칸이 갱신되지 않아 대기 목록에 남아 있었다. 실측: `remakeOfContentId` 참조 18건(spec 제외)이며 `contents.service.ts`에 쓰기 존재 | shared DTO 1필드 + api 영속 1줄이면 닫힌다(#98보다 훨씬 가볍다). 반려 사유를 새 콘텐츠에 승계할지는 착수 시 판정 |
| 100 | **배관 공백 전수조사 C축 + 조율자 재현**(2026-08-15) — **지사 부활(dormant→operating) 버튼이 없다. 프로젝트가 스스로 MVP라 선언한 동작이다.** `POST /v1/stations/:id/transitions`는 가드·전이맵·RBAC(`admin`·`center_operator`)까지 완비돼 있고 컨트롤러 주석도 "지사 부활(dormant→operating) 등"이라 적혀 있는데 **3앱 어디에서도 호출하지 않는다**(관제는 `listStations`/`getStation` GET 2건뿐). `POST /stations`(생성)·`PATCH /stations/:id`(수정)도 앱 호출 0건이라 지사는 `seed.ts`가 Prisma로 직접 upsert한다. CLAUDE.md §11 **"MVP 우선 제안: 휴무 중인 애월·제주시 2개 지사 부활을 최소 실행안으로"**와 정면 충돌 | `apps/control-center`(지사 관리 화면 — 부활·생성·수정 액션) + 필요 시 `StationOverview` 집계 엔드포인트(관제 `queries.ts:9`가 "집계 엔드포인트는 부재 → 사용하지 않는다(집계 날조 금지)"로 이미 자체 문서화) | 조율자 실측 2026-08-15: `grep -rn "transitions" apps/control-center/src apps/reporter/src --include='*.ts' --include='*.tsx' \| wc -l` → **0** | **MVP 판정 시점**(제품이 "지사 부활"을 최소 실행안으로 삼는 한 이 경로 없이는 MVP가 성립하지 않는다) | **수신 완료(2026-08-16 — T-W2-30, Wave 8a3)** — 기존 `(tabs)/stations.tsx` 로스터에 액션을 붙였다(화면 신설 불요). `api/stations.ts` 쓰기 3종 + 부활(dormant→operating)·휴무 전환·운영 시작 + **admin 전용** 생성·수정. **RBAC를 엔드포인트 단위 술어 2개로 분리**(`canTransitionStation`=admin∨center_operator / `canManageStations`=admin만) — 앱의 기존 게이트 `isCenterConsoleUser`는 둘을 한 덩어리로 통과시켜 이 차이를 표현하지 못했다. 권한 없으면 **disabled가 아니라 미렌더**(뮤테이션으로 실증). 액션·목표는 shared `STATION_STATUS_TRANSITIONS` 파생이고 **합법 엣지 전수에 라벨이 있는지 순회 검사**해 엣지 추가 시 버튼이 조용히 사라지는 대신 빨간불이 된다 | 계획(E2 69태스크)에 지사 관리 화면을 소유한 태스크가 있는지 먼저 대조할 것 — 없으면 신규 태스크. 현재는 seed 우회로 운영 가능해 **차단성이 즉시 발현되지는 않으나**, 13번째 지사가 생기는 순간 또는 지사 상태를 바꿔야 하는 순간 막힌다 |
| 101 | **배관 공백 전수조사 A축**(2026-08-15) — **아나운서 프롬프터의 외부 SNS 댓글을 숨길 수단이 0건.** 구독자 채팅(`ChatMessage`)에는 `POST /live-sessions/:id/chat/:messageId/hide`가 있는데, **프롬프터에 실제로 뜨는 것은 `LiveComment`**(외부 YouTube·Meta 등 수집분)이고 이쪽은 `LiveCommentStatus.Hidden`(주석 "필터링(욕설 등)·수동 숨김") 값을 세팅하는 코드도 엔드포인트도 없다. 라이브 중 욕설·비방이 아나announcer 화면에 그대로 올라가면 막을 방법이 없다 | `services/api/src/live/live-comments.service.ts` + `live-sessions.controller.ts`(hide 엔드포인트) + 관제 프롬퍼터 패널 UI | 조사 실측 2026-08-15: `grep -rn "'hidden'" services/api/src/live --include='*.ts'` → `chat.service.ts` 2건(ChatMessage 전용)뿐, LiveComment 0건 | **첫 실 라이브 방송 전**(04 §H-1 "첫 방송 게이트"에 편입 판정 필요) | **미수신(2026-08-15 등재)** | 실 SNS 댓글 수집이 목 어댑터인 현재는 발현되지 않으나, 실 어댑터 전환과 동시에 차단성이 된다. 04 §H-1 첫 방송 체크리스트에 항목으로 넣을지 판정 |
| 102 | **배관 공백 전수조사 B·C축**(2026-08-15) — **WS 서버 이벤트 11종 중 6종이 emit 0건이고, 그중 2종은 폴링으로 우회돼 계약과 구현이 갈렸다.** 실제 emit은 `live.broadcaster.ts` 5종뿐(`live.status_changed`·`live.viewer_count`·`chat.new`·`chat.moderated`·`prompter.comments`). 나머지 `content.status_changed`·`content.progress`·`recommendation.updated`·`publication.updated`·`commerce.order_paid`·`commerce.stock_changed`는 0건이다. 코드가 스스로 시인한 곳 2건: `pipeline.service.ts:79` `onProgress`가 `// no-op 로그(WS 미푸시)`, `live.broadcaster.ts:15` "향후 PipelineModule의 content.status_changed 푸시도 이 export 지점을 주입해 재사용한다". 추천·송출은 실시간 대신 **폴링**(관제 추천 10s)으로 대체돼 있다 | `services/api/src/pipeline/pipeline.service.ts`(진행률·상태 push) + `services/api/src/live/live.broadcaster.ts`(주입 지점 재사용) — `content.*` 2종은 CLAUDE.md 로드맵 2번("실시간 WS 진행률 푸시")에 명시, `commerce.*` 2종은 커머스 도메인 자체 부재 | 조사 실측 2026-08-15: `grep -rn "\.emit(" services/api/src` → 전부 `live.broadcaster.ts` | `content.*`는 로드맵 2번 착수 시 · `recommendation.updated`/`publication.updated`는 **계약을 폐기하든 배선하든 택일**(선언만 남겨두면 다음 구현자가 "이미 있다"고 오인한다) | **미수신(2026-08-15 등재)** | 정상 대기(로드맵 명시)와 계약-구현 불일치를 구분해 처리한다. 폴링 우회 2종은 **shared 계약에서 빼거나 배선하거나** 둘 중 하나 — 현 상태는 "선언은 있고 아무도 안 쓰며 대체 수단이 따로 도는" 3중 상태다 |
| 103 | **배관 공백 전수조사 A·B·C축 — 저심각 잔여 묶음**(2026-08-15, 개별 추적용 목록) — ① `Job` 도메인 애그리거트 전체가 죽은 계약(`packages/shared/src/job/job.ts` — `JOB_STATUS_TRANSITIONS` 소비자가 정의 파일 자신뿐, 실제 4개 큐는 각자 별도 wire 타입 사용 = **대체된 설계의 잔존물**) ② `JobListQuery` DTO 소비 0(계획 문서 언급도 0 — `/jobs` 라우트 자체 부재) ③ `RevisionTarget`의 `{kind:'recommendation'}` 분기를 유일 소비자인 `content.mapper.ts:94`가 **명시적으로 throw**(추천 쪽은 raw Prisma로 우회 — "추천 수정이력 조회"를 자연스럽게 만들면 매 행 500) ④ `PublicationStatus='canceled'` 전이 실행 코드 0(발행 전 채널 취소 수단 없음, `retry`·`retract`만) ⑤ `ChannelAccount` CRUD 부재(`status`·`connectedAt`·`expiresAt`·`credentialRef` 쓰기 0, seed 정적 upsert만 — 그런데 `resolveTargets()`가 `status==='connected'`를 실제 송출 게이트로 소비 → **실 카카오/유튜브 전환 시 토큰 만료를 반영할 코드가 없다**) ⑥ 주민 업로드 PII(`uploaderContact`·`consentAgreedAt`)가 기록만 되고 읽는 코드 0(07 §3-15 준수로 수집한 개인정보를 아무도 못 본다 — **#86 검수 화면과 동반 해소**) ⑦ `Content.tags` 영구 빈 배열(생성 DTO에 필드 없음) ⑧ `isTerminalState` 유틸 호출 0 | 각 항목별 상이 — ⑤는 CLAUDE.md 로드맵 1번("채널 계정 CRUD")에 명시, ⑥은 대장 #86 묶음, 나머지는 소유 태스크 없음 | 각 항목의 재현 명령은 조사 보고서 원문(본 세션) 참조. 대표: `grep -rln "JOB_STATUS_TRANSITIONS" services apps packages`(node_modules·dist 제외) → **정의 파일 자신뿐** | **개별 상이** — ⑤는 실 SNS 어댑터 전환 전, ⑥은 #86과 동시, ③은 추천 수정이력 기능 착수 전, 나머지는 비차단 | **미수신(2026-08-15 등재)** | 8건 모두 즉시 조치 대상은 아니나 **개별 추적 대상**이다. 특히 ①②는 "죽은 계약이 남아 다음 구현자를 오도하는" 유형이라 **삭제가 곧 조치**일 수 있다(코드를 늘리지 않고 닫히는 드문 경우) |
| 104 | **T-W2-20 게이트② qa-verifier 관찰 + 조율자 재현**(2026-08-15) — **UUID v7의 근접 예측 가능성(리포 전역).** `uuid` 라이브러리의 v7은 같은 밀리초 내에서 **단조 카운터**를 쓴다 — 조율자 실측(연속 3회 생성)에서 `01a004d4-12a4-7369-b5d7-4`까지 **84비트가 공통**이고 잔여도 단조 증가했다(게이트② 측정 80비트보다 더 큰 공통 접두). 리포는 모든 도메인 ID를 UUID v7로 발급하므로(`common/ids.ts` `newId`), **유효 ID 하나를 아는 공격자는 그 근처 ID를 v4보다 훨씬 쉽게 추측**할 수 있다. **T-W2-20 귀책이 아니라 리포 전역 성질**이며, 실사용 생성 패턴은 밀리초가 벌어져 잔여 탐색공간이 2^84 수준이라(게이트② 실 DB 실측) 즉시 위험은 낮다 | `services/api/src/common/ids.ts`(발급 방식) — 변경 시 **기존 데이터와 혼재**되므로 마이그레이션 판단 동반. 대안: 노출 ID를 별도 불투명 토큰으로 분리, 또는 접근 제어를 ID 비밀성에 의존하지 않게 유지(현재도 그렇다) | 조율자 실측 2026-08-15: `cd services/api && node -e "const {v7}=require('uuid'); console.log([v7(),v7(),v7()].join('\n'))"` → 앞 21 hex digit 공통 | **비차단**(현 접근 제어가 ID 비밀성에 의존하지 않음) — 공개 표면에 ID를 노출하는 신규 기능 착수 시 재판정 | **미수신(2026-08-15 등재)** | 조치의 핵심은 "ID를 못 맞히게" 만드는 것이 아니라 **ID를 맞혀도 안전하도록 접근 제어를 유지**하는 것이다(현재 `loadOwned`/`loadReadable`이 그 역할). 등재 목적은 향후 "ID만 알면 되는" 설계가 들어올 때 이 성질을 기억하기 위함 |
| 105 | **T-W2-20 게이트② qa-verifier 관찰 + 조율자 재현**(2026-08-15) — **`contents.remake_of_content_id`에 DB FK가 없다.** psql 실측상 `contents`의 FK는 4개(station·reporter·approved_by·minor_consent_confirmed_by)뿐이고 remake 참조는 빠져 있다. 스키마에도 `@relation`이 없다 → 참조 무결성이 **애플리케이션 검증에만** 의존한다(T-W2-20의 `assertRemakeSource`). 원본 콘텐츠가 삭제되면 dangling 참조가 남는다 | `services/api/prisma/schema.prisma`(`remakeOfContentId`에 self-relation 추가 + 삭제 정책 판정) — **선재 스키마 결정**이며 T-W2-20은 스키마 무변경이 정답이었다(마이그레이션은 SOLO 웨이브 소요) | 조율자 실측 2026-08-15: `grep -n "remakeOfContentId" services/api/prisma/schema.prisma` → `String? @map(...)`만, `@relation` 없음 | **콘텐츠 하드 삭제 경로가 생기는 시점**(현재 삭제 API 부재라 dangling이 발생하지 않는다) | **미수신(2026-08-15 등재)** | self-relation은 `onDelete` 정책 판정이 동반된다 — 원본 삭제 시 ⓐ Restrict(재작업본이 있으면 원본 삭제 금지) ⓑ SetNull(계보 끊김) 중 택일. 감사 성격상 Restrict가 유력하나 삭제 정책 자체가 미정이라 함께 결정 |
| 106 | **T-W2-20 게이트② qa-verifier**(2026-08-15, **뮤테이션 분석으로 적발**) — **검증 순서 회귀 가드가 없었다.** T-W2-20의 최우선 성질은 "타 지사 콘텐츠의 **상태**를 노출하지 않는다"(존재→지사→상태 순)인데, 검증 순서를 교환한 복제 로직에 신규 스펙 8종을 전부 투입하니 **결과가 달라지는 건수가 0**이었다 — 즉 순서를 뒤집어도 테스트가 전부 그린이다. 원인은 판별력 있는 입력의 부재: 순서를 가르는 **유일한 조합은 "다른 지사 + 비허용 상태"**인데 그 케이스가 없었고, `contents.service.spec.ts:139`는 이름이 "지사 경계 **우선** 판정"이라 주장하면서 입력이 "다른 지사 + rejected"라 어느 순서든 `forbidden`이 나와 판별하지 못했다 | `services/api/src/contents/contents.service.spec.ts`(판별력 있는 케이스 1건 추가) | 게이트② 실측 2026-08-15: 순서 교환 복제 로직 × 신규 스펙 8종 → 결과 상이 **0건**. 뮤테이션 6종 중 나머지 5종(호출 제거·지사 검사 제거·상태 검사 제거·존재 검사 제거·`?? null` 제거)은 **전부 검출**돼 "항상 통과하는 테스트"는 아니었다 | **즉시**(같은 웨이브) | **해소 완료(2026-08-15, 같은 웨이브)** — 조율자가 구현자를 보강 재기동해 "다른 지사 + `draft` → `forbidden`" 케이스를 추가 | **테스트가 그린이라는 것과 그 테스트가 의도한 성질을 지킨다는 것은 다르다.** 게이트②가 AC 재현에 그치지 않고 **뮤테이션(구현을 일부러 망가뜨려 테스트가 잡는지)** 을 돌린 덕에 나왔다 — 대장 #80(교차검증)과 같은 계열의 "검증 방법이 값을 한" 사례로 남긴다 |
| 107 | **T-W2-20 게이트② qa-verifier 관찰**(2026-08-15) — **전역 요청 레이트리밋(Throttler)이 없다.** api에 `@nestjs/throttler` 등 전역 스로틀이 도입돼 있지 않아, 인증된 사용자의 반복 요청(예: ID 프로빙·목록 폭주)에 대한 일반 완화책이 부재하다. 현재 레이트리밋은 **telemetry·resident-links 2모듈이 각자 인메모리 토큰버킷**으로 구현한 지점별 방어뿐이다(#79 조치④·T-W2-08) | `services/api/src/app.module.ts`(전역 가드) + 스로틀 정책 판정 — 다중 인스턴스 시 Redis 백엔드 필요 | 조사 실측: 전역 스로틀 모듈 미도입, 지점별 리미터 2건만 실재 | **공개 표면이 늘어나는 시점**(현재 Quick Tunnel로 구독자 웹+api가 인터넷에 열려 있음 — #24 ④) | **미수신(2026-08-15 등재)** | 지점별 리미터를 계속 늘리는 것보다 전역 1개 + 민감 지점 강화가 유지보수상 낫다. 단 **전역 도입은 기존 2모듈의 개별 리미터와 이중 계산**이 되므로 통합 판정이 동반된다 |
| 108 | **T-W2-22 게이트② qa-verifier 관찰(O5) + 조율자 재현**(2026-08-16) — **관제 상세 액션바 폴백이 `regenerating`을 하드코딩한다.** EXEC-DECISIONS #29 ④가 레지스트리 파생 대상으로 지목한 5곳(문구·라벨·톤·`AUTO_PROGRESS_STATUSES`·**액션바 폴백**) 중 **이 1곳만 미파생**이다. `apps/control-center/app/(app)/contents/[id].tsx:765`가 `content.status === 'regenerating'`으로 정지 폴백 문구를 고른다. 오늘은 참이지만 **`auto_edit`이 구현되면 "기자·파이프라인 모두 이 상태를 진행시키지 않습니다"가 거짓으로 바뀌고 그것을 잡는 테스트가 없다** — 즉 #29가 막으려던 결함이 정확히 이 한 줄에 남았다 | `apps/control-center/app/(app)/contents/[id].tsx`(하드코딩 분기 → shared `isStalledAutomationContentStatus()` 파생 교체) + 그 관계를 고정하는 단위 테스트 1건 | 조율자 실측 2026-08-16: `sed -n '765p' "apps/control-center/app/(app)/contents/[id].tsx"` → `content.status === 'regenerating'` 하드코딩 확인. 해소 후 `grep -c "isStalledAutomationContentStatus" "apps/control-center/app/(app)/contents/[id].tsx"` ≥1 | **`auto_edit` 착수 전**(그 전까지는 사용자 노출 거짓이 없다 — 우선순위 중) | **미수신(2026-08-16 등재)** | T-W2-22가 `packages/shared` SOLO라 `apps/control-center` 파일이 소유 밖이었다. 앱 태스크로 분리해 처리한다. **이 1건이 남은 것 자체가 #29 2계층의 가치를 보여준다** — 파생된 4곳은 자동으로 따라오고 파생 안 된 1곳만 사람이 기억해야 한다 |
| 109 | **T-W2-22 계측이 발견**(2026-08-16) — **배관 공백 전수조사(#98~#107)에 없던 미구동 2건.** ① `published→archived` — 보관 구동부 0건(조율자 실측: api src에서 `archived` 참조가 **주석 2건뿐**, 프로덕션 코드 0). 콘텐츠를 보관 상태로 보내는 경로가 제품에 없다 ② `analysis_failed→preview_generating` — 전이맵 주석이 "분석 생략 진행(센터 판단 — 분석 실패가 방송을 막지 않게)"이라 명시하는데 그 구동부가 0건이다(`retry()`는 `CONTENT_RETRY_TARGET`만 소비해 `analysis_failed→analyzing`으로만 간다). **AI 분석이 실패하면 센터가 "분석 없이 진행"을 선택할 수단이 없다** — 정본이 의도한 완화책이 미구현 | ① `services/api/src/contents/*`(보관 엔드포인트 또는 명시적 비범위 선언) ② `services/api/src/contents/content-workflow.service.ts`(분석 생략 진행 경로) | 조율자 실측 2026-08-16: `grep -rn "archived" services/api/src --include='*.ts' \| grep -v '\.spec\.'` → **주석 2건뿐** / `CONTENT_RETRY_TARGET.analysis_failed = 'analyzing'`(`workflow.ts:113`)이 유일 소비 | ①은 비차단(보관은 운영 요구가 생길 때) · ②는 **ai-worker 실 제공자 주입 전**(스텁 단계에서는 분석이 거의 실패하지 않아 잠복) | **미수신(2026-08-16 등재)** | **정적 조사(#28)가 3축 22건을 찾고도 놓친 2건을 런타임 계측이 잡았다** — 두 방법이 상보적임을 보여주는 사례. 정적 조사는 "선언 대비 참조"를 보고 계측은 "실제 실행"을 본다 |
| 110 | **T-W2-22 게이트②·구현자 관찰 묶음**(2026-08-16, 계측 인프라 후속 3건) — ① **계측 범위가 api 단위 스위트뿐이다.** `untested` 21건 중 6건(PipelineService 전이)은 실제로 e2e가 실증하는데, 단위 스펙이 `ContentWorkflowService`를 목으로 둬서 `applyHop`에 닿지 않아 미관측으로 잡힌다. e2e로 계측을 넓히면 목록이 더 정직해지나 게이트가 Docker 의존이 된다(트레이드오프) ② **관측 21건이 전부 `content-workflow.service.spec.ts` 한 파일에서 나온다** — 계측 축의 신뢰가 스펙 1개에 집중돼 있다(그 파일이 사라지면 즉시 레드라 은폐되지는 않는다) ③ **레지스트리를 나머지 열거 가능 계약으로 확장**(enum 값·`JobType`·`PublicationStatus`·`LiveSessionStatus` 등) — 현재는 `contentTransitions` 1키뿐이다 | `services/api/test/wiring/*`(계측 범위) · `packages/shared/src/content/not-wired.ts`(키 확장) | 조율자 실측 2026-08-16: 등재 26건 중 `kind: 'untested'` **21건** · 관측 21건의 출처 파일 1개 | **다음 계측 관련 태스크 착수 시**(현재 인프라가 이미 작동하므로 비차단) | **미수신(2026-08-16 등재)** | ③이 가장 가치가 크다 — #29 ⑥이 명시한 "열거 가능한 계약"의 나머지를 덮으면 `LiveComment.Hidden`(#101)·`PublicationStatus.canceled`(#103④) 같은 건이 **다음부터는 자동 검출**된다. ①은 트레이드오프 판단이 필요하고 ②는 관찰 기록 |
| 111 | **T-W2-24 게이트② qa-verifier 런타임 실증 + 조율자 재현**(2026-08-16) — **원본 자산이 없는 건을 승인하면 결정만 커밋되고 인큐가 500으로 터진다.** 검수 결정(`approved`+검수자+시각)은 기록되는데 `QueueProducerService.requireOriginal`이 **plain `Error`**를 던져(주석: "정상 흐름에선 도달 불가") 500 "서버 내부 오류"가 나가고, 그 건은 대기열(`awaiting_branch_review`)에서 **사라진 채** 콘텐츠는 `uploaded`에 남는다 — T-W2-24가 `requirePipeline()`으로 막으려던 **"승인만 기록되고 처리되지 않는 교착"의 다른 얼굴**이다. 도달 경로는 완료 통지의 `ensureOriginalAsset`이 트랜잭션 밖이라 실패해도 검수 대기 상태가 이미 커밋되기 때문이며(그 코드 주석도 "재전송이 보정한다"고 인정하나 **익명 업로더가 재전송하지 않으면 보정되지 않는다**), **신규 승인 경로가 그 "도달 불가"를 도달 가능하게 만들었다** | `services/api/src/resident-links/resident-reviews.service.ts`(결정 기록 **이전** 선확인) | 게이트② 런타임 실증 2026-08-16: 원본 없는 건 승인 → 500 + `awaiting_branch_review`에서 이탈. 조율자 재현: `queue-producer.service.ts:77` `throw new Error(...)`(DomainException 아님) | **즉시**(같은 웨이브) | **해소 완료(2026-08-16, 같은 웨이브 보강)** — `requirePipeline()` 바로 다음·`decide()` 이전에 `requireEnqueueableSource` 신설. `conflict`(409) + "업로드된 원본 영상을 찾을 수 없어 승인할 수 없습니다 — 주민에게 다시 올려달라고 요청하거나 반려해주세요". **판정 함수는 생산자가 쓰는 `findOriginal`을 재사용**(사본을 두면 선확인이 통과시킨 건이 인큐에서 다시 터진다). 반려에는 걸지 않았고 그 사실을 `findOriginal` **호출 0회**로 증명 | **"정상 흐름에선 도달 불가"라는 주석은 그 시점의 호출자 집합에 대한 진술이지 불변식이 아니다.** 새 호출자가 생기면 무효가 되는데 주석은 그대로 남는다 — #29가 다룬 "계약을 구현으로 오독" 계열의 변종(이번엔 *주석을 불변식으로 오독*) |
| 112 | **T-W2-24 구현자 신고 + 게이트② 확인**(2026-08-16) — **반려된 주민 업로드의 Content가 `uploaded`에 영구 잔류한다.** 게이트가 파이프라인 진입을 막으므로 안전하나 종결되지 않는다. `reporterId=null`이라 `ContentWorkflowService.cancel`의 소유 기자 판정을 통과할 수 없어 배선 자체가 별도 판단이다. **부수 영향**: 그 콘텐츠가 `AUTO_PROGRESS_CONTENT_STATUSES`(`uploaded` 포함) 파생상 UI에서 **"처리 중"으로 보인다** — T-W2-08부터의 선존 조건이나 반려 기능이 생기면서 눈에 띄게 됐다 | `services/api/src/contents/content-workflow.service.ts`(무주 콘텐츠 종결 경로) 또는 `resident-links/*`(반려 시 종결 연쇄) — `cancel`의 액터 판정 완화 여부가 설계 판단 | 조율자 실측 2026-08-16: 반려 후 `contents.status`가 `uploaded` 유지 | **주민 링크 실사용 전**(반려 건이 쌓이기 시작하는 시점) | **수신 완료(2026-08-16 — T-W2-31, Wave 8a3)** — `reject()`가 `decide` + `cancelBySystem`을 **한 트랜잭션**으로 실행(부분 성공 없음). 시스템 액터 전이·멱등·이미 `uploaded`를 떠났으면 덮어쓰지 않음(`enterPipeline` 선례 답습)·`contentId` null은 무해 no-op. e2e 신규 스위트 7건으로 실 HTTP 왕복 실증. **부수효과("처리 중" 오표기)는 UI 변경 0으로 해소**(`canceled`는 `SYSTEM_DRIVEN_CONTENT_STATUSES` 후보가 아니다). ⚠️ **`not-wired.ts`의 `uploaded→canceled` 등재 사유가 거짓이었음이 이 과정에서 드러났다** — `untested`("cancel() 실재, 이 from을 밟는 테스트만 없다")로 적혀 있었으나 이 엣지에 도달하는 콘텐츠는 무주(`reporterId=null`)라 `cancel()`의 `requireOwnerOrCenter`를 **애초에 통과할 수 없었다**. 등재 삭제는 임의 판단이 아니라 **전이 계측이 지시**했다(`NOT_WIRED에 있는데 실제로 관측됨 1건`) | 종결 상태 선택(`canceled` vs `rejected`)과 액터(시스템 전이 vs 검수자) 판정이 동반된다. `rejected`는 "기자/센터 검토 반려"의 의미라 주민 검수 반려와 성격이 다를 수 있다 |
| 113 | **T-W2-24 구현자 신고**(2026-08-16) — **반려 사유를 저장할 자리가 없어 반려 API가 사유를 아예 받지 않는다.** `resident_uploads`에 사유 컬럼이 없고 shared `TransitionEntityType`에 `resident_upload`가 없어 `status_transition_logs` 우회도 불가하다. 07 §3-15가 요구하는 **"불법촬영물 의심 시 즉시 반려 + 판단 기준 표준화"**를 감사 가능하게 하려면 사유가 남아야 한다. **받아놓고 버리는 대신 안 받는 쪽을 택한 것은 정직한 선택**이나(계약이 거짓말하지 않는다), 법적 통제 요구가 미충족 상태로 남는다 | `services/api/prisma/schema.prisma`(사유 컬럼) **또는** `packages/shared`(`TransitionEntityType`에 `resident_upload` 추가) — 둘 다 **SOLO 웨이브 필요**(Prisma·shared 공용 자산) | 조율자 실측 2026-08-16: 반려 스키마에 사유 필드 부재 | **07 §3-15 법적 통제 문구 확정 시**(외부 법률자문 산출물과 함께 판정 — #25 ② 게이트) | **미수신(2026-08-16 등재)** | 업로더 통지 경로도 함께 판정할 것(반려 사실을 주민에게 알릴 수단이 현재 0 — 무인증이라 연락처가 유일 채널이고 그건 `uploaderContact`에 있다) |
| 114 | **T-W2-24 구현자 신고 + 조율자 재현**(2026-08-16) — **e2e `resetDb`의 TRUNCATE 목록에 주민 링크 2테이블이 빠져 있다.** `resident_upload_links`·`resident_uploads`가 없어 주민 검수 e2e를 붙이면 이전 실행 잔여가 남는다(T-W2-08 선존 — 그 태스크가 e2e를 만들지 않아 드러나지 않았다) | `services/api/test/e2e-app.ts`(TRUNCATE 목록 +2) | 조율자 실측 2026-08-16: `grep -c "resident" services/api/test/e2e-app.ts` → **0** | **주민 검수 e2e 작성 전**(선행 조건) | **수신 완료(2026-08-23 — feat/116-minor-consent-cas)** — 재현 재실행 **3**(TRUNCATE 목록 2 + 주석 1 — 이 명령은 주석까지 세므로 값이 문안에 따라 흔들린다. 판정 기준은 "≥1"로 볼 것). 두 테이블을 `contents` 앞에 명시 편입. ⚠️ **다만 이 행의 전제("빠지면 잔여가 남는다")는 틀렸다** — `resident_upload_links.station_id`가 `stations` FK라 `TRUNCATE … stations CASCADE`가 **이미 두 테이블을 함께 비우고 있었다**(실증 2026-08-23: gachinol_test에서 행 1건 삽입 후 TRUNCATE 시 `NOTICE: truncate cascades to table "resident_upload_links"/"resident_uploads"`, before 1 → after 0). 그래서 칸5의 기한("주민 검수 e2e 작성 전")이 **이미 초과돼 있었는데도**(`resident-review.e2e-spec.ts`가 2026-08-16 커밋 `a873cf7`에 생겨 주민 테이블을 14곳에서 쓴다) 아무 증상이 없었다. 이번 수정의 실질 가치는 누수 차단이 아니라 **CASCADE 우연 의존 제거**다 — FK가 SetNull로 바뀌거나 `station_id`가 nullable이 되는 순간 조용히 누수가 시작되고, 그때는 원인이 이 파일에 안 보인다 | 1줄 수정이라 e2e를 붙이는 태스크가 함께 처리하면 된다. 현재 기존 e2e에는 무영향(주민 테이블을 쓰지 않는다) |
| 115 | **T-W2-23 게이트② qa-verifier 관찰 + 조율자 재현**(2026-08-16, **2026-08-23 `resident_link` 전용으로 축소** — `live_vod` 분은 #162로 분리) — **`origin='resident_link'` 콘텐츠는 어떤 액터도 미성년자 플래그를 켤 수 없다 — 법적 게이트가 그 경로에서 여전히 무효다.** T-W2-23이 쓰기 경로를 열었으나 두 관문 모두 `reporter_upload`에만 닿는다: `createDraft`는 기자 전용이라 origin이 항상 `reporter_upload`이고, `update()`는 ① 센터·admin의 이 필드 수정을 금지하며 ② 기자는 `loadOwned`의 `reporterId === user.id`를 통과해야 하는데 이 origin은 **`reporterId=null`이 불변식**이다. **주민 업로드는 미성년자 등장 확률이 가장 높은 경로**(무인증 제보, 마을 행사·아이들)인데 게이트가 닿지 않는다 | `services/api/src/resident-links/*`(검수 화면에서 지사 담당자가 플래그를 켜는 경로) **또는** `contents.service.ts`(무주 콘텐츠에 대한 센터 쓰기 허용) — 액터 판정이 설계 결정이다 | 조율자 실측 2026-08-16: `grep -c "hasMinorSubject" services/api/src/resident-links/resident-links.service.ts` → **0**. ⚠️ **이 명령은 해소 위치를 전제한다**(규율 17) — 검수 화면이 아니라 `contents.service.ts` 쪽으로 고치면 0인 채 해소된다. 방식 독립 보조: `grep -c "resident_link" services/api/test/contents.e2e-spec.ts` → **0**(2026-08-23 실측). 어느 액터로 고치든 e2e가 그 경로를 증명해야 한다 | **07 §3-15 법적 통제 문구 확정 시**(#113과 같은 묶음 — 주민 링크의 법적 통제는 외부 법률자문 산출물과 함께 판정, #25 ② 게이트) | **미수신(2026-08-16 등재 · 2026-08-23 범위 축소)** | 주민 업로드의 동의 주체는 기자 촬영과 다르다(찍은 사람이 주민이라 촬영 동의서 체계가 성립하지 않는다) — 07 §3-3의 동의서 모델을 그대로 이식할 수 없고, §3-15의 이용허락 동의와 어떻게 결합할지가 선결이다. **임의 구현 금지**(범위 발명). ★ **차단 사유는 이 origin에만 걸린다** — §3-15는 주민 링크만 다루므로 `live_vod`를 같은 줄에 묶어 두면 **법무 대기가 아닌 항이 법무 대기로 보인다**(2026-08-23 분리 근거, #162) |
| 116 | **T-W2-23 게이트② qa-verifier 관찰**(2026-08-16) — **동의 확인·철회가 read-then-write이며 트랜잭션·CAS가 없다.** ⓐ 동시 확인 2건이 둘 다 멱등 조기반환을 통과해 **두 번째가 최초 확인자를 덮어쓸 수 있다** — 코드 주석이 명시한 "최초 확인자가 감사 기록의 원천"이 경합에서 깨진다 ⓑ 게이트 로그 조회와 update 사이에 승인이 끼어들면 "승인됐는데 철회 성공"이 되어 D5가 막으려던 거짓 안심이 그 창에서 되살아난다. 리포의 다른 상태 변경(`applyHop`·`beginPublishing`)은 전부 CAS인데 이 2메서드만 예외다 | `services/api/src/contents/contents.service.ts`(`confirmMinorConsent`·`withdrawMinorConsent`를 `$transaction` + 조건부 `updateMany` CAS로) | ⚠️ **등재 시 재현 명령이 틀렸다** — `confirmMinorConsent` 블록의 `transaction`만 셌는데, ⓐ의 해소는 **단일 조건부 `updateMany`**(원자 연산이라 트랜잭션 불요)이고 트랜잭션이 필요한 쪽은 ⓑ(withdraw)다. 결함 2개를 한 명령으로 재려다 **양쪽 다 못 잡았다**. 교체(둘 다 실행): ⓐ `awk '/async confirmMinorConsent/,/^  }$/' services/api/src/contents/contents.service.ts \| grep -c 'updateMany'` → **1**(등재 시 0) · ⓑ `awk '/async withdrawMinorConsent/,/^  }$/' services/api/src/contents/contents.service.ts \| grep -cE '\$transaction\|updateMany'` → **2**(등재 시 0) | **센터 운영자 2인 이상 동시 운용 시작 전**(현재 실사용 1인이라 발현 확률 낮음) | **수신 완료(2026-08-23 — feat/116-minor-consent-cas)** — ⓐ 확인 쓰기를 `{hasMinorSubject:true, minorConsentConfirmedAt:null}` 조건부 `updateMany`로 바꿔 판정·쓰기를 한 원자 연산에 합쳤다. 미적중(count=0)은 실패가 아니라 경합 사실이므로 재조회해 판정한다(선점당함=최초 확인자 그대로 멱등 반환 / 플래그 내려감=validation_failed). ⓑ 게이트 로그 조회+쓰기를 `$transaction`으로 묶고 **`status`를 CAS 조건에 넣었다** — 승인은 반드시 status를 바꾸므로 그 사이 승인이 끼어들면 미적중→409. ⚠️ **트랜잭션 자체는 이 경합을 못 막는다**(Read Committed에서 타 트랜잭션의 로그 INSERT는 우리 SELECT에 안 보인다) — 실제 방어는 where 절이며 코드 주석에 명시했다. 유닛 +6(경합 6종)·e2e 멱등 케이스 편입, api 936→942 | 법적 감사 기록이라 "누가 확인했나"가 흔들리면 안 된다는 점에서 일반 경합보다 무게가 있다. 다만 확인 자체는 멱등이라 **데이터 파손이 아니라 귀속 오류**다 |
| 117 | **T-W2-23 게이트② qa-verifier 관찰**(2026-08-16) — **동의 확인이 generation-scoped가 아니다.** 수정요청→재촬영으로 `generation`이 올라가도 `hasMinorSubject`가 켜진 채면 **이전 세대에 대한 확인 기록이 새 영상의 게이트를 그대로 통과시킨다**. 센터는 새 영상의 동의서를 확인한 적이 없다. `MediaAsset`·`AiAnalysis`가 전부 `generation`으로 정합되는 것과 대비된다(확인 기록만 세대 무관) | `services/api/src/contents/contents.service.ts`(재생성 시 확인 기록 무효화) **또는** `schema.prisma`(확인 기록에 `generation` 컬럼 — SOLO 필요) | ⚠️ **등재 시 재현 명령이 틀렸다** — `awk '/async confirmMinorConsent/,/^  }$/' … \| grep -c 'generation'`은 칸3이 제시한 두 선택지 중 **schema.prisma 컬럼 방식만** 검출한다. 실제 구현은 다른 쪽(재생성 시 무효화)이라 해소 후에도 계속 **0**이 나왔다. 교체: `awk '/if \(to === .regenerating.\)/,/^    }$/' services/api/src/contents/content-workflow.service.ts \| grep -c 'minorConsentConfirmedAt'` → **1**(등재 시 0) | **`auto_edit`·재생성 경로 실사용 전**(현재 재생성 워커가 미구동이라 잠복 — 대장 #98 계열) | **수신 완료(2026-08-20 — auto_edit Phase 1/PR #54가 동시 해소, 2026-08-23 대장 반영)** — `applyHop`의 `regenerating` 전이에서 `hasMinorSubject`면 확인 기록(`minorConsentConfirmedByUserId`·`minorConsentConfirmedAt`)을 같은 CAS update에 실어 무효화한다. 무효화 시점을 **generation 증가 지점**에 건 쪽(칸3의 전자)이라 스키마 변경 0. **07 정본 대조 완료(2026-08-23)**: §3-3에 재생성·세대 문언이 없어 완화안("화면 구성이 바뀐 재생성만")을 지지할 근거가 없다 → **보수적 매번 무효화 유지**로 확정, 코드 주석의 "대조 전" 단서도 갱신 | D3(플래그를 끄면 확인 기록도 지움)의 사각이다 — 플래그를 **켠 채로** 재촬영하는 경로는 D3가 발동하지 않는다. 무효화 시점을 `generation` 증가 지점에 걸지, 확인 기록 자체에 세대를 박을지가 판정 |
| 118 | **T-W2-23 게이트② qa-verifier 관찰 + 조율자 재현**(2026-08-16) — **센터가 "동의 확인 대기" 콘텐츠를 발견할 수단이 없어 `reporter_only` 경로가 교착한다.** T-W2-23은 정본(02 §E-20)대로 확인 액터를 센터 전용으로 뒀는데, `reviewPolicy='reporter_only'`(교양·날씨)는 센터 검토를 아예 거치지 않는다 → 플래그가 켜지면 기자 승인이 차단되고, 센터는 **그런 콘텐츠가 존재한다는 사실 자체를 알 수 없다**. `ContentSummary`(목록·검토 보드의 투영)에 `hasMinorSubject`가 없고 `GET /v1/contents` 쿼리에 해당 필터도 없어, 노출은 상세 1건씩(`ContentDetail`)뿐이다 — 전수 상세 조회 없이는 발견 불가 | `packages/shared/src/content/content.ts`(`ContentSummary`에 플래그 추가 — **SOLO**) + `services/api/src/contents/*`(목록 필터) + `apps/control-center`(대기 목록·배지) | 조율자 실측 2026-08-16: `awk '/interface ContentSummary/,/^}/' packages/shared/src/content/content.ts \| grep -c "hasMinorSubject"` → **0**. 해소 후 ≥1 | **T-W2-14(Wave 12) 착수와 동시** — 그 태스크가 체크박스를 만드는 순간 기자가 플래그를 켤 수 있게 되고, 그때부터 이 교착이 실제로 발생한다. 현재는 API로만 켤 수 있어 미발현 | **수신 완료(2026-08-16 — T-W2-27 + T-W2-32, Wave 8a3. 교착 해소)** — ⓐ **발견 수단**(T-W2-27): `ContentSummary`에 `hasMinorSubject`·`minorConsentConfirmedAt` 2필드 + shared 술어(`isMinorConsentPending`) + `GET /v1/contents?minorConsent=pending` 필터 + 관제 검토 보드 "동의 확인 대기" 뷰·배지(뮤테이션 3건 전부 적발). ⓑ **확인·철회 UI**(T-W2-32, 같은 웨이브 — #130으로 분리 등재 후 당일 해소): 센터가 앱 안에서 확인을 완료할 수 있고 철회 불가 조건을 미리 판정해 버튼을 감춘다 → **`reporter_only` 교착이 실제로 풀렸다**. 구현자 실측: 확인 여부의 원천은 `status_transition_logs`가 아니라 `minorConsentConfirmedAt` 컬럼이며(`policyGuard ④`가 읽는 것), 조율자가 위임문에 적은 전제가 틀렸다 | 대장 #96과 **같은 계열의 4회차**(백엔드 가드와 프론트 입력만 나누고 그 사이 배관을 아무에게도 주지 않음)이며, 이번에는 **T-W2-23 자신이 만든 신규 사각**이다. 대안으로 확인 액터를 넓히는 길도 있으나 정본 문언(승인 단계=센터 검토)에서 벗어나므로 **발견 수단을 만드는 쪽**이 정합적이다 |
| 119 | **T-W2-23 게이트② qa-verifier 관찰**(2026-08-16) — **신규 2엔드포인트의 e2e가 0건이라 CI가 미성년자 게이트를 지키지 않는다.** 게이트②가 「플래그 켬 → 승인 409 차단 → 센터 확인 → 승인 통과」 실 HTTP 루프를 **임시 하네스로 돌려 통과를 확인**했으나 그 스펙은 리포에 남지 않았다. 단위 스펙 26건은 Prisma를 목으로 대체하므로 **라우트 배선·RBAC 가드·실 DB 제약을 통과하는지는 커버하지 않는다** | `services/api/test/contents.e2e-spec.ts`(승인 차단→확인→통과 루프 편입) | 조율자 실측 2026-08-16: `grep -rl "minor-consent" services/api/test/ \| wc -l` → **0**. 해소 후 ≥1 | **T-NC-07(W2 DoD) 판정 전** — W2 DoD 최종 항목이 "미성년자 승인 전이가드 구현 완료 확인"이라 그 증적이 자동 재현 가능해야 한다 | **수신 완료(2026-08-23 — feat/116-minor-consent-cas)** — 재현 재실행 **1**. `contents.e2e-spec.ts`에 7건 편입(11→18): 승인 차단(fail-closed) · **RBAC**(기자는 자기 콘텐츠라도 확인 불가) · 전체 루프(차단→센터 확인→승인 통과) · 멱등 귀속 보존 · 철회의 통과 전/후 분기 · **`reporter_only` 경로**(센터 검토가 없는 갈래) · D3 fail-closed(끔→켬 우회 차단). ★ **reviewPolicy 두 갈래를 모두 밟는다** — policyGuard ④가 지키는 엣지가 정책별로 달라 한쪽만 검증하면 다른 쪽 가드가 사라져도 CI가 통과한다. 확인 액터는 **center_operator 실계정**이다(admin으로 때우면 대장 #106과 동형이 된다). 차단 코드는 `conflict`가 아니라 **`invalid_transition`**(409)임을 실측으로 확정 | 대장 #114(e2e `resetDb` TRUNCATE 목록 누락)와 함께 처리하면 e2e 보강 태스크 하나로 닫힌다. 법적 게이트라 회귀 시 비용이 크다는 점에서 우선순위가 일반 e2e 공백보다 높다 |
| 120 | **T-W2-25b 게이트② qa-verifier 관찰 + 조율자 재현**(2026-08-16) — **검수 단건 조회 엔드포인트(`GET /v1/resident-uploads/:id`)가 없어 상세 화면이 목록 캐시에 의존한다.** 그 결과 웹에서 **새로고침·북마크·URL 공유로 상세에 재진입하면 화면을 열 수 없다**(캐시가 비어 목록으로 유도된다). 최초 구현은 항목 전체를 route param에 실어 이를 회피했으나 그 방식은 **검수자 전용 PII(`uploaderContact`)를 브라우저 주소창·히스토리에 평문 노출**시켜(qa-verifier가 실 URL 558자로 실증) 같은 웨이브에서 캐시 조회로 전환했다 — **PII를 지키려면 새로고침 생존을 포기해야 하는 구조**이고, 그 트레이드오프의 근인이 단건 조회 부재다. 부수로 **낡은 캐시가 옛 상태를 현재 사실처럼 렌더**할 수 있다(행위 자체는 안전 — 서버 409→목록 복귀·이미 승인 건은 멱등 200) | `services/api/src/resident-links/resident-reviews.controller.ts`(`@Get(':id')` — 목록과 동일 화이트리스트 투영·지사 경계) + `apps/reporter`(상세를 캐시 대신 단건 조회로) | 조율자 실측 2026-08-16: `grep -cE "@Get\(':id'\)" services/api/src/resident-links/resident-reviews.controller.ts` → **0**. 해소 후 ≥1 | **주민 링크 실사용 개시 전** — 검수자가 상세 URL을 공유하거나 새로고침하는 것은 실무에서 기본 동작이다 | **해소(2026-08-22)** — `GET /v1/resident-uploads/:id` 신설(`loadForReview`·`toReviewItem` 조합이라 경계·투영 사본 0) + 기자 앱 상세를 단건 조회로 전환. 목록 캐시는 **첫 페인트 시드**로만 남겨 깜빡임 없는 진입은 유지하고, 새로고침·북마크·URL 공유 진입은 서버에서 받아온다. 낡은 캐시가 옛 상태를 사실처럼 보여주던 부수 항목도 함께 닫혔다(마운트마다 재검증) | 서버 편집이라 이번 FE 태스크 범위 밖이었다. 해소 시 상세 쿼리 키도 `residentUploadKeys.detail(id)`로 정규화할 것 — 현재는 `['media-access-url', id]` 같은 리터럴 키가 `keys.ts`의 "캐시 키는 팩토리로만" 규약에서 벗어나 있다 |
| 121 | **T-W2-25b 게이트② qa-verifier 관찰 + 조율자 재현**(2026-08-16) — **3앱의 화면 계층(`app/**`)이 jest `testMatch` 밖이라 커버리지가 구조적으로 0%다.** `testMatch: ['**/src/**/__tests__/**/*.test.ts?(x)']`가 `app/` 디렉터리를 아예 배제한다. 그 결과 화면에 직접 쓰인 판정(확인 다이얼로그 경유 여부·버튼 활성 조건·경고 표시)은 **뮤테이션에 무방비**다 — qa-verifier가 T-W2-25b에서 "확인 다이얼로그 우회"·"승인 버튼 조건 제거" 뮤턴트를 **테스트가 잡지 못함**으로 실증했다. 완화책은 판정 로직을 `src/features/`로 추출하는 것이고 T-W2-25b가 그렇게 했으나, **화면에 로직을 쓰면 조용히 무보호가 된다는 구조는 그대로**다 | `apps/{reporter,control-center,subscriber}/jest.config.js`(testMatch 확장 + `app/` 테스트 관례 수립) — **3앱 공통이라 단일 태스크로 묶을 것** | 조율자 실측 2026-08-16: 화면을 실제로 렌더하는 테스트 수 `ls apps/*/src/**/__tests__/*screen*.test.tsx apps/*/src/**/__tests__/*render*.test.tsx \| wc -l` → **3**(등재 시 0). 뮤테이션 재현: 승인 다이얼로그 게이트 제거 시 reporter **2건 실패**(등재 시에는 같은 제거가 그린이었다) | **T-NC-07(W2 DoD) 판정 전** — W2 DoD가 화면 시나리오를 요구하는데 그 계층에 단위 안전망이 0이다 | **수신 완료(2026-08-16 — T-W2-26, Wave 8a2. #126과 한 태스크로 해소)** | ⚠️ **등재 시 진단이 부정확했다**: 해법은 `testMatch` 확장이 **아니었다**. expo-router `_ctx-shared.js` 실측 결과 `app/` 하위는 `+api`/`+html`/`+native-intent`/`+middleware` 외에 **`__tests__`·`.test.` 파일명도 예외 없이 라우트로 흡수**하므로, `app/`에 테스트를 두면 **프로덕션 번들에 라우트가 실린다**. 실제 해법은 테스트를 `src/**/__tests__/`에 두고 `app/`의 화면을 상대 경로로 import하는 것이며, **기존 `testMatch`가 이미 그 패턴을 커버해 `jest.config.js`는 3앱 전부 무변경**이다. 즉 실제 병목은 #126(렌더러 부재) 하나였고 본 항의 "경로 설정" 진단은 오진이었다 |
| 122 | **T-W2-25b 게이트② qa-verifier 관찰 + 조율자 재현**(2026-08-16) — **CI가 앱·shared·ui 코드를 한 줄도 린트하지 않는다.** 3개 Expo 앱에 `lint` 스크립트도 ESLint 설정 파일도 **없고**, `packages/shared`는 스크립트 부재, `packages/ui`는 `scripts` 자체가 없다. `pnpm lint`(=`turbo run lint`)는 "7 packages"를 선언하지만 실제 실행은 **`@gachinol/api`·`@gachinol/media-worker` 2건뿐**이고 나머지는 no-op으로 통과한다. `.github/workflows/ci.yml`의 Lint 스텝이 이것을 그대로 부르므로 **앱 코드의 유일한 정적 게이트는 `tsc --noEmit`**이다 | `apps/{reporter,control-center,subscriber}/package.json` + eslint 설정(`packages/config`가 공유 eslint를 소유한다고 리포 CLAUDE.md §6이 선언 — 실재 여부 먼저 확인할 것) | 조율자 실측 2026-08-16: 앱 3종 eslint 설정 파일 **0개**, `package.json`에 `"lint"` 스크립트 **0개**. 해소 후 각각 ≥1 | **W2 종료(Wave 15b, T-W2-17이 CI 잡을 확정하는 시점) 전** | **해소(2026-08-21)** — `packages/config` 공유 eslint 프리셋 신설(base + RN/Expo용 react) + **8개 워크스페이스 전건 배선**(lint 스크립트·설정·의존성). api·media-worker의 중복 설정도 프리셋으로 통합. 도입 시 실측 위반 17건 전건 해소, **뮤테이션 5/5로 게이트 실동작 확인**(규율 7) | **선존 조건**(웹 피벗 이전부터). 리포 CLAUDE.md §6이 `packages/config`를 "공유 eslint/tsconfig/prettier" 소유자로 선언하는데 앱이 그것을 소비하지 않는 상태라, **문서가 선언한 구조와 실체가 어긋난 건**이기도 하다 |
| 123 | **T-W1-07b 게이트② qa-verifier 판정(AC2 FAIL) + 사용자 결정**(2026-08-16) — **간단 모드가 정밀 모드와 완전히 동일하다.** 위저드가 `index→scenes→classify→upload`인데 모드 선택 UI가 자막 단계 **뒤**에 있었다. `scenes.tsx`가 `validateScenes` 실패 시 진행을 막고 `validation.ts`가 빈 caption을 거부하므로 classify 도달 시 모든 scene에 자막이 있고, "빈 caption 치환" 로직은 **항상 항등함수**였다. 03 §C-4가 요구한 "촬영 → **자막 생략** → 분류 → 업로드"는 미구현이며 걷어내야 할 `scenes` 단계가 그대로 필수다. **사용자 결정으로 T-W1-07b는 모드 선택 UI와 `mode_selected` 계측을 함께 제거**했다(있지도 않은 기능의 선택지를 계측하면 `simpleAdoptionRate` KPI가 무의미한 수치를 쌓는다) | `apps/reporter/app/(app)/contents/new/{index,scenes,classify,_layout}.tsx`(위저드 흐름 재설계 — 모드 선택을 scenes **앞**으로 + scenes 조건부 생략) + `mode_selected` 재도입 | 조율자 실측 2026-08-16: `grep -rn "mode_selected" apps/reporter/src apps/reporter/app --include='*.ts' --include='*.tsx' \| grep -v __tests__ \| wc -l` → **0**(프로덕션 코드에서 제거 완료. `__tests__` 제외가 필수 — 제거를 고정하는 **부정 단언**(`expect(names).not.toContain('mode_selected')`)이 남아 있어 그것까지 세면 명령이 자기 무효화된다). 재구현 시 ≥1 | **03 §C-4 KPI("간단 모드 채택률") 측정 개시 전** — 그 전까지는 지표 자체가 성립하지 않는다 | **수신 완료(2026-08-16 — T-W2-34, Wave 8a3)** — 모드 선택을 scenes **앞**으로 옮기고 간단 모드는 그 단계를 **건너뛰어 빈 배열로 저장**한다(같은 조작에 모드만 바꿔 결과가 갈리는 것 + "간단 모드는 입력된 자막까지 버린다"를 테스트로 고정 = 항등함수가 아님의 증거). `mode_selected`는 shared 카탈로그(T-W2-29 산출물)를 소비해 재도입하고 배치 큐를 경유하며, 제거를 고정하던 부정 단언은 삭제했다. ⚠️ **조율자 착수 전 조사에서 대장에 없던 병목이 드러났다** — 서버가 `draft`·`revision_requested`에서만 수정을 허용하고(`EDITABLE_STATUSES`) `loadOwned`가 소유 기자만 통과시켜 **정본이 말한 "지사 담당자의 사후 자막 입력"이 원천 불가**였다(정본 자신도 "C-5 검수 게이트 안에 옮기면 된다"고 적었으나 **기자 경로에는 그 게이트가 없다** = 정본의 공백). **사용자 결정으로 "송출 허용 + 사후 보강"** 채택 → `PATCH /v1/contents/:id/captions`(같은 지사 기자·`published` 전까지·자막 필드만, 지사 경계는 `loadReadable` 재사용으로 사본 0) + **발견 수단**(`captions=needed` 대기열 + 목록 첫 화면 건수 카드). 필터와 쓰기 게이트가 같은 shared 원천에서 파생해 **"목록엔 떠 있는데 열면 409"가 구조적으로 불가능**하다. 자막 0의 실측 영향: 파이프라인 무영향(media-worker·ai-worker에 scene 참조 0건, 주민 제보가 이미 `scenes: []`로 완주 중), 구독자는 오버레이 미렌더(크래시·빈 박스 없음). **승인 홉 가드는 사용자가 명시적으로 배제**했다. **정본 정정 완료(2026-08-16)** — 사용자가 "정본에 공백이 있어 오류가 반복될 것 같으면 보고 후 고칠 수 있다"고 지시해 **03 §C-4에 사실 정정 블록을 신설**했다(舊 "신규 인프라 불요"가 거짓인 이유 · 채택된 구조 · 자막 0의 실측 영향 · **접근성 대가가 실재한다는 것**까지 명시). 원문을 지우지 않고 이력을 남기는 기존 형식을 따랐다 | **신규 태스크 채번 필요**. 자막 없는 콘텐츠가 파이프라인(AI 분석 힌트·자막 오버레이)과 접근성(03 큰 자막 모드)에 미치는 영향 판단이 동반된다 — 단순 UI 분기가 아니다 |
| 124 | **T-W2-10 게이트② qa-verifier 관찰**(2026-08-16) — **`archived` 전이를 일으킬 제품 경로가 없어 공개 객체 제거·CF 퍼지가 잠복 코드다.** 유일한 도달 통로는 범용 수동 전이 `POST /v1/contents/:id/transitions`인데, 관제 앱 `features/contents/actions.ts`가 `manualTransitionTargets`를 **`revision_requested`일 때만** 채운다 → published 콘텐츠에 보관 버튼이 없다. 전용 archive/delete 엔드포인트도 없다(대장 #109 계열). 즉 정본 §E-14의 "삭제·비공개 전환 시 공개 객체 제거" 대칭이 **코드 수준에서는 성립하나 제품 수준에서는 미성립**이며, Swagger·curl 원시 호출로만 도달 가능하다 | `apps/control-center`(보관 액션 노출) **또는** `services/api`(전용 archive/delete 엔드포인트) — 어느 쪽인지가 설계 판단 | 조율자 실측 2026-08-16: `grep -c "manualTransitionTargets" apps/control-center/src/features/contents/actions.ts` → 1이나 `revision_requested` 조건부. 해소 후 published에서도 보관 경로 노출 | **공개 렌디션 서빙 활성화 시**(`MEDIA_PUBLIC_BASE_URL` 설정 시점 — 그때부터 회수 불가 사본이 실제로 쌓인다) | **수신 완료(2026-08-16 — T-W2-32, Wave 8a3)** — 사용자 결정대로 **전용 엔드포인트 없이 관제에 보관 액션 노출**. 핵심은 게이트를 **상태 이름 0개로 파생**한 것이다: "전용 액션이 이미 있거나(canDecide·canRetry·canDistribute) 출구에 자동 진행 상태가 하나라도 있으면 닫는다" → 전 23상태 중 `revision_requested`·`published` 정확히 2종만 열린다. **auto_edit이 구동되면 `revision_requested` 탈출구가 저절로 닫힌다**(EXEC-DECISIONS #29 ④ 동형). 보관 경고에 삭제 대상(공개 렌디션·썸네일 + CF 퍼지)·남는 것(원본·전이 이력·AI 분석)·되돌릴 수 없음(`archived: []`)을 코드 근거와 함께 명시, 확인은 `confirmDialog`(RNW `Alert.alert`는 빈 함수) | 보강으로 복사 쪽에 env 게이트를 걸어 **꺼진 상태에서는 사본이 안 쌓이게** 했으므로 즉시 차단성은 없다. 켜는 순간 이 항이 선결 조건이 된다 |
| 125 | **T-W2-10 게이트② qa-verifier 관찰**(2026-08-16) — **`not-wired.ts`의 사유 문구가 stale해졌다.** `packages/shared/src/content/not-wired.ts`의 `published→archived` 항목 사유가 "api src 전체에 문자열 `archived` 참조 **0건**"인데, T-W2-10이 `content-workflow.service.ts`에 그 참조를 만들었다. **멤버십 판정(런타임 계측)은 여전히 정확해 스위트는 그린**이며 거짓인 것은 사유 문구뿐이다 — 그래서 자동 검증이 잡지 못한다 | `packages/shared/src/content/not-wired.ts`(사유 문구 갱신) — **G3 단독 슬롯 필요** | 조율자 실측 2026-08-16: `grep -c "archived" services/api/src/contents/content-workflow.service.ts` → **≥1**(문구가 주장하는 0과 불일치) | **다음 `packages/shared` SOLO 웨이브에 동승** | **수신 완료(2026-08-16 — T-W2-27, Wave 8a3. #118과 한 태스크로 해소)** — 사유를 사실에 맞게 정정(`archived` 참조 5건은 전부 T-W2-10의 **도달 후 부작용**이지 전이를 일으키는 곳이 아니다)하고, 레지스트리 헤더에 **"★ 한계 ② — stale될 수 없는 것은 멤버십뿐이다"** 절을 신설해 `reason`이 사람 몫임을 명시. `kind`는 `unimplemented` 유지. ⚠️ **같은 날 2차 stale이 발생했다** — 정정한 새 사유("제품 액션 0건")를 **같은 웨이브의 T-W2-32(#124)가 몇 시간 만에 반증**했다(관제 보관 버튼 신설). 재정정하면서 **"0건"류 전수 주장을 사유에 쓰지 말 것**을 레지스트리 규칙으로 명시했다 — 검증 불가능한 형태라 반드시 stale된다. 판정 근거만 적는다 | [[contract-wiring-gate]]가 경계한 "레지스트리가 stale될 수 없다"는 **멤버십에만 해당**하고 **사유 문구는 여전히 사람 몫**임이 드러난 첫 사례다. 그 한계를 레지스트리 주석에도 반영할 것 |
| 126 | **T-W1-03·T-W1-07b 게이트② qa-verifier 관찰**(2026-08-16) — **3앱에 컴포넌트 렌더러가 설치돼 있지 않아 화면 테스트가 원천 불가하다.** `@testing-library/react-native`·`react-test-renderer` 부재로 UI 요건은 "코드 읽기"로만 확인된다. 대장 #121(화면 계층이 jest `testMatch` 밖)과 **원인이 둘이었음**이 이번에 확인됐다 — 경로 설정을 고쳐도 렌더러가 없으면 여전히 못 쓴다. 이번 웨이브의 실비용: 폴백 UI·hls.js 어댑터·위저드 분기가 전부 커버리지 0%였고, 검증자가 각 분기를 **통째로 삭제해도 전 스위트 그린**임을 3건 모두 실증했다 | `apps/{reporter,control-center,subscriber}/package.json`(렌더러 의존성) + jest 설정 — **lockfile 갱신이라 D6-1상 웨이브당 1건 제약**, #121과 한 태스크로 묶을 것 | 조율자 실측 2026-08-16: `grep -l "@testing-library/react-native" apps/*/package.json \| wc -l` → **3**(등재 시 0) | **T-NC-07(W2 DoD) 판정 전** — W2 DoD가 화면 시나리오를 요구한다 | **수신 완료(2026-08-16 — T-W2-26, Wave 8a2. #121과 한 태스크로 해소)** — `@testing-library/react-native@14.0.1` + `test-renderer@1.1.0`(react 19.1.0 피어 정합 실측) 3앱 동일 버전 도입, 각 앱에 화면 렌더 테스트 1스위트. reporter 210/20→**215/21** · control-center 191/16→**195/17** · subscriber 82/13→**86/14**(회귀 0) | **본 항이 진짜 병목이었다** — #121이 지목한 경로 설정은 오진이었고(그쪽 열 참조) 렌더러 부재 하나만 막고 있었다. 프로덕션 코드 무변경으로 도입했으며, **화면에 로직을 쓰면 조용히 무보호가 되던 구조가 이로써 닫힌다**(같은 분기 제거 뮤테이션이 등재 시 그린 → 현재 2건 실패) |
| 127 | **T-W1-03 게이트② qa-verifier 관찰 + 조율자 재현**(2026-08-16) — **재생 실패 폴백의 대체 경로(지사 전화·YouTube)가 공개 계약에 없다.** 전화번호는 `user/community-figure.ts`가 PII로 **명시 제외**했고 Station·LiveSession에 YouTube 필드가 없다. 현재는 앱 env(`EXPO_PUBLIC_SUPPORT_TEL`·`EXPO_PUBLIC_LIVE_YOUTUBE_URL`)가 유일 공급원이라 **지사별로 다른 값을 줄 수 없다**(빌드 1개 = 값 1개). 03 §A-6이 상정한 "지사에 연락"이 지사 단위로 성립하지 않는다 | `packages/shared`(Station에 공개 연락 채널 필드 — **SOLO**) + `services/api`(피드·지사 응답 투영) + `apps/subscriber`(env 폴백 → 서버 값 우선) | 조율자 실측 2026-08-16: `grep -rc "supportTel\|youtubeUrl" packages/shared/src` → **0**. 해소 후 ≥1 | **12개 지사 확장 시**(2개 지사 운영 중에는 env 단일값으로 버틸 수 있으나 3번째부터 즉시 깨진다) | **수신 완료(2026-08-16 — T-W2-28, Wave 8a3)** — Prisma `stations.support_tel`·`youtube_url` 2컬럼(마이그레이션 `20260816000000_station_public_contact`) + shared `Station`·`StationSummary` 필드 + 공개 화이트리스트 투영(`toStationSummary`) + 구독자 **서버 값 우선·env 폴백·값 없으면 버튼 미렌더**(뮤테이션 2건 적발) + 시드는 **빈 값일 때만** 채움(운영자 실 값 보호). PII 판정 근거를 `station.ts` JSDoc에 명시(조직 업무 연락처 ≠ `community-figure.ts`가 제외한 개인 연락처) | 보강으로 "다시 시도"가 env 무관 항상 제공되므로 **폴백이 죽지는 않는다**(대체 경로가 1개로 줄 뿐). PII 취급 판정이 동반된다 — 지사 대표번호는 개인 연락처와 성격이 다르다 |
| 128 | **T-W1-07b 게이트② qa-verifier 관찰**(2026-08-16) — **계측 이벤트 이름이 서버 카탈로그와 계약으로 묶여 있지 않고, 배치를 쓰지 않아 레이트리밋에 조기 도달한다.** ⓐ 클라이언트는 리터럴 6개, 서버는 `TelemetryEventName` 상수 9종인데 **공유 원천이 0**이다. 신규 테스트도 같은 리터럴을 자기참조로 단언할 뿐 서버 카탈로그를 참조하지 않아, **클라와 테스트를 함께 바꾸는 커밋**(실제 drift의 형태)은 무검출이고 카탈로그 밖 이름은 서버가 조용히 버린다 ⓑ 서버는 `TELEMETRY_MAX_BATCH_SIZE=100`을 받는데 클라는 **항상 길이 1 배열**이라, 지사 NAT 공유 IP에서 `TELEMETRY_RATE_LIMIT_CAPACITY=30`·refill 2s(지속 0.5 req/s) 상한에 불필요하게 빨리 닿고 429는 조용히 삼켜져 관측이 유실된다 | `packages/shared`(이벤트 이름 카탈로그 승격 — **SOLO**, T-W2-25a 선례 동형) + `apps/reporter/src/telemetry`(배치 큐) | 조율자 실측 2026-08-16: `grep -rc "TelemetryEventName" packages/shared/src` → **0**. 해소 후 ≥1 | **계측 수치를 실제 의사결정에 쓰기 전**(03 KPI 판정 개시 시점) | **수신 완료(2026-08-16 — T-W2-29, Wave 8a3)** — `packages/shared/src/telemetry/telemetry-event.ts` 신설로 이름 카탈로그·배치 상한·payload 상한을 승격하고 api는 그것을 소비(사본 0). **drift가 tsc·테스트 양쪽에서 잡힌다**(카탈로그 밖 이름 → TS2345 + 테스트 2건 실패, 산출물로 실증). 배치 큐는 건수 20·시간 5s·강제(`upload_complete`·백그라운드) 3조건 + 백오프 10s 1회 재시도, 유실 전량이 카운터로 관측. **본 항 진단 일부 정정**: 배치 상한·레이트리밋은 env가 아니라 **하드코딩 상수**다(코드에 `env 미도입` 주석 명시). 레이트리밋 상수는 IP 단위라 클라가 자기 몫을 계산할 수 없어 **의도적으로 승격하지 않았다** | ⓐ는 T-W2-25a(`ResidentUploadStatus` shared 승격)와 **정확히 같은 계열**이다 — 서버 내부에 둔 계약을 앱이 소비하기 시작하면 공유 계약이 된다 |
| 129 | **T-W2-10 게이트② qa-verifier 관찰**(2026-08-16) — **공개 서빙을 켜기 전 선결 2건.** ⓐ `FeedService.list()`가 항목마다 `resolvePublicUrl`→S3 HEAD를 **순차** 호출한다 — `MEDIA_PUBLIC_BASE_URL`을 켜는 순간 피드 1페이지(20건)당 최대 20회 오리진 왕복이 발생해 **CDN 서빙의 목적(오리진 왕복 제거)과 정면으로 상충**한다 ⓑ `S3Service.copyObject`가 `CacheControl`·공개 ACL을 설정하지 않아 엣지 TTL이 오리진 기본값에 좌우되고, R2는 ACL이 없어 **버킷 단위 공개 설정이 암묵적 전제**인데 그 전제가 코드·문서 어디에도 없다 | `services/api/src/feed/feed.service.ts`(HEAD 제거 — 공개 키를 결정적으로 파생하거나 DB에 기록) + `services/api/src/media/s3.service.ts`(`CacheControl`) + `docs/infrastructure.md`(버킷 공개 설정 전제 문서화) | 조율자 실측 2026-08-16: `grep -c "CacheControl" services/api/src/media/s3.service.ts` → **0**. 해소 후 ≥1 | **`MEDIA_PUBLIC_BASE_URL` 설정 직전**(꺼져 있는 동안은 HEAD 0회라 무회귀) | **수신 완료(2026-08-16 — T-W2-33, Wave 8a3)** — ⓐ `media_assets`에 `public_bucket`·`public_key`·`public_copied_at` 기록(마이그레이션 `20260816120000_media_public_copy`)으로 HEAD 판정을 대체. **공개 서빙 ON·20건 조회에서 `headObject` 0회**를 스파이 카운트로 실증(변경 전이라면 20회). **기록 오류의 방향을 한쪽으로 고정**한 것이 설계의 핵심 — "있다"고 잘못 말하면 404 URL로 재생이 깨지지만(HEAD보다 나쁨) "없다"면 서명 URL 폴백이라 무해하므로, **복사는 성공 후에만 기록하고 삭제는 기록을 먼저 비운다**. 키는 기록된 것을 그대로 쓴다(재파생 금지 — 프리픽스가 바뀌어도 고아가 안 남는다). ⓑ `copyObject`에 `CacheControl`(엣지 1년 = URL에 세대가 박히고 `published`의 유일 출구가 종결이라 바이트 불변 + CF Purge 가능 / 브라우저 1시간 = purge가 닿지 않는 유일한 층이라 비공개 전환 24h SLA 안쪽). **`MetadataDirective: REPLACE`가 필수인데 그러면 `ContentType`이 날아가 `binary/octet-stream`이 되어 재생이 깨진다** — 항상 짝으로 전달하고 e2e가 s3rver에서 두 헤더를 실측. ⓒ `docs/infrastructure.md` §4-C-1에 버킷 공개 전제·TTL 정책 문서화. 마이그레이션은 전부 nullable(기존 행 NULL = "모른다" = 서명 URL 폴백)이라 이미 사본이 있는 배포에서도 재생 무회귀 | ⓐ는 현재 성능 문제가 아니라 **켜는 순간 성능이 나빠지는** 형태라 활성화 판단과 반드시 묶어야 한다. T-W2-10이 스키마 무변경을 택한 결과(공개 사본 존재를 DB가 아니라 HEAD로 판정)의 대가이며, 해소는 Prisma 컬럼 추가(SOLO)가 유력하다 |
| 130 | **T-W2-27 구현자 발견**(2026-08-16) — **센터에 미성년자 동의 "확인" UI 자체가 없다. #118 교착의 나머지 절반이다.** T-W2-23이 만든 `POST`/`DELETE /v1/contents/:id/minor-consent`(둘 다 `center_operator`·`admin`)를 **3앱 어디에서도 호출하지 않는다**. T-W2-27이 발견 수단(목록 필터·배지·전용 뷰)을 만들어 센터가 대기열을 보고 상세로 갈 수는 있게 됐으나, 상세 화면에 확인 버튼이 없어 **앱 안에서 처리를 완료할 수 없다** — Swagger·curl이 유일 경로다. 대장 #96 계열의 5회차이며 이번엔 T-W2-27이 소유 범위를 "발견 수단"으로 한정한 결과다 | `apps/control-center/app/(app)/contents/[id].tsx` + `features/contents/actions.ts`(확인·철회 액션) + `src/api/contents.ts` | 조율자 실측 2026-08-16: `grep -rn "minor-consent" apps --include='*.ts' --include='*.tsx' \| wc -l` → **0**. 해소 후 ≥1 | **#118과 동일**(T-W2-14 착수와 동시) — 발견만 되고 처리가 안 되면 교착은 그대로다 | **수신 완료(2026-08-16 — T-W2-32, Wave 8a3. 등재 당일 해소)** — 확인·철회 2액션 배선. **철회 거부 조건을 UI가 미리 판정해 버튼을 감춘다** — 서버는 `status_transition_logs`에 게이트 대상 전이가 기록됐는지로 판정하고 그 엣지는 reviewPolicy로 갈린다(`reporter_only`면 `awaiting_reporter_review→reporter_approved`, 그 외 `awaiting_center_review→center_approved`). 전이 이력이 페이지네이션 미완결이면 **fail-closed**(철회 버튼 대신 "이력 마저 불러오기"). `hasMinorSubject=false` 사전 확인 거부(400)도 UI가 반영. 뮤테이션 3건 전부 적발(보관 파생·확인 파생·**철회 불가 조건 제거**) | 확인은 되돌리기 어려운 승인성 조작이라 확인 다이얼로그가 필요한데 **RNW `Alert.alert`는 빈 함수**(대장 #92)다 → 앱의 기존 시트/모달 패턴(관제 추천 탭 2000자 note 시트) 재사용. 철회는 승인 후 거부되므로 그 조건도 UI에 반영해야 한다 |
| 131 | **T-W2-29 구현자 발견**(2026-08-16) — **서버 계측 카탈로그 9종 중 3종을 아무도 보내지 않는다. `apps/subscriber`에 계측 코드가 단 한 줄도 없다**(grep 0건). `playback_start`·`playback_progress`·`large_caption_mode_toggle`이 그것이며, 02 §E-16 ①소비 트랙(T-W1-07a)이 미배선이라 **KPI "조회 집계"·"큰 자막 모드 활성 비율"은 현재 구조적으로 영원히 0/null**이다. 서버는 미지의 이름을 400이 아니라 **조용히 버리므로**(unknownEventCount만 증가) 이 공백은 어떤 실패도 내지 않는다 | `apps/subscriber/src/telemetry/*`(신설 — T-W2-29의 `telemetry-batch.ts` 재사용 판단 동반) + 재생 화면·큰 자막 토글 호출부 | 조율자 실측 2026-08-16: `grep -rn "telemetry" apps/subscriber/src apps/subscriber/app --include='*.ts' --include='*.tsx' \| wc -l` → **0**. ⚠️ **2026-08-23 이 명령은 무효화됐다** — T-W2-11(링크아웃 계측)이 `src/telemetry/send-events.ts`를 만들어 **5건**이 되었으나 **이 항의 잔여인 소비 트랙 3종은 여전히 0건**이다(명령이 링크아웃 몫까지 세어 잔여를 가린다, 규율 17). 교체: `grep -rn "playback_start\|playback_progress\|large_caption_mode_toggle" apps/subscriber/src apps/subscriber/app \| wc -l` → **0**(2026-08-23 실측). 해소 후 ≥1 | **03 KPI 판정 개시 전**(#128과 동일 기한 — 카탈로그를 계약으로 묶어도 발신자가 없으면 지표는 여전히 0이다) | **부분 해소(2026-08-22)** — 링크아웃 몫은 T-W2-11이 닫았다(`commerce_linkout_click` 발신, `apps/subscriber` 최초 계측 코드). **소비 트랙(`playback_start`·`playback_progress`·`large_caption_mode_toggle`)은 여전히 발신 0** — T-W1-07a(Wave 12) 소유. 미수신은 그 잔여분 | #128이 "이름이 맞는가"를 닫았다면 본 항은 **"보내기는 하는가"**다. 배치 큐가 reporter 로컬이라 구독자가 소비하려면 앱 공용 위치로 승격할지 판단이 붙는다(소비자 1개일 때는 승격하지 않았다) |
| 132 | **T-W2-28 구현자 발견**(2026-08-16) — **`PlaybackInfo`에 `stationId`가 없어 시청 화면이 비정규화 `stationName` 정확 일치로 지사를 찾는다.** T-W2-28이 지사별 연락처를 계약에 넣었으나, 재생 화면이 "이 콘텐츠의 지사"를 식별할 ID가 없어 이름 문자열 매칭에 의존한다. 구현자는 **동명 지사가 2곳 이상이면 매칭을 포기**(엉뚱한 지사로 전화 걸리는 것보다 안전)하도록 하고 테스트로 고정했으나, 그 경우 폴백 경로가 조용히 사라진다 | `packages/shared/src/subscriber/dto.ts`(`PlaybackInfo.stationId` 1필드 — **SOLO**) + `services/api/src/feed/feed.mapper.ts`(투영) + `apps/subscriber`(이름 매칭 제거) | 조율자 실측 2026-08-16: `awk '/interface PlaybackInfo/,/^}/' packages/shared/src/subscriber/dto.ts \| grep -c "stationId"` → **0**. 해소 후 ≥1 | **13번째 지사 또는 동명 지사 발생 시** — 2개 지사(애월·제주시) 운영 중에는 이름 충돌이 없어 미발현 | **미수신(2026-08-16 등재)** | 1필드 추가로 끝나는 저비용 건인데 T-W2-28의 소유 파일 밖이라 미실행됐다. **후속 권장 1순위**(구현자 판정) |
| 133 | **T-W2-28 구현자 발견**(2026-08-16) — **`LiveSessionPublic`에 지사 참조가 아예 없어 라이브 재생 실패 폴백이 여전히 env 전용이다.** VOD 쪽은 #127로 지사별 값이 들어갔으나 라이브는 세션 공개 투영에 지사 필드가 0이라 `EXPO_PUBLIC_LIVE_YOUTUBE_URL` 단일값을 그대로 쓴다 — 12지사 확장 시 #127과 **같은 형태로 깨진다** | `packages/shared/src/live/live-session.ts`(공개 투영에 지사 참조 — **SOLO**) + `services/api/src/live/*`(`toLiveSessionPublic`) + `apps/subscriber/app/live/[id].tsx` | 조율자 실측 2026-08-16: `awk '/interface LiveSessionPublic/,/^}/' packages/shared/src/live/live-session.ts \| grep -c "station"` → **0**. 해소 후 ≥1 | **#127과 동일**(12개 지사 확장 시) | **미수신(2026-08-16 등재)** | `app/live/[id].tsx`에 사유와 배선 지시("지사 참조가 생기면 `resolveStationContact`로 갈아끼우면 된다")를 주석으로 남겨 뒀다. 공개 투영은 화이트리스트라 `streamKeyRef` 유출 없이 지사 요약만 실어야 한다 |
| 134 | **T-W2-28 구현자 발견**(2026-08-16, 저심각도·선재) — **지사 수정 API가 값 지우기(null)를 지원하지 않는다.** `UpdateStationRequest`는 기존 규약(`thumbnailUrl`·`foundedAt`)대로 **부재=미변경**이라, #127로 추가한 `supportTel`·`youtubeUrl`을 **한 번 넣으면 앱에서 지울 수단이 없다**(번호 폐지·YouTube 채널 폐쇄 시). 규약 자체를 바꾸는 일이라 T-W2-28은 미실행 | `packages/shared/src/station/dto.ts` + `services/api/src/stations/*`(null 허용 규약 도입 — 기존 4필드에 일괄 적용) | 조율자 실측 2026-08-16: `grep -c "null" packages/shared/src/station/dto.ts`(UpdateStationRequest 블록) → **0**. ⚠️ **2026-08-23 이 명령은 오검출한다** — 블록 안 한 줄 주석(*"null 지우기 미지원"*)이 세어져 **1**이 나온다(#114·#137과 동형: `grep -c`는 주석을 센다). 교체: `awk '/interface UpdateStationRequest/,/^}/' packages/shared/src/station/dto.ts \| grep -v '/\*\*' \| grep -c null` → **0**(2026-08-23 실측). 해소 후 ≥1. ⚠️ 명령에 **리터럴 파이프를 쓰지 않는다** — 대장 표는 파이프를 `\|`로 이스케이프하므로 명령 내부의 파이프 문자(예: 타입 `string \| null` 검색)와 구분되지 않아 재현 도구에서 명령이 쪼개진다 | **지사 연락처를 실제로 운영에 쓰기 시작한 뒤 첫 변경 요청 시** | **미수신(2026-08-16 등재)** | 선재 규약이라 #127이 만든 결함이 아니지만, 공개 노출 필드가 생기면서 **틀린 번호를 못 지우는** 형태로 심각도가 올라갔다. DB 직접 수정이 우회로라 즉시 차단성은 없다 |
| 135 | **T-W2-31 구현자 발견**(2026-08-16) — **`NOT_WIRED` 취소 엣지 10건의 사유가 하나의 뭉뚱그린 문장이라 같은 종류의 거짓이 더 잠복해 있을 수 있다.** T-W2-31이 `uploaded→canceled`를 구동시키며 그 항의 사유(`'cancel() 실재 — 이 from 상태를 밟는 단위 테스트만 없다.'`)가 **거짓**임을 발견했다(무주 콘텐츠라 `cancel()`의 `requireOwnerOrCenter`를 애초에 통과 불가 = `untested`가 아니라 사실상 미구동). 그런데 이 사유는 취소 11건을 **한 문장으로 묶어** 쓴 것이라, 나머지 10건 각각에 대해 그 진술이 참인지 **아무도 검증하지 않았다**. `kind`가 `untested`↔`unimplemented`로 잘못 분류돼 있으면 `AUTO_PROGRESS_CONTENT_STATUSES` 파생(=`unimplemented`만 소비)이 바뀌어 **UI가 정상 콘텐츠에 거짓 경보를 띄우거나 그 반대**가 된다 | `packages/shared/src/content/not-wired.ts`(취소 10건 사유 개별 검증·분해 — **SOLO**) | 조율자 실측 2026-08-16: 취소 엣지 10건이 단일 주석 블록 1문장을 공유. 해소 후 각 엣지의 도달 가능 액터·경로가 개별 기재 | **다음 `packages/shared` SOLO 웨이브** — `kind` 오분류는 UI 파생을 바꾸므로 방치 시 조용히 잘못된 화면이 된다 | **미수신(2026-08-16 등재)** | 대장 #125의 **3회차**다(①"api src 참조 0건"→T-W2-10이 반증 ②"제품 액션 0건"→T-W2-32가 반증 ③본 항). 세 번 모두 **멤버십 판정은 정확했고 사유 문구만 거짓**이었다 — 자동 검증이 닿지 않는 영역이 반복해서 틀리고 있다는 뜻이며, 개별 정정이 아니라 **사유 작성 규칙**(전수 주장 금지·엣지별 개별 기재)이 근본 대책이다 |
| 136 | **T-W2-34 구현자 발견**(2026-08-16) — **주민 제보 콘텐츠의 제목·분류를 고칠 수 있는 액터가 0명이다.** `resident-links.service.ts:311`이 *"제목·분류는 검수 때 지사 담당자가 확정(03 §C-5)"*이라 적어 두었으나, 그 콘텐츠는 `reporterId=null`이라 **어떤 기자도 `loadOwned`를 통과하지 못하고**, 센터는 `update()`에서 `targetChannelAccountIds`만 허용되며, 상태도 `uploaded`라 `EDITABLE_STATUSES` 밖이다 → **경로가 0개**. 주민이 대충 적은 제목이 그대로 송출된다 | `services/api/src/contents/**`(자막과 같은 방식으로 메타 편집 경로 확장 — T-W2-34의 `PATCH /:id/captions` 구조 답습) 또는 `resident-links/**`(검수 시 확정) | 조율자 실측 2026-08-16: 주민 제보 Content(`reporterId=null`, `uploaded`)에 제목·분류를 쓸 수 있는 엔드포인트 **0건**. 해소 후 ≥1 | **주민 링크 실사용 시**(제보가 실제로 들어오기 시작하는 시점) | **미수신(2026-08-16 등재)** | 대장 #123과 **정확히 같은 형태**다 — 정본이 "지사 담당자가 확정한다"고 약속했는데 서버에 그 경로가 없다. T-W2-34는 자막만 열었고 제목·분류는 손대지 않았다. **자막 경로를 그대로 확장하면 저비용**이며, 액터 판정(`loadReadable`의 지사 경계)도 이미 검증돼 있다. **정본 범위 명확화 완료(2026-08-16)** — 03 §C-5가 "분류·자막"만 적어 제목이 빠져 있었는데 서버가 기본 제목(`RESIDENT_UPLOAD_DEFAULT_TITLE`)을 넣으므로 **제목도 확정 대상**임을 정본에 반영하고, 구현 현황(자막만 경로 존재·제목/분류는 액터 0명)을 같은 자리에 적었다. api 주석의 "제목·분류는 검수 때 확정(03 §C-5)"은 인용이 부정확했으나 **취지는 맞았고 이제 정본이 그것을 담는다** |
| 137 | **T-W2-34 구현자 발견**(2026-08-16) — **`mode_selected`의 payload 값이 타입으로 묶여 있지 않다.** api 롤업(`telemetry.service.ts`)이 `'simple'`/`'precise'` 리터럴로 분기하는데 shared 카탈로그에는 **payload 계약이 없어**, 앱(`apps/reporter/src/features/contents/mode.ts`)의 값과 서버 분기가 **한쪽만 바뀌면 조용히 집계가 0**이 된다. T-W2-29가 이벤트 **이름**은 계약으로 묶었으나 **값**은 남았다 | `packages/shared/src/telemetry/telemetry-event.ts`(이벤트별 payload 계약 — **SOLO**) + api 롤업 + reporter 로컬 상수 제거 | 조율자 실측 2026-08-16: `grep -c "simple" packages/shared/src/telemetry/telemetry-event.ts` → **0**. ⚠️ **2026-08-23 이 명령은 오검출한다** — 36행 **주석**이 세어져 **1**이 나온다(#114·#134와 동형). 교체: `grep -v '^\s*\*' packages/shared/src/telemetry/telemetry-event.ts \| grep -c "simple"` → **0**(2026-08-23 실측). 해소 후 ≥1 | **03 KPI("간단 모드 채택률") 판정 개시 전** — 값이 어긋나면 채택률이 0으로 나오는데 그것이 "아무도 간단 모드를 안 쓴다"와 구별되지 않는다 | **미수신(2026-08-16 등재)** | 대장 #128의 **잔여분**이다(이름은 닫혔고 값이 남았다). `playback_progress`의 `percent` 마일스톤도 같은 성질이라 함께 판정할 것. 현재는 양쪽에 상호 참조 주석을 달아 두었다 |
| 138 | **T-W2-33 구현자 발견**(2026-08-16) — **`MEDIA_PUBLIC_BASE_URL`을 켜도 `CF_ZONE_ID`·`CF_API_TOKEN`이 없으면 비공개 전환 24시간 SLA가 실효하지 않는다.** 보관 시 캐시 퍼지가 `attempted:false` no-op이 되어 **엣지에 1년(`s-maxage=31536000`) 캐시된 사본이 그대로 살아 있다**. T-W2-33이 엣지 TTL을 길게 잡은 근거가 "purge로 강제 무효화 가능"인데, 그 전제가 env로만 성립하고 코드가 강제하지 않는다 | `services/api/src/config/env.schema.ts`(`MEDIA_PUBLIC_BASE_URL` 설정 시 CF 키 2종 동반을 부팅 refine으로 강제) 또는 운영 체크리스트 | 조율자 실측 2026-08-16: 공개 서빙 ON + CF 키 미설정 조합이 부팅에서 거부되지 않는다. 해소 후 거부 또는 경고 | **`MEDIA_PUBLIC_BASE_URL` 설정 직전** — #129와 동일 시점의 **짝 조건**이다 | **미수신(2026-08-16 등재)** | 구현자가 문서 경고로만 남기고 코드 강제는 env 계약 변경이라 보류했다. 06 삭제 절차 SLA와 직결되므로 **운영 문서가 아니라 부팅 게이트**가 맞다는 판단이면 SOLO 슬롯 필요 |
| 139 | **T-W2-33 구현자 발견**(2026-08-16, 저심각도) — **공개 복사 대상 자산 선택 규칙이 코드 사본 2벌**이다. `PublicMediaService.selectPublicAssets`와 `FeedService`의 자산 선택(720p 렌디션 우선, 없으면 최신 + 최신 썸네일)이 각각 구현돼 있고 주석으로만 "의도적 동기화"라 묶여 있다. 어긋나면 **복사한 것과 피드가 요구하는 것이 달라져** 공개 URL이 있어도 못 쓰거나, 지운 줄 알았는데 다른 렌디션이 남는다 | `services/api/src/media/**`(공통 selector 추출) | 조율자 실측 2026-08-16: 두 파일에 같은 선택 규칙이 각각 존재 | **렌디션 프로파일이 늘어날 때**(현재 720p 단일이라 두 사본이 우연히 일치) | **미수신(2026-08-16 등재)** | 지금은 규칙이 단순해 드러나지 않는다. HLS 패키징·다중 렌디션이 들어오는 순간 어긋난다 |
| 140 | **T-W1-04 구현자 발견 + 조율자 재현**(2026-08-16) — **정본 02 §D-T5 서비스워커 갱신 정책 2번이 한 문장 안에서 모순이고, 4번은 `/sw.js`를 퍼지 대상에서 빠뜨렸으며, nginx가 `/sw.js`에 1년 immutable을 건다**(3중 결함이 같은 자산 하나를 향한다). ⓐ 2번 원문 *"신규 SW 감지 시 `skipWaiting()`+`clients.claim()`으로 **대기 없이 즉시 활성화**하고 … **자동 강제 새로고침은 금지**"* — install 시점 무조건 `skipWaiting`은 `clients.claim()`으로 **구 번들 실행 중인 탭까지 제어**해 버전 스큐를 만들고, 신 SW의 `activate`가 구 캐시를 지우면 그 탭이 참조하던 해시 자산이 사라진다. "즉시 활성화"와 "사용자 트리거 재로드"는 **함께 성립 불가** ⓑ 4번의 퍼지 대상이 "엔트리 HTML·매니페스트"뿐인데, **`/sw.js`는 스코프 제약상 루트 고정 이름이라 해시 캐시버스팅(1번)의 보호를 못 받는 유일한 자산**이다 → 4번이 유일 방어선인데 대상에서 빠졌다 ⓒ `infra/docker/nginx.conf`의 `\.js$` immutable 규칙이 `/sw.js`를 삼켜, **sw.js 내용만 바뀌는 배포**(앱 번들 해시 불변)에서 엣지가 구본을 1년간 서빙한다 | `docs/plan/02-web-architecture.md` §D-T5 2·4항(정본 정정) + `infra/docker/nginx.conf`(구독자 vhost 2곳에 `location = /sw.js` 예외) | 조율자 실측 2026-08-16: nginx 컨테이너 실기동 후 `curl -I /sw.js` → 정정 전 `immutable`, 정정 후 **`no-cache`**(해시 번들 대조군은 `immutable` 유지 = 무회귀) | **T-W1-04 착수와 동시** — 구현이 정본을 그대로 따랐다면 버전 스큐가 프로덕션에 들어갔다 | **수신 완료(2026-08-16 — T-W1-04, Wave 8b. 사용자 지시로 정본 수정 권한 행사)** — ⓐ 2번을 "사용자 확인 후 활성화"로 정정하고 사실 정정 블록에 버전 스큐 근거를 남김(원문 보존) ⓑ 4번 퍼지 대상에 `/sw.js` 추가 + reporter·center vhost는 **T-W3-02 동반 소유**로 명시 ⓒ nginx 구독자 vhost 2곳(터널·watch.)에 `location = /sw.js` exact 매칭 예외(nginx 우선순위상 `=`가 정규식보다 먼저). 구현은 `skipWaiting`을 사용자 확인 뒤로 옮기고 `install` 시점 자동 적용을 되살리는 뮤테이션을 테스트로 고정 | **정본 자신이 틀린 두 번째 사례**(첫 번째는 03 §C-4). #123이 "정본이 빠뜨린 것"이었다면 본 항은 **"정본이 적은 것이 서로 모순"**이다 — 구현자가 그대로 따랐다면 게이트 전체가 통과한 채 프로덕션에 들어갔을 결함이다. ⇒ **정본 문언은 착수 전 확인의 대상이며, 모순이면 고친다** |
| 141 | **T-W1-04 구현자 발견**(2026-08-16) — **Workbox 툴체인 전량 도입이 불가능하다. 계획의 암묵 전제가 성립하지 않는다.** E2가 `public/sw.js`를 "Workbox 설정"이라 적었으나, `infra/docker/Dockerfile.web`이 `expo export`를 **직접 호출**해 `workbox-build`(`injectManifest`/`generateSW`) 후처리를 끼워 넣을 단계가 없고, CDN `importScripts`는 오프라인·CSP를 깬다. → T-W1-04는 **`workbox-window` 1개만** 도입하고 `sw.js`는 프리캐시 매니페스트가 필요 없는 **런타임 캐싱 전용**으로 직접 작성했다 | `docs/plan/02-web-architecture.md` §B(경계 명시 — **완료**) + `infra/docker/Dockerfile.web`(빌드 후처리 단계를 넣을지 판단) + E2 T-W3-02 산출물란 | 조율자 실측 2026-08-16: `grep -c "workbox-build" package.json apps/*/package.json` → **0**(window 패키지만 도입) | **T-W3-02(Wave 17a) 착수 전** — 그 태스크가 같은 제약을 물려받는다 | **미수신(2026-08-16 등재)** | 즉시 차단성은 없다(런타임 캐싱만으로 §D-T5 1~3항이 충족된다). 다만 **오프라인 프리캐시가 필요해지는 시점**에는 빌드 후처리 단계가 선결이며, T-W3-02가 "T-W1-04와 동형"이라는 계획 문언만 보고 착수하면 같은 벽에 부딪힌다. §B에 경계를 적어 뒀다 |
| 142 | **T-W1-04 구현자 관찰**(2026-08-16, 정보성) — **서비스워커만으로는 PWA가 되지 않는다. 02 §E-18 installability 게이트는 W1 시점에 구조적으로 통과 불가다.** `manifest.json` 3종은 **T-W3-02(Wave 17a) 소유**라 구독자 앱에 아직 없다 → 설치 가능 조건(manifest + SW + HTTPS) 중 하나가 W3까지 비어 있다 | 판정 문서 쪽 문제 — `docs/plan/02-web-architecture.md` §C installability 행 또는 E5 게이트 판정 절차에 "W3 이전 미달은 정상"임을 명시 | 조율자 실측 2026-08-16: `ls apps/subscriber/web/manifest.json` → 부재(T-W3-02 산출물) | **W1·W2 DoD 판정 시** — 게이트를 기계적으로 돌리면 FAIL로 잡히는데 그것이 결함이 아니다 | **미수신(2026-08-16 등재)** | 계획 순서 자체는 문제가 없다(SW를 W1에, manifest를 W3에 둔 것은 의도된 배치). **판정 시 오해를 막는 문언만 필요**하다 — 대장 #122(CI 린트 누락)처럼 "장치가 아무것도 안 말해 주는" 유형이 아니라 "장치가 말하는 것을 사람이 오독할" 유형이다 |
| 143 | **T-W1-11c 구현자 발견 + 조율자 재현**(2026-08-16) — **배포 경로가 구조적으로 성립하지 않았다.** `infra/docker/docker-compose.xeon.yml`의 `web` 서비스에 **`image:` 키가 없어** `docker compose pull web`이 "빌드 대상"이라며 건너뛰고 `up`이 **제온에서 재빌드**를 시도한다 — DCP 파이프라인과 CPU를 공유하는 호스트에서 금지 사항이고(대장 #78 계열), `Dockerfile.web` 주석이 T-W0-03 시점에 선언한 *"CI가 만든 이미지를 `pull && up`으로 배포"* 가 **실체와 어긋나** 있었다. 배포 워크플로가 GHCR에 이미지를 올려도 제온이 그것을 쓰지 않는다 | `infra/docker/docker-compose.xeon.yml`(`image:` 1줄) | 조율자 실측 2026-08-16: `python3 -c "import yaml; print(yaml.safe_load(open('infra/docker/docker-compose.xeon.yml'))['services']['web'].get('image'))"` → 정정 전 `None`, 정정 후 `${WEB_IMAGE:-gachinol-prod-web}` | **실배포 게이트 활성화 직전** — 켜는 순간 발현한다 | **수신 완료(2026-08-16 — 조율자 직접 수정, Wave 8c)** — `image: ${WEB_IMAGE:-gachinol-prod-web}` 1줄. **기본값이 compose 자동 생성 태그(프로젝트명 `gachinol-prod` + 서비스명 `web`)와 동일**해 기존 로컬·제온 빌드 흐름은 무회귀이고, CI는 `WEB_IMAGE`로 GHCR 태그를 주입한다(양쪽 `docker compose config`로 치환 실증). 워크플로는 이미 `WEB_IMAGE`를 주입하도록 작성돼 있었다 | **선언과 실체의 괴리를 주석이 감췄다** — `Dockerfile.web` 주석이 배포 방식을 확정적으로 서술해 후속 작업자가 그것을 구현의 증거로 읽을 수 있었다([[contract-wiring-gate]] "주석을 불변식으로 오독" 변종). 워크플로를 만드는 태스크가 그 경로를 끝까지 따라가 보지 않았다면 **실배포 당일에 발견**됐을 것이다 |
| 144 | **T-W1-11c 구현자 발견**(2026-08-16) — **계획이 "번들 예산 게이트"를 두 태스크에 이중 배정했다.** E2가 T-W1-11b(`ci.yml` 3종 게이트 중 하나)와 T-W1-11c(`deploy-web.yml` 4스텝 중 하나) **양쪽에** 같은 게이트를 넣어 뒀다. 두 곳에 예산 수치가 박히면 서로 어긋나고, 어느 쪽이 정본인지 알 수 없게 된다 | `docs/plan/exec/E2-work-breakdown.md`(T-W1-11b 또는 T-W1-11c 산출물란에서 중복 제거) — 조율자 판정 | 조율자 실측 2026-08-16: E2에서 두 태스크 행 모두 "번들 예산 게이트" 문구 보유 | **T-W1-11b(Wave 13b) 착수 전** | **미수신(2026-08-16 등재)** | 즉시 차단성은 없다 — T-W1-11c가 예산 수치를 `infra/scripts/bundle-budget.mjs` **한 곳**에만 두고 `--dist <앱>=<경로>` 인자로 **재사용 가능하게** 만들어 뒀으므로, T-W1-11b는 같은 스크립트를 호출하기만 하면 된다(수치 이중화는 이미 구조적으로 막혔다). 남은 판단은 **게이트를 두 워크플로에 다 둘지, 하나로 합칠지**뿐이다 |
| 145 | **T-W1-11c 구현자 발견**(2026-08-16, 보안·정보노출) — **`_sitemap.html`이 3앱 프로덕션 산출물에 포함된다.** expo-router가 자동 생성하며, **기자·관제 앱의 전 라우트 목록이 익명에게 노출**된다(그 두 앱은 내부 사용자용이다). 현재 기자·관제 웹은 인터넷 미개방이라 즉시 노출은 없으나, 도메인 확정 후 서브도메인 vhost가 정규 경로가 되면 그대로 열린다 | `apps/{reporter,control-center}/app.config.ts`(sitemap 비활성) 또는 `infra/docker/nginx.conf`(해당 경로 차단) 또는 `Dockerfile.web`(산출물에서 제거) — 어느 계층인지가 판단 | 조율자 실측 2026-08-16: `expo export` 산출물에 `_sitemap.html` 존재(3앱) | **기자·관제 웹 인터넷 개방 시**(도메인 확정 후 서브도메인 vhost 정규화 시점) | **미수신(2026-08-16 등재)** | 구독자 앱은 애초에 전체 공개라 무해하다. **기자·관제만 대상**이며, 라우트 이름 자체가 기밀은 아니나 공격 표면 정찰을 돕는다. 07 보안 문서와 함께 판정할 것 |
| 146 | **T-W1-10 구현자 발견 + 조율자 재현**(2026-08-17) — **`EXPO_PUBLIC_*` 값이 어떤 배포 경로로도 번들에 들어가지 않는다.** `infra/docker/Dockerfile.web`과 `.github/workflows/deploy-web.yml`이 **`EXPO_PUBLIC_API_URL` 하나만** build-arg로 전달한다. `.env.example`에는 등재돼 있어 **설정하면 되는 것처럼 보이는데** 빌드가 그 값을 인라인하지 않는다 → **T-W2-28(#127)이 만든 "서버 값 우선 · env 폴백"의 폴백이 죽어 있었다**(`EXPO_PUBLIC_SUPPORT_TEL`·`EXPO_PUBLIC_LIVE_YOUTUBE_URL`). T-W1-09·T-W1-10이 추가한 4건도 같은 운명이었을 것 | `infra/docker/Dockerfile.web`(ARG/ENV) + `.github/workflows/deploy-web.yml`(build-args) + `.env.example`(안내) — **세 곳이 짝** | 조율자 실측 2026-08-17: 정정 전 `grep -c "^ARG EXPO_PUBLIC" infra/docker/Dockerfile.web` → **1**, 정정 후 **7**. 인라인 실증: `EXPO_PUBLIC_SUPPORT_TEL=064-999-1234`로 export 후 번들에서 그 문자열 검출 | **env 기반 기능을 실제로 쓰는 시점** — #127 폴백·문의하기·편성표가 전부 이 경로에 걸린다 | **수신 완료(2026-08-17 — 조율자 직접 수정, Wave 9)** — ARG/ENV 6키 추가 + build-args 멀티라인 확장(2지점) + `.env.example`에 **"세 곳을 함께 거쳐야 한다"**는 규칙 명문화. 기본값은 **의도적으로 비워 둔다**(앱이 빈 값을 "미설정"으로 보고 UI를 숨기는 구조라 가짜 기본값은 죽은 버튼을 만든다) | **`.env.example` 등재가 "배선됨"의 증거가 아니다** — 이 리포가 반복해 온 "계약은 있는데 구동부가 없다"의 **인프라판**이다([[contract-wiring-gate]]). 신규 EXPO_PUBLIC 키 도입 태스크는 세 파일을 함께 갱신해야 하며 그 규칙을 세 파일 주석에 상호 참조로 박았다 |
| 147 | **T-W2-09 구현자 발견**(2026-08-17) — **주민 임시 업로드 링크를 발급할 UI가 어디에도 없다.** `POST /v1/resident-links`는 `@Roles('reporter','admin')`으로 구동 중인데, `apps/reporter`에는 **검수 화면만**(`app/(app)/resident-uploads/*`, T-W2-25b) 있고 발급 화면이 **0건**이다. 지사 담당자가 Swagger·curl 없이는 링크를 만들 수 없어 **03 §C-5 주민 공급 경로 전체가 실사용 불가**다. T-W2-09가 소비 화면을 완성해도 그 링크를 만들 사람이 없다 | `apps/reporter`(링크 발급 화면 — 검수 화면과 같은 자리) | 조율자 실측 2026-08-17: `grep -rn "resident-links" apps --include='*.ts' --include='*.tsx' \| grep -v __tests__ \| grep -c "POST"` → **0**(발급 호출 0건). 해소 후 ≥1 | **주민 링크 실사용 시작 전** — 지금은 발급 자체가 불가능하다 | **태스크 채번 완료 — T-W2-35(미착수, 2026-08-17)** — 사용자 지시: *"해결할 수 있도록 작업목록에 포함시켜 두고 추후 구현하자."* ⇒ E2 §C에 **T-W2-35(기자 웹 주민 링크 발급 화면)** 신설 등재. 배치는 T-W2-02·03(PoC 게이트 대기)과 무관하므로 **다음 여유 웨이브에 투입 가능**하다 | 대장 #100(지사 부활 버튼)·#130(동의 확인 UI)과 **같은 계열의 8회차**다 — 서버는 완비인데 그것을 부르는 화면이 없다. **소비 화면(T-W2-09)을 만들면서 발급 화면이 없다는 것이 드러난 구조**이며, 태스크 경계를 "사용자가 일을 끝낼 수 있는가"로 긋는 규율(#34 ②)의 적용 대상이었다 |
| 148 | **T-W2-09 구현자 발견**(2026-08-17) — **스토리지 CORS가 설정돼 있지 않아 웹 업로드가 실배포에서 실패할 수 있다.** 브라우저의 presigned PUT은 프리플라이트(OPTIONS)를 거치므로 R2/MinIO 버킷에 CORS(허용 오리진 · `PUT` 메서드 · `Content-Type` 헤더)가 필요하다. 기자 **네이티브** 앱 경로(`expo-file-system`)에는 프리플라이트가 없어 지금까지 설정될 이유가 없었다 — **웹 피벗이 만든 신규 요구사항**이다 | `infra/**`(MinIO CORS) + `docs/infrastructure.md`(R2 버킷 CORS 전제 문서화) | 조율자 실측 2026-08-17: `grep -rn "cors\|CORS" infra/ --include='*.yml' --include='*.md' \| grep -ic "s3\|minio\|r2"` → **0** | **웹 업로드 실배포 직전** — T-W2-09(주민)·T-W2-02(기자) 어느 쪽이든 웹에서 처음 업로드하는 순간 | **문서화 완료(2026-08-17) — 실설정은 R2 계정 확보 후** — 사용자 질의로 원인·증상을 규명하고 `docs/infrastructure.md` **§4-C-2**를 신설했다: 왜 필요한지(presigned PUT은 교차 오리진이라 프리플라이트를 거친다) · 왜 지금까지 없었는지(**네이티브에는 CORS 개념이 없다 — 웹 피벗이 만든 신규 요구사항**) · 왜 개발 중엔 안 보이는지(**로컬 MinIO는 어떤 오리진이든 허용**, 조율자 `curl OPTIONS` 실측으로 확인) · 증상(**서버 로그에 흔적 0** — 요청이 도달하기 전에 브라우저가 막는다) + **R2 설정 체크리스트 5항목** + 설정 후 검증 `curl` 명령. **코드로 닫을 수 있는 항이 아니다**(Cloudflare 대시보드 설정) — R2 계정이 없어 지금은 설정 자체가 불가능하며, 도메인 확정(G9 ①) 시 오리진 값과 함께 확정한다 | 로컬 MinIO는 기본 CORS가 관대해 개발 중에는 드러나지 않는다 — **켜는 순간 실패하는** #129 계열이다. 02 §D-T4가 "R2·MinIO CORS 설정도 별도 필요"라고 이미 적어 두었으나 **그것을 수행하는 태스크가 계획에 없다** |
| 149 | **T-W1-10 구현자 발견**(2026-08-17) — **방송 시작 시각이 어느 정본에도 없어 04 §A SLO#2가 측정 불가다.** 01 §C-5 편성표는 요일·주체·회당 2시간까지만 정하고 **시:분이 없다**. 그런데 04 §A SLO#2는 *"편성 시각 기준 5분 이내"*를 판정 기준으로 삼는다 → 기준점이 정의되지 않아 SLO가 성립하지 않는다 | `docs/plan/01-product-strategy.md` §C-5(편성 시각 확정 — **제품 결정이라 사용자 판단 필요**) + 04 §A SLO#2(측정 기준 명확화) | 조율자 실측 2026-08-17: 01 §C-5 편성표에 시:분 표기 **0건** | **04 §A SLO 판정 개시 전** 또는 **첫 정규 방송 편성 시** | **사용자 확인 완료(2026-08-17) — 미정 유지, 확정 시 반영** | 사용자 답변: *"방송시작시각은 아직 정해지지 않았다. 미정인 상태로 기록해두고, 정해지면 알려주겠다."* ⇒ **정본 01 §C-5에 "시각 미정 + 확정 주체 = 사용자 + 04 §A SLO#2는 그때까지 판정 불능"을 명기했다**(2026-08-17). 확정 시 갱신할 곳 3군데를 그 자리에 함께 적어 뒀다: ⓐ 01 §C-5 편성표에 시:분 ⓑ 04 §A SLO#2 측정 기준 개방 ⓒ `apps/subscriber/app/schedule.tsx` 화면 문구. ⓒ는 **편성표에 시:분이 들어가면 실패하는 테스트**가 걸려 있어 빠뜨리면 CI가 잡는다 — 미정 상태가 조용히 방치되지 않도록 한 장치다 | T-W1-10은 지어내지 않고 화면에 "시작 시각은 방송 전에 카카오톡 채널로 알려 드립니다"로 표기하고, **편성표에 시:분이 들어가면 실패하는 테스트**를 걸어 두었다(정본이 시각을 소유하면 그 테스트를 함께 고친다). 정본을 고칠 권한은 있으나 **방송 시각은 제품 결정**이라 조율자가 임의로 정하지 않는다 |
| 150 | **조율자 실측**(2026-08-17, 저심각도·개발 함정) — **Metro 캐시가 `EXPO_PUBLIC_*` 변경을 반영하지 않는다.** `expo export`를 `--clear` 없이 반복하면 env 값을 바꿔도 **이전 번들의 값이 그대로 남는다**(실측: 같은 명령이 `--clear` 없이는 검출 0건, 붙이면 검출). #146을 검증하다 실제로 밟았고, 하마터면 "build-arg 추가가 동작하지 않는다"고 오판할 뻔했다 | `docs/` 개발 안내 또는 `apps/*/README` — 재현 절차에 `--clear` 명시 | 조율자 실측 2026-08-17: `EXPO_PUBLIC_SUPPORT_TEL=064-999-1234 expo export` → 번들 검출 0건 / 같은 명령 `--clear` → 검출 1건 | **env 관련 실측을 할 때마다** | **미수신(2026-08-17 등재)** | **Docker 빌드는 안전하다** — `Dockerfile.web`의 cache mount는 `/pnpm/store`뿐이고 Metro 캐시는 컨테이너마다 새로 만들어지며, `ENV` 변경은 레이어 캐시를 무효화한다(조율자 확인). 영향은 **로컬 반복 검증**뿐이나, 그 오판이 인프라 결함 진단을 뒤집을 수 있어 등재한다 |
| 151 | **사용자 지시**(2026-08-17) — **`auto_edit`이 파이프라인 정식 단계로 승격됐으나 "무엇을 편집하는가"가 어느 정본에도 없다.** 사용자가 파이프라인을 *"업로드 → 제온 수신 → **자동편집** → 미리보기 반환 → 기자/센터 승인 → 송출"*으로 재확정했다. 종전에는 T-AI 병렬 트랙의 선택 항목이었다. 계약(`JobType.AutoEdit`·`MediaAssetKind.EditedMaster`·`regenerating` 3엣지)은 이미 있고 **구동부만 0건**(#98) — 즉 이 항은 #98의 **범위 정의**분이다 | ① `CLAUDE.md` §11·§12 + `docs/ROADMAP.md`(승격·범위 미정 명기 — **완료**) ② **범위 확정**(사용자 판단 대기) ③ 확정 후 구현 태스크 채번 | `CLAUDE.md` grep `범위 확정 전 착수 금지` · `docs/research/2026-08-17-xeon-local-llm.md` 존재 | **범위 확정 전 착수 금지** — 계약만 있고 구동부가 없는 상태를 임의 구현으로 메우지 않는다 | **PoC 완료 — 범위 권고 제출, 사용자 판단 대기(2026-08-17)** — ① 조사 보고(`docs/research/2026-08-17-xeon-local-llm.md`) 제출 ② 사용자 지시 *"PoC 진행해줘"* → **PoC 완주**(`docs/research/2026-08-17-auto-edit-poc.md`). 결과: **구간 판단은 로컬 LLM으로 불가**(4B 편집효과 0% · 14B 822초 + JSON 파싱 실패), 그러나 **기계 편집은 AI 0으로 8.7초 실증**. ⇒ 권고 = auto_edit을 **1층 기계편집(지금 구현 가능) / 2층 텍스트생성(4B 초안) / 3층 구간판단(보류)** 으로 분할 | **#123 계열의 재발이다** — "정본 자신이 빠뜨린 것"은 대장이 못 잡는다(규율 13). `auto_edit`은 **계약·주석·UI 문구·not-wired 등재까지 전부 갖췄으면서 정작 사양이 없다** — 구동부 부재(#98)보다 앞선 공백이다. 조사가 답한 것은 *"돌아가는가"*(예, 9 tok/s)이고 **답하지 못한 것은 *"쓸 만한 판단을 하는가"*** 로, 후자는 실 촬영본 PoC로만 확인된다 |
| 152 | **사용자 결정**(2026-08-17) — **송출 채널을 카카오톡 + YouTube 2채널로 확정.** 사용자 지시: *"송출되는 채널은 현재 정확히는 미정이나, 카카오톡, 유튜브가 유력하다."* 종전 정본은 라이브를 **YouTube + Facebook 동시**로 두고 **Meta App Review(리드타임 2~4주 + 팔로워 100↑·운영 60일↑)를 크리티컬 패스**에 올려 두었다 — 그런데 그 선행 요건이 **첫 방송 전에는 충족 자체가 불가능**해 순환 의존이었다 | `CLAUDE.md` §3-1·§7·§8 + `docs/infrastructure.md` §0·§5-2-1 + `docs/ROADMAP.md` Phase 5 | `docs/infrastructure.md` grep `5-2-1` · `CLAUDE.md` grep `YouTube 단독` | 즉시 | **수신 완료(2026-08-17 — 조율자 직접 반영, 4문서 5곳)** — 기존 §5-2 원문은 취소선으로 남기고 §5-2-1을 현행으로 신설(정정 이력 보존 규율 13) | **코드 변경 0건이다** — `meta`/`x`/`threads` 어댑터가 이미 **목 기본 + env 게이트** 구조라 채널 계정을 만들지 않으면 호출되지 않는다. 삭제하지 않고 보존하며, 재개 시 env만 연다. ⇒ 대장 **#101**(프롬프터 SNS 댓글 숨김 불가)의 판정 범위가 **YouTube 어댑터 한정**으로 축소됐다 |
| 153 | **조율자 판정**(2026-08-17, 스토리지 자체 호스팅 확정으로 부각) — **미디어 원본이 제온 1벌뿐이다.** `/srv/dcpwork/minio`가 유일 사본이고 오프사이트 백업이 없다. R2 전제일 때는 클라우드 제공자의 내구성에 기대고 있었으나, **자체 호스팅으로 확정되면서 3-2-1 원칙의 최소선(오프사이트 1벌)이 사라졌다.** NAS 3종은 infra §4-A ④ 주의에 따라 대체 저장소가 아니다(2종 읽기전용, 1종은 타 프로젝트 실자산 2.1TB 점유) | `docs/infrastructure.md` §4-C-0(문서화 완료) + **백업 수단 실구현**(수신처 미정 — 증설 HDD 2차 사본 / R2 콜드 스토리지 / NAS 신규 공유 중 택1) | 조율자 실측 2026-08-17: `ssh xeon 'df -h /srv/dcpwork'` → 사본 1개, 오프사이트 0 | **published 콘텐츠가 유의미하게 쌓이기 전** — 지금은 실증분 3건뿐이라 손실 비용이 낮다 | **해소·가동 중(2026-08-23)** — 사용자가 NAS 전용 공유 `gachinol-backup`을 만들고 제온에 마운트했다. systemd timer **등록 확인**(`systemctl list-timers` → 다음 실행 08-24 04:07 KST) + **첫 실행 성공**(567,719,832 bytes · 49MB/s · PG 덤프 17테이블 · 종료코드 0). 복구 리허설도 완료(복원본이 원본과 완전 일치). ⚠️ **오프사이트는 아니다** — NAS가 같은 건물이라 화재·도난·낙뢰는 못 막는다. R2 콜드는 별건으로 존속 | 자체 호스팅의 대가는 용량비가 아니라 **내구성 책임의 이전**이다. 촬영 원본은 재취득이 불가능하고(현장·시점 1회성), 07 §3-15 동의 체계상 재촬영도 간단치 않다 |
| 154 | **PoC 실측 발견**(2026-08-17) — **STT(Whisper) 환각이 자동편집의 최상류 리스크다.** 실기 촬영본 2건 중 **1건에서 반복 루프 환각**이 났다(`ggml-large-v3-turbo`, 기본 파라미터, 한국어): 동일 문장 *"한계의 모든 걸 고려하자!"* 가 30~64초에 **7회 연속** 생성됐다. 더 나쁜 것은 **4B·14B 어느 로컬 모델도 이를 전사 실패로 인지하지 못하고** 사실로 받아들여 제목·요약을 지어냈다는 점이다(프롬프트에 "알 수 없으면 '내용 불명'이라 적으라"는 규칙을 명시했음에도). 또 Whisper가 63.8초 영상에 `64.0s` 세그먼트를 내자 **두 모델 모두 그 값을 검증 없이 EDL에 승계**했다 — 전사 오차가 편집 오차로 직결된다 | `services/ai-worker`(실 STT 제공자 주입 시 환각 완화 필수: VAD 전처리 · `--entropy-thold`/`--temperature-inc` 튜닝 · 또는 RTZR API) + auto_edit 3층 착수 게이트 | 조율자 실측 2026-08-17: `whisper-cli -m ggml-large-v3-turbo.bin -f IMG_0049.wav -l ko -t 32` → 동일 문장 7회 반복. 정상 건(`IMG_0109`)은 68세그먼트·제주 방언 "보말"까지 정확 | **auto_edit 3층(구간 판단) 착수 전** — 입력 신뢰도가 없으면 어떤 모델을 붙여도 무의미하다 | **미수신(2026-08-17 등재)** — PoC 권고 §5-1의 **1번 조건**이며 나머지(메모리 증설·출력 제어·클라우드 위임)보다 **선행**한다 | 이 리포가 반복해 온 *"계약은 있는데 구동부가 없다"* 와는 다른 계열이다 — **구동부는 있는데 그 입력을 신뢰할 수 없는** 경우이며, 상류 품질 결함이 하류 전체를 무효화한다. 환각률은 **측정 대상으로 삼아야 한다**(표본 2건은 통계적 근거로 부족) |
| 155 | **T-W2-11 착수 전 확인 발견**(2026-08-21) — **라이브커머스 상품 카드가 렌더할 데이터의 출처를 정본도 E2도 정하지 않았다.** ⓐ `products` 테이블 부재(Prisma `product_ids`는 `FK 없음(커머스 미도입)`) ⓑ `LiveSessionPublic`에 상품 필드 0 → 구독자 앱이 상품을 받을 경로가 없음 ⓒ shared `Product`는 가격·재고·상태머신을 가진 **2단계(자체 결제)용**이라 **링크아웃의 본체인 외부 판매 URL 필드가 없다**. 계획된 3파일(구독자 앱)만 만들었다면 컴포넌트는 완성·테스트는 그린인데 **화면은 비었을 것** — Wave 8a 3건과 같은 "무해하나 무효" | **해소(2026-08-21, 같은 웨이브)** — 사용자 결정으로 `live_sessions.product_cards`(JSONB) 1컬럼 + shared `ProductCard` 계약 신설. 02 §E-19에 **사실 정정 블록** 기재(규율 13). 범위는 3파일 → shared·Prisma·api·구독자·관제 입력 UI로 확대(관제에 입력 수단이 없으면 규율 12 위반) | **#123 계열의 재발이다 — 대장은 "구현이 계약을 못 따라간 것"을 잡지 "계약 자신이 빠뜨린 것"은 못 잡는다.** 이번엔 규율 5(착수 전 계약 구동 확인)가 잡았다: `productIds`가 shared·Prisma·api 세 곳에 **문법적으로 존재**했으나 조회 대상이 없어 구동은 0이었다 — **타입 선언·컬럼 존재는 구동이 아니다**의 새 변종(계약이 2단계용으로 앞서 만들어져 1단계에 맞지 않는 경우) |
| 156 | **#122 착수 중 발견**(2026-08-21) — **CI가 PR #54부터 계속 실패 중이었고 아무도 보지 않았다.** `gh run list`가 main·feature 브랜치 연속 `failure`를 보여준다(#54·#55·**#56 포함**). 원인은 media-worker spec 7건의 `noUncheckedIndexedAccess` 위반(`const [m] = ...`이 `T｜undefined`)이며, **로컬에서 `pnpm test`만 돌리고 `pnpm typecheck` 전체를 돌리지 않아** 유입됐다. 같은 실수가 Wave 10에서 재발했다 — mapper 테스트를 추가한 뒤 `test`만 재실행하고 `typecheck`를 다시 돌리지 않아 `live.mapper.spec.ts` 2건이 같은 형태로 들어갔다 | **해소(2026-08-21, #122와 같은 PR)** — 9건 전건 수정 후 CI 3게이트를 로컬에서 동일 재현(typecheck 8/8·lint 8/8·test 7/7) | **#122와 같은 계열의 반대편이다**: #122는 "게이트가 있는데 검사하지 않음", 이건 "게이트가 실패하는데 아무도 보지 않음". 둘 다 초록불의 의미를 비운다. ⇒ **PR을 올린 뒤 CI 결과를 확인하는 절차가 없었다** — 머지 전 `gh run list` 확인을 규율로 세운다. ⚠️ 로컬 검증은 `test`만으로 부족하다. **CI가 도는 3게이트(lint·typecheck·test)를 그대로 돌릴 것** |
| 157 | **#122·#156 착수 중 발견**(2026-08-21) — **화면 렌더 테스트가 CI에서만 타임아웃으로 죽는다.** typecheck를 고쳐 test 게이트까지 도달하자 드러났다(그 전에는 typecheck가 먼저 막아 **가려져 있었다**): control-center `detail-screen.test.tsx` 1건 + reporter `resident-uploads/detail-screen.test.tsx` 1건이 `Exceeded timeout of 5000 ms`. **어설션 실패가 아니라 순수 시간 초과**다 | **해소(2026-08-21)** — 앱 3종 `jest.config.js`에 `testTimeout: 30000`. 무제한이 아니라 상한을 두어 진짜 무한대기는 계속 실패로 잡는다 | ★**원인은 테스트 로직이 아니라 트랜스파일 비용의 배분이다** — jest-expo가 react-native 트리를 변환하는 비용을 **그 앱의 첫 렌더 테스트 하나가 통째로 떠안는다**. 같은 테스트 실측: 로컬 warm **106ms** → 로컬 `--no-cache` **1,097ms**(10배) → CI **16,473ms**(거기서 다시 15배). **두 배율(캐시 부재 × 러너 성능)이 곱해진다.** ⇒ 로컬 통과는 CI 통과의 증거가 아니며, 특히 **캐시가 warm한 로컬에서 잰 시간은 CI를 예측하지 못한다**. 처음에 15초로 잡았다가 CI 실측(16.5초)보다 작아 되돌렸다 — **임계값은 실측 뒤에 정한다** |
| 158 | **#120 구현 중 자초한 회귀**(2026-08-22) — **형태가 다른 캐시가 같은 prefix를 공유하면 prefix 조회가 안전하지 않다.** 단건 조회 키를 `['resident-uploads','detail',id]`로 두어 기존 `all`(`['resident-uploads']`) prefix 아래 넣었더니, 목록 캐시를 훑는 `findResidentUploadInCache`가 **단건 객체를 `InfiniteData`로 오인**해 `TypeError: data.pages is not iterable`로 크래시했다. `useResidentUpload`가 그 함수를 시드용으로 호출하므로 **자기 자신의 캐시를 만나 죽는** 구조였다 | **해소(같은 PR)** — 목록 전용 앵커 `lists()` 신설 후 그 prefix로 좁히고, 형태 방어(`Array.isArray(data.pages)`)를 남겼다 | ★**이 결함을 잡은 것은 같은 PR에서 새로 쓴 "시드 없이 진입" 테스트다.** 기존 테스트는 전부 시드가 있어 크래시 경로를 밟지 않았고, 게다가 목에 `getResidentUpload`가 없어 **조회가 깨져도 `initialData`가 화면을 그려 통과**했다 — Wave 8a의 "분기를 통째로 지워도 그린"과 같은 형태다. ⇒ **신규 API는 테스트 목에 반드시 함께 등재**하고, 캐시 헬퍼는 `all`이 아니라 용도별 앵커로 조회한다 |
| 159 | **대장 재검증 실측**(2026-08-22, 사용자 지시) — **대장 자신이 세 가지로 부정확했다.** ① **상태 갱신 누락**: 코드에서 해소된 #98·#99가 대기 목록에 남아 있었다(규율 8이 실제로 어겨진 사례). ② **총계 재현 명령이 취약**: `grep -cE '^\| [0-9]+ \|.*미수신'`은 **행 전체**를 보므로 상태 칸이 아니라 본문·비고에 그 단어가 있기만 해도 세어진다 — 해소 문구에 "미수신으로 남아 있었다"라고 쓰자 즉시 오카운트됐다. ③ **재현 명령 부재 17건(35%)**: 48건 중 31건만 실행 가능한 명령을 갖고 있어 나머지는 매번 사람이 코드를 열어야 한다 | **①은 해소**(#98·#99 갱신 → 대기 48→46건) · **②는 정확한 명령으로 교체**(아래) · **③은 신규 등재 규율로** | ★**재현 명령을 임의로 바꾸면 판정이 뒤집힌다** — 조율자가 #117을 `sed '/Consent/,...'`로 넓게 잡아 `2`(해소)로 오판했으나, 대장 원문의 `awk '/async confirmMinorConsent/,/^  }$/'`로는 `0`(**미해소**)이다. 사용자에게 "해소"라고 보고한 뒤 정정했다. ⇒ **재현은 대장에 적힌 명령을 그대로 쓴다.** 정확한 총계는 **행 전체가 아니라 상태 칸(뒤에서 두 번째 열)만 추출해** 센다 — `awk -F' \| '`로 `$(NF-1)`을 뽑은 뒤 상태 문자열을 센다. ⚠️ **이 행이 그 결함을 스스로 입증했다**: 여기 명령 예시에 상태 문자열을 그대로 적었더니 이 행마저 대기 항목으로 세어져 총계가 46→47이 됐다. 그래서 예시에서 그 단어를 뺐다. ⇒ **카운트 대상 문자열을 본문에 쓰지 않는다** |
| 160 | **#153 착수 전 확인 발견**(2026-08-22) — **PG 백업 스크립트가 있는데 한 번도 돈 적이 없었다.** `infra/backup/pg-dump-to-r2.sh`(T-W0-04)는 작성·반입돼 있었으나 제온의 `crontab -l`·`systemctl list-timers` 어디에도 등록이 없었다(실측). 제온의 파일 타임스탬프는 Wave 10 배포 때 `git archive`로 반입된 시각이었다 | **해소(2026-08-22)** — `media-to-nas.sh`가 미디어와 PG를 함께 다루고 systemd timer로 등록된다. README의 마지막 단계를 **`systemctl list-timers` 등록 확인**으로 고정했다 | ★**배관 공백의 인프라판이다** — 코드에서는 "계약은 있는데 구동부가 없다"였고 여기서는 "스크립트는 있는데 스케줄이 없다"다. 둘 다 **산출물의 존재를 동작의 증거로 착각**한 것이며, 리포 문서에 "백업 파이프라인"이 완료 항목으로 적혀 있어 더 드러나지 않았다. ⇒ **인프라 항목도 "구동 확인"(규율 5)의 대상이다** |
| 161 | **PR #61 CI 확인에서 발견**(2026-08-23) — **프로덕션 이미지가 3일째 빌드되지 않고 있었다.** `build api`·`build media-worker`가 main에서 2026-08-21(PR #57 머지)부터 연속 failure. 원인은 #122 해소가 `@gachinol/config@workspace:*`를 두 서비스 devDependencies에 추가하면서 **두 Dockerfile의 매니페스트 복사 목록을 갱신하지 않은 것** — `pnpm install`(--prod 아님)이 workspace 링크를 해석하려면 그 패키지가 워크스페이스에 실재해야 하는데 없어 `ERR_PNPM_WORKSPACE_PKG_NOT_FOUND`로 죽었다. **배포 차단성**(이미지가 안 만들어져 제온 배포 경로가 막힌다) | **해소(2026-08-23 — fix/ci-dockerfile-config)** — `COPY packages/config/package.json` 1줄씩. **소스가 아니라 매니페스트만** 복사하면 되는 이유는 devDependency라 `deploy --prod` 트리에 안 들어가기 때문이다. 로컬 `docker build` 2건 실증 후 PR. 재현: `grep -c "packages/config" services/api/Dockerfile services/media-worker/Dockerfile` → 각 **2**(수정 전 0) | ★ **확립된 규율 2**("공용 패키지 신설 태스크는 그 패키지를 소비하는 컨테이너 빌드 경로도 함께 갱신")를 어겨서 생긴 회귀이고, **규율 15**(CI 확인)를 안 지켜 3개 PR 동안 안 보였다. 규율 15는 #156에서 신설됐는데 **그때 고친 것은 `node` 워크플로였고 `build-images`는 여전히 빨간 채였다** ⇒ **"CI를 고쳤다"는 워크플로 단위로 확인한다** (하나가 초록이어도 다른 하나가 빨갈 수 있다). 또한 이 건은 **로컬 `pnpm` 3게이트가 전부 초록인데도 컨테이너 빌드만 죽는** 형태라, 규율 15 ⓐ(로컬을 CI와 동일하게)로도 못 잡는다 — 컨테이너 경로는 별도 축이다 |
| 162 | **#115에서 분리 등재**(2026-08-23, 사용자 지시 — 원 등재는 2026-08-16 T-W2-23 게이트②) — **`origin='live_vod'` 콘텐츠에도 미성년자 플래그를 켤 액터가 없다.** 차단 경로는 #115와 동일하다(`createDraft`=기자 전용 · `update()`=센터·admin의 이 필드 수정 금지 + `loadOwned`의 `reporterId === user.id`, 그런데 `live_vod`는 **`reporterId=null`이 불변식**). 라이브 녹화본에 미성년자가 등장하면 게이트가 닿지 않는다 | `services/api/src/contents/contents.service.ts`(무주 콘텐츠에 대한 센터 쓰기 허용 — `update()` 완화 또는 전용 경로). **액터 판정은 자명하다**: `live_vod`는 `preview_generating→awaiting_center_review` **직행**이라(`policyGuard ②`) 센터가 반드시 검토하며, 확인 액터도 이미 센터 전용이다(02 §E-20) | `grep -c "live_vod" services/api/test/contents.e2e-spec.ts` → **0**(2026-08-23 실측). 해소 후 ≥1. ★ **e2e를 기준으로 잡은 이유**(규율 17): `contents/` 소스의 `live_vod`는 **이미 5곳**이라 소스 grep은 판정 불능이고, `update()` 완화든 전용 엔드포인트든 **어느 방식으로 고쳐도** "센터가 `live_vod` 콘텐츠의 플래그를 켤 수 있다"는 실 HTTP로 증명돼야 한다 | **#163 해소 후**(선행 부재 — 2026-08-23 착수 전 확인으로 판정 변경. 舊 "착수 가능 — 차단 없음"은 VOD 전환 경로가 존재한다는 **미확인 전제**였다) | **미수신(2026-08-23 분리 등재)** | ★ **분리 근거 — 07 정본 대조(2026-08-23)**: §3-15(무인증 주민 임시 업로드 링크의 법적 통제)는 **주민 링크만** 다루고 `live_vod`는 그 소관이 아니다. `live_vod`는 **촬영 주체가 방송국**이라 §3-3의 촬영 동의서 모델이 그대로 성립하며, #115가 "임의 구현 금지"인 이유(찍은 사람이 주민이라 동의서 체계가 성립하지 않음)가 여기엔 적용되지 않는다. ⇒ EXEC-DECISIONS #30의 금지는 `resident_link`에만 유효하다. **두 origin을 한 줄에 묶어 둔 탓에 이 구분이 일주일간 보이지 않았다.** ⚠️ **2026-08-23 착수 시 판정 정정**: 법무 차단이 없다는 판정은 유효하나 **착수 가능은 아니다** — 착수 전 확인(규율 5) 결과 **`live_vod` origin의 Content를 만드는 코드가 0**이었다(#163). 지금 구현하면 존재하지 않는 대상을 위해 쓰기 경로를 여는 것이라 **#155와 동형**이 되고, e2e도 정규 경로가 없어 인위적 픽스처를 써야 한다. **등재 시점에 규율 5를 적용했어야 했다** — 분리 판정(법무 축)은 맞았으나 착수 가능성(구동 축)을 확인하지 않았다 |
| 163 | **#162 착수 전 확인에서 발견**(2026-08-23, 규율 5) — **라이브 종료 → 녹화본 Content 전환 경로가 코드에도 계획에도 없다.** shared `live/live-session.ts`는 규약을 확정적으로 선언한다(*"종료 후 녹화본을 Content로 전환 시 연결. 해당 Content는 `origin='live_vod'`·`reporterId=null`로 `uploaded` 상태 진입"*)는데 ⓐ `origin: 'live_vod'`로 Content를 만드는 코드가 **0** ⓑ `vodContentId`는 mapper·fixture·타입에만 있고 **쓰기 경로가 0** ⓒ **E2 실행계획에 이 전환을 소유한 태스크가 없다**(VOD 언급은 전부 구독자 재생 화면 `watch/[id].tsx`이고, T-W2-15·16a는 CF Stream **Live Input 발급·webhook**이지 VOD 전환이 아니다). 유일한 `live_vod` 데이터는 `seed.ts`의 **합성 시드**이며 주석이 정직하게 밝히고 있다(*"합성 시드 — reporterId=null 불변식 충족(user 시드 불요)"*) — 피드 데모용으로 origin을 빌린 것이지 라이브 전환의 산물이 아니다 | `packages/shared/src/live/live-session.ts`(규약 원천) + **전환 로직을 소유할 태스크 신설**(E2 §C) — `services/api/src/live/*`(세션 종료 훅에서 Content 생성) 또는 별도 워커. **선행 판정 필요**: 녹화본 수급이 CF Stream 종속인지(T-W2-15는 T-NC-20 실계정 게이트에 막혀 있다) | `grep -rn "origin: 'live_vod'" services/api/src \| grep -v spec \| wc -l` → **0**(2026-08-23 실측). 해소 후 ≥1. ★ 이 명령은 **생성 경로의 실재**만 재며 구현 방식(세션 종료 훅·워커·수동 전환)을 전제하지 않는다(규율 17) | **#162 착수 전**(선행) · 라이브 실운용 개시 전 | **미수신(2026-08-23 등재)** | ★ **대장 #123·#155의 합집합 형태다** — #123("계약 자신이 빠뜨린 것은 대장이 못 잡는다")처럼 **정본에 태스크가 아예 없고**, #155("계약이 미래 단계용으로 앞서 만들어져 현재에 안 맞는다")처럼 **shared 규약만 먼저 서 있다**. 차이는 #155의 `productIds`가 *잘못된 모양*이었던 데 비해 여기 규약은 **모양이 옳은데 실현자가 없다**는 것이다. ⇒ **shared에 "~할 때 이렇게 된다"는 규약을 쓰면, 그 "~할 때"를 일으키는 태스크가 계획에 있는지 같은 스윕에서 확인해야 한다.** 이 항이 없으면 #162뿐 아니라 `live_vod`를 전제한 모든 분기(policyGuard ②·전이맵 `preview_generating→awaiting_center_review`)가 **영구 미구동**으로 남는다 |
| 164 | **T-W2-14 착수 확인에서 발견**(2026-08-26, 규율 13) — **03 §C-2-1의 "동의서 사진 첨부 UI"는 계약도 소유 태스크도 없다.** 03은 체크 시 "법정대리인 동의서를 촬영·첨부해 주세요 + 사진 첨부 UI"를 요구하지만 ⓐ 백엔드에 첨부를 받을 계약·저장 필드가 없다 — 구현된 데이터 모델은 **최소수집**(hasMinorSubject 불린 + 확인자·확인시각뿐, shared content.ts 주석 "아동 식별정보 컬럼 없음") ⓑ E2에 첨부 UI를 소유한 태스크가 없다(T-W2-14 DoD는 02 §E-20 "체크박스 입력 필드"뿐. 02 §B 신규파일 목록의 "동의서 첨부 입력 UI" 1파일은 E2 판정표 16행이 T-W2-14에 귀속시켰으나 그 태스크 행 산출물에는 없다 — 계획 내부 불일치) ⓒ **동의서 사진을 서버에 저장하는 것 자체가 수집 확대**라 07 법무 판단 사안이다(#115와 같은 결). T-W2-14는 03의 안내 문구까지만 구현하고 첨부 UI는 제외했다(계약 없는 입력 UI는 #96 계열 결함의 재생산) | 07 §3-3(법률자문 판정) → 판정에 따라 03 §C-2-1·02 §B 문언 정합화 **또는** 첨부 계약 신설(shared dto + api 저장·서명URL + 기자 앱) — 방식 판정이 선행 | `grep -c "사진 첨부 UI" docs/plan/03-accessibility-ux.md` → 등재 시 **1**. ⚠️ 이 명령은 03 문언의 존재만 재며 해소 방식을 전제하지 않는다(규율 17) — 첨부를 구현하는 쪽으로 해소되면 값이 1인 채 해소되므로, 판정은 이 행의 법무 기록 여부로 사람이 한다 | #115와 동일(07 §3-15 외부 법률자문 수신 시) — **임의 구현 금지**(EXEC-DECISIONS #30 결) | **미수신(2026-08-26 등재)** | 법률자문 수신 전에는 첨부 UI 태스크를 만들지 않는다. 수신 후 첫 조치는 구현이 아니라 **문언 정합화 방향 판정**이다 |
| 165 | **T-W2-14 착수 확인에서 발견**(2026-08-26, 규율 15) — **`Deploy Web` 워크플로가 도입(T-W1-11c, Wave 8c) 이래 성공 0회다.** "3일째 실패"가 아니라 **한 번도 초록인 적이 없다**(`gh run list --workflow deploy-web.yml` 성공 계수 0 실측). 직접 원인은 build 잡의 이미지 참조 조립: `github.repository_owner`가 원문 케이스(HomeDCP)인데 docker CLI는 저장소명 소문자를 요구해 Extract 스텝의 `docker create`가 `invalid reference format`으로 죽는다(metadata-action이 빌드한 실제 태그는 소문자 — 조립 참조만 어긋남). 잠복 결함 2호: job `outputs.image`가 env 컨텍스트를 직접 참조하는데 **job outputs에서 env는 평가되지 않아** deploy 잡의 `WEB_IMAGE`가 빈 값이 된다(Extract를 고쳐도 다음에 여기서 깨졌을 것). node·Build Images가 초록이라 아무도 몰랐다 — #161과 같은 "CI는 워크플로 단위로" 사각의 재현 | `.github/workflows/deploy-web.yml`(build 잡 — 소문자 참조 스텝 경유로 조립 교체) | `gh run list --workflow deploy-web.yml --json conclusion --jq '[.[] \| select(.conclusion=="success")] \| length'` → 등재 시 **0**. 해소 = **main에서 ≥1**(성공 실측이 본질 — 수정 방식 무전제, 규율 17) | 즉시 — 웹 배포 전면 단절 상태이며 T-W2-14 산출물도 이 경로로만 실서비스에 도달한다 | **수신 완료(2026-08-26 — PR #68, 등재 당일 해소)** — 재현 명령 실측: main 성공 계수 **0→2**(#68 머지분 50s 캐시·#67 머지분 — 후자는 번들 예산 게이트까지 신 코드로 완주). ⚠️ 두 런 모두 deploy·purge 잡은 **skipped**(preflight 시크릿 게이트 — `WEB_DEPLOY_*` 미설정, G9 ① 사용자 결정 대기와 일관): 이 "성공"은 빌드·예산 게이트까지이며 **제온 실배포 배선은 별개 잔여**다 | 번들 예산 게이트는 Extract보다 뒤라 **한 번도 실행된 적이 없다** — 핫픽스 후 게이트가 처음 실측되므로 초과 red가 나오면 그것은 회귀가 아니라 첫 측정이다 |
