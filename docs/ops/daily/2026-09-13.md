# 일간 2026-09-13 (일)

## ① 사용자 결정 대기 — 지금 필요한 것 **6건**

요청 시점 정본은 [QUEUE.md](../../plan/exec/QUEUE.md) "사용자 실행 항목" 절. 오늘 정본 갱신(커밋
`ed29eb5`)이 QUEUE §C-6②를 충족으로 갱신하고 §C-7을 신규 등재했다. 아래는 조율자 세션이 오늘
판단한 요청이며, scribe는 코드·CI·문서 상태만 재확인했다(판정은 하지 않는다).

1. **PR #102 머지** — `fix/upload-path-recovery`→`main`. scribe 재확인(`gh pr view 102 --json
   state,statusCheckRollup,mergeable`): `state: OPEN` · `mergeable: MERGEABLE` · 체크 5개 전부
   `SUCCESS`(`build api`·`node (lint·typecheck·test)`·`build media-worker`·`build ai-worker`·
   `ai-worker (ruff·pytest)`) + `preflight`·`deploy` 2개 `SKIPPED`(PR이라 배포 잡 정상 스킵). 머지되면
   main 자동배포로 대장 #213(4xx 관측성 로그)·#212(재시도 영구교착 수리)가 실가동한다.
2. **PR #103 머지** — `feat/multipart-upload`→`fix/upload-path-recovery`(스택 PR, #102 머지 후 base가
   자동으로 `main`이 된다). ⚠️ **scribe가 이 세션에서 처음 조회했을 때 체크 3개(`build api`·`node
   (lint·typecheck·test)`·`build web`)가 `IN_PROGRESS`였다** — 위임문의 "둘 다 CI 전건 통과"는 그
   시점 기준으로는 아직 부정확했다. 15초 간격으로 최대 9회 폴링해 완주를 기다린 뒤 재확인:
   `gh pr view 103 --json state,statusCheckRollup` → **8개 `SUCCESS` + 3개 `SKIPPED`**(`preflight`×2·
   `deploy`·`purge`), `state: OPEN`·`mergeable: MERGEABLE`. **지금은 전건 통과가 맞다** — 위임문
   수치를 그대로 옮긴 것이 아니라 이 세션이 재실행해 확정한 값이다.
3. **갇힌 콘텐츠 복구** — `01a09166-aac7-774e-b95b-5efc617d8a06`를 `uploading`→`upload_failed`.
   ⚠️ **#102 배포 후에** 할 것(먼저 풀면 수리 전 코드가 또 교착시킨다). scribe는 프로덕션 DB 접근
   권한이 없어 이 콘텐츠의 현재 상태를 독립 재확인하지 못했다 — **확인 불가**로 남긴다(아래 ④
   참조, PIVOT-PLAN 대장 #212 비고의 조율자 인용을 그대로 전달할 뿐이다).
4. **MinIO 라이프사이클**(대장 #219, QUEUE §C-7) — `AbortIncompleteMultipartUpload`. 요청 시점은
   **#103이 머지되어 멀티파트가 실제 트래픽을 받기 직전**. 지금은 고아 멀티파트 0건·디스크 1.8T
   여유(조율자 실측 인용, scribe는 제온 SSH·MinIO 자격증명 비접근이라 독립 재현 못함)라 급하지 않다.
5. ⚠️ **조율자가 프로덕션 DB에 SELECT조차 못 한다** — HANDOFF.md 원문 재확인(`docs/plan/exec/
   HANDOFF.md:641`): *"`ssh xeon ... psql ...`류 명령이 auto mode 분류기에 차단된다(이번 세션에서
   실제로 겪음)."* DB 실측이 필요할 때마다 사용자 실행이거나 권한 조정이 선행돼야 한다.
6. ⚠️ **주간 보고서가 한 건도 없다** — scribe 재확인: `ls docs/ops/weekly/` → `No such file or
   directory`(디렉터리 자체가 없다). 어제(2026-09-12, 토요일 — `python3 -c "import datetime;
   print(datetime.date(2026,9,12).strftime('%A'))"` → `Saturday`)분도 없다. CLAUDE.md §0-4가 토요일
   주간 보고서를 요구하므로 **규율 미준수 상태**이며 scribe가 이를 그대로 보고한다(이번 세션은
   일요일이라 일간만 생성한다). 사용자 결정: 소급 생성할지, 다음 토요일부터 시작할지.

## ② 오늘 무엇이 움직였나

### PR #101 머지 완료 (전날 작업의 완결, `daf16ef`)

`fix/media-reachability`가 오늘 새벽 머지됐다(`gh pr view 101 --json mergedAt,mergeCommit` →
`mergedAt: 2026-09-13T00:32:49Z`(09:32:49 KST) · `mergeCommit: daf16ef13f5f3cb73f75d8484a495f06027f7d17`).
어제(09-12) 일간보고서가 "커밋 `48cfa70`은 로컬에만 있다"고 남긴 다음 세션 예정 1번이 이걸로 완결됐다
(상세는 ⑥ 참조).

### "업로드 경로 복구" 슬라이스 — PR #102·#103 (조율자 세션, 오늘)

2026-09-12 실기 검증이 드러낸 5건 중 api 쪽 3건(#213·#212·#211)을 수리했다. QUEUE 사다리 판정:
**#213=L-obs(계측기) → #211=L1(도달 경로) → #212=L2(실발생)**. 셋을 "업로드 경로 복구" 한 슬라이스로
묶어 #213·#212를 #211의 **동반 선행**으로 처리했다(#211 해소 판정이 수동 실기인데 그 실기에서 4xx가
나면 로그가 없고, 재시도하면 #212의 교착을 밟기 때문).

- **PR #102**(`fix/upload-path-recovery`) — `a4a2d8a`(#213, 4xx 예외 관측성 로그) · `812a173`(#212,
  업로드 2차 재시도 영구교착 수리). scribe 재확인 커밋 존재: `git log --oneline origin/fix/upload-path-
  recovery` → 두 커밋 모두 확인됨.
- **PR #103**(`feat/multipart-upload`, base가 #102인 스택 PR) — `41f93c6`(#211 서버, 멀티파트 3라우트)
  · `9a086f3`(#211 클라, 기자 웹 분할 업로더) · `ed29eb5`(정본 갱신, 이 세션의 로컬 HEAD).

**수리 내용(PIVOT-PLAN 대장 원문 재확인, scribe는 코드를 직접 고치지 않는다)**:
- **#213**: 4xx 분기 4종(Zod·Domain·Prisma·HttpException)에 `warnClientError`(status·code·method·
  path) 배선. 5xx는 error+스택. 바디·헤더·쿠키·Authorization·쿼리스트링·zod issues·details는 로그
  제외. 동반 의무(D1)로 `all-exceptions.filter.spec.ts` 8건 신설(舊 0건).
- **#212**: 불변식 2개 — I-1(재발급이 실패 이력을 지운다: `createOriginalPending` upsert
  `update:{}`→`{status:'pending',...}`) · I-2(완료가 어떤 이유로 실패하든 콘텐츠가 `uploading`에
  안 남는다: 실패 분기 3종 전부 롤백). 동반 의무(D1)로 재시도 **연속 시퀀스** 테스트 신설(기존
  17건은 전부 단건 호출만 봐서 이 분기가 살아남았다).
- **#211**: 서버 멀티파트 3라우트 + 기자 웹 분할 업로더 + 파트 크기 상수를 `packages/shared`로
  단일 원천화(값이 어긋나면 큰 파일 마지막 파트가 다시 100MiB를 넘어 413이 재발하기 때문).

### 조율자 실측 — 대장 #211의 미확정 전제를 확정 (PIVOT-PLAN 대장 #211 원문 재확인)

프로덕션 경로에서 서명 없는 PUT(MinIO가 403으로 거부해 데이터 무영향)으로 이분 측정:
**95MB·99MB·100MiB(104,857,600B) 전부 403(=통과)** / **101MB·105MB는 413**. 확정된 것 3가지:
① 한도가 **Tunnel 경로에도 적용**된다(공식 문서가 침묵하던 공백) ② 경계는 정확히 100MiB이고 그
값 자체는 통과 ③ Cloudflare는 `Content-Length`만 보고 2.4MB 수신 시점 1.4초 만에 사전 거부한다
(끝까지 받은 뒤 자르지 않는다). 부가로 MinIO CORS가 `Etag`를 이미 expose함을 확인해 인프라 설정
변경이 불요임도 확정했다. scribe는 이 실측을 재실행하지 않았다(제온 프로덕션에 서명 없는 PUT을
날리는 부수효과 있는 명령이라 위임 범위 밖) — PIVOT-PLAN 대장 #211 원문을 그대로 인용했다.

### 게이트②가 실제로 잡아낸 것 (검증 분리가 작동한 증거, PIVOT-PLAN 대장 #216·#221 원문 재확인)

- **무반응 뮤테이션 2건**: ① #212 — `ISSUABLE`에 `'uploading'`을 넣는 가짜 수리(증상만 없애고
  원인은 남기며 상태머신 경계를 무너뜨린다)를 스위트가 못 잡았다 ② #211 — `Math.ceil`→`Math.floor`로
  바꾸면 173MB 파일의 마지막 파트가 109MB가 되어 한도를 그대로 초과하는데(#211 결함 자체의 재발)
  기존 단언이 전부 통과했다(나머지가 항상 마지막 파트로 흡수돼 `sum===total`이 항등이다). 둘 다
  보완됐다.
- **조율자 판정이 뒤집힌 것 2건**(양방향): ① admin 술어 비대칭 — 조율자는 "대칭이라 안전"이라
  했으나 verifier가 맞았다(발급과 완료를 같은 액터가 한다고 암묵 가정한 오류, 신규 채번 #216)
  ② 역할 게이트 "테스트 0건" — verifier가 틀렸다(`controller-role-gate.mjs`가 76라우트를 검사하고
  PASS다. jest 참조만 찾아 결론낸 것).

### 정본 갱신 (커밋 `ed29eb5`, 로컬 HEAD)

#213·#212·#211을 "미수신"→**"수리 완료·PR 대기"**로(⚠️ 해소 아님 — 셋 다 해소 판정이 수동 실기이고
배포 전이다) · **#169 부분 해소**(재생·썸네일 축만, QUEUE §C-6② 충족 — scribe 재확인: `curl -s
"https://api.bapfull.com/v1/feed?limit=1"` → `thumbnailUrl` 호스트가 `https://media.bapfull.com`,
업로드 축은 #211 실기가 남아 행 전체는 미해소) · **#214 수신 완료**(근거는 코드 존재가 아니라
`media-reachability.mjs`의 프로덕션 대상 실제 PASS) · **#216~#221 신규 채번 6건**(등재만, QUEUE
편입은 조율자 판단 대기 — scribe 재확인: `grep -n "#215\|#216\|#217\|#218\|#220\|#221"
docs/plan/exec/QUEUE.md` → **0건**, 아직 QUEUE에 편입되지 않았다) · QUEUE §C-6② 충족·§C-7 신설·
2단계 재배치(신규 2-1=#213·#212·#211, 舊 2-1=#177이 2-2로 밀림) · HANDOFF.md 새 절
("⭐⭐⭐⭐ 지금의 다음 1건", `:592`).

## ③ 막힌 것

- **대장 #211·#212·#213 — 셋 다 "수리 완료·PR 대기"일 뿐 해소가 아니다.** 해소 판정이 전부 수동
  실기(#211=한도 초과 실촬영 업로드 1건 완주, #212=1차 실패 후 2차 재시도 완주, #213=4xx를 실제
  유발해 컨테이너 로그에 남는지 확인)이고 배포 이후에만 실증 가능하다. **PR #102·#103 머지가
  선행돼야 한다.**
- **대장 #216(admin 술어 비대칭)** — `loadOwned`(admin 통과)와 `requireOwnerReporter`(admin 차단)가
  다르다. 정상 완료 경로에서도 자산 ready·콘텐츠 uploading 불일치가 남을 수 있다. 처방 후보만
  제시, QUEUE 미편입, 조율자 판단(감사 로그 액터 설계) 선행 필요.
- **대장 #217(api 유닛 저빈도 플레이크)** — 이 세션의 `pnpm --filter @gachinol/api test` 1회
  실행에서는 **987/987 전건 통과, 실패 0건**이었다(아래 ④). 재실행이 필요할 조건(실패 발생)
  자체가 오늘은 발생하지 않아 재실행하지 않았다 — 이 사실 자체가 #217의 관측 표본 1건이다(저빈도
  = 매번 나는 게 아니다, 이번엔 안 남).
- **대장 #218(monitor 실패 로그에 라우트 없음)** — #213과 같은 부류의 공백이 감시 쪽에도 있다.
  처방 무전제, QUEUE 미편입.
- **대장 #219(MinIO 라이프사이클) · #220(BigInt toEqual 크래시) · #221(멀티파트 재시도 시퀀스
  테스트 부재)** — 전부 등재만, QUEUE 미편입(#219는 QUEUE §C-7로 이미 신규 등재됨, 착수 자체는
  #103 머지 이후).
- **QUEUE 2단계 2-2(舊 2-1, 대장 #177 — 전이맵 `to`-상태 grep 테스트)** — 여전히 미착수. scribe
  재확인: `docs/plan/exec/QUEUE.md:297`에 `| 2-2 | 대장 #177 | ... |` 행 그대로. 2단계 완료(→D-2
  카카오 실 송출 범위 결정 트리거) 도달이 이 1건에 걸려 있다.
- **대장 #207(07 §3-10·§4·§6 법률 문언 정확성 판정)** — 07 §6 14항목 법률자문 계약 완료 후로 이월,
  여전히 미착수. scribe 재확인: 상태 칸 그대로 **미수신(2026-09-11 등재)**.
- **대장 #208·#209(QUEUE 편입·처방 판정)** — 변경 없음, scribe 재확인 둘 다 **미수신**.
- **대장 #215(제온 수동 배포 IMAGE_TAG 누락 사고, 2026-09-12)** — scribe 재확인: PIVOT-PLAN 대장
  #215 상태 칸 그대로 **미수신(2026-09-12 등재)**, `grep -n "#215" docs/plan/exec/QUEUE.md` → 0건.
  **09-12 일간보고서 ⑤가 예정한 "처방·QUEUE 편입 판정"이 이 세션까지 이뤄지지 않았다** — 오늘의
  조율자 세션은 업로드 경로 복구에 집중했고 #215는 손대지 않았다(⑥에서 다시 짚는다).
- **갇힌 콘텐츠 1건(`01a09166-aac7-774e-b95b-5efc617d8a06`)** — 여전히 `uploading`으로 갇혀 있을
  것으로 추정되나(PIVOT-PLAN 대장 #212 비고의 조율자 인용), scribe는 프로덕션 DB 접근 권한이 없어
  이번 세션에서 재확인하지 못했다 — **확인 불가**.
- **`05-monetization.md:583` 다운스트림 인용 편입 여부** — 이 세션에서 열람하지 않았다 —
  **확인 불가**로 남긴다(여러 세션째 이월).

## ④ 지금 상태 【자동 산출 — 사람의 판단 없음】

| 항목 | 값 | 재현 |
|---|---|---|
| 이 작업 디렉터리 HEAD | `ed29eb5`(브랜치 `feat/multipart-upload`, PR #103 헤드와 일치) | `git rev-parse --short HEAD && git branch --show-current` |
| origin/main | `daf16ef`(PR #101 병합커밋) | `git fetch -q origin main && git rev-parse --short origin/main` |
| 전체 열린 PR | **2**건(#102·#103) | `gh pr list --state open --json number,title --jq '.'` |
| PR #102 CI | 5 `SUCCESS` + 2 `SKIPPED`(preflight·deploy), `mergeable: MERGEABLE` | `gh pr view 102 --json state,statusCheckRollup,mergeable` |
| PR #103 CI | 8 `SUCCESS` + 3 `SKIPPED`(preflight×2·deploy·purge), `mergeable: MERGEABLE`(⚠️ 최초 조회 시 3개 `IN_PROGRESS` — 15초×9회 폴링 후 완주 확인) | `gh pr view 103 --json state,statusCheckRollup,mergeable` |
| 배포 서빙 중 — api | `daf16ef13f5f3cb73f75d8484a495f06027f7d17` = origin/main과 일치 | `curl -s https://api.bapfull.com/health/version` |
| readiness / liveness | 200 / 200 | `curl -s -o /dev/null -w "%{http_code}" https://api.bapfull.com/health/readiness`(+`/health/liveness`) |
| 배포 승인 대기(`waiting`·`action_required`) | **0**건 / **0**건 | `gh api "repos/HomeDCP/gachinol/actions/runs?status=waiting" --jq '.total_count'`(+`?status=action_required`) |
| api 유닛(선행: `pnpm --filter @gachinol/shared build`) | 78스위트 **987/987** pass, 1.921s, 실패 0(플레이크 #217 미관측 — 재실행 불요) | `pnpm --filter @gachinol/shared build && pnpm --filter @gachinol/api test` |
| reporter 앱 테스트 | 32스위트 **333/333** pass | `pnpm --filter @gachinol/reporter test` |
| reporter `test:web`(대장 #211 보완) | 1스위트 **4/4** pass(멀티파트 분기 웹 해석 경로 완주 포함) | `pnpm --filter @gachinol/reporter test:web` |
| 로컬 게이트(infra/scripts) | 11스위트 **481/481** pass, exit 0(불변, PR #101 이후 변화 없음) | `pnpm run test:scripts` |
| 대장 재현 | 대상 **68**행(舊 66, #216~#221 신규 6 · 판정대상 제외 표 이동 등으로 순증 +2) · 어긋남 **1**(#173, carry) · 수동 55 · 오류 0 · 표 무결성 **0**행 | `node infra/scripts/daejang-recheck.mjs` |
| monitor.yml 간격(2주 관측, 기산 2026-09-11T07:52:33Z부터 14개 구간, 이 세션 재계산) | 최소 **1.76h**·최대 **4.96h**·평균 **2.96h**·7h 허용치 여유 **2.04h**. **6h 초과 0건.** 실패 2건(09-12 `14:13:48Z`·`17:17:00Z`)은 각각 대장 #215 사고 검출 런·SHA 일치 상태의 일시적 라우트 요청 실패로 이미 설명됨(대장 #215·#218 참조), 그 이후 4개 틱 전부 success 복귀 | `gh run list --branch main --workflow monitor.yml --limit 30 --json createdAt,event,conclusion --jq '.[]\|select(.event=="schedule" or .event=="workflow_dispatch")\|[.createdAt,.event,.conclusion]\|@tsv'\|sort` |
| 주간 보고서 | `docs/ops/weekly/` **디렉터리 자체가 없음** | `ls docs/ops/weekly/` |
| 갇힌 콘텐츠 1건 현재 상태 | **확인 불가**(scribe 프로덕션 DB 비접근) | — |
| 미커밋 변경 | 0건(`git status --short` 빈 출력) | `git status --short` |

## ⑤ 다음 세션 예정

1. **PR #102 머지**(사용자) — CI 전건 통과·독립 완결. 머지 즉시 main 자동배포로 #213·#212 실가동.
2. **PR #103 머지**(사용자, #102 다음) — 스택 PR이라 base가 자동으로 `main`이 된다. 머지 즉시
   #211 실가동.
3. **갇힌 콘텐츠 수동 복구**(사용자/조율자, #102 배포 확인 후) — `01a09166-aac7-774e-b95b-
   5efc617d8a06`를 `uploading`→`upload_failed`.
4. **#211·#212·#213 해소 판정을 위한 수동 실기** — 배포 후: 한도 초과 실촬영 업로드 1건 완주(#211)
   · 1차 실패 후 2차 재시도 완주(#212) · 4xx 1건을 실제로 유발해 컨테이너 로그에 남는지 확인(#213).
5. **MinIO 라이프사이클 규칙 신설**(사용자, #103 머지 직전 — 대장 #219, QUEUE §C-7).
6. **신규 채번 #216~#221 QUEUE 편입·처방 판정**(조율자) — 전부 등재만 된 상태.
7. **대장 #215(IMAGE_TAG 누락 사고) 처방·QUEUE 편입 판정**(조율자) — 09-12 보고서가 이미 한 번
   예정했으나 이번 세션까지도 이뤄지지 않았다(위 ③ 참조). 이월 2회째.
8. **QUEUE 2단계 2-2(대장 #177) 착수 여부**(조율자).
9. (이월, 변경 없음) 대장 #207 법률 문언 판정 · #208·#209 QUEUE 편입 판정 ·
   `05-monetization.md:583` 편입 판정.
10. **주간 보고서 소급 생성 여부 결정**(사용자, 위 ① 6번) — 결정되는 대로 scribe가 생성한다.
11. **2주 report-only 관측 지속**(종료 2026-09-25, 남은 12일) — 지금까지 6h 초과 0건, 임계 재조정
    불요 상태 유지 확인.

## ⑥ 지난 보고서(2026-09-12) 예정 대비

09-12 보고서 ⑤가 예정한 11항목을 오늘 자료와 대조한다:

1. **커밋 `48cfa70`(미디어 도달성 검사) PR 제출·머지** → ✅ **됐다** — PR #101, 병합 `daf16ef`,
   `2026-09-13T00:32:49Z`(위 ②).
2. **대장 #214 상태 칸 갱신** → ✅ **됐다** — "미수신"→"수신 완료(2026-09-13, 조율자 확인)"(위 ②,
   scribe 재확인).
3. **HANDOFF.md "지금의 다음 1건" 절에 사고(IMAGE_TAG) 반영** → ✅ **됐다** — 09-12 보고서 작성
   시점엔 "원인 불명, 조율자가 확인할 것"이었으나, 같은 날 늦은 시각 커밋(`43ce81c`)이 원인·
   타임라인·복구까지 전부 반영한 절을 HANDOFF.md에 남겼다(scribe가 이번 세션에서 원문 확인,
   `docs/plan/exec/HANDOFF.md:525-590` "舊" 절). 지금은 오늘 작업분("⭐⭐⭐⭐" 절)로 대체됐지만
   이력은 보존돼 있다.
4. **대장 #211 처방·QUEUE 편입 판정** → ✅ **됐다** — PR #103(멀티파트), QUEUE 2단계 2-1 신설(위 ②).
5. **대장 #212 처방** → ✅ **됐다** — PR #102 `812a173`(위 ②).
6. **대장 #213 처방** → ✅ **됐다** — PR #102 `a4a2d8a`(위 ②).
7. **QUEUE 2단계 대장 #177 착수 여부** → **안 됐다** — 2-2로 밀렸을 뿐 착수되지 않았다(위 ③).
8. **2주 report-only 관측 지속** → 계속 진행 중, 6h 초과 여전히 0건(위 ④).
9. **다음 monitor.yml 스케줄 틱이 success로 복귀하는지 확인** → ✅ **확인됐다** — 사고 이후
   `19:28:22Z`부터 지금(`2026-09-13T01:16:03Z`)까지 4개 틱 전부 success(위 ④).
10. **(이월) 대장 #207 법률 문언 판정 · #208·#209 QUEUE 편입 판정 · `05-monetization.md:583` 편입
    판정** → **셋 다 안 됐다** — 변경 없음(위 ③).
11. **대장 #215(IMAGE_TAG 누락) 처방·QUEUE 편입 판정** → **안 됐다** — scribe 재확인 결과 여전히
    "미수신", QUEUE 미편입이다(위 ③). 09-12 보고서가 예정했던 것이 이 세션까지 두 번째로 미뤄졌다
    — 오늘 세션이 업로드 경로 복구에 집중하며 #215는 범위 밖이었던 것으로 보인다(단정 아님,
    조율자만 알 수 있는 우선순위 판단).
