# 일간 2026-08-31 (일)

## ① 사용자 결정 대기 — 지금 필요한 것 **2건**

**요청 시점 정본은 [QUEUE.md](../../plan/exec/QUEUE.md) "사용자 실행 항목" 절이다.**
나머지는 그 표의 시점에 도달할 때 여기 올라온다 — 미리 몰아 묻지 않는다.
⚠️ 舊 항목("PR #80 머지")은 **충족됐다**(`gh pr view 80` — merged, `.claude/agents/` main 반영 확인) — 아래 2건으로 교체.

| # | 무엇 | 왜 나에게 오나 | 선택지 | 미결 시 영향 | 대기 |
|---|---|---|---|---|---|
| **1** | **스토어 계정 정본 정정** — Apple 공식 문서상 개인사업자는 조직 계정 등록 불가(Individual 트랙, D-U-N-S 불요)라 07 §6·08 §A·QUEUE B-4의 *"조직 계정·D-U-N-S 선행·8주 리드타임"*이 사실과 다르다 | §0-2 ④ 결정의 문제(리드타임 판정이 바뀌는 정정이라 승인 없이 못 고침) | ⓐ 정정 반영(07·08·QUEUE 3곳 동시 수정) ⓑ 보류(舊 8주 전제 유지) | 승인 없이는 잘못된 8주 전제가 정본에 남는다. 정정 시 **크리티컬 패스 8주가 사라지고** Google 개인 계정 *"테스터 12명×14일"*이 새 리드타임이 된다 | 신규 |
| **2** | **촬영자 책임 모델(대장 #166)의 법적 유효성 확인을 07 §6 체크리스트(14항목)에 등재할지** | §0-2 ④ 결정의 문제 | ⓐ §6에 15번째 항목으로 등재 ⓑ §3-15 자문 시점까지 유지(각주로만) | 미결정 시 그 책임 구조가 **검증되지 않은 채** 첫 촬영 재개 판단에 쓰인다 | 신규 |

### 지금 병행 시작을 권하는 것 (코드와 무관, 리드타임 때문)
**법률자문 계약 → 07 §6 14항목**(舊 "13항목" — 재계수 정정, 지사 촬영 재개 자체를 막는다) ·
**웹접근성 판정 의뢰**(회신 2~4주, 원천은 07 §3-11) · **스토어 개발자 계정**(위 결정 대기 ①에 따라 리드타임·트랙이 바뀔 수 있음).
⇒ **사업자등록은 확보됐다**(2026-08-31, 개인사업자·일반과세자·방송프로그램 제작업 등재 — 등록번호 등
개인정보는 미기재). 후속 신고·계약(카카오 비즈니스 인증·YouTube 수익화·통신판매업·PG)은 별개로 남아 있다.

### 다음에 요청드릴 것 (시점 도달 시 자동으로 여기 올라옴)
`0-2 완료 시` Quick Tunnel 고정 방식 · `0-3 완료 시` Actions vars 교정 ·
`0-4 완료 시` 제온 `DROP COLUMN` 승인 · `1-1 직전` GitHub Environment 게이트 ·
`2단계 완료 시` 카카오 실 송출 범위.

## ② 어제~오늘 무엇이 움직였나

- **대장 #186 슬라이스 완주 — 빌드 스탬프 + 배포 후 SHA 대조.** 구현 → 게이트② 독립 검증 PASS(뮤테이션
  3건 red) → [PR #81](https://github.com/HomeDCP/gachinol/pull/81) → **머지**(`325fd8c`) → **제온 실배포 +
  배포 후 SHA 대조가 실서버에서 실제로 통과**(web 컨테이너 `<meta name="build-sha">` 실측 일치).
  api·워커 대조는 배포 경로 자체가 없어 미배선(대장 #180이 이어받음). 근거: WORKLOG 2026-08-31 · PR #81
- **대장 #190 신규 채번** — `apps/reporter` 웹 번들이 QR 스캐너 경로에서 jsDelivr CDN을 env 게이트 없이
  무조건 로드(공급망 리스크 + 07 §3-10 국외이전 미판정). QUEUE 백로그 편입(코드 축이 아니라 외부
  판단이 착수 조건). 근거: PIVOT-PLAN #190 · 커밋 `5744149`
- **QUEUE 사실 오류 2건 정정**(scribe 재실행 검증): ⓐ 07 §6 "첫 촬영 재개 전" 체크리스트 "13항목"→
  **14항목**(재계수: `grep -c "^- \[ \]"` 범위 한정 실행 → 14, 07 원문 "13항목" 문자열 0건이었다)
  ⓑ "지연 상한 2주 + 2단 분리 규칙"의 귀속을 03→**07 §3-11**로 정정(03 §A-1 리스크 1은 대칭 이식본,
  발동 트리거가 다름 — 03은 어르신 패널 완료율<90%, 07은 접근성 법정 판정 회신 지연)
- **사업자등록 확보** — 개인사업자(일반과세자, 업종 "영화·비디오물 및 방송 프로그램 제작 관련
  서비스업")로 등재. 대장 #185·QUEUE B-1이 막던 선행 중 등록 자체는 충족, 후속 신고·계약은 별개
  (등록번호·대표자명 등 개인정보는 공개 리포에 미기재)
- **국외이전 대상 코드 실측** — 07 §3-10이 적은 4개(R2·Cloudflare Stream·RTZR·OpenAI) 중 **3개가
  코드에 없다**: `grep -rl "R2_\|r2\.cloudflarestorage" services/` → 0건(스토리지는 제온 MinIO,
  S3 호환 env로 추상화돼 "R2" 리터럴 자체가 없음) · `grep -rl "RTZR\|rtzr" services/` → 0건(STT는
  로컬 whisper.cpp로 이미 전환) · OpenAI는 `services/ai-worker/app/analyzers/openai_provider.py`에
  등록만 돼 있고 실호출은 `NotImplementedError`(대장 #174와 동일 사실). 반대로 07 §3-10에 **없는 국외
  통신 경로 3개가 실재**한다 — jsDelivr(#190) · Cloudflare Tunnel 경유 트래픽 · 시청자 브라우저의
  presigned URL 직접 재생(제온 도메인은 국내이나 Cloudflare Tunnel 경유 시 트래픽이 그 네트워크를
  통과). **07 §3-10 본문 갱신은 사용자 확인 대기 중이라 이번 회차 스코프 밖** — 다음 슬라이스 예정
- **(이력, 08-30~31 새벽, 40행 상한으로 압축 — 상세는 링크)** 차기 슬라이스 재판정(후보 7건 중 3건 착수
  불가, [PR #80](https://github.com/HomeDCP/gachinol/pull/80)) → 전수 정독+하네스 재설계(대장 #173~#189
  채번 17건·AI팀 6롤·규율 리포 이주·QUEUE 신설) → QUEUE 독립 검증(13건 교정). 상세: WORKLOG 2026-08-30.

## ③ 막힌 것

| 무엇 | 몇 번째 시도 | 다음 조치 |
|---|---|---|
| 대장 대기 총계를 셀 수 없다 | 1 | 판정 도구가 대장 아닌 행 57개를 센다(#182). 도구 정정이 2단계 2-3 |

## ④ 지금 상태 【자동 산출 — 사람의 판단 없음】

| 항목 | 값 | 재현 |
|---|---|---|
| origin/main | `325fd8c`(PR #81 머지) | `git fetch -q origin main && git log --oneline -1 origin/main` |
| 열린 PR | **0** | `gh pr list --state open` |
| CI 3종 | CI ✅ · Build Images ✅ · Deploy Web ✅(headSha=325fd8c 전부 success) | `gh run list --branch main -L 3 --json workflowName,conclusion,headSha` |
| **배포 이미지 나이** | **web 0일**(오늘 배포, `<meta name="build-sha" content="325fd8c...">` 제온 실측 일치) · media-worker 3일(기능 누락 0) · api **10일**⚠️(`/health/version` 아직 404 — 구빌드라 라우트 자체가 없음, 코드 세대는 라우트 이분탐색으로 8/20~8/22 사이 고정 확인) · ai-worker **22일**⚠️(단 그 구간 코드 변경 커밋 0건 — 기능 회귀는 0). ⭐ **원인 정정(2026-08-31, ops-prober)**: 이미지가 없는 게 아니라 **제온이 pull하지 않는 것** — Build Images는 매 머지마다 GHCR push 성공 중(오늘자 이미지도 이미 있음). ⚠️ 컨테이너 `CreatedAt`(api `Up 30h`)은 재기동일 뿐 배포일이 아니다 — 지표로 쓰지 말 것(대장 #170) | 제온 `docker images`(ops-prober, SSH) + `gh run list --workflow build-images.yml`(scribe 재실행 확인 — 매 main push success) |
| **인터넷 경로** | **❌ 502**(불변 — Quick Tunnel 호스트 불일치 #179/#187, 이번 슬라이스와 무관) | `curl -sI <터널 URL>` |
| 테스트 | api **903/903**(74 스위트, 舊 901 대비 +2 — #186 슬라이스 신규 유닛) | `pnpm --filter @gachinol/shared build && pnpm --filter @gachinol/api test` |
| 대장 | **190행** / 대기 **64**(`#182 미수리 — 총계 신뢰 불가`) | `node infra/scripts/daejang-recheck.mjs` |
| 빌드 스탬프 | **5건**(api·media-worker·ai-worker 3개 Dockerfile + infra/docker/inject-build-sha.mjs·Dockerfile.web) — "배포된 것이 어느 커밋인가"를 이제 web은 물을 수 있다(코드는 3서비스 전부 있으나 **배포 경로가 없어** api·워커는 실측으로는 아직 못 묻는다 — 대장 #180) | `grep -rl "GIT_SHA" services/*/Dockerfile infra/docker` |

## ⑤ 다음 예정 — 1건

**0-1 · 대장 #182 — `daejang-recheck` 자기검사·정정**

- **왜 이것인가**: L-obs(계측기 복구). 대장 판정 도구 자체가 대장 아닌 행까지 세고(§6-1~§6-10 무관 표
  포함, 실측 상 오차 근원) 표 무결성이 흔들려 **대기 총계를 아무도 확정할 수 없다**(오늘 ④의 대장
  행도 "총계 신뢰 불가" 딱지가 붙은 채로 나갔다). 이걸 고치지 않으면 0~2단계 전부를 **고장 난 자로
  재며** 진행하게 된다.
- **선행 충족**: 없음(舊 0-1이던 대장 #186이 오늘 해소돼 행이 제거되며 #182가 당겨졌다)
- **동반 의무**: 없음
- **차단자**: 없음

## ⑥ 어제 예정 대비

예정 `#168 + #170 묶음 착수` → 실제 `전수 정독 + 하네스 재설계`.
**차이 이유**: 사용자 지시로 개발 착수 전 근본 해결·개발원칙 개정이 선행됐다.
그 과정에서 **#168보다 앞서는 결함 4건**(#186 빌드 스탬프·#187 터널 재발·#188 `app/` 미검증·#189
설정됨≠올바름)이 드러나 순서가 바뀌었다 — 어제의 1순위 판정은 **범위가 좁았다**.
