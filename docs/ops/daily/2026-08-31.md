# 일간 2026-08-31 (일)

## ① 사용자 결정 대기 — 4건

| # | 무엇 | 왜 나에게 오나 | 선택지 | 미결 시 영향 | 대기 |
|---|---|---|---|---|---|
| 1 | **제온 마이그레이션 `20260827203549` 적용 승인** — `DROP COLUMN` 2개(비가역) | §0-2 ③ 권한 | 적용 / 보류 | 백엔드 배포(0-3)가 막히고 #168 수리가 실기에 도달 못 함 | 2일 |
| 2 | **Quick Tunnel 고정 방식** — named tunnel(무료·도메인 불요) vs 자동 재기입 스크립트 | §0-2 ③ 외부 배선 | named / 스크립트 | **인터넷 경로가 재기동마다 100% 재발**(#187) | 신규 |
| 3 | **카카오 실 송출 범위** — 게시자산 준비만 / YouTube 어댑터까지 / 전체 | §0-2 ④ 결정 | A / B / C | MVP 정의의 가운데 토막이 계속 비어 있다(#175·#176·#184) | 신규 |
| 4 | **죽은 계획 문서 4개 처리** — E1·E3·E4·E5(합계 4,435행, 15~23일 무갱신) | §0-2 ④ 결정 | `archive/` 이동 / 헤더 동결만 / 존치 | 서브에이전트가 grep으로 찾아 죽은 지침을 따른다(E1이 지정한 BE 리드는 0회 기동했다) | 신규 |

## ② 어제~오늘 무엇이 움직였나

- **차기 슬라이스 재판정** — 후보 7건 중 3건이 선행 미충족으로 착수 불가 판정. 기록치 8건 정정.
  근거: WORKLOG 2026-08-30 · [PR #80](https://github.com/HomeDCP/gachinol/pull/80)
- **전수 정독 + 하네스 재설계** — 계획·백엔드·프론트·대장·인프라 5축 정독 후 자문단 4위원 토론.
  근본원인 문서화 · 대장 **#173~#189 채번(17건)** · AI팀 6롤 설치 · 규율 리포 이주 · 순서계획도 신설.
- **순서계획도 독립 검증** — 새로 만든 QUEUE를 planner 규칙으로 검증시켜 **13건 교정**.
  가장 아픈 지적: *"사다리로 단계를 정한 것이 아니라 단계로 L을 붙였다"*(단계 번호와 L이 거의 일치).
  #168을 4-2→2-1로 올리고(L2가 L3보다 뒤에 있었다), 누락 6건 복원, **ROOT-CAUSE의 오귀속 1건 정정**
  (경보 7종은 전부 보안·자원 축이라 502·스테일을 하나도 알려주지 않는다).

## ③ 막힌 것

| 무엇 | 몇 번째 시도 | 다음 조치 |
|---|---|---|
| 대장 대기 총계를 셀 수 없다 | 1 | 판정 도구가 대장 아닌 행 57개를 센다(#182). 도구 정정이 2단계 2-3 |

## ④ 지금 상태 【자동 산출 — 사람의 판단 없음】

| 항목 | 값 | 재현 |
|---|---|---|
| origin/main | `2e02065` | `git log --oneline -1 origin/main` |
| 열린 PR | **1** (#80) | `gh pr list --state open` |
| CI 3종 | CI ✅ · Build Images ✅ · Deploy Web ✅ | `gh run list --branch main -L 3` |
| **배포 이미지 나이** | api **10일**⚠️ · ai-worker **22일**⚠️ · web 0일 | 제온 `docker images` |
| **인터넷 경로** | **❌ 502** | `curl -sI <터널 URL>` |
| 테스트 | api **901/901** | `pnpm --filter @gachinol/shared build && pnpm --filter @gachinol/api test` |
| 대장 | **189행** / 대기 **집계 불가**(#182) | `node infra/scripts/daejang-recheck.mjs` |
| 빌드 스탬프 | **0건** — "배포된 것이 어느 커밋인가"를 물을 수 없다 | `grep -rl "GIT_SHA" services/*/Dockerfile infra/docker` |

## ⑤ 다음 예정 — 1건

**0-1 · 대장 #186 — 빌드 스탬프 + 배포 후 SHA 대조**

- **왜 이것인가**: L1. 이게 없으면 0-2~0-4의 *"고쳤다"*가 전부 자기 신고다.
  레드팀 판정 — *"프로브 48개를 짜기 전에 SHA 한 줄이 같은 문제를 더 넓게 닫는다."*
- **선행 충족**: 없음(즉시 착수 가능)
- **동반 의무**: 없음
- **차단자**: 없음. 단 0-3(제온 배포)은 결정 대기 ①-1에 묶여 있다

## ⑥ 어제 예정 대비

예정 `#168 + #170 묶음 착수` → 실제 `전수 정독 + 하네스 재설계`.
**차이 이유**: 사용자 지시로 개발 착수 전 근본 해결·개발원칙 개정이 선행됐다.
그 과정에서 **#168보다 앞서는 결함 4건**(#186 빌드 스탬프·#187 터널 재발·#188 `app/` 미검증·#189
설정됨≠올바름)이 드러나 순서가 바뀌었다 — 어제의 1순위 판정은 **범위가 좁았다**.
