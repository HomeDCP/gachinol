# 일간 2026-09-05 (토)

## ① 사용자 결정 대기 — 지금 필요한 것 **2건**

요청 시점 정본은 [QUEUE.md](../../plan/exec/QUEUE.md) "사용자 실행 항목" 절. §B·§C·§D 재확인
결과 **오늘 새로 도달한 요청 시점 0건**(B-2 법률자문 계약은 2026-09-01 통보 이후 계속 진행 중 ·
B-3 웹접근성 판정 회신 대기 계속 · B-4는 W3 코드 완료 전이라 미도달 · D-2는 2단계 완료 전이라
미도달 — 신규 항목 아님).

⚠️ **대신 아래 2건은 이 세션 재현으로 새로 확인된 실행 대기다** — QUEUE 표의 "요청 시점" 절과는
별개로, 승인 게이트(GitHub Environment `production` required reviewer) 자체가 **사람의 클릭을
기다리는 지점**이라 매 배포마다 반복 발생한다.

1. **舊 대기(PR #93분, 80da6cf) — 사용자 승인 완료·정상화 확인.** 위 26시간 대기분을 사용자가 오늘
   승인했다. 재현(scribe, 2026-09-05): `curl -s https://api.bapfull.com/health/version` →
   `{"sha":"80da6cf11328b590f4f4230ffb48464cbfd37138"}`(당시 main HEAD와 일치) · `curl -s -o /dev/null
   -w '%{http_code}' https://watch.bapfull.com` → `200` · `curl -s https://watch.bapfull.com | grep
   -o '<meta name="build-sha"[^>]*>'` → `content="80da6cf11328b590f4f4230ffb48464cbfd37138"`(api·web
   동일 SHA로 정상 서빙 확인).
2. **PR #94(대장 #180 잔여 — 배포 후 스모크+마이그레이션 게이트) 머지 → 승인 대기 2건이 즉시
   재발했다.** PR #94는 브랜치 `feat/deploy-smoke-migration-gate`에서 생성됐고 CI 3종(headSha
   `25df6d2`) 전부 success 확인 후 머지됐다(재현: `gh pr view 94 --json state,mergedAt,mergeCommit` →
   `{"state":"MERGED","mergedAt":"2026-09-05T01:28:57Z","mergeCommit":{"oid":
   "1776c4616e7ee87cd13be1ea537e650265a73eea"}}`). **머지 직후 main에서 트리거된 새 런이 같은 패턴을
   그대로 재현한다**: `gh run list --branch main -L 3 --json workflowName,conclusion,status,headSha`
   → `CI: success`(headSha `1776c461`) · `Build Images: waiting` · `Deploy Web: waiting`(둘 다 같은
   `headSha`). **오늘 새로 채번한 대장 #202(배포 승인이 구조적으로 2회 — 아래 ②)가 같은 날 실물로
   재현된 사례다** — 다음에 필요한 사용자 조치는 이 2건의 승인이다.
3. **⭐ 후속(같은 날, scribe 재현) — 위 2건 승인이 완료돼 `1776c461` 실배포까지 끝났다.** `gh run
   list --branch main --json workflowName,conclusion,status,headSha -L 3` → `CI`·`Build Images`·
   `Deploy Web` **전부 `success`**(headSha `1776c4616e7ee87cd13be1ea537e650265a73eea`). `gh run view
   33936291475 --json jobs`(Build Images) → `build api/media-worker/ai-worker` 全 success 후
   `deploy (api·media-worker·ai-worker → 제온)` 시작 `02:25:57Z`·종료 `02:27:28Z`, `build` 종료
   시각(`01:31:49Z`대)과의 간격 **약 54분**이 승인 대기 시간이다. `gh run view 33936291477 --json
   jobs`(Deploy Web) → `deploy` 시작 `02:26:10Z`·종료 `02:26:37Z`(build 종료 `01:33:19Z` 대비 약 53분
   대기 — 두 워크플로 승인이 거의 동시에 처리됐다). 실물 확인: `curl -s
   https://api.bapfull.com/health/version` → `{"sha":"1776c4616e7ee87cd13be1ea537e650265a73eea"}` ·
   `curl -s https://watch.bapfull.com | grep -o '<meta name="build-sha"[^>]*>'` →
   `content="1776c4616e7ee87cd13be1ea537e650265a73eea"`(api·web 동일 SHA, main HEAD와 일치) ·
   스모크 판정 라우트 3종 재현: `/v1/resident-uploads/x`→**401**·`/v1/feed`→**200**·
   `/v1/does-not-exist-abc123`→**404**(#180 처방 ②가 실배포에서 실제로 판정 가능함을 확인 — 아래 ④
   표는 이 시점 기준으로 갱신했다). ⚠️ 위 ①-2 문단(작성 시점 "waiting")은 **그 직후 상태**이므로
   지우지 않고 이 항목으로 후속만 덧붙인다(규율 13).

## ② 오늘 무엇이 움직였나 — QUEUE 1-1(대장 #180) 잔여분 완주 + 실배포·승인 처리 + 대장 #202 채번

- **대장 #202 채번 — 배포 승인이 구조적으로 2회다(하나의 배포가 두 워크플로 런으로 쪼개져 있다).**
  발견 경로: 사용자 질문(*"승인버튼 한 번 눌러서 빌드이미지·디플로이 웹 두 가지를 한 번에 승인할 수는
  없을까?"*) → 조율자가 `build-images.yml`·`deploy-web.yml` 트리거·배포 대상을 실측 대조. **대장
  #200과 중복 여부를 먼저 확인**: #200은 증상(`up`이 의존 서비스를 함께 처리해 되돌림, `--no-deps`로
  해소됨) / #202는 그 증상을 낳은 구조(하나의 배포를 두 워크플로로 쪼갠 것 자체, 미해소) — **중복
  아님, 신규 채번 확정**. 확인 방법은 해소 방식을 전제하지 않는 구조 판정(규율 17·18): `awk
  '/branches: \[main\]/{a[FILENAME]=1} /environment: production/{b[FILENAME]=1} END{for(f in a) if(f
  in b) c++} END{print c+0}' .github/workflows/*.yml` → 등재 시 **2**(build-images.yml·
  deploy-web.yml, 해소 후 **1** 기대). 재현: `node infra/scripts/daejang-recheck.mjs` → 대상
  **59행**(舊 58)·어긋남 **0**(#202가 "일치"로 자동 분류돼 수동 판정으로 밀리지 않았다). ⚠️ 아직
  실발생하지 않은 위험(승인 순서 역전 시 옛 SHA가 최신을 덮음)과, 오늘 실물로 재현된 사실(위 ①-2,
  PR #94 머지 직후 승인 대기 2건 재발)을 구분해 등재했다 — 전자는 "아직", 후자는 "확인됨"으로 표기.
  QUEUE 편입·우선순위는 조율자 판정(본 채번은 등재만).

**scribe는 코드를 만들지 않는다** — 아래는 이미 커밋된 `41d8fec`→`25df6d2`(브랜치
`feat/deploy-smoke-migration-gate`, PR #94로 main에 머지 완료 — 병합 커밋 `1776c461`,
2026-09-05T01:28:57Z)의 내용을 정본 3곳에 반영한 기록이다.

- **처방 ①·②·③ 구분이 이번에 드러났다.** 대장 #180의 처방은 셋이었다(api·워커 배포 경로 신설 /
  배포 후 스모크 / 파괴적 마이그레이션 사전 확인 게이트). 舊 2026-09-03 "해소" 표기는 **①만
  이행된 상태**를 통째로 "해소"로 적은 것이었다 — ②③은 그 시점까지 **0건**이었다. #180이 스스로
  적어 둔 해소 판정 기준(*"의도적으로 라우트를 빠뜨린 이미지를 배포했을 때 파이프라인이 red가 되는
  것"*)은 **SHA 대조(#186)만으로는 원리적으로 충족될 수 없다** — 라우트가 빠진 이미지도 커밋 SHA는
  정상 이미지와 동일할 수 있다.
- **② 배포 후 라우트 스모크** — `infra/scripts/deploy-smoke.mjs`. 배포된 api에 미인증 요청을 던져
  `≠404`를 실재 판정 기준으로 삼는다(**없는 경로 음성 대조가 판정의 전제** — 그것 없이는 catch-all
  서버에서도 전부 200이 나와 거짓 통과가 된다). `resident-uploads`를 필수 라우트에 넣었다 — 그 404가
  이 결함(#180)의 최초 발견 경로였다(#120이 회귀한 게 아니라 배포된 적이 없었다). `build-images.yml`
  deploy 잡에 배선.
- **③ 파괴적 마이그레이션 사전 확인 게이트** — `infra/scripts/check-destructive-migrations.mjs`.
  파괴적 구문 9종을 오프셋 기반 전역 스캔으로 탐지해 `build-images.yml` preflight·`ci.yml` PR
  게이트에서 red로 세운다(사용자 채택 ⓐ안 — *"red로 세우고 사람이 명시적으로 푼다"*). 승인 마커
  (마이그레이션 디렉터리 안 `DESTRUCTIVE-APPROVAL.md`)로 해제. 기존 마이그레이션
  `20260827203549_drop_minor_consent_confirmation`에 소급 마커를 남겼다(손실 실측 0건·복원점
  `gachinol-20260902-040924.sql.gz` 기재 — T-W2-36이 이미 2026-08-27 적용한 것을 사후 기록).
- ⭐ **게이트②가 이 슬라이스를 한 번 반려했다(그대로 기록한다)**: 독립 검증이 FAIL을 냈다 — 승인
  마커의 필수 칸(손실 실측·복원점)에 `-`·`n/a`·같은 글자 반복 같은 **무의미 placeholder**를 넣어도
  게이트가 통과했다. **"있는 척"(규율 21)을 막으려던 장치가 스스로 "있는 척"을 한 것** — 헤더 주석은
  "규율 21 방지"를 주장했지만 실제로는 빈칸만 막았고, 자기 테스트도 빈칸만 커버해 이 구멍을 못 잡았다.
  수리는 금칙어 목록이 아니다(`.`·`x` 등으로 즉시 우회된다) — **실제 절차를 밟으면 자연히 생기는
  도메인 신호**(손실 실측=수치+단위, 복원점=백업 파일 식별자)를 요구하는 쪽으로, 최소 길이·문자
  다양성을 함께 걸었다. 개행 분리 `DROP`⏎`COLUMN` 미탐도 같이 닫았다(오프셋 기반 전역 스캔으로 교체,
  행 번호 정확도는 유지). 조율자 재현(커밋 메시지 인용): `-`/`n/a` → exit 1 · `TODO`/`TODO` → exit 1 ·
  같은 글자 반복 → exit 1 · 소급 마커(20260827203549) → exit 0(PASS). **남는 한계는 숨기지 않는다** —
  마커 검증은 형태만 보고 진실성은 못 본다(`999행 확인`·`fake-99999999.sql.gz`도 통과한다). 정규식
  게이트의 구조적 한계이며 헤더 주석에 명시돼 있다. 실질 방어는 마커가 PR diff에 드러난다는 것이다.
- **문서 갱신(scribe, 이 세션)**: PIVOT-PLAN 대장 #180 상태 칸에 정정 블록 추가(규율 13 — 원문
  "해소(2026-09-03, ...)"는 지우지 않고 그 아래 "정정(2026-09-05)"으로 ②③ 이행 사실·SHA 대조의
  원리적 한계·머지 전 미검증 caveat을 덧붙였다) · QUEUE 1단계 1-1(배포 후 스모크) 행 제거 + 舊
  1-2(가용성 감시)를 1-1로 당김 + "배포 직후 1회 판정의 원리적 한계"·"지속 감시가 그 사각을 덮는
  이유" 각주를 번호 갱신하며 보존(소실 0) + D-1·0단계 이력 블록에 후속 각주 추가(규율 13, 원문
  삭제 0) · 백로그 표의 "가용성 감시(1-3)" 참조도 번호 갱신.

### 추가 슬라이스(같은 날) — QUEUE 2-1·2-2(대장 #168·#181) 해소 + 동반 의무 D1

브랜치 `fix/station-gate-upload-atomicity`(커밋 `a804165`·`d362f95`, PR #96 열림)가 **대장 #168·#181을
해소했다.** 위 ⑤가 예정한 "가용성 감시 신설"보다 먼저 처리된 별도 슬라이스다(예정 이월은 아래 ⑤
원문 그대로 두고 여기 새로 기록, 규율 13 — 원문 삭제 0).

- **대장 #168(QUEUE 2-1) — upload-complete 복구 원자화.** `assets.markFailed`→`workflow.failUploadTx`
  두 쓰기를 단일 `prisma.$transaction` 콜백 안으로 이동(`resident-reviews.service.ts:286` tx-인자
  패턴 이식, 새 패턴 0). ⭐ **게이트②가 구현자의 공백을 메웠다** — 구현자는 로컬 Postgres 부재로 mock
  단위 테스트까지만 확인했는데, 검증자가 `pnpm infra:up`으로 실 Postgres를 띄워 **e2e 12스위트 87건
  전건 통과** + 별도 프로브로 **콜백 예외 시 첫 쓰기가 실제로 롤백됨(부분 커밋 0건)**을 실증했다
  (구현자·검증자 실측 인용, scribe 미실행 — 로컬 Postgres 필요). scribe 재현(2026-09-05):
  `cd services/api && pnpm test` → **75 suites / 921 tests 전건 통과**(CLAUDE.md 舊 기재 901은 stale —
  이 세션 작업 전 실측 903 대비도 +18).
- **대장 #181(QUEUE 2-2) — 지사 조회 2종 역할 게이트.** 허용 역할을 실제 호출부 실측으로 확정:
  `GET /stations`→`@Roles('center_operator')`(control-center만 호출) · `GET /stations/:id`→
  `@Roles('reporter','center_operator')`(reporter+control-center). subscriber는 호출 0(`@Public
  /v1/feed/stations` 사용). admin은 `roles.guard.ts:27` 수퍼롤로 암묵 통과.
- **동반 의무 D1 — `infra/scripts/controller-role-gate.mjs` 신설.** 전수 실측 결과 게이트 미명시는
  리포 전체 **4건뿐**(2건=#181, 2건=`auth.logout`·`auth.me`는 역할 무관이 정당해 사유와 함께 허용
  목록 등재). scribe 재현(2026-09-05): `node infra/scripts/controller-role-gate.mjs` → **PASS —
  컨트롤러 17개, 라우트 73건 전부 권한 게이트 보유(허용목록 경유 2건, 죽은 엔트리 0건)**.
  ⚠️ **조율자 실측 함정(규율 13 기록 가치)**: 이 리포는 `@Roles`를 라우트 데코레이터 **아래**
  (`@Post()` 다음 줄)에 쓴다 — 위쪽만 보면 오탐 42건이 난다. 스크립트는 위아래+클래스 레벨을 모두
  본다. 게이트②가 짚은 잔여 위험 2건은 같은 슬라이스에서 처리됐다: ① **허용목록 부패**(존재하지
  않는 라우트를 등재해도 조용히 통과) → 역방향 매칭 fail-closed로 수리 ② **사유 검증의 어휘적
  한계**(자연문 사유는 정규식으로 못 막음) → 로직은 두고 헤더 주석을 사실에 맞게 좁힘(직전 슬라이스
  주석 과장 함정 재발 방지).
- **문서 갱신(scribe, 이 세션)**: PIVOT-PLAN 대장 #168·#181 상태 칸을 해소로 갱신(재현 명령·결과
  동봉, 원문 보존) · QUEUE 2단계 2-1·2-2 행 제거 + 舊 2-3·2-4·2-5를 2-1·2-2·2-3으로 당김(백로그 표
  "2-5 이후" 트리거도 "2-3 이후"로 갱신) · 이 일간보고서 ②·④ 보강.
- **재현(scribe, 2026-09-05, 전건 재실행)**: `pnpm run test:scripts` → **235/235**(舊 194 —
  `controller-role-gate.test.mjs` 41건 신설) · `cd services/api && pnpm test` → **75 suites / 921
  tests 전건 통과** · `node infra/scripts/controller-role-gate.mjs` → **PASS**(위와 동일) ·
  `node infra/scripts/daejang-recheck.mjs` → **대상 57행(舊 59)·어긋남 0**(#168·#181이 "미수신"에서
  빠져 대상에서 제외됨 — 상태 갱신이 recheck 대상에도 정확히 반영됨을 재확인).

## ③ 막힌 것

**착수 자체는 막혀 있지 않다.** 다음 1건(QUEUE 새 1-1 — 가용성 감시 신설, 舊 1-2)은 선행 0(舊 0-1
대상이 2026-09-01 named tunnel 전환으로 이미 충족), 동반 의무는 채번 선행(규율 24-1)뿐이다. 단
⚠️ 위 ①-2의 **새 승인 대기**(PR #94 머지분, headSha `1776c461`)는 다음 실배포 큐를 밀어낼 수 있어
코드 착수와는 별개로 조율자가 살펴야 한다 — 舊 PR #93분 대기는 오늘 해소됐다(위 ①-1).

## ④ 지금 상태 【자동 산출 — 사람의 판단 없음】

| 항목 | 값 | 재현 |
|---|---|---|
| 이 브랜치 HEAD | `25df6d2`(브랜치 `feat/deploy-smoke-migration-gate` — PR #94로 main에 머지 완료) | `git log --oneline -1` |
| origin/main | `1776c461`(PR #94 머지 커밋, 2026-09-05T01:28:57Z — 舊 `80da6cf`에서 전진) | `git fetch -q origin main && git log --oneline -1 origin/main` |
| 열린 PR | **0**(PR #94는 이미 머지됨) | `gh pr list --state open --json number` |
| main 최신 CI 3종(headSha=1776c461) | 작성 당시 CI ✅ success · Build Images **waiting** · Deploy Web **waiting** → **⭐ 후속(같은 날): 승인 완료 후 3종 전부 success**(위 ①-3 참조) | `gh run list --branch main -L 3 --json workflowName,conclusion,status,headSha` |
| 배포 이미지 — api(현재 서빙 중) | 작성 당시 `sha-80da6cf` → **⭐ 후속(같은 날): 승인 완료·재배포로 `sha-1776c461`(=origin/main)로 갱신, 실서빙 확인** | `curl -s https://api.bapfull.com/health/version` → `{"sha":"1776c4616e7ee87cd13be1ea537e650265a73eea"}` |
| 배포 이미지 — web(현재 서빙 중) | 작성 당시 api와 동일 SHA(80da6cf) → **⭐ 후속(같은 날): `sha-1776c461`로 갱신, api와 동일 SHA 유지 확인** | `curl -s https://watch.bapfull.com \| grep -o '<meta name="build-sha"[^>]*>'` → `content="1776c4616e7ee87cd13be1ea537e650265a73eea"` |
| PR #94 머지 diff 범위 | `.github/workflows`·`infra/scripts`·`docs/plan`·`package.json`·마이그레이션 승인 마커 1개, **apps/services/packages 0줄** | `git diff --stat 80da6cf..1776c461` |
| 테스트 — infra/scripts | ~~194/194~~ → **235/235**(舊 121 — `controller-role-gate.test.mjs` 41건 신규, 추가 슬라이스 재실행 확인) | `pnpm run test:scripts` |
| 파괴적 마이그레이션 게이트(현 리포 상태) | **PASS, exit 0**(파괴적 구문 있는 마이그레이션 전건에 승인 마커 확인됨, 이 세션 재실행 확인) | `node infra/scripts/check-destructive-migrations.mjs` |
| 컨트롤러 역할 게이트(D1, 대장 #181 동반) | **PASS — 컨트롤러 17개·라우트 73건 전부 권한 게이트 보유(허용목록 경유 2건, 죽은 엔트리 0건)**(추가 슬라이스 신설, 재실행 확인) | `node infra/scripts/controller-role-gate.mjs` |
| services/api 유닛 테스트 | **75 suites / 921 tests 전건 통과**(CLAUDE.md 舊 기재 901은 stale, 이 세션 작업 전 실측 903 — 추가 슬라이스 재실행 확인) | `cd services/api && pnpm test` |
| 대장 | ~~202행 / 대기 59~~ → **202행 / 대기 57**(추가 슬라이스에서 #168·#181 해소 -2, 신규 채번 0) | `node infra/scripts/daejang-recheck.mjs` |

## ⑤ 오늘(다음 세션) 예정 — 2건

1. **완료(이 세션 중) — PR #94 생성·CI 확인·머지.** 예정대로 진행돼 2026-09-05T01:28:57Z 머지 완료
   (병합 커밋 `1776c461`). 다음으로 필요한 것은 머지가 다시 만든 **승인 게이트 2건**(Build
   Images·Deploy Web deploy 잡, headSha `1776c461`)의 처리다 — 위 ①-2 참조. 이 대기가 실제로 스모크·
   마이그레이션 게이트 처방(#180 ②③)이 **실배포에서 발동하는지**를 검증할 첫 기회다(이 문서 ②가
   못박아 둔 대로 "머지 전 검증은 없다").
   **⭐ 후속(같은 날) — 완료.** 사용자가 두 승인 게이트를 모두 통과시켰고, `1776c461` 실배포가
   끝나 스모크 판정 라우트(`resident-uploads`→401·`feed`→200·없는 경로→404)가 실물에서 확인됐다
   (위 ①-3·④ 표 참조). #180 ②③ 처방은 이제 "커밋됨"이 아니라 "실배포에서 발동 확인됨" 상태다.
2. **QUEUE 새 1-1(舊 1-2) — 가용성 감시 신설**(인터넷 경로·배포 신선도·api 헬스 지속 감시, 대장
   #72 경보 7종과는 다른 축). 선행 없음. 동반 의무 채번 선행. ⚠️ 설계 시 "배포 직후 1회 판정은 그
   이후의 되돌림을 원리적으로 못 잡는다"(대장 #200 실측 근거, 이번에 재확인해 소실시키지 않고
   QUEUE에 보존)는 전제를 그대로 지고 간다 — 이것이 바로 그 사각을 지속적으로 덮는 층이다.

## ⑥ 어제(2026-09-03) 예정 대비

2026-09-03 보고서 ⑤가 예정한 것: *"QUEUE 1-1(새 번호)·대장 #180 소유 — 배포 후 스모크. 선행 없음.
동반 의무 없음."* → 실제로는 **예정된 스모크(②)에 더해 마이그레이션 게이트(③)까지 같은 슬라이스로
완주**했고(둘 다 #180의 처방 항목), **완주 과정에서 게이트② 자신이 자기 검증에서 한 번 반려당해
당일 수리**됐다(위 ② 상세 — "있는 척"을 막던 장치가 스스로 "있는 척"을 한 사례). 예정에는 없던
자기수리 사이클이 하루 더 걸린 셈이나, 커밋은 완결된 채 푸시됐다. **아직 main 미병합**이므로
"완료"는 이 세션의 기록 갱신에 한정되며, 실배포 검증은 다음 세션(위 ⑤-1)으로 이월한다.

⚠️ **위 문단은 작성 시점(main 미병합) 기준이며, 같은 날 이후 PR #94가 머지됐다**(위 ①·②·④ 참조,
규율 13 — 원문은 지우지 않고 여기 갱신만 덧붙인다). 실배포 검증(스모크·마이그레이션 게이트가 실제로
발동하는지)은 여전히 **머지 직후 생긴 새 승인 대기 2건**(headSha `1776c461`)이 처리돼야 확인된다 —
이월 대상이 바뀐 것은 아니고, "다음 승인"의 대상 SHA만 `80da6cf`→`1776c461`로 넘어갔다.
