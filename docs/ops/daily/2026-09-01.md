# 일간 2026-09-01 (화)

## ① 사용자 결정 대기 — 지금 필요한 것 **3건**

요청 시점 정본은 [QUEUE.md](../../plan/exec/QUEUE.md) "사용자 실행 항목" 절. 앞 2건은 어제 이월(미결),
3번째는 오늘 **QUEUE §C-1 요청 시점 도달**로 신규.

| # | 무엇 | 왜 나에게 오나 | 선택지 | 미결 시 영향 | 대기 |
|---|---|---|---|---|---|
| 1 | **스토어 계정 정본 정정** — 개인사업자는 Apple 조직 계정 불가(Individual, D-U-N-S 불요) | §0-2④ 결정의 문제(리드타임 판정이 바뀜) | ⓐ정정(07·08·QUEUE 3곳) ⓑ보류 | 승인 없이는 잘못된 8주 전제 잔존. 정정 시 **8주가 사라지고** Google 개인계정 *"테스터 12명×14일"*이 새 리드타임 | 2일째 |
| 2 | **촬영자 책임 모델(대장 #166) 유효성 확인**을 07 §6 14항목에 등재할지 | §0-2④ 결정의 문제 | ⓐ §6에 15번째 등재 ⓑ §3-3 각주로 유지 | 미결정 시 검증 안 된 책임 구조가 촬영 재개 판단에 쓰인다 | 2일째 |
| 3 ⭐ | **Quick Tunnel 고정 방식**(신규 도달 — 대장 #182 완료가 요청 시점) | §0-2③ 외부배선 필요 | ⓐ named tunnel(무료·도메인 불요·고정 호스트, **권장**) ⓑ 로그에서 호스트를 읽어 재기입하는 스크립트 | 이걸 정하지 않으면 **QUEUE 0-1(인터넷 경로 복구) 착수가 이 결정을 기다린다** | 신규 |

## ② 어제~오늘 무엇이 움직였나

- **대장 #182 완주** — `daejang-recheck.mjs` 판정 도구 자기검사·정정: 표 물리 복구·헤더 기반 열 판정·
  기대값 문법(ⓐ~ⓗ) 규정·표 분절(§6-11 빈 줄 2행) 수리·실행 안전. 재실행: 표 무결성 **0행**(259행 전건)·
  수동 판정 **45건**(등재 시 56 대비 감소).
- **게이트②가 FAIL 2회를 잡았다** → 각각 신규 채번·같은 슬라이스에서 즉시 해소: **#191**(`isSafe`
  화이트리스트 바이너리(sed·find·awk)의 위험 하위 기능 3종을 못 막던 것) · **#192**(도구 자신의 `\|`
  이스케이프 해제가 grep BRE alternation 표기와 충돌해 재현 명령이 리터럴로 뭉개지던 것).
- **舊 파서가 해소 사실을 가리고 있었다** — `--all`(전건 감사) 재판정에서 **#86·#127·#148**이 "일치"→
  "어긋남"으로 전환됐다(셋 다 이미 "수신 완료"/"문서화 완료" 상태). **판정 완료(조율자, 2026-09-01) —
  정상 동작, 조치 불요**: `--all`은 등재시값 대 현재값을 전수 대조하는 감사 모드이므로 이 전환은 설계된
  목적이고, 오히려 alternation이 살아나면서 **그 행들의 해소가 실제로 코드에 반영됐음을 역으로 확증**한
  것이다. 상태 칸 수정 불요.
- **scribe: 대장 #87·#103⑥ 상태 갱신** — 둘 다 코드는 이미 해소돼 있었다(#87은 2026-08-15 커밋
  `5ff75e2`, #103⑥은 2026-08-16 커밋 `772fffd`)는데 대장이 따라가지 못했다. #103은 8항목 중 **⑥만
  해소**이고 ①②③④⑤⑦⑧ 7건은 여전히 미수신이라 **행 전체를 해소로 바꾸지 않고 부분 해소로 기재**.
- QUEUE·HANDOFF·ROOT-CAUSE·DISCIPLINES **갱신 4건**(0단계 renumbering·§C-1 요청시점 도달·대장 #182
  해소 반영·규율 5·17 검사방법 칸 갱신). 상세: WORKLOG 2026-09-01(scribe 신규 기록).

## ③ 막힌 것

| 무엇 | 몇 번째 시도 | 다음 조치 |
|---|---|---|
| 인터넷 경로 502(Quick Tunnel 호스트 불일치, #179·#187) | 1 | QUEUE 0-1 착수 — 위 ①-3 사용자 결정 대기 중 |

## ④ 지금 상태 【자동 산출 — 사람의 판단 없음】

| 항목 | 값 | 재현 |
|---|---|---|
| origin/main | `8301eae`(PR #82 머지) | `git fetch -q origin main && git log --oneline -1 origin/main` |
| 열린 PR | **0** | `gh pr list --state open` |
| CI 3종 | CI ✅ · Build Images ✅ · Deploy Web ✅(headSha=8301eae 전부 success). **관측(조율자, 2026-09-01)**: 어제 Deploy Web이 **두 번** 돌았다(`325fd8c`=PR #81 머지 · `8301eae`=PR #82 머지, `gh run list --branch main` 확인) — `deploy-web.yml`의 `paths` 필터가 `pull_request` 트리거에만 있고 `push: branches:[main]`에는 없어 문서 전용 PR에도 웹이 재배포됐다. **결함 채번 안 함**(규율 19 — 낭비이나 누락보다 안전, 착수 트리거 없음). ⭐ 그 결과 배포 후 SHA 대조가 **두 번 연속 통과** — 대장 #186 계측기가 일회성이 아니라 **반복 작동함을 실증** | `gh run list --branch main -L 3 --json workflowName,conclusion,headSha` |
| **배포 이미지 나이** | **조율자 실측(2026-09-01, LAN HTTP — SSH 아님)**: **web 당일 최신**(`8301eae`, meta 태그 `<meta name="build-sha" content="8301eae...">` 직접 확인, main HEAD와 일치) · **api 미갱신**(`/health/version` → **404**, `/health/liveness` → 200이라 서비스 자체는 살아있음. **404는 측정 실패가 아니라 성공한 측정값** — 그 라우트는 `325fd8c`에 포함돼 있으므로 404는 api가 그 커밋을 아직 못 받았다는 뜻, 대장 #180 미해소의 실물 증거) · ai-worker·media-worker는 어제 ops-prober 실측치 유지(각 ~22일·~3일, 오늘 배포 없음) | `curl http://192.168.0.101:4000/health/version`(조율자 실행, LAN HTTP) |
| **인터넷 경로** | **❌ 502**(불변 — Quick Tunnel 호스트 불일치, 이번 슬라이스와 무관) | `curl -sI https://betty-roller-motel-summaries.trycloudflare.com`(공개 URL 읽기 전용 확인, SSH 아님) |
| 테스트 | api **903/903**(74스위트, 불변) + `infra/scripts` **77/77**(`verify-deployed-sha` 18 + `daejang-recheck` 59, #191·#192 신규 17건 포함·舊 49 — ⚠️ `&&` 순차 실행이라 요약 블록이 두 번 찍혀 마지막 것(59)만 보면 합계로 오독한다) | `pnpm --filter @gachinol/shared build && pnpm --filter @gachinol/api test` / `pnpm run test:scripts` |
| 대장 | **192행 / 대기 60**(舊 190행/대기64) | `node infra/scripts/daejang-recheck.mjs` |
| 빌드 스탬프 | **5건**(불변) | `grep -rl "GIT_SHA" services/*/Dockerfile infra/docker` |

## ⑤ 오늘 예정 — 1건

**0-1 · 대장 #179·#187 — 인터넷 경로 복구 + Quick Tunnel 재발 차단**

- **왜 이것인가**: L1(지금 사용자에게 도달하는 경로가 끊겨 있다) — 502는 사용자가 실제로 보는
  유일한 경로다. 재기동마다 100% 재발하는 구조(#187)라 값만 고치면 다음 재기동에 또 끊긴다.
- **선행 충족**: 없음(舊 0-1이던 대장 #182가 오늘 해소돼 행이 제거되며 당겨졌다)
- **동반 의무**: 없음
- **차단자**: named tunnel(권장 선택지)은 외부 배선이라 **사용자 승인 필요**(위 ①-3)

## ⑥ 어제 예정 대비

예정 `0-1(대장 #182) 착수` → 실제 `0-1(대장 #182) 완주`. **일치.**
