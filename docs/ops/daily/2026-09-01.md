# 일간 2026-09-01 (화)

## ① 사용자 결정 대기 — 지금 필요한 것 **2건**

요청 시점 정본은 [QUEUE.md](../../plan/exec/QUEUE.md) "사용자 실행 항목" 절.
⭐ **舊 3번째(Quick Tunnel 고정 방식)는 오늘 오후 사용자가 직접 실행해 해소됐다** — 도메인 `bapfull.com`
구입 + named tunnel `bapfull-xeon` 개통(QUEUE §C-1 충족). 아래 ②·④ 참조.
⭐ **舊 1번째(스토어 계정 정본 정정)는 오늘 사용자 결정(ⓐ정정)에 따라 반영 완료 — 해소** — 개인사업자는
Apple **individual 계정**(D-U-N-S 불요)으로 등록하고, 대신 Google Play 개인 계정 트랙에 **"테스터
12명×14일"**이 새 리드타임으로 확정됐다. 07 §6·08 §A W3 행·§E 1-1번·`exec/QUEUE.md` B-4·
`exec/E2-work-breakdown.md` T-NC-08 5곳 전부 정정 반영(scribe, 이 세션). 대신 아래 표에 **③(언론사
등록↔아동 동의 관계)을 법률자문 질문으로 신규 등재**한다 — 사용자가 "등록하면 아동 동의 문제가
풀린다"고 이해 중이나 07 정본은 그렇게 적지 않아(§3-3/§3-5), 확정 전제로 굳지 않게 **법률자문에
명시적으로 물을 질문**으로 기록했다(07 §3-3·§3-5 양쪽).

| # | 무엇 | 왜 나에게 오나 | 선택지 | 미결 시 영향 | 대기 |
|---|---|---|---|---|---|
| 1 | **촬영자 책임 모델(대장 #166) 유효성 확인**을 07 §6 14항목에 등재할지 | §0-2④ 결정의 문제 | ⓐ §6에 15번째 등재 ⓑ §3-3 각주로 유지 | 미결정 시 검증 안 된 책임 구조가 촬영 재개 판단에 쓰인다 | 3일째 |
| 2 | **언론사 등록↔아동 동의 관계 — 법률자문 질문 확인**(07 §3-3·§3-5에 "검증 대상 가설"로 기록 완료, 신규) | §0-2③ 외부배선·시크릿 아닌 결정의 문제이나, 법률자문 계약이 사용자 개인 진행이라 **질문을 실제로 전달하는 주체가 사용자** | ⓐ 기록된 질문 문구 그대로 법률자문에 전달 ⓑ 문구 수정 후 전달 | 미전달 시 "등록하면 아동 동의 의무가 풀린다"는 미검증 전제가 촬영 재개 판단에 계속 남는다(촬영은 되돌릴 수 없다) | 신규 |

## ② 어제~오늘 무엇이 움직였나

- **대장 #182 완주** — `daejang-recheck.mjs` 판정 도구 자기검사·정정: 표 물리 복구·헤더 기반 열 판정·
  기대값 문법(ⓐ~ⓗ) 규정·표 분절(§6-11 빈 줄 2행) 수리·실행 안전. 재실행: 표 무결성 **0행**(259행 전건)·
  수동 판정 **45건**(등재 시 56 대비 감소).
- **게이트②가 FAIL 2회를 잡았다** → 각각 신규 채번·같은 슬라이스에서 즉시 해소: **#191**(`isSafe`
  화이트리스트 바이너리(sed·find·awk)의 위험 하위 기능 3종을 못 막던 것) · **#192**(도구 자신의 `\|`
  이스케이프 해제가 grep BRE alternation 표기와 충돌해 재현 명령이 리터럴로 뭉개지던 것).
- **舊 파서가 해소 사실을 가리고 있었다** — `--all`(전건 감사) 재판정에서 **#86·#127·#148**이 "일치"→
  "어긋남"으로 전환됐다(셋 다 이미 "수신 완료"/"문서화 완료" 상태). **판정 완료(조율자, 2026-09-01) —
  정상 동작, 조치 불요**: `--all`은 등재시값 대 현재값을 전수 대조하는 감사 모드이므로 이 전환은 설계된
  목적이고, 오히려 alternation이 살아나면서 **그 행들의 해소가 실제로 코드에 반영됐음을 역으로 확증**한
  것이다. 상태 칸 수정 불요.
- **scribe: 대장 #87·#103⑥ 상태 갱신** — 둘 다 코드는 이미 해소돼 있었다(#87은 2026-08-15 커밋
  `5ff75e2`, #103⑥은 2026-08-16 커밋 `772fffd`)는데 대장이 따라가지 못했다. #103은 8항목 중 **⑥만
  해소**이고 ①②③④⑤⑦⑧ 7건은 여전히 미수신이라 **행 전체를 해소로 바꾸지 않고 부분 해소로 기재**.
- QUEUE·HANDOFF·ROOT-CAUSE·DISCIPLINES **갱신 4건**(0단계 renumbering·§C-1 요청시점 도달·대장 #182
  해소 반영·규율 5·17 검사방법 칸 갱신). 상세: WORKLOG 2026-09-01(scribe 신규 기록).

### 오늘 오후 — 인터넷 경로 개통 (⭐ 이 세션의 핵심 사건)

- **사용자가 도메인 `bapfull.com`을 구입하고 Cloudflare named tunnel(`bapfull-xeon`)을 개통했다.**
  Published application routes 5개(`watch`·`reporter`·`center`·`api`·`go` → 전부 `http://web:80`) 배선.
  **3앱 200 + 로그인 정상 동작**(scribe 재현, 2026-09-01): `curl -s -o /dev/null -w "%{http_code}"
  https://watch.bapfull.com` → 200 · `reporter.bapfull.com`/`center.bapfull.com` → 200 ·
  `api.bapfull.com/health/liveness` → 200. **대장 #179·#187·#172 해소**(QUEUE 0단계 renumbering 동반).
- **Deploy Web 4잡(build·preflight·deploy·purge) 전부 첫 성공** — purge는 도메인 미확정 동안 계속
  skip돼 오다 **이번이 최초 실행**이다(`gh run view 33443330150 --json jobs` 재확인, scribe).
- ⭐ **어제 만든 빌드 스탬프(대장 #186)가 오늘 실사고를 잡았다.** 사용자가 터널을 켜려 제온에서
  `docker compose up -d`를 수동 실행하자 web 서비스가 `WEB_IMAGE` 미설정 + 로컬 태그 폴백
  (`gachinol-prod-web`) 때문에 **`--build` 없이는 재빌드하지 않고 2026-08-21 빌드(빌드 스탬프 커밋보다
  10일 앞선 이미지)를 그대로 서빙**했다 — `<meta name="build-sha">` 대조로 발각. 화면은 200이라 빌드
  스탬프 없이는 알 수 없었다. **신규 채번 대장 #194.**
- 같은 사고 처리 과정에서 **제온 `~/gachinol`이 git 체크아웃이 아니라 파일 복사본**이라는 사실이
  드러났다(`git status` → *"깃 저장소가 아닙니다"*, 조율자 실측) — 리포 수정이 제온에 자동 전달되지
  않고, 배포 워크플로도 `compose pull && up`만 하며 compose 파일 자체는 갱신하지 않는다(scribe 구조
  재확인: `grep -c "rsync\|scp \|git pull\|git clone\|git checkout" .github/workflows/deploy-web.yml`
  → 0). **신규 채번 대장 #195**, #180과 같은 층으로 묶어 QUEUE 1-1 동반 의무로 편입.
- 대장·QUEUE·HANDOFF 3문서 갱신(scribe, 이 세션) — 상세는 각 문서 diff·WORKLOG 2026-09-01.

## ③ 막힌 것

**막힌 것 없음** — 인터넷 경로 502(#179·#187)가 오늘 오후 해소되며 유일한 막힌 항목이 사라졌다.

## ④ 지금 상태 【자동 산출 — 사람의 판단 없음】

> **2026-09-01 오후 재실측**(scribe, 인터넷 경로 개통 슬라이스 문서 정리 시점) — 아래 표는 오전 값을
> 대체한다. main이 `8301eae`(PR #82)에서 **PR #83·#84 머지로 `99c1e4f`까지 전진**했다.

| 항목 | 값 | 재현 |
|---|---|---|
| origin/main | `99c1e4f`(PR #84 머지 — "fix: cloudflared-compose-placement") | `git fetch -q origin main && git log --oneline -1 origin/main` |
| 열린 PR | **0** | `gh pr list --state open` |
| CI 3종 | CI ✅ · Build Images ✅ · Deploy Web ✅(headSha=99c1e4f 전부 success, **purge 잡 포함 4잡 전부 success** — 도메인 확정 후 첫 실행) | `gh run list --branch main -L 3 --json workflowName,conclusion,headSha` |
| **배포 이미지 나이** | **scribe 재현(2026-09-01, 인터넷 경유 curl — SSH 아님)**: **web 당일 최신**(`99c1e4f`, `curl -s https://watch.bapfull.com \| grep -o '<meta name="build-sha"[^>]*>'` → `content="99c1e4fc21d9151b473909a20919394d40964513"`, main HEAD와 일치) · **api 여전히 8/21 미갱신**(`curl -s -o /dev/null -w "%{http_code}" https://api.bapfull.com/health/version` → **404**, `/health/liveness` → 200이라 서비스 자체는 살아있음. **404는 측정 실패가 아니라 성공한 측정값** — 대장 #180 미해소의 실물 증거, #194·#195와 같은 층) · ai-worker·media-worker는 8/30 ops-prober 실측치 유지(각 ~22일·~3일, 오늘 배포 없음) | `curl -s -o /dev/null -w "%{http_code}" https://api.bapfull.com/health/version` |
| **인터넷 경로** | **✅ 200**(오늘 오후 복구 — 舊 502에서 전환. named tunnel `bapfull-xeon` + `bapfull.com`) | `curl -s -o /dev/null -w "%{http_code}" https://watch.bapfull.com` |
| 테스트 | api **903/903**(74스위트, 불변 — `git diff --stat 8301eae..99c1e4f -- apps services packages` → 0줄이라 재실행 없이 인용, 코드 diff 자체가 재현 근거) + `infra/scripts` **77/77**(`verify-deployed-sha` 18 + `daejang-recheck` 59, 재실행 확인 — PR #84는 `docs/`·`infra/docker/`만 건드려 스크립트 테스트는 불변이나 오늘 재실행으로 재확인) | `pnpm run test:scripts` |
| 대장 | **195행 / 대기 59**(舊 192행/대기60 — #179·#187·#172 해소(-3) + #194·#195 신규(+2)) | `node infra/scripts/daejang-recheck.mjs` |
| 빌드 스탬프 | **5건**(불변) | `grep -rl "GIT_SHA" services/*/Dockerfile infra/docker` |

## ⑤ 오늘 예정 — 1건

**(오전 계획, 오후 사건으로 대체됨 — 이력 보존)** ~~0-1 · 대장 #179·#187 — 인터넷 경로 복구 + Quick
Tunnel 재발 차단~~ — 왜: L1(사용자 도달 경로 불통) · 선행 없음 · 차단자: named tunnel은 외부 배선이라
사용자 승인 필요(위 ①-3, 구 문언). **오후에 사용자가 직접 실행해 이 항목 자체가 해소됐다** — 아래 ⑥.

**다음 날(2026-09-02) 예정 — 1건**: **QUEUE 0-1 · 대장 #189 — URL 형태 검증**(`new URL()` 파싱,
`<` 포함 시 실패 — `bundle-budget.mjs` 자리에 붙인다). 선행 없음·차단자 없음(순수 코드 작업, 사용자
실행 불요). 그다음(0-2)은 **대장 #170**(제온 백엔드 풀스택 배포) — C-3(`DROP COLUMN` 승인, 위 ①)
미도달이라 착수해도 완결은 승인 이후다.

## ⑥ 어제 예정 대비

예정 `0-1(대장 #182) 착수` → 실제 `0-1(대장 #182) 완주`. **일치.**

### 오늘 예정 대비 (오후 추가분)

예정 `0-1(대장 #179·#187) 우리 팀이 착수` → 실제 `사용자가 도메인 구입 + named tunnel 개통으로
직접 해소(같은 결과, 다른 실행 주체)`. **결과는 일치, 경로는 계획과 다름** — 착수 전 사용자 승인
대기 중이던 항목을 사용자가 승인과 동시에 직접 실행해 우리 팀의 착수 자체가 불필요해졌다. 부산물로
**신규 결함 2건**(#194 web 이미지 롤백·#195 제온 non-git)이 발견돼 순변화는 대장 대기 -1(59, 위 ④).
