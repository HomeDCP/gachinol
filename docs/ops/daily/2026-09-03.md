# 일간 2026-09-03 (목)

## ① 사용자 결정 대기 — 지금 필요한 것 **0건**

요청 시점 정본은 [QUEUE.md](../../plan/exec/QUEUE.md) "사용자 실행 항목" 절. 오늘 새로 도달한
요청 시점 0건(§D-2 카카오 실 송출 범위는 2단계 완료 시 요청 — 아직 미도달).

⭐ **오늘 이미 승인받은 것 2건**(아래 표에는 없음, ②에 상세): GitHub Environment `production`
required reviewer 게이트를 통해 **배포 승인 2건** — Build Images 워크플로의
`deploy (api·media-worker·ai-worker → 제온)` 잡 승인 1건 + Deploy Web 워크플로의
`deploy (정적 산출물 → 제온 web 컨테이너)` 잡 승인 1건. `gh run view <id> --json jobs`의
`startedAt` 간격(build 종료 `05:02` → deploy 시작 `05:41`, 약 39분 대기)이 승인 대기가 실제로
걸렸다는 정황이다.

(오늘 신규로 도달한 결정 대기 0건)

## ② 오늘 무엇이 움직였나 — QUEUE 1-1(대장 #180) 완주 + 신규 결함 실발생·당일 해소(대장 #200)

- **대장 #180 해소 — api·워커 자동 배포 경로 실가동.** PR #91(`ci(deploy): api·워커 자동 배포 경로
  신설 + 규율 2 기계화`, 커밋 `76014bc`+`f95a9ee`, 병합 `5a5d56e` · 2026-09-02T15:12:48Z)이
  `.github/workflows/build-images.yml`에 api·media-worker·ai-worker deploy 잡을 신설했다(`deploy-web.yml`
  잡 패턴 복제 — `pull && up -d --no-build`+배포 후 SHA 대조). 어제까지 api 12일·ai-worker 24일
  스테일이던 것이 오늘 해소됐다.
- **신규 결함 실발생 — 대장 #200.** PR #91 병합 직후 첫 실배포(2026-09-02 15:17~15:20)가 즉시 새
  결함을 드러냈다: `docker compose up -d <service>`는 `--no-deps` 없이는 의존 서비스도 함께
  처리한다 — `deploy-web.yml`이 `up -d --no-build web`을 돌리자 `depends_on: api`인 api까지 함께
  평가돼 `${IMAGE_TAG:-latest}`(제온에 캐시된 옛 이미지)로 되돌아갔다. 15:18의 SHA 대조는 실제로
  통과했었다 — 2분 뒤 다른 워크플로가 덮었을 뿐이다. ⚠️ **이 결함은 어제까지 존재할 수 없었다** —
  api 배포 경로 자체가 없었기 때문이다. 두 번째 자동 배포 경로가 생기는 순간 처음 성립했고, 그 첫
  실배포에서 바로 드러났다.
- **당일 수리 — PR #92**(`fix(deploy): 두 배포 워크플로가 서로의 배포를 되돌린다 — up에 --no-deps`,
  커밋 `63b7a05`, 병합 `6651666` · 2026-09-03T04:59:44Z). 두 워크플로의 `up`에 `--no-deps`를 대칭
  적용(`pull`에는 넣지 않음 — `pull`은 기본 방향이 반대). 로컬 재현(구현자, 동형 최소 compose):
  `up --no-build <downstream>` → upstream Recreate·기본 태그로 되돌아감(사고 재현) /
  `up --no-build --no-deps <downstream>` → upstream 유지(수리 확인).
- **최종 실배포 확인(2026-09-03, scribe 재현)**: `curl -s https://api.bapfull.com/health/version` →
  `{"sha":"665166691ae0d998e82f841978f1f7cb186d60d2"}`(舊 404). CI 잡 단위(`gh run view 33717077694
  --json jobs`): preflight✅·build api/media-worker/ai-worker✅·**deploy✅** — 로그 원문 `SHA 일치
  확인(kind=api): 6651666...`·`(media-worker): 6651666...`·`(ai-worker): 6651666...`. 수리 후
  deploy-web 로그에 `Container gachinol-prod-web-1 Recreate`만 있고 **api 언급 0건**(舊 실발생
  배포 잡엔 `gachinol-prod-api-1 Recreate/Recreated` 2줄).
- **규율 2 기계화(동반 의무 D1, 같은 PR #91)** — `infra/scripts/dockerfile-workspace-deps.mjs` 신설
  (워크스페이스 의존 ⊆ Dockerfile COPY 목록 판정, fail-closed). DISCIPLINES 규율 2의 "검사 방법" 칸을
  *"기계화 가능 — 미구현"*에서 **구현됨**으로 갱신. `pnpm run test:scripts` → **121건**(舊 92건).
- **문서 갱신(scribe, 이 세션)**: PIVOT-PLAN 대장 #180 해소 갱신 + 신규 #200 채번(200행/대기 57) ·
  QUEUE 1-1 행 제거(번호 당김 — 舊 1-2가 새 1-1, 舊 1-3이 새 1-2) + 1-1·1-2 양쪽에 "배포 직후 검증의
  원리적 한계"·"지속 감시가 그 사각을 덮는 이유" 각주 신설(순서는 조율자 판정으로 불변) ·
  DISCIPLINES 규율 2 갱신 · HANDOFF "다음 1건"을 새 1-1(배포 후 스모크)로 전진.

## ③ 막힌 것

**착수 자체는 막혀 있지 않다.** 새 1-1(대장 #180 소유 — 배포 후 스모크)은 선행 0(舊 1-1 해소로
충족), 동반 의무 없음. 단 ⚠️ **설계상 한계를 전제하고 설계할 것** — 오늘 실측(대장 #200)이 "배포
직후 1회 판정은 그 이후의 되돌림을 원리적으로 못 잡는다"는 것을 증명했다. 그 사각은 스모크가 아니라
다음 순번인 새 1-2(가용성 감시 신설, 지속 감시 축)의 몫이다.

## ④ 지금 상태 【자동 산출 — 사람의 판단 없음】

| 항목 | 값 | 재현 |
|---|---|---|
| origin/main | `6651666`(PR #92 머지) | `git fetch -q origin main && git log --oneline -1 origin/main` |
| 열린 PR | **0**(#91·#92 둘 다 병합 완료) | `gh pr list --state open` |
| CI 3종(main, headSha=6651666) | CI ✅ · Build Images ✅ · Deploy Web ✅ 전부 success | `gh run list --branch main -L 3 --json workflowName,conclusion,headSha` |
| **배포 이미지 — api** | **✅ main HEAD와 일치**(舊 404 → 오늘 배포로 해소) | `curl -s https://api.bapfull.com/health/version` → `{"sha":"665166691ae0d998e82f841978f1f7cb186d60d2"}` |
| **배포 이미지 — web** | ✅ main HEAD와 일치 | `curl -s https://watch.bapfull.com \| grep -o '<meta name="build-sha"[^>]*>'` → `content="665166691ae0d998e82f841978f1f7cb186d60d2"` |
| `/v1/health/version`(전역 prefix 안) | **404**(설계대로 — version은 prefix `exclude` 목록에 있어 prefix를 안 탄다) | `curl -s -o /dev/null -w "%{http_code}" https://api.bapfull.com/v1/health/version` |
| 인터넷 경로 | ✅ **200**(watch·reporter·center·api liveness 4곳 전부) | `curl -s -o /dev/null -w "%{http_code}" https://watch.bapfull.com` 외 3곳 동형 |
| 테스트 — api | **903/903**(불변 — `git diff --stat 4368ba5..6651666 -- apps services packages` → 0줄, PR #91·#92는 `.github/workflows`·`infra/scripts`만 건드림) | `pnpm --filter @gachinol/api test` |
| 테스트 — infra/scripts | **121/121**(舊 92 — `dockerfile-workspace-deps.test.mjs` 29건 신규) | `pnpm run test:scripts` |
| 대장 | **200행 / 대기 57**(舊 199/58 — #180 해소로 대기 이탈, 신규 #200은 등재와 동시에 해소돼 순증 0) | `node infra/scripts/daejang-recheck.mjs` |
| 빌드 스탬프 | `grep -rl "GIT_SHA" services/*/Dockerfile infra/docker` → **6**(불변) | 위 명령 |

## ⑤ 오늘 예정 — 1건

**QUEUE 1-1(새 번호) · 대장 #180 소유 — 배포 후 스모크**(미인증 요청의 `≠404`로 라우트 실재 판정,
없는 경로 음성 대조 필수). 선행 없음. 동반 의무 없음. ⚠️ 설계 시 "배포 직후 1회 판정의 원리적
한계"(오늘 대장 #200 실측 근거)를 전제할 것 — 스모크만으로 "재발 방지 완결"이라 보고하지 않는다.

## ⑥ 어제 예정 대비

예정 `QUEUE 1-1 · 대장 #180 — api·워커 배포 경로 신설(web 잡 복제)` → 실제 **완주** + **계획에
없던 신규 결함 1건(대장 #200)이 완주 과정에서 실발생해 같은 배포 층에서 당일 해소**. 어제 시점엔
"차단자(승인 게이트) 해제됐으니 착수 가능"까지만 적었는데, 오늘 실제로 착수해 **PR #91로 경로를
열자마자 그 경로의 첫 실배포가 상호 되돌림을 실물로 드러냈고, PR #92로 같은 날 닫았다.** 이 리포가
반복해 온 패턴대로 — "결함 하나를 닫으면 그 자리에서 다음 계층의 결함이 드러난다" — 가 배포 인프라
축에서도 그대로 재현됐다.
