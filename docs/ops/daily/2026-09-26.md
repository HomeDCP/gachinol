# 일간 2026-09-26 (토)

⚠️ **2일 공백** — 직전 일간보고서는 `2026-09-23.md`다(`ls docs/ops/daily/ | sort | tail -1` →
`2026-09-23.md`). 2026-09-24·25는 소급 생성하지 않는다 — 없던 실측을 지어내게 되므로 공백
사실만 기록한다. ⚠️ 오늘은 토요일이라 CLAUDE.md §0-4가 주간보고서도 요구하지만, 이번 발주
범위가 아니라 만들지 않았다(`docs/ops/weekly/` 디렉터리 자체가 없는 상태가 계속 이월 중,
아래 ⑤ 참조).

## ① 사용자 결정 대기 — 지금 필요한 것 2건

1. ⭐ **Environment `required_reviewers` 해제**(사용자 실행, GitHub 저장소 설정) — **대장 #243
   (배포 자동 롤백 구현) 머지 후에 실행**한다. 사용자가 머지 후 자동 배포 전환을 요청했고
   (원문: *"내가 머지 후 승인을 하지마는 … 머지하기만 하면 deploy web 그리고 build images 가
   자동으로 진행되도록 하면 더 편할것 같은데"*) 조율자가 "자동 롤백 선구현 → 게이트 제거" 순서를
   제안해 사용자가 승인했다. **지금은 아직 실행 시점이 아니다** — #243이 처방·구현·머지되기
   전에 해제하면 그 사이가 보호 공백이 된다.
2. **대장 #243(배포 자동 롤백 구현) 처방 판정**(조율자·planner) — scribe는 판정하지 않는다.
   상세는 ②.

## ② 오늘 완료

- **대장 #240·#241 해소** — PR #116·#117 전부 머지·배포 완료. scribe 재확인:
  `curl -s https://api.bapfull.com/health/version` → `{"sha":"0f9be3f..."}` = main HEAD와
  일치(프로덕션 실배포 확인). `gh pr view 116 --json state,mergedAt` → `MERGED` /
  `2026-09-26T03:43:05Z`(머지커밋 `ecbb5a9`) · `gh pr view 117 --json state,mergedAt` →
  `MERGED` / `2026-09-26T03:49:31Z`(머지커밋 `0f9be3f`, main HEAD와 일치). ⚠️ **단 "세로
  실촬영본 종단 실증"은 아직 없다** — `MEDIA_MASTER_LONG_EDGE`·`MEDIA_MASTER_SHORT_EDGE` 값
  자체는 조율자 실측 인용(scribe는 제온 SSH 금지라 독자 재현 못함)이며, 세로 신규 촬영본이
  실제로 1920 긴 변으로 나오는 것은 태스크①(대장 #232) 선례처럼 별도 확인이 필요해 사용자
  대기 목록에 남겼다(위 ① 참조 — 이번엔 필수 대기는 아니고 관찰 유지 항목).
- **대장 #243 신규 채번 — 배포 후 검증 실패 시 자동 복구 장치가 없다**(조율자 실측 2026-09-26,
  인용 — scribe는 `.github/**`·제온 SSH 비접근이라 독자 재현 못함, 단 GitHub API 조회는 직접
  재확인함 — 아래 참조). `build-images.yml`의 `deploy` 잡이 이미지를 `pull`+`up -d`한 **다음에**
  검증 3종(SHA 대조·라우트 스모크·SHA 대조)을 돈다 — 검증 실패 시점엔 이미 새 이미지가 서빙
  중이라는 뜻. 복구는 수동이며 2026-09-12에 `IMAGE_TAG` 누락으로 옛 `latest` 롤백 사고가 이미
  있었다. **⭐ scribe 독자 재현(2026-09-26, `gh api` — 파일 읽기가 아니라 GitHub API 호출이라
  `.github/**` 금지 대상 아님)**: `gh api repos/HomeDCP/gachinol/environments/production` →
  `protection_rules[0].type: "required_reviewers"`·`prevent_self_review: false`·
  `reviewers[0].reviewer.login: "HomeDCP"` — 조율자가 인용한 "본인이 본인 배포를 승인하는
  구조"를 그대로 확인했다. 완화 요인(과장 금지, 조율자 실측 인용): 배포에 DB 마이그레이션
  단계가 없어 데이터 파괴 경로 자체가 없고, `preflight`가 승인 전에 파괴적 마이그레이션 검사·
  시크릿 fail-closed를 이미 수행한다 — "데이터 유실 위험"이 아니라 "복구 자동화 부재". 처방
  미정(planner 발주됨). scribe 재확인: `grep -n "^| 243 " docs/plan/PIVOT-PLAN.md` → 행 실재,
  `grep "^| 243 " docs/plan/PIVOT-PLAN.md | grep -c "롤백"` → 1 이상.
- **대장 #242 진행 상황 — 변화 없음**(처방 미정 유지). PIVOT-PLAN·HANDOFF 양쪽에 그대로 기록.
- **HANDOFF "지금의 다음 1건" 절 6차 갱신**(scribe) — 새 절을 쌓지 않고 기존 절을 갱신(규율 13).
  PR #116·#117 머지 반영, #240·#241 해소, #243 신규 채번 문단, 다음 1건 = #243, 사용자 대기
  목록에 ⓙ(세로 실촬영 실증)·ⓚ(required_reviewers 해제, #243 후행 조건 명시) 추가. scribe
  재확인: `grep -c "^#.*다음 1건" docs/plan/exec/HANDOFF.md` → **1**.

## ③ 오늘 드러난 것

- ⭐ **배포 승인 게이트가 자기 검토 구조였다** — `required_reviewers`의 유일한 리뷰어가 배포를
  트리거하는 본인이고 `prevent_self_review: false`라 자기 승인이 허용된다(scribe 독자 재현,
  위 ② 참조). "리뷰할 능력이 없어서 사실상 리뷰없이 승인된다"는 사용자의 관찰이 구조적으로
  맞았다 — 이미 대장 #202(승인 26시간 방치, 2026-09-20 감시가 잡은 4.48h·9.67h 방치)라는
  실패 모드로 나타난 바 있다.
- **배포 파이프라인에 자동 롤백이 없다** — 검증이 실패해도 이미 서빙 중인 새 이미지를 되돌리는
  자동 경로가 없고, 유일한 안전판은 제온에 보존된 이전 이미지 세대(`docker image prune` 금지
  정책 덕분)와 사람의 수동 개입뿐이다(대장 #243, 위 ② 참조).

## ④ 확인 불가 (scribe 독자 재현 수단 없음)

- **제온 실물 배포 확인**(`sha-0f9be3f` 이미지 태그, `deploy` 잡 실제 실행 여부) — 제온 SSH
  권한이 필요하다. 조율자 실측 인용.
- **`/app/dist/env.js`의 `MEDIA_MASTER_LONG_EDGE`·`MEDIA_MASTER_SHORT_EDGE`·`MEDIA_MASTER_HEIGHT`
  fail-fast 코드 실측값** — `services/**`·제온 SSH가 필요하다. 조율자 실측 인용.
- **제온에 보존된 이전 이미지 세대 목록**(`sha-ecbb5a9`·`sha-9a6a0e9`·`sha-e974eec`·
  `sha-3a92169`) — 제온 SSH가 필요하다. 조율자 실측 인용, `docker images` 재현 명령은 사용자·
  조율자 실행 필요로 대장 #243 행에 남겨 뒀다.

## ⑤ 지금 상태 【자동 산출 — 사람의 판단 없음】

| 항목 | 값 | 재현 |
|---|---|---|
| 이 작업 디렉터리 HEAD | `bd9a412`(브랜치 `docs/ffmpeg-divergence-242`, 머지 완료 — 조율자가 새 브랜치를 팔 예정) | `git rev-parse --short HEAD && git branch --show-current` |
| 미커밋 변경 | `docs/plan/PIVOT-PLAN.md`·`docs/plan/exec/HANDOFF.md` 수정(오늘 scribe 세션이 만든 것) + `docs/ops/daily/2026-09-26.md` 신규 + `.claude/launch.json`(세션들과 무관한 기존 파일) — 전부 미커밋, `main`·`services/**`·`infra/**`·`.github/**`는 접근 안 함 | `git status --short` |
| 열린 PR | **0**건(PR #116·#117 전부 머지 완료) | `gh pr list --state open --json number,title` → `[]` |
| 배포 서빙 중 — api | `0f9be3fb108b682102d143ea8349033b6881a2bf` = main HEAD와 **일치**(프로덕션 실배포 확인) | `curl -s https://api.bapfull.com/health/version` |
| readiness / liveness | 200 / 200 | `curl -s -o /dev/null -w "%{http_code}" https://api.bapfull.com/health/readiness`(+`/health/liveness`) |
| 배포 승인 대기(`waiting`·`action_required`) | **0**건 / **0**건 | `gh api "repos/HomeDCP/gachinol/actions/runs?status=waiting" --jq '.total_count'`(+`?status=action_required`) |
| production Environment 보호 규칙 | `required_reviewers`(리뷰어 `HomeDCP` 1명, `prevent_self_review: false`) | `gh api repos/HomeDCP/gachinol/environments/production` |
| 대장 재현(전건) | 대상 **78**행 · 표 무결성 **0**행 · 어긋남 **1**(#173, carry — 무관) · 일치 25 · 수동 74 · 오류 0 | `node infra/scripts/daejang-recheck.mjs` |
| 대장 재현(오늘 반영분) | 240·241·242·243 표 무결성 **0**행 · 어긋남 **0**건 · 오류 **0**건 | `node infra/scripts/daejang-recheck.mjs --only 240,241,242,243` |
| PIVOT-PLAN 최대 번호 | **243**(舊 242, 오늘 #243 채번) | `grep -oE "^\| [0-9]+ " docs/plan/PIVOT-PLAN.md \| tr -d "\| " \| sort -n \| tail -1` |
| HANDOFF/ARCHIVE/PIVOT-PLAN/verifier 줄수 | `HANDOFF.md` **1370**줄 · `HANDOFF-ARCHIVE.md` **820**줄 · `PIVOT-PLAN.md` **637**줄 · `verifier.md` **59**줄 | `wc -l docs/plan/exec/HANDOFF.md docs/plan/exec/HANDOFF-ARCHIVE.md docs/plan/PIVOT-PLAN.md .claude/agents/verifier.md` |
| HANDOFF "다음 1건" 절 개수 | **1**(같은 절 6차 갱신, 늘지 않음) | `grep -c "^#.*다음 1건" docs/plan/exec/HANDOFF.md` |
| HANDOFF 함정 절 순서 | 09-23 → 09-22 → 09-20~21 → 09-20 → 09-19(시간 역순 유지, 09-26엔 신설 없음) | `grep -n "^## 함정" docs/plan/exec/HANDOFF.md` |
| 주간 보고서 | `docs/ops/weekly/` **디렉터리 자체가 없음**(계속 이월, 오늘이 토요일이라 정본상 발주 대상이나 이번 작업 범위 밖) | `ls docs/ops/weekly/` |

## ⑥ 다음 세션 예정

1. **대장 #243 처방 판정**(조율자·planner) — 배포 후 검증 실패 시 자동 롤백 방식 설계.
2. **대장 #243 구현·머지**(구현자) — 처방 확정 후.
3. **Environment `required_reviewers` 해제**(사용자 실행) — #243 머지 후에만.
4. **세로 실촬영본 1건 업로드로 #240·#241 종단 실증**(사용자) — 선택적 확인, 필수 차단은 아님.
5. **대장 #242 처방 판정**(조율자·planner) — 여전히 미정.
6. **CLAUDE.md §11 재실측 갱신 발주**(조율자, 09-23 보고서부터 이월) — media-worker 35(stale)
   →44→52, api 1067.
7. **존재하지 않는 "대장 #244" 코드 주석 정정**(구현자, 09-23 보고서부터 이월) — 7곳.
8. **주간 보고서 생성 여부 결정**(사용자, 여러 세션째 이월 — 오늘이 토요일이라 정본상 발주
   대상이지만 이번 세션 범위 밖).
9. **일간보고서 공백(09-14~09-21, 09-24~09-25) 소급 여부**(사용자, 이월).
10. **대장 #230·#234 원본 화질 감사 공백 관찰 후속**(조율자, 이월).
11. **대장 #239·#240 처방 설계**(조율자·사용자) — 짝지어 함께 설계할 것(이월, #240은 해소됐지만
    설계 원칙 상호 참조는 여전히 유효).
