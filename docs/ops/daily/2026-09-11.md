# 일간 2026-09-11 (금)

## ① 사용자 결정 대기 — 지금 필요한 것 **1건**

요청 시점 정본은 [QUEUE.md](../../plan/exec/QUEUE.md) "사용자 실행 항목" 절.

1. **PR #99 머지 승인.** 브랜치 `fix/reporter-capture-stt-canon`, 상태 `OPEN`, `mergeable: CONFLICTING`
   (scribe 재확인: `gh pr view 99 --json state,mergeable` — origin/main이 PR #98 머지로 앞서 나가면서
   이 브랜치와 문서 2개(`docs/ops/daily/2026-09-11.md`·`docs/plan/exec/HANDOFF.md`)에서 충돌했다).
   **이 세션(scribe)이 충돌 마커를 제거하고 양쪽 내용을 통합했다**(아래 ②) — `docs/plan/PIVOT-PLAN.md`·
   `docs/plan/exec/QUEUE.md`는 git이 이미 자동 병합했고 scribe가 내용 무결성만 확인했다(양쪽 채번·상태
   전부 생존, 아래 ②·④ 참고). 남은 것은 조율자가 `git add`·커밋·푸시로 PR을 갱신하고 CI 재통과를 확인한
   뒤 머지하는 것. 충돌 전 마지막 CI(scribe 재확인 `gh pr checks 99`, 완료 시각 2026-09-11T01:13~01:17 UTC)는
   체크 **3개 SUCCESS**(`node (lint·typecheck·test)`·`ai-worker (ruff·pytest)`·`build web(export+번들
   예산 게이트)`), **3개 SKIPPED**(`preflight`·`deploy`·`purge` — PR이라 배포 잡은 원래 스킵). 병합
   커밋 반영 후에는 재확인이 필요하다. **문서+앱 코드 PR**(2-1 유령 영상 수리 `apps/reporter/**` +
   3-3 STT 정본 정합 `docs/**`, 대장 #207 채번 + 게이트②가 잡은 #208·#209 신규 채번).
   ~~PR #98 머지 승인~~은 이미 완료됐다(`mergedAt 2026-09-11T07:52:33Z`, 병합커밋 `7088dbe` —
   아래 ② 참조. 더 이상 결정 대기가 아니다).

## ② 오늘 무엇이 움직였나

오늘 두 갈래 작업이 각각 다른 브랜치에서 독립적으로 진행되다 겹쳤다 — **PR #98**(`docs/monitor-203-closeout`,
내용 커밋 `a63d98e` 07:42 KST)이 먼저 작성되고 한참 뒤(16:52 KST) 머지됐고, 그사이 **PR #99**(이 브랜치,
`fix/reporter-capture-stt-canon`, 커밋 `5d86291`·`7f4ff33` 10:12 KST)가 PR #98이 아직 열려 있는 상태를
보며 독립적으로 진행됐다(scribe `git log --oneline --date=iso-local --pretty="%h %ad %s"` 재확인). 아래는
그 두 갈래를 시간순으로 기록한다.

### PR #98 — 확인 방법 ⓐ 실증 + 대장 #203 해소 + QUEUE 1단계 완주 (scribe, 머지 완료 `7088dbe`)

**scribe는 코드를 만들지 않는다.** 이 세션은 조율자가 전달한 사용자 실측(Better Stack 임시 모니터)을
정본 3곳(대장·QUEUE·HANDOFF)에 반영하고, 근거 수치를 전부 그 자리에서 재실행해 확인한 기록이다.

- **확인 방법 ⓐ 실증(사용자 실행, 2026-09-10)** — Better Stack에 존재하지 않는 URL
  (`https://api.bapfull.com/__monitor_test_404__`, scribe 재확인: `curl -s -o /dev/null -w
  "%{http_code}" https://api.bapfull.com/__monitor_test_404__` → **404**)로 3분 주기 임시 모니터를
  만들어 **알림 수신 확인 후 즉시 삭제**(사용자 보고 원문: *"알림 왔다 확인 후 삭제도 했다"*). 채널은
  텔레그램이 아니라 **Better Stack 앱 푸시·이메일**이었다(Better Stack은 텔레그램 미지원 — scribe
  재확인: `curl -s -o /dev/null -w "%{http_code}" -L
  https://betterstack.com/docs/uptime/integrations/telegram` → **404**). ⓐ가 요구한 "운영자 휴대폰
  도달"은 채널이 ⓑ(텔레그램)와 달라도 충족이다. 임시 모니터는 삭제돼 영구 오탐 소스로 남지 않았다.
- **대장 #203 상태 칸을 해소로 갱신**(`docs/plan/PIVOT-PLAN.md`) — 확인 방법 ⓐⓑ 모두 실증돼 결함
  본문 세 축(인터넷 도달성·배포 신선도·api readiness를 묻는 주체)이 전부 채워졌다. ⚠️ **해소되지
  않은 채로 남긴 것**: 선언 주기(15분)와 실측 평균 간격의 괴리, 임계(1h/6h)가 첫날 표본 기반
  임시값이라는 것(아래 ③에서 최신치로 갱신).
- **QUEUE 1단계 완주**(`docs/plan/exec/QUEUE.md`) — 1단계의 유일한 행(1-1, 대장 #203)이 제거돼 표가
  비었다. §C C-4·C-5도 완료로 갱신(C-5는 이제 ⓐ 실증까지 됨).
- **24시간 실측(scribe 재실행 2026-09-10T22:26:48Z, 조율자 전달값과 일치)** — `gh run list --branch
  main --workflow monitor.yml --limit 60 --json event,createdAt,conclusion --jq '.[] | select(.event==
  "schedule" or .event=="workflow_dispatch") | [.createdAt,.event,.conclusion] | @tsv' | sort` → 당시
  간격 7개: 최소 **1.34h**·최대 **5.21h**·평균 **3.31h**·7h 허용치 대비 여유 **1.79h**(이후 추가 관측치는
  아래 ④에서 이 세션이 재집계).
- **재현 확인(변경 부작용 없음)** — `node infra/scripts/daejang-recheck.mjs` → 대상 **61→60**행(1건
  감소, #203이 더 이상 미해소로 카운트되지 않음) · 어긋남 **0**(불변) · `pnpm run test:scripts` →
  8스위트 **341/341** pass(불변 — 문서만 바뀌어 코드 회귀 없음, 이후 PR #99가 스위트를 9개로 늘림 —
  아래 참고·④).

**머지 완료(2026-09-11T07:52:33Z UTC, 병합커밋 `7088dbe`, `gh pr view 98 --json mergedAt,mergeCommit`
재확인)** — 위 항목 전부 지금 origin/main에 반영돼 있다.

### PR #99 — STT 로컬 전환 정본 정합(대장 #207) + 게이트②가 잡은 비용표 모순 수리 (scribe, `docs/**`만) — 2-1(유령 영상 수리)과 병행

**scribe는 코드를 만들지 않는다.** 이 세션은 같은 브랜치(`fix/reporter-capture-stt-canon`)에서
구현자가 별도로 진행 중인 2-1과 병행해, 직전 3-3 세션(대장 #207)이 빠뜨린 곳을 게이트②(독립 검증)가
잡아낸 것을 수리한 기록이다.

- **2-1(QUEUE — 대장 #173·#188, "유령 영상" 수리 + `app/**` 로직 금지 린트, D1 적용례)** — 같은
  브랜치에서 구현자가 동시 진행했다(코드 변경, 이 daily의 scribe는 `apps/**`·`infra/scripts/**` 접근
  금지라 상세를 확인하지 않았다). `git status --short`로 관측되는 범위만 기록한다: `apps/reporter/src/capture/`
  (신규 디렉터리) + upload 서비스 3파일 수정 + 신규 게이트 스크립트
  `infra/scripts/reporter-app-thinness-gate.mjs`(+ 테스트 파일). 이 세션이 재실행한
  `pnpm run test:scripts`는 신규 게이트 스위트(24건) 포함 **9스위트 365/365 pass**를 보였다(당시 값 —
  머지 후 최신 재실행은 아래 ④).
  `node infra/scripts/daejang-recheck.mjs`의 **#173 어긋남 1건**(기재 0 → 실측 3,
  `grep -rl "launchCameraAsync" apps/reporter/app apps/reporter/src`)은 이 수리가 진행 중이라
  등재값과 달라진 것으로, 위임문이 예고한 **정상 상태**다.
- **3-3(대장 #207 — STT 로컬 전환 정본 정합)** — 직전 세션이 2026-08-20 STT 로컬 전환(舊 RTZR API →
  whisper.cpp+Silero VAD, 근거는 데이터 주권)을 파일별 `RTZR` 문자열 grep으로 대상을 찾아 **7개 파일
  24건**을 정정했다(`01`·`02`·`03`·`05-monetization.md`·`07`·`08`·`docs/infrastructure.md`,
  `docs/plan/exec/HANDOFF.md` 2026-09-11 절 참고). **게이트②(독립 검증)가 그 방법론의 빈틈을
  잡았다**: `docs/infrastructure.md`는 MVP·확장(12지사) 두 표 모두 STT 행이
  "~~舊 값~~ 재산정 필요(미실측)"로 고쳐졌는데, `docs/plan/05-monetization.md` §3 "확장기(4~6지사)"
  표의 STT 행(당시 274행, `STT (~132h@12지사) | ~$83 | $22~35`)은 **안 고쳐진 채 남아 있었다** —
  같은 데이터포인트(12지사 STT 舊 $83)를 두 문서가 서로 다르게 표기하는 모순이었다.
- **왜 놓쳤는가(이번 발견의 핵심)**: scribe 재확인 — `grep -c "RTZR" docs/plan/05-monetization.md`
  → 정정 직후 **2**(236·246행뿐). 274행에는 "RTZR" 문자열이 **전혀 없다**(달러 금액만 인용하고
  provider명을 반복하지 않는다) — 그래서 provider 이름 문자열 grep이라는 탐색 방법론 자체가 이 행을
  원리적으로 못 찾았다. 대장 #207 비고에 이 발견과 재검토 결과를 기록했다(아래 참고).

#### 모순 수리 상세(이 세션, `docs/**` 3파일)

1. **`docs/plan/05-monetization.md:274`(정정 후 현재 위치 **276행** — 아래 참고)** —
   `infrastructure.md` §3 확장표(90행)와 동일 패턴으로 정정:
   `STT (~132h@12지사) | ~~$83~~ 재산정 필요(미실측, infrastructure.md §3 확장 표 인용 — 舊 RTZR→로컬
   전환 2026-08-20) | ~~$22~35~~ 재산정 필요(미실측 — $83 보간값이라 원천과 함께 무효)`. ⚠️ **행 번호가
   274→276으로 밀렸다** — 이 STT행 자체의 정정(1줄→1줄 치환)이 아니라, **이보다 앞선 "MVP 총계"
   경고 블록쿼트 삽입(아래 3번, +2줄)이 원인**이다. `daejang-recheck.mjs` 등 자동 도구가 아니라 이
   보고서를 손으로 읽을 때 착오하기 쉬운 지점이라 명시해 둔다.
2. **소계 행 4곳** — STT가 미실측이 되며 소계도 미실측 구간이 됐다: `05-monetization.md:248`(MVP
   소계, 위치 불변)·`278`(확장 소계, 정정 전 276행 — 1번과 동일한 이유로 +2 밀림)·
   `docs/infrastructure.md:77`(MVP 소계)·`93`(확장 소계). 전부
   "~~舊 산술값~~ 재산정 필요(미실측 — STT 구성요소 포함)"로 정정 — **새 합계는 계산하지 않았다.**
3. **하위 KRW 총계 표 2곳(05 §B-1, M3~M12)** — "MVP 총계"(M3·M6, 265행 부근)·"확장기 총계"(M12,
   294행 부근) 표 바로 뒤에 경고 블록쿼트를 추가해, 두 표의 보수/기본/낙관 합계가 舊 STT 전제
   위에서 계산된 소계를 상속한다는 것을 명시했다(라이브 비용 자체는 STT와 무관해 그대로 유효).
   라운드2 "산술 오류 정정" 블록쿼트(296행 부근, 확장기 총계 경고와 같은 블록쿼트로 이어짐)에도
   STT 항이 이제 미실측이라는 각주를 덧붙였다 —
   역사 기록(당시 기준 정확했던 산술 검증)은 지우지 않았다.
4. **대장 #207 비고 보강**(`docs/plan/PIVOT-PLAN.md`) — 위 발견·후속 수리·확인 방법 재검토 결과를
   기록했다. 결론: **확인 방법(grep) 칸 자체는 무변경으로 유효**하다(해소 판정은 원래부터 의미
   판단이지 문자열 카운트가 아니라고 명시돼 있었다) — 사각은 그 칸이 아니라 **발주처 칸의 정정
   대상 탐색 방법**(provider 이름 문자열 grep)에 있었다. 교훈: provider 이름이 아니라 그 provider
   전제 위에서 파생된 **비용 수치 자체**까지 대조해야 사각이 닫힌다(규율 17·18). ⚠️ **이 정정 자체가
   같은 현상을 재현했다** — 274행에 이력 문구를 추가하며 파일 내 RTZR 매치 수가 **2 → 3**으로 다시
   늘었다(scribe 재확인, 당시 값) — #207이 스스로 예고했던 "정정이 끝나도 매치 수가 0으로 안 떨어지고
   오히려 늘 수 있다"가 이 세션에서 한 번 더 재현됐다. **이 충돌 해소 세션에서 다시 재실행하니 05 파일
   RTZR 개수는 3**(아래 ④ — 같은 패턴이 한 번 더 반복됐을 가능성, 원인은 단정하지 않는다).

## ③ 막힌 것

- **대장 #207은 여전히 "미수신"** — scribe는 문구를 고쳤을 뿐 `07-legal-license.md` §3-10·§4·§6의
  법률적 정확성을 판정할 권한이 없다. 전환은 조율자·외부 법률자문 검토 이후(07 §6 14항목 법률자문
  계약 완료 후 실제 질의서 발송 — 확정 리드타임 수치는 이 리포에 없다).
- **STT 비용 셀은 의도적으로 미실측 상태로 비워 뒀다** — 로컬 STT(whisper.cpp+Silero VAD)의 운영비
  (전기·하드웨어 감가)는 실측 자료가 없다. API 종량제에서 전기·하드웨어 구조로 바뀌어 舊 $5·舊 $83과
  직접 비교도 안 된다. 실측 전까지 이 값을 다시 채우지 않는다.
- **(신규 발견, 이번 수리 범위 밖)** `docs/plan/05-monetization.md:583`("확장 미실현(2지사 고착)
  시나리오" 비교표의 "인프라비용(월, M12)" 행)이 `(§B-1 확장기 총계)`를 인용해 같은 STT 미실측
  전제를 상속한다. 같은 행 안에서 인건비·매출·손익 열이 각자 다른 라운드의 별도 산정 로직과 얽힌
  3단계 하류라 이번 세션은 손대지 않았다 — 조율자가 편입 여부를 판단해야 한다.
- **신규 채번 #208·#209(scribe, 같은 세션) — 2-1 구현자가 위임 범위 밖에서 발견해 "실측하지 못했다"고
  보고한 것 2건을 대장에 등재만 했다(처방 미확정, 규율 17 — 방식 무전제). ⭐ 발견 경로가 값지다**:
  구현자가 범위 밖 콜사이트를 스스로 추적하다 발견하고 **고치지 않고 보고**했고(옳은 처리), 그중
  하나(#209)는 **게이트②(독립 검증)가 주석 과장을 실행으로 잡았다** — 하위 에이전트를 새로 기동하지
  않고 기존 체계(구현자 자기신고 + 게이트② 실행 대조)만으로 적발됐다.
  - **#208** — `apps/reporter/app/(app)/contents/new/upload.tsx:26-35`의 Mock 업로드 시절 잔재
    `mock://placeholder` 폴백이 유령 미디어 방어 3계층을 어디까지 통과하는지 조율자가 실측
    (2026-09-11): ①(`assertRealVideoInput`)은 **통과**하지만 ②(웹 `fetch(uri)` reject)·③(웹
    `body.size<=0`·네이티브 `sizeBytes<=0`)에서 `POST .../upload-url`(서버 최초 접촉) **이전**에
    막힌다(scribe가 3계층 전부 그 자리에서 재확인 — 대장 #208 발주처 칸에 명령·값 기록, `fetch`
    reject는 `node -e`로 독자 재현). **서버 도달은 없다 — 결함은 UX**(원인 불명 에러 메시지 — 영상이
    유실됐다는 사실을 말해주지 않는다)와 **Mock 잔재 정리**다. L2(실발생 결함)로 적지 않았다.
  - **#209** — 같은 슬라이스가 `upload-service.ts`·`video-capture.ts`의 "assertRealVideoInput이
    최종 관문" 과장 주석 2건은 정정했다(둘 다 이제 실제 검사 범위 — uri·mimeType만, URI 스킴·
    sizeBytes는 안 봄 — 를 정직하게 서술, `upload-service.ts:57` 자체가 "2026-09-11 검증자가 함수를
    직접 실행해 재현"이라 명시). 그런데 `draft-context.tsx:16-22`의 같은 과장은 위임 지정 파일
    (`upload-service.ts`·`video-capture.ts`·`http-upload-service.ts`·`xhr-upload-service.ts`·
    `contents/new/index.tsx`) 밖이라 남았다. 이 리포에서 "주석이 실제로 막는 범위를 과장한다"가
    적발된 **세 번째** 사례(① `check-destructive-migrations.mjs` "규율 21 방지" ② `monitor.yml`
    "15분마다" ③ 본 건). 확인 방법 칸은 **문자열 카운트가 아니라 구조 판정**으로 설계했다
    (#207이 겪은 "정정 후 표현만 바뀌면 문자열 카운트가 사각에 빠진다" 선례를 그대로 피하기 위해).
  - **QUEUE 편입은 다음 세션 판단** — PR #99 세션은 QUEUE.md를 건드리지 않았다(순서 변경 금지, 대장
    채번만 수행). 기한 칸은 둘 다 "2단계 이후"(#208)·"#208과 같은 슬라이스"(#209)로만 적었다.
- **대장 #207(L601) 표 무결성 — 이 충돌 해소 세션에서 재실행하니 0건이었다**(PR #99 세션 당시 보고된
  "실제 9칸/헤더 7칸" 경고는 지금 재현되지 않는다): `node infra/scripts/daejang-recheck.mjs` →
  `■ 표 무결성 0행`(아래 ④ 명령·출력 동일). scribe는 이 병합 세션에서도 PIVOT-PLAN.md를 고치지 않았다
  (자동 병합, 읽기만 함) — 왜 경고가 사라졌는지는 **단정하지 않는다**(스크립트 자신의 방침과 동일).
  다음 세션이 원인을 가볍게라도 확인해 둘 것 — 재발 가능성을 배제하지 않는다.
- **선언(15분 cron)-실측 간격 괴리의 원인 규명은 범위 밖** — GitHub Actions 무료 티어 cron의 실제
  발동 지연이 원인으로 추정되나 확정하지 않았다. 최신 간격 분포는 아래 ④ 참고(이 세션이 3개
  구간 추가 재집계 — 10개 중 최신 구간(4.92h)은 직전 두 구간(2.02h·1.96h)보다 커서, PR #98의
  "추세는 짧아지는 중" 서술은 이 시점 기준으로는 더 이상 단정할 수 없다).
- **2주 report-only 관측 기산일은 PR #98 머지 시각(2026-09-11T07:52:33Z UTC)으로 확정됐다** —
  관측을 "시작"하는 별도 액션은 없고 그 시각부터 스케줄 런 데이터가 자연히 쌓인다(HANDOFF.md
  "지금의 다음 1건" 참조). 종료(+14일)·임계 재조정 판단은 그 기간이 찬 뒤 다음 세션 몫이다.

## ④ 지금 상태 【자동 산출 — 사람의 판단 없음】

| 항목 | 값 | 재현 |
|---|---|---|
| 이 작업 디렉터리 HEAD | `7f4ff33`(브랜치 `fix/reporter-capture-stt-canon`, 병합 진행 중 — `.git/MERGE_HEAD` 존재) | `git log --oneline -1` |
| origin/main | `7088dbe`(PR #98 병합커밋) | `git fetch -q origin main && git rev-parse --short origin/main` |
| 전체 열린 PR | **1**건(#99) | `gh pr list --state open --json number --jq 'length'` |
| PR #99 상태 | `OPEN`·`mergeable: CONFLICTING`(병합 전 마지막 관측) | `gh pr view 99 --json state,mergeable` |
| 배포 서빙 중 — api | `e0e65d4...`(= 병합 전 origin/main과 일치 — PR #98이 코드 변경 0줄이라 재배포 불요) | `curl -s https://api.bapfull.com/health/version` |
| watch/reporter/center/api-readiness/liveness | 전부 **200** | `curl -s -o /dev/null -w "%{http_code}" https://watch.bapfull.com`(+reporter/+center/+`api.bapfull.com/health/readiness`·`/health/liveness`) |
| monitor.yml schedule 간격(직전 10개, 舊 7개에서 갱신) | 최소 **1.34h**·최대 **5.21h**·평균 **3.21h**(舊 3.31h — 구간 3개 추가로 갱신)·7h 허용치 대비 여유 **1.79h**. 설정변경(1h/6h) 이후 구간 6개(舊 3개): 3.59·3.25·2.56·2.02·1.96·**4.92h** — 전부 허용치 안이나 최신 구간이 직전 두 구간보다 커 "추세 단조 감소"는 더 이상 성립하지 않는다 | `gh run list --branch main --workflow monitor.yml --limit 100 --json event,createdAt,conclusion --jq '.[]\|select(.event=="schedule" or .event=="workflow_dispatch")\|[.createdAt,.event,.conclusion]\|@tsv'\|sort` |
| 대장 재현 | 대상 **63**행 · 일치 27 · 어긋남 **1**(#173 — 2-1 수리 반영 중이라 등재값과 다름, 정상 상태) · 수동 **46** · 오류 0 · **표 무결성 0행**(舊 PR #99 세션 보고 1건에서 갱신, 위 ③ 참고) | `node infra/scripts/daejang-recheck.mjs` |
| 로컬 게이트(infra/scripts) | 9스위트 **365/365** pass, exit 0 | `pnpm run test:scripts` |
| reporter 앱 테스트 | 31스위트 **323/323** pass, exit 0 | `cd apps/reporter && pnpm test` |
| 05·infra STT 표기 일치 | 두 문서 STT·소계 행 전부 "~~舊값~~ 재산정 필요(미실측)" 패턴으로 일치(05:246·248·276·278, infra:75·77·90·93) | `grep -n "STT" docs/plan/05-monetization.md docs/infrastructure.md` |
| RTZR 문자열 개수(7개 파일, 참고) | `infrastructure.md`**6**·`01`**2**·`02`**1**·`03`**3**·`05`**3**·`07`**7**·`08`**3** — 舊 세션 기록치(01:2·02:1·03:5·05:2·07:6·08:3·infra:5)와 여러 파일이 다르다. #207이 예고한 "이력 보존 문구가 RTZR 문자열을 계속 포함해 매치 수가 0으로 안 떨어진다"는 패턴대로, 두 세션(3-3→게이트②)에 걸쳐 이력 문구가 누적되며 계속 변동한 것으로 보인다(단정 아님) | `grep -c "RTZR" docs/infrastructure.md docs/plan/01-product-strategy.md docs/plan/02-web-architecture.md docs/plan/03-accessibility-ux.md docs/plan/05-monetization.md docs/plan/07-legal-license.md docs/plan/08-rollout-transition.md` |
| 이 세션(충돌 해소) 변경 파일 | `docs/ops/daily/2026-09-11.md`(양쪽 초안 통합, 신규 파일이라 마커 2개) · `docs/plan/exec/HANDOFF.md`(마커 1개 해소) — `docs/plan/PIVOT-PLAN.md`·`docs/plan/exec/QUEUE.md`는 git 자동 병합, scribe는 무결성만 확인하고 손대지 않음. 전부 git 미add(scribe는 git 쓰기 금지, 조율자가 `git add` 몫) | `git status --short` |

## ⑤ 다음 세션 예정

1. **PR #99 머지 승인**(사용자·조율자) — 이 세션이 충돌을 해소했으니 `git add`·커밋·푸시 → CI
   재확인 → 머지.
2. **대장 #207 법률 문언 판정** — `07-legal-license.md` §3-10·§4·§6 정정 문언의 법률적 정확성을
   조율자·외부 법률자문이 확인(07 §6 14항목 법률자문 계약 완료 후).
3. **`05-monetization.md:583` 다운스트림 인용 편입 여부 판정**(조율자) — 위 ③ 참고.
4. **대장 #208·#209 QUEUE 편입 여부 판정**(조율자) — 처방(수리 방식)·순서 배치 모두 미정.
5. **대장 #207(L601) 표 무결성 경고 재발 여부 관찰**(조율자/scribe) — 이번엔 0건이었으나 원인을
   단정하지 않았으므로 다음 재현에서도 계속 0인지 지켜볼 것.
6. **2주 report-only 관측 지속** — 기산일 2026-09-11T07:52:33Z(PR #98 머지)부터 +14일. schedule
   간격 분포·DOWN 발화 건수(사유별)·`monitor-freshness` 판정 분포를 계속 추적한다([HANDOFF.md](../../plan/exec/HANDOFF.md)
   "지금의 다음 1건" 참조). 6h 넘는 공백이 관측되면 임계(현재 period 1h·grace 6h) 재조정.
7. **관측 종료 후 2단계 2-2(`.web.*` 로드 경로) 착수** — **2-1이 아니다**: 2-1(대장 #173·#188, 유령
   영상 수리)은 PR #99에 이미 포함돼 머지를 기다리는 중이다. 순서는 QUEUE.md가 이미 확정해 둔 것이다.

## ⑥ 지난 보고서(2026-09-10) 예정 대비

2026-09-10 보고서 ⑤가 예정한 것: *"① PR #96 머지 승인 확인 ② 머지 후 배포 승인 게이트 2건 확인·
통과 ③ 배포 확인 ④ monitor.yml 첫 스케줄 런 확인 ⑤ 확인되면 S4(2주 report-only 관찰) 착수."* →
①~④는 같은 날(2026-09-10) 연장 세션에서 전부 이뤄졌다(PR #96 머지 → 감시 실가동이 자기 주기 전제를
반증 → PR #97로 수리 → PR #97도 머지, [HANDOFF.md](../../plan/exec/HANDOFF.md) "⭐ 2026-09-10(PR #96
머지 후)" 절 참조). **⑤(S4 착수)는 그날 시작되지 않았다** — 대신 그 자리에 사용자가 확인 방법
ⓐ(Better Stack 임시 모니터)를 직접 실증하는 일이 끼어들었다. 오늘(2026-09-11) 그 실증이 PR #98로
정본에 반영되고 머지되며 **#203이 해소됐고, 그로써 S4(2주 관측) 기산일이 확정됐다**(위 ⑤ 참조) —
같은 날 별도 브랜치에서는 2-1(유령 영상)·3-3(STT 정본)이 진행돼 PR #99로 머지를 기다리고 있고, 이
문서 자체가 그 두 갈래(PR #98·#99)가 같은 문서를 건드려 생긴 충돌을 해소한 기록이다.
