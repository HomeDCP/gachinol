# 일간 2026-09-11 (금)

## ① 사용자 결정 대기 — 지금 필요한 것 **1건**

요청 시점 정본은 [QUEUE.md](../../plan/exec/QUEUE.md) "사용자 실행 항목" 절.

1. **PR #98 머지 승인.** 브랜치 `docs/monitor-203-closeout`, 상태 `OPEN`·`MERGEABLE`, 체크 **2종
   전부 SUCCESS**(`node (lint·typecheck·test)`·`ai-worker (ruff·pytest)`) —
   `gh pr view 98 --json state,mergeable,statusCheckRollup`. **문서 전용 PR**(대장 #203 해소 +
   QUEUE 1단계 완주, 코드 변경 0줄) — PR #96·#97(감시체계 S0~S3)이 머지된 뒤 그 위에서 확인 방법
   ⓐ·ⓑ가 전부 실증돼 상태 칸을 "해소"로 전환하는 문서 슬라이스다.

## ② 오늘 무엇이 움직였나 — 게이트②가 잡은 비용표 모순 수리 (scribe, `docs/**`만)

**scribe는 코드를 만들지 않는다.** 이 세션은 같은 브랜치(`fix/reporter-capture-stt-canon`)에서
구현자가 별도로 진행 중인 2-1과 병행해, 직전 3-3 세션(대장 #207)이 빠뜨린 곳을 게이트②(독립 검증)가
잡아낸 것을 수리한 기록이다.

- **2-1(QUEUE — 대장 #173·#188, "유령 영상" 수리 + `app/**` 로직 금지 린트, D1 적용례)** — 같은
  브랜치에서 구현자가 동시 진행 중이다. scribe는 `apps/**`·`infra/scripts/**` 접근 금지라 상세를
  확인하지 않았다 — `git status --short`로 관측되는 범위만 기록한다: `apps/reporter/src/capture/`
  (신규 디렉터리) + upload 서비스 3파일 수정 + 신규 게이트 스크립트
  `infra/scripts/reporter-app-thinness-gate.mjs`(+ 테스트 파일). 이 세션이 재실행한
  `pnpm run test:scripts`는 신규 게이트 스위트(24건) 포함 **9스위트 365/365 pass**를 보였다(아래 ④).
  `node infra/scripts/daejang-recheck.mjs`의 **#173 어긋남 1건**(기재 0 → 실측 3,
  `grep -rl "launchCameraAsync" apps/reporter/app apps/reporter/src`)은 이 수리가 진행 중이라
  등재값과 달라진 것으로, 위임문이 예고한 **정상 상태**다.
- **3-3(대장 #207 — STT 로컬 전환 정본 정합)** — 직전 세션이 2026-08-20 STT 로컬 전환(舊 RTZR API →
  whisper.cpp+Silero VAD, 근거는 데이터 주권)을 파일별 `RTZR` 문자열 grep으로 대상을 찾아 **7개 파일
  24건**을 정정했다(`01`·`02`·`03`·`05-monetization.md`·`07`·`08`·`docs/infrastructure.md`,
  `docs/plan/exec/HANDOFF.md` 2026-09-11 절 참고). **게이트②(독립 검증)가 그 방법론의 빈틈을
  잡았다**: `docs/infrastructure.md`는 MVP·확장(12지사) 두 표 모두 STT 행이
  "~~舊 값~~ 재산정 필요(미실측)"로 고쳐졌는데, `docs/plan/05-monetization.md` §3 "확장기(4~6지사)"
  표의 STT 행(당시 274행, `STT (~132h@12지사) | ~$83 | $22~35`)은 **안 고쳐진 채 남아 있었다** —
  같은 데이터포인트(12지사 STT 舊 $83)를 두 문서가 서로 다르게 표기하는 모순이었다.
- **왜 놓쳤는가(이번 발견의 핵심)**: scribe 재확인 — `grep -c "RTZR" docs/plan/05-monetization.md`
  → 정정 직후 **2**(236·246행뿐). 274행에는 "RTZR" 문자열이 **전혀 없다**(달러 금액만 인용하고
  provider명을 반복하지 않는다) — 그래서 provider 이름 문자열 grep이라는 탐색 방법론 자체가 이 행을
  원리적으로 못 찾았다. 대장 #207 비고에 이 발견과 재검토 결과를 기록했다(아래 참고).

### 모순 수리 상세(이 세션, `docs/**` 3파일)

1. **`docs/plan/05-monetization.md:274`(정정 후 현재 위치 **276행** — 아래 참고)** —
   `infrastructure.md` §3 확장표(90행)와 동일 패턴으로 정정:
   `STT (~132h@12지사) | ~~$83~~ 재산정 필요(미실측, infrastructure.md §3 확장 표 인용 — 舊 RTZR→로컬
   전환 2026-08-20) | ~~$22~35~~ 재산정 필요(미실측 — $83 보간값이라 원천과 함께 무효)`. ⚠️ **행 번호가
   274→276으로 밀렸다** — 이 STT행 자체의 정정(1줄→1줄 치환)이 아니라, **이보다 앞선 "MVP 총계"
   경고 블록쿼트 삽입(아래 3번, +2줄)이 원인**이다. `daejang-recheck.mjs` 등 자동 도구가 아니라 이
   보고서를 손으로 읽을 때 착오하기 쉬운 지점이라 명시해 둔다.
2. **소계 행 4곳** — STT가 미실측이 되며 소계도 미실측 구간이 됐다: `05-monetization.md:248`(MVP
   소계, 위치 불변)·`278`(확장 소계, 정정 전 276행 — 1번과 동일한 이유로 +2 밀림)·
   `docs/infrastructure.md:77`(MVP 소계)·`93`(확장 소계). 전부
   "~~舊 산술값~~ 재산정 필요(미실측 — STT 구성요소 포함)"로 정정 — **새 합계는 계산하지 않았다.**
3. **하위 KRW 총계 표 2곳(05 §B-1, M3~M12)** — "MVP 총계"(M3·M6, 265행 부근)·"확장기 총계"(M12,
   294행 부근) 표 바로 뒤에 경고 블록쿼트를 추가해, 두 표의 보수/기본/낙관 합계가 舊 STT 전제
   위에서 계산된 소계를 상속한다는 것을 명시했다(라이브 비용 자체는 STT와 무관해 그대로 유효).
   라운드2 "산술 오류 정정" 블록쿼트(296행 부근, 확장기 총계 경고와 같은 블록쿼트로 이어짐)에도
   STT 항이 이제 미실측이라는 각주를 덧붙였다 —
   역사 기록(당시 기준 정확했던 산술 검증)은 지우지 않았다.
4. **대장 #207 비고 보강**(`docs/plan/PIVOT-PLAN.md`) — 위 발견·후속 수리·확인 방법 재검토 결과를
   기록했다. 결론: **확인 방법(grep) 칸 자체는 무변경으로 유효**하다(해소 판정은 원래부터 의미
   판단이지 문자열 카운트가 아니라고 명시돼 있었다) — 사각은 그 칸이 아니라 **발주처 칸의 정정
   대상 탐색 방법**(provider 이름 문자열 grep)에 있었다. 교훈: provider 이름이 아니라 그 provider
   전제 위에서 파생된 **비용 수치 자체**까지 대조해야 사각이 닫힌다(규율 17·18). ⚠️ **이 정정 자체가
   같은 현상을 재현했다** — 274행에 이력 문구를 추가하며 파일 내 RTZR 매치 수가 **2 → 3**으로 다시
   늘었다(scribe 재확인, 아래 ④) — #207이 스스로 예고했던 "정정이 끝나도 매치 수가 0으로 안 떨어지고
   오히려 늘 수 있다"가 이 세션에서 한 번 더 재현됐다.

## ③ 막힌 것

- **대장 #207은 여전히 "미수신"** — scribe는 문구를 고쳤을 뿐 `07-legal-license.md` §3-10·§4·§6의
  법률적 정확성을 판정할 권한이 없다. 전환은 조율자·외부 법률자문 검토 이후(07 §6 14항목 법률자문
  계약 완료 후 실제 질의서 발송 — 확정 리드타임 수치는 이 리포에 없다).
- **STT 비용 셀은 의도적으로 미실측 상태로 비워 뒀다** — 로컬 STT(whisper.cpp+Silero VAD)의 운영비
  (전기·하드웨어 감가)는 실측 자료가 없다. API 종량제에서 전기·하드웨어 구조로 바뀌어 舊 $5·舊 $83과
  직접 비교도 안 된다. 실측 전까지 이 값을 다시 채우지 않는다.
- **(신규 발견, 이번 수리 범위 밖)** `docs/plan/05-monetization.md:583`("확장 미실현(2지사 고착)
  시나리오" 비교표의 "인프라비용(월, M12)" 행)이 `(§B-1 확장기 총계)`를 인용해 같은 STT 미실측
  전제를 상속한다. 같은 행 안에서 인건비·매출·손익 열이 각자 다른 라운드의 별도 산정 로직과 얽힌
  3단계 하류라 이번 세션은 손대지 않았다 — 조율자가 편입 여부를 판단해야 한다.
- **신규 채번 #208·#209(scribe, 같은 세션) — 2-1 구현자가 위임 범위 밖에서 발견해 "실측하지 못했다"고
  보고한 것 2건을 대장에 등재만 했다(처방 미확정, 규율 17 — 방식 무전제). ⭐ 발견 경로가 값지다**:
  구현자가 범위 밖 콜사이트를 스스로 추적하다 발견하고 **고치지 않고 보고**했고(옳은 처리), 그중
  하나(#209)는 **게이트②(독립 검증)가 주석 과장을 실행으로 잡았다** — 하위 에이전트를 새로 기동하지
  않고 기존 체계(구현자 자기신고 + 게이트② 실행 대조)만으로 적발됐다.
  - **#208** — `apps/reporter/app/(app)/contents/new/upload.tsx:26-35`의 Mock 업로드 시절 잔재
    `mock://placeholder` 폴백이 유령 미디어 방어 3계층을 어디까지 통과하는지 조율자가 실측
    (2026-09-11): ①(`assertRealVideoInput`)은 **통과**하지만 ②(웹 `fetch(uri)` reject)·③(웹
    `body.size<=0`·네이티브 `sizeBytes<=0`)에서 `POST .../upload-url`(서버 최초 접촉) **이전**에
    막힌다(scribe가 3계층 전부 그 자리에서 재확인 — 대장 #208 발주처 칸에 명령·값 기록, `fetch`
    reject는 `node -e`로 독자 재현). **서버 도달은 없다 — 결함은 UX**(원인 불명 에러 메시지 — 영상이
    유실됐다는 사실을 말해주지 않는다)와 **Mock 잔재 정리**다. L2(실발생 결함)로 적지 않았다.
  - **#209** — 같은 슬라이스가 `upload-service.ts`·`video-capture.ts`의 "assertRealVideoInput이
    최종 관문" 과장 주석 2건은 정정했다(둘 다 이제 실제 검사 범위 — uri·mimeType만, URI 스킴·
    sizeBytes는 안 봄 — 를 정직하게 서술, `upload-service.ts:57` 자체가 "2026-09-11 검증자가 함수를
    직접 실행해 재현"이라 명시). 그런데 `draft-context.tsx:16-22`의 같은 과장은 위임 지정 파일
    (`upload-service.ts`·`video-capture.ts`·`http-upload-service.ts`·`xhr-upload-service.ts`·
    `contents/new/index.tsx`) 밖이라 남았다. 이 리포에서 "주석이 실제로 막는 범위를 과장한다"가
    적발된 **세 번째** 사례(① `check-destructive-migrations.mjs` "규율 21 방지" ② `monitor.yml`
    "15분마다" ③ 본 건). 확인 방법 칸은 **문자열 카운트가 아니라 구조 판정**으로 설계했다
    (#207이 겪은 "정정 후 표현만 바뀌면 문자열 카운트가 사각에 빠진다" 선례를 그대로 피하기 위해).
  - **QUEUE 편입은 다음 세션 판단** — 이번 세션은 QUEUE.md를 건드리지 않았다(순서 변경 금지, 대장
    채번만 수행). 기한 칸은 둘 다 "2단계 이후"(#208)·"#208과 같은 슬라이스"(#209)로만 적었다.
- **(신규 발견, 이번 세션 범위 밖 — 조율자 보고)** `daejang-recheck.mjs`가 이번 회차부터 **표
  무결성 검사**로 대장 **#207(L601)이 실제 9칸/헤더 7칸**임을 잡아냈다 — `05-monetization.md`의
  옛 STT 표 행("`STT (~132h@12지사) | ~$83 | $22~35`")을 그대로 인용하며 파이프 2개를 이스케이프
  안 한 것이 원인으로 보인다(단정은 스크립트도 하지 않는다). **scribe는 이 행을 만들지 않았고
  손대지도 않았다** — `git diff --stat docs/plan/PIVOT-PLAN.md` = "1 file changed, 3
  insertions(+)"(순수 추가, #208·#209 2행뿐)로 확인. #207 자체는 직전 세션(3-3) 소관이라 이번
  세션은 수리하지 않는다 — 조율자가 수리 여부·시점을 판단해야 한다.

## ④ 지금 상태 【자동 산출 — 사람의 판단 없음】

| 항목 | 값 | 재현 |
|---|---|---|
| 이 작업 디렉터리 HEAD / origin/main | 둘 다 `e0e65d4`(브랜치 `fix/reporter-capture-stt-canon`, 미커밋 변경 있음 — PR 미생성) | `git log --oneline -1` · `git fetch -q origin main && git rev-parse --short origin/main` |
| 열린 PR | **1**(#98) | `gh pr list --state open --json number` |
| PR #98 상태 | `OPEN`·`MERGEABLE`, 체크 **2 SUCCESS**(node lint·typecheck·test / ai-worker ruff·pytest), 실패 0 | `gh pr view 98 --json state,mergeable,statusCheckRollup` |
| 대장 재현 | 대상 **64**행(舊 62 — #208·#209 신규 채번 반영) · 일치 27 · 어긋남 **1**(#173 — 2-1 수리 중이라 등재값과 다름, 위임문이 예고한 정상 상태·舊와 동일해 안 늘었음) · 수동 **47**(舊 42, +5 — 신규 2행이 안전 화이트리스트 grep 5건을 수동 목록에 추가: #208×3·#209×2) · 오류 0 · ⚠️ 신규 진단: 표 무결성 경고 **1**건(#207, L601 — 실제 9칸/헤더 7칸, 위 ③ 참고) | `node infra/scripts/daejang-recheck.mjs` |
| 로컬 게이트(infra/scripts) | 9스위트 **365/365** pass, exit 0 | `pnpm run test:scripts` |
| reporter 앱 테스트 | 31스위트 **323/323** pass, exit 0 | `cd apps/reporter && pnpm test` |
| 05·infra STT 표기 일치 | 두 문서 STT·소계 행 전부 "~~舊값~~ 재산정 필요(미실측)" 패턴으로 일치(05:246·248·276·278, infra:75·77·90·93) | `grep -n "STT" docs/plan/05-monetization.md docs/infrastructure.md` |
| RTZR 문자열 개수(05, 참고) | **3**(236·246·276행 — 정정 전 2, 이번 274행 정정이 이력 문구를 추가하며 +1) | `grep -c "RTZR" docs/plan/05-monetization.md` |
| 이 세션 변경 파일(scribe 소관) | `docs/infrastructure.md`(소계 2행) · `docs/plan/05-monetization.md`(STT행 1·소계 2·KRW 총계 경고 2·산술정정 각주 1) · `docs/plan/PIVOT-PLAN.md`(#207 비고 보강 1) — 3개 파일, 전부 git 미커밋(scribe는 git 쓰기 금지) | `git status --short` |
| 이 세션 변경 파일(scribe #208·#209 채번, 후속) | `docs/plan/PIVOT-PLAN.md`(#208·#209 2행 순수 추가) · `docs/ops/daily/2026-09-11.md`(본 갱신) — QUEUE.md는 이번 세션 미변경(편입 여부는 다음 세션 조율자 판단). 대장 변경은 git diff 기준 "1 file changed, 3 insertions(+), 0 deletions" — 기존 행 무변경 확인 | `git diff --stat docs/plan/PIVOT-PLAN.md` |

## ⑤ 다음 세션 예정

1. **PR #98 머지 승인**(사용자).
2. **대장 #207 법률 문언 판정** — `07-legal-license.md` §3-10·§4·§6 정정 문언의 법률적 정확성을
   조율자·외부 법률자문이 확인(07 §6 14항목 법률자문 계약 완료 후).
3. **`05-monetization.md:583` 다운스트림 인용 편입 여부 판정**(조율자) — 위 ③ 신규 발견 참고.
4. **2-1(유령 영상 수리+게이트) 완주 확인** — 구현자 세션 후속. `pnpm run test:scripts`·
   `node infra/scripts/daejang-recheck.mjs`(#173 어긋남 0 기대)로 재확인.
5. **대장 #208·#209 QUEUE 편입 여부 판정**(조율자) — 처방(수리 방식)·순서 배치 모두 미정. 이번
   세션은 채번만 하고 QUEUE.md는 건드리지 않았다(위 ③ 참고).
6. **대장 #207(L601) 표 무결성 경고 수리 여부 판정**(조율자) — 실제 9칸/헤더 7칸(원인: 인용된
   옛 STT 표 스니펫의 파이프 2개 미이스케이프로 추정, 단정 아님). scribe가 이번 세션에 새로
   만든 결함이 아니다(`git diff --stat` = 순수 추가 3줄만, 위 ③·④ 참고) — 수리 시
   `\|`로 이스케이프하되 일괄 치환은 금지(이미 이스케이프된 곳까지 재이스케이프해 표를 파손한
   이력이 있음).
