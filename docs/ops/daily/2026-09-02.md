# 일간 2026-09-02 (수)

## ① 사용자 결정 대기 — 지금 필요한 것 **0건**

요청 시점 정본은 [QUEUE.md](../../plan/exec/QUEUE.md) "사용자 실행 항목" 절.
⭐ **오늘 이미 승인받은 것 2건**(아래 표에는 없음, ②에 상세): `DROP COLUMN` 마이그레이션 적용 승인 ·
제온 배포 묶음(SSH 풀스택 배포) 착수 승인.
⭐ **오늘 도달한 요청 2건도 같은 날 사용자가 모두 실행했다**(아래 표에는 없음, ②·QUEUE·PIVOT-PLAN 대장
갱신 참조):
1. **옛 Quick Tunnel systemd 정지**(대장 #199) — 사용자 명령
   `ssh 192.168.0.101 'sudo systemctl disable --now gachinol-quick-tunnel.service'` 실행. 조율자 검증
   (2026-09-02): `systemctl is-enabled` → `disabled`·`systemctl is-active` → `inactive`(등재 시
   `enabled`·`active running`에서 전환). `WantedBy=multi-user.target` 링크 제거로 재부팅 생존 경로
   차단 — **대장 #199 해소**.
2. **GitHub Environment 승인 게이트 설정**(required reviewer) — 사용자가 GitHub 웹에서 `production`
   환경 생성 + required reviewer `HomeDCP` 1명 + 배포 브랜치 `main` 한정으로 설정 완료, 그리고
   "Allow administrators to bypass configured protection rules" 토글도 **껐다**(조율자 권고를
   그대로 반영). scribe 재현(읽기전용 API, 2026-09-02): `gh api repos/HomeDCP/gachinol/environments/production
   --jq '{can_admins_bypass,prevent_self_review,reviewers:[.protection_rules[].reviewers[]?.reviewer.login]}'`
   → `can_admins_bypass:false`(관리자 우회 차단 확정)·`prevent_self_review:false`(의도된 상태 — 켜면
   혼자 쓰는 리포에서 아무도 배포 못 한다)·`reviewers:["HomeDCP"]`. **QUEUE §D-1의 "사용자 실행" 몫은
   완전히 충족**됐다. **"코드 몫"(워크플로 YAML에 `environment: production` 배선)도 같은 날 끝났다**
   — `deploy` 잡에 `environment: production` 배선 확인(조율자 PyYAML 파싱 인용: `deploy.environment
   = 'production'`·`purge.needs`에 `deploy` 포함이라 별도 게이트 불요). ⚠️ 단 이 변경은 **아직
   미커밋**이다(`git show origin/main:.github/workflows/deploy-web.yml | grep -c "environment:"` →
   `0`, 워크트리에는 있음) — 커밋·PR·머지가 남아 있고, **승인 대기가 실제로 걸리는지는 다음 main
   머지에서만 확인된다**(검증된 것으로 적지 않는다).

(오늘 신규로 도달한 결정 대기 0건 — 위 콜아웃 2건 참조. 표는 결정 대기가 생기면 다시 채운다)

## ② 오늘 무엇이 움직였나 — 배포 슬라이스 종료

- **대장 #170 해소 — 제온 백엔드 풀스택 실배포.** 사용자가 `DROP COLUMN` 마이그레이션 승인 → 조율자가
  SSH로 `docker compose pull + up -d --no-build api ai-worker media-worker` 실행. 조율자 실측 인용
  (scribe 제온 SSH 금지): api·ai-worker·media-worker 이미지 전부 `2026-09-01T21:0x:xxZ`로 갱신,
  `minor_consent_*` 컬럼 2→0, `_prisma_migrations` 11→12건(`RUN_MIGRATIONS:-true`가 부팅 전 자동 적용).
  적용 전 손실 실측 0건, 복원점 `gachinol-20260902-040924.sql.gz` 확보.
  **scribe 독립 재현**(인터넷 경로, SSH 아님): `curl -s https://api.bapfull.com/v1/health/version` →
  `{"sha":"3f8e87316d991576c13e91570e90ffa9077eaa95"}` — main HEAD와 SHA 일치. **QUEUE 0단계 표가
  완전히 비어 0단계가 완주됐다.**
- **대장 #194·#195 해소**(커밋 `0fc1949`). #194: web 이미지 로컬 태그 폴백을 GHCR 한정 중첩 폴백으로
  정정(`grep -n '^\s*image: \${' infra/docker/docker-compose.prod.yml infra/docker/docker-compose.xeon.yml
  | grep -vc 'ghcr.io'` → **0**, scribe 재현). ⚠️ QUEUE 舊 2-6의 처방 문언(`WEB_IMAGE` 참조 자체를
  없애는 단순 대체안)은 **부정확**했다 — 실채택안은 `WEB_IMAGE` 참조를 유지한 중첩 폴백이다(규율 13
  정정, QUEUE 2단계 절 참조). #195: 제온에서 사람이 직접 주석 해제한 `cloudflared:` 블록을 리포로
  역동기화(`TUNNEL_TOKEN`이 env 참조라 그대로 옮길 수 있었다). scribe 재현: `grep -n "^  cloudflared:"
  infra/docker/docker-compose.xeon.yml` → `235:  cloudflared:`(주석 해제 확인). ⚠️ 이 해소는 **손편집
  미동기 증상만** 닫는다 — "SSH 배포 잡 자체가 api·media-worker·ai-worker엔 없다"는 구조적 잔여는
  **QUEUE 1-1(대장 #180)로 범위 이관**돼 그쪽이 계속 추적한다.
- **health/version 프리픽스 결함 수리**(커밋 `a8963e8`·`fd872d1`). `setup-app.ts`의 전역 prefix
  `exclude`가 liveness·readiness만 담고 version을 빠뜨려 `/health/version`이 404였다(대장 #180이
  이 경로를 그대로 베꼈다면 배포 스모크가 fail-closed로 첫 배포부터 실패했을 뻔). 유닛으로는 원천적으로
  못 잡는 결함이라(컨트롤러 직접 인스턴스화 스펙은 prefix를 안 탄다) e2e에 실증을 뒀는데 **CI가
  `test:e2e`를 한 번도 부르지 않고 있었다** → smoke 스위트를 CI에 신규 편입. scribe 재현: run
  `33563266139`의 `Test (e2e smoke)` 스텝 로그에 `PASS test/smoke.e2e-spec.ts`·`Tests: 5 passed`
  (스킵이 아니라 실통과). 검증 뮤테이션이 조율자 위임문의 전제(`/v1` 404 단언)를 반증해 주석 정정
  (`fd872d1`).
- **PR #88**(문서, node·ai-worker 2잡)·**PR #89**(배포 묶음, node·ai-worker·build 3종 5잡) —
  둘 다 **CI 전부 SUCCESS, `MERGEABLE`/`CLEAN`, 머지 대기**(scribe 재확인, `gh pr view {88,89}
  --json statusCheckRollup,mergeable,mergeStateStatus`). #89는 #88 위에 쌓여 있어 #88을 먼저 머지해야
  한다.
- 대장·QUEUE·HANDOFF 3문서 갱신(scribe, 이 세션) — 대장 #170·#194·#195 상태 칸을 해소로 갱신, QUEUE
  0단계 완주 마감(표 제거 + 이력 서술, 절 자체는 보존) + 2-6(#194) 제거 + C-3·D-1 충족/도달 갱신,
  HANDOFF "다음 1건"을 1-1(대장 #180)로 전진.

## ③ 막힌 것

**코드 착수 자체는 막혀 있지 않다** — 1-1(대장 #180, api·워커 배포 경로 신설)의 워크플로 YAML 작성은
지금 시작할 수 있다. 다만 **실배포로 굳히려면 GitHub Environment 승인 게이트(required reviewer)가
먼저 서야 한다**(위 ①-2, 사용자 실행) — 그 게이트 없이 자동 배포를 켜면 로컬 push와 프로덕션 사이에
아무 장치가 없어진다(DISCIPLINES §24-0).

## ④ 지금 상태 【자동 산출 — 사람의 판단 없음】

| 항목 | 값 | 재현 |
|---|---|---|
| origin/main | `3f8e873`(PR #87 머지, 불변 — #88·#89는 아직 열린 PR) | `git fetch -q origin main && git log --oneline -1 origin/main` |
| 열린 PR | **2**(#88 문서·#89 배포 묶음 — 둘 다 CI 전부 success, #89는 #88 위에 쌓임) | `gh pr list --state open` |
| CI 3종(main, headSha=3f8e873) | CI ✅ · Build Images ✅ · Deploy Web ✅ 전부 success | `gh run list --branch main -L 3 --json workflowName,conclusion,headSha` |
| **배포 이미지 — api** | **✅ main HEAD와 일치**(舊 8/21 스테일 → 오늘 배포로 해소) | `curl -s https://api.bapfull.com/v1/health/version` → `{"sha":"3f8e87316d991576c13e91570e90ffa9077eaa95"}` |
| **배포 이미지 — web** | ✅ main HEAD와 일치(불변) | `curl -s https://watch.bapfull.com \| grep -o '<meta name="build-sha"[^>]*>'` → `content="3f8e87316d991576c13e91570e90ffa9077eaa95"` |
| `/health/version`(prefix 밖) | **404**(설계대로 — `/v1/health/version`이 정식 경로, 대장 #180이 이 프리픽스를 확인하고 베껴야 한다) | `curl -s -o /dev/null -w "%{http_code}" https://api.bapfull.com/health/version` |
| 인터넷 경로 | ✅ **200**(watch·reporter·center·api liveness 4곳 전부) | `curl -s -o /dev/null -w "%{http_code}" https://watch.bapfull.com` 외 3곳 동형 |
| 테스트 — api | **903/903**(불변 — `git diff --stat 99c1e4f..3f8e873 -- apps services packages` → 0줄, 코드 diff 자체가 재현 근거) | `pnpm --filter @gachinol/api test` |
| 테스트 — infra/scripts | **92/92**(`verify-deployed-sha` 18 + `daejang-recheck` 59 + `bundle-budget` 15, 재실행 확인 — 舊 77은 `bundle-budget`의 `--check-urls` 스위트 15건이 PR #87로 신설되기 전 값) | `pnpm run test:scripts` |
| 대장 | **199행 / 대기 59**(舊 198/61 — #170·#194·#195 3건 해소로 대기 이탈, #199는 이미 등재 상태로 반영돼 순증 없음) | `node infra/scripts/daejang-recheck.mjs` |
| 빌드 스탬프 | `grep -rl "GIT_SHA" services/*/Dockerfile infra/docker` → **6**(舊 5 — 신규 1건은 `docker-compose.xeon.yml`의 **주석 내 언급**일 뿐 실제 선언은 여전히 5파일. 값이 늘었다고 기계적으로 "해소 진전"으로 읽으면 안 된다는 사례) | 위 명령 |

## ⑤ 오늘 예정 — 1건

**QUEUE 1-1 · 대장 #180 — api·워커 배포 경로 신설**(`deploy-web.yml`의 deploy 잡을 web과 동형으로
복제해 api·media-worker·ai-worker에 `compose pull + up`을 붙인다). 선행 없음(SHA 대조 인프라는 대장
#186 해소로 이미 충족). 동반 의무: 규율 2 검사(워크스페이스 의존 ⊆ Dockerfile COPY) + 대장 #195 잔여
범위(SSH 배포 잡 자체 신설). **실배포 확정은 GitHub Environment 게이트(위 ①-2) 이후**.

## ⑥ 어제 예정 대비

예정 `0-1 · 대장 #189 — URL 형태 검증, 그다음(0-2) 대장 #170 — 제온 백엔드 풀스택 배포(C-3 승인
미도달이라 착수해도 완결은 승인 이후)` → 실제 `#189 완주(별도 세션, PR #87) + #170 완주(사용자 승인
도착 → 조율자 SSH 배포로 완결) + 부수로 #194·#195까지 해소`. **계획보다 앞섰다** — 어제 시점엔
"승인 이후로 완결이 미룰 수 있다"고 적었는데 오늘 승인 자체가 도착해 완결까지 갔고, 같은 배포 묶음
작업 중 #194·#195 두 건이 함께 닫혔다. **0단계 자체가 오늘로 소진**돼 다음 순번이 1단계로 넘어갔다.
