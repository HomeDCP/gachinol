# 일간 2026-09-12 (토)

## ① 사용자 결정 대기 — 지금 필요한 것 **0건**

요청 시점 정본은 [QUEUE.md](../../plan/exec/QUEUE.md) "사용자 실행 항목" 절.

QUEUE §C-6 ②(제온 설정값 `S3_PUBLIC_ENDPOINT` 전환)가 그 문서 최종 기재 시점엔 "지금 필요"로
남아 있었으나, scribe가 이 세션에서 재확인하니 **이미 실행 완료 상태**다 — `curl -s
"https://api.bapfull.com/v1/feed?limit=1"` → `thumbnailUrl` 호스트가 `https://media.bapfull.com`
(舊 `http://192.168.0.101:9000` 아님, 상세는 ② 참조). §C-1~§C-5·§D-1·§D-3은 이미 완료로 기재돼
있고, §D-2(카카오 실 송출 범위 결정)는 트리거("2단계 완료 시")가 미도달이다 — QUEUE 2단계 표에
대장 #177(전이맵 `to`-상태 grep 테스트) 1건이 아직 남아 있다. 배포 승인 게이트 대기도 0건
(`gh api "repos/HomeDCP/gachinol/actions/runs?status=waiting" --jq '.total_count'` → `0`,
`?status=action_required`도 `0`). **QUEUE.md 표 자체는 이 세션이 고치지 않는다**(위임 범위 밖 —
§C-6 ②를 완료로 갱신하는 것은 다음 scribe 세션 몫, ⑤ 참조).

## ② 오늘 무엇이 움직였나

### PR #100 머지·배포 완주

- `chore/web-jest-resolution` → main, 병합커밋 `2035cf2d0618d06e1b9a9756f7d40c1f80d231cd`,
  병합시각 `2026-09-12T13:52:14Z`(22:52:14 KST) — scribe 재확인: `gh pr view 100 --json
  state,mergedAt,mergeCommit` → `MERGED`.
- 이 머지로 대장 #178(`.web.*` 로드 경로 확보)이 해소돼 QUEUE 2단계에서 제거됐다(어제 세션이 이미
  대장·QUEUE·HANDOFF에 반영 — 이 세션은 그 기재를 다시 고치지 않았다, 읽기만 함). scribe가 오늘
  다시 재실행해 불변 확인: `pnpm --filter @gachinol/reporter test:web` → 1스위트 3/3 pass ·
  `pnpm --filter @gachinol/subscriber test:web` → 3스위트 10/10 pass, 합 **13/13**(舊 보고 수치와
  일치 — 새로 재확인한 것이지 베낀 것이 아니다).
- 배포 3워크플로 전부 success(headSha `2035cf2` 대상, scribe 재확인 `gh run list --branch main
  --json name,headSha,conclusion --limit 20`): `CI` success · `Build Images` success ·
  `Deploy Web` success, 전부 `13:52:16Z` 트리거.
- Build Images 배포 잡 CI 로그 자체를 재열람(`gh run view 34697629544 --log`)해 배포 직후 상태를
  재확인: `13:57:56.9763828Z gachinol-prod-api-1 ghcr.io/***/gachinol-api:sha-2035cf2 ...
  Up Less than a second (health: starting)` — **CI는 정확히 `sha-2035cf2`를 올렸다**(아래 "조율자
  실수" 절의 대조군으로 쓴다).

### 대장 #169 부분 수리 — 재생 경로 복구, 업로드는 여전히 미해소

QUEUE §C-6 ①(터널 라우트)은 이미 완료로 기재돼 있었다. ②(제온 `S3_PUBLIC_ENDPOINT` 전환 + api
재시작)를 scribe가 이 세션에서 독자 재확인하니 완료 상태였다:

- `curl -s "https://api.bapfull.com/v1/feed?limit=1"` → `items[0].thumbnailUrl` 호스트가
  `https://media.bapfull.com`(舊 `http://192.168.0.101:9000` — PIVOT-PLAN 대장 #169 비고의 舊
  기재값과 다르다, 그 기재 이후 전환된 것).
- 썸네일 실 GET(서명 URL 그대로 사용): `HTTP/2 200`, `content-type: image/jpeg`,
  `size_download 24124`. `file` 판정: `JPEG image data, ... 640x360, components 3`(24,124바이트 —
  `wc -c`로 재확인, 임의 추정 아님).
- `playback.hlsUrl`(720p 렌디션) Range GET(`Range: bytes=0-1999999`): `HTTP/2 206`,
  `content-type: video/mp4`, `content-range: bytes 0-1999999/30526545`. `ffprobe` 판정:
  `codec_name=h264 width=1280 height=720` + `codec_name=aac`.
- ⚠️ **#169는 여전히 미수신이다** — PIVOT-PLAN 대장 #169 비고가 이미 적어 둔 그대로, **재생은
  풀렸지만 업로드는 안 풀린다**(대장 #211 — Cloudflare 요청 바디 100MB 한도, 원본 10건 중 6건
  60% 초과). 이 일간보고는 그 판정과 일치시킨다 — "해소"로 적지 않는다.

### 공개 미디어 도달성 검사 축 신설 (커밋 `48cfa70`, 로컬 — 아직 PR 미제출)

대장 #169·#214의 동반 의무(D1) 수리다. scribe가 코드를 그 자리에서 직접 읽고 확인한 것만 적는다:

- `infra/scripts/media-reachability.mjs`(628행, `wc -l` 재확인) 신규 — ① 호스트 판정(네트워크
  0회): 사설 IP(RFC1918)·루프백·내부 TLD·http 스킴·**Tailscale CGNAT `100.64.0.0/10`**(둘째
  옥텟 64~127만, 경계 4점 테스트로 고정). ② 도달 판정(실제 GET): 200+content-type, 404·403·
  5xx·timeout·content-type 불일치를 사유별로 구분.
- **사본 없음** — scribe 재확인: `deploy-smoke.mjs:99`·`monitor-freshness.mjs:91` 둘 다 같은
  `checkMediaReachability`를 import(`grep -n "checkMediaReachability" infra/scripts/deploy-smoke.mjs
  infra/scripts/monitor-freshness.mjs`로 확인).
- `build-images.yml:356`에 `--media-base-url` 배선 — **ssh 래핑 밖, 러너에서 직접 실행**(scribe
  코드 재확인: `:331-332` 주석 — "컨테이너 내부에서 돌리면 안 된다 — 제온과 같은 LAN이라 사설
  IP도 멀쩡히 열려 원 결함(#169)이 다시 숨는다"). 빈 값이면 fail-closed(exit 1, `:342`).
- `monitor.yml`은 이미 `--api-url`을 넘기고 있어(`:299`) **무배선으로 다음 스케줄 틱부터 자동
  동작**한다 — 이 커밋이 `monitor.yml` 자체를 건드리지 않았음도 확인(`git show --stat 48cfa70`
  파일 목록에 없음).
- `pnpm run test:scripts` 전체 재실행(11스위트): **481/481 pass, exit 0**(커밋 메시지 자기보고
  "410→481(+71)"과 합계가 일치 — scribe가 그 자리에서 독립 재실행해 확인한 것이지 베낀 수치가
  아니다). 11스위트 중 이 커밋이 늘린 것은 신규 `media-reachability.test.mjs` 1개(64 테스트) +
  기존 `deploy-smoke.test.mjs`·`monitor-freshness.test.mjs` 확장분(커밋 stat: 각 `+44`·`+112`행).
- ⚠️ **"게이트② 뮤테이션 5/5 red·과장 0건"은 커밋 메시지의 구현자·검증자 자기보고를 인용한
  것이다** — scribe는 뮤테이션 테스트(코드를 일부러 훼손해 검사가 실제로 red를 내는지 보는
  절차) 자체를 이 세션에서 재실행하지 않았다(재현이 명령 1줄이 아니라 코드 임시 변형 여러 번이라
  이 보고서 작성 세션 범위에서 하지 않았다). **이 수치는 "확인 불가"로 남긴다** — `test:scripts`
  481/481과는 성격이 다른 주장이다.
- 이 커밋은 **로컬에만 있다** — `gh pr list --state open` → 빈 배열, `git status` → "origin/main
  보다 1개 커밋 앞". PR 제출·머지는 scribe 권한 밖(git 쓰기 금지)이라 ⑤로 넘긴다.

### 신규 채번 #211~#214 (요약 — 상세는 PIVOT-PLAN 대장)

2026-09-12 실기 검증(기자 웹 촬영→업로드)이 드러낸 것 중 어제(09-11) 보고서 이후 새로 등재된
4건이다. scribe는 판정하지 않는다 — 대장 원문을 요약만 한다. DB 실측치는 이전 세션(SSH 경로)이
낸 것이고 이 세션은 재조회하지 않았다(scribe는 제온 SSH 비접근).

- **#211** — Cloudflare 요청 바디 100MB 한도. 원본 10건 중 **6건(60%) 초과**, 최대 **173MB**,
  평균 **128MB**(대장 #211 발주처 칸 인용).
- **#212** — 업로드 2차 재시도가 콘텐츠를 영구 `uploading` 교착시킨다. **사용자의 실제 콘텐츠
  1건이 지금도 갇혀 있다**(대장 #212 발주처 칸 인용).
- **#213** — `all-exceptions.filter.ts`가 4xx 전부를 로그 없이 반환(`logger.error` 호출이 파일
  전체에 500 분기 1곳뿐).
- **#214** — 감시가 "페이지 200"만 보고 미디어 실재 여부는 안 묻던 공백. **위 항목의 커밋이 그
  수리다**(코드는 완료됐지만 origin 미병합·조율자 미확인이라 대장 상태 칸은 여전히 "미수신"으로
  남아 있다 — 규율대로 scribe가 임의로 해소로 바꾸지 않는다).

### ⚠️ 조율자 실수 — 프로덕션이 10일 전 이미지로 롤백됐다 (오늘 가장 중요한 교훈, 정직 기록)

조율자가 사용자에게 전달한 api 재시작 명령(#169 §C-6② 실행용)에 `IMAGE_TAG`가 빠져 있었다(조율자
자기보고 인용 — 명령 원문·제온 셸 이력은 scribe 비접근이라 재현하지 않는다):

```
docker compose ... up -d --no-build --no-deps api
```

`infra/docker/docker-compose.prod.yml`은 `image:
${REGISTRY:-ghcr.io/homedcp}/gachinol-api:${IMAGE_TAG:-latest}`(scribe 재확인: `grep -n
"IMAGE_TAG" infra/docker/docker-compose.prod.yml` → `:58`)라 `IMAGE_TAG` 미지정 시 `latest`로
떨어지고, 제온 로컬 `latest`는 2026-09-02 시점 이미지(`3f8e873`)였다.

**타임라인**(KST, 전부 scribe가 이 세션에서 명령을 재실행해 얻었거나 원문 그대로 인용했다 — 구분
표시함):

- **22:57:56** — Build Images가 `gachinol-prod-api-1`을 `sha-2035cf2`로 정확히 재생성(scribe
  재확인, 위 "PR #100" 절과 동일 CI 로그).
- **23:04:01** — 컨테이너가 `:latest`(→`3f8e873`, 2026-09-02 커밋)로 재생성. 원인을 특정할
  GitHub Actions 런은 없음(사람 개입으로 추정, 단정 아님) — HANDOFF.md "⭐⭐⭐ 지금의 다음 1건"
  절 원문 인용(scribe 이전 세션 기록 — 이 순간 자체는 지나간 상태라 이 세션이 재현할 방법이
  없다).
- **23:04~23:09** — scribe(이전 세션)가 `curl .../v1/health/version`→`3f8e873`·
  `curl .../health/version`→404를 발견, 조율자에게 보고만 하고 손대지 않음(권한 밖) — 원문 인용.
- **23:13:48~23:14:30** — monitor.yml 스케줄 틱이 **failure**(이 세션이 이번에 새로 찾은
  증거 — 아래 상세) → Healthchecks.io 실패 핑 전송.
- **(시각 미상)** — 조율자가 원인을 특정한 뒤 사용자에게 복구 명령을 전달, 사용자가 제온 SSH에서
  실행(scribe 비접근 — 조율자 보고 인용): `export IMAGE_TAG=sha-2035cf2 && docker compose ...
  pull api media-worker ai-worker && ... up -d --no-build --no-deps api media-worker ai-worker`.
- **이 보고 작성 시각(2026-09-13 00:04 KST)** — 복구 유지 확인: `curl -s
  https://api.bapfull.com/health/version` → `2035cf2d0618d06e1b9a9756f7d40c1f80d231cd` =
  `git rev-parse origin/main`.

**이 세션이 독자적으로 찾은 것 — HANDOFF에 없던 corroborating 증거**: `gh run list`로 headSha
`2035cf2` 대상 워크플로를 조회하던 중 `Monitor Freshness`가 **`failure`**로 떠 있는 것을
발견했다(run `34698670144`, 스케줄 틱 `2026-09-12T14:13:48Z`). `gh run view 34698670144 --log`
재확인 결과:

```
[monitor-freshness] FAIL (bad-sha) — SHA가 40자 hex가 아님(단축 SHA는 GitHub API에서 조용히
0건이 되는 함정 방지): api
"ok": false, "primaryCode": "bad-sha", "expectSha": "2035cf2d0618d06e1b9a9756f7d40c1f80d231cd",
"apiSha": null, "webSha": "2035cf2d0618d06e1b9a9756f7d40c1f80d231cd"
```

같은 로그에 Healthchecks.io 실패 핑(`JOB_STATUS: failure` → `curl ... "$URL/fail"` → `OK`)도
찍혀 있다. **오늘 새로 만든 미디어 도달성 축이 아니라, 대장 #203이 이미 갖추고 있던 SHA 신선도
축이 이 사고를 잡았다** — `apiSha: null`은 롤백된 舊 이미지가 `/health/version`(무프리픽스)
라우트 자체를 갖고 있지 않아(그 라우트는 2026-09-02 이후 코드에만 있다) SHA 추출이 아예
실패했기 때문이다.

⚠️ **같은 함정의 다른 입구다.** `build-images.yml:251` 주석은 이미 이 함정을 경고하고
있었다(scribe 재확인, 원문): *"이 워크플로도 `web` 쪽과 같은 실수(2026-09-02, 대장 #180 직후 —
`deploy-web.yml`이 `--no-deps` 없이 `up web`을 돌려 캐시된 옛 `latest` api로 되돌린 사고)를
원리적으로 반복할 수 있으므로 대칭으로 막는다."* 단 이 주석이 경계하는 것은 **의존 서비스
드래그**(`--no-deps` 누락)이고, 오늘 사고는 입구가 달랐다 — 조율자의 명령엔 `--no-deps`가
**들어 있었으나**, `IMAGE_TAG` 자체가 애초에 export되지 않아 단일 대상 서비스조차 기본값
(`latest`)으로 떨어졌다. 같은 파일(`build-images.yml:260,265`, scribe 재확인)이 정답 순서
(`IMAGE_TAG` 계산 → `export IMAGE_TAG='$IMAGE_TAG'` → `pull` → `up -d --no-build --no-deps`)를
이미 담고 있었다 — 조율자가 그 스텝을 참고하지 않고 명령을 새로 구성하면서 같은 함정의 다른
입구에 걸렸다. **정직하게 기록한다**: 이번 사고는 코드·리포 문서의 결함이 아니라 **조율자
자신의 운영 실수**였다.

**대장 #215로 채번됐다**(조율자 지시, 이 세션 추가) — 위 서술과 같은 사실을 담되, 대장
기재는 `docker-compose.prod.yml:58`·`build-images.yml:246-267`의 정확한 행 번호 인용과 처방
후보 3가지(워크플로 주석 보강·HANDOFF 수동배포 절차 명문화·배포 스크립트화)를 덧붙였다.
비고에 **대장 #200과의 관계**(같은 증상의 다른 원인 — #200의 `--no-deps` 수리는 지금도
정상 작동하며 이 항은 그 수리가 닿지 않는 별도 입구, #212가 #168에 대해 갖는 관계와 동형)와
**판정 코드의 부정확성**(`bad-sha`로 떨어졌지 `api-stale`이 아니었다 — 신호는 맞았고 사유
분류가 틀렸다)을 명시했다. 재현: `node infra/scripts/daejang-recheck.mjs` → 대상 **67**행
(舊 66, +1) · 수동 **51**(舊 49, +2) · 어긋남 **1**(불변, #173) · 오류 **0**(불변) · 표 무결성
**0**행(불변 — 새 행도 칸 밀림 없음).

### 실기 검증이 잡은 것 — 자동 검사가 원리적으로 못 잡는 축 (ROOT-CAUSE 이틀 연속 실증)

어제(09-11, PR #100 사전 작업)는 `.web.*` 플랫폼 해석을 jest+jsdom으로 검증하는 테스트 13건을
신설했다(위 "PR #100" 절 재확인: reporter 3/3 + subscriber 10/10). **jsdom에는 실 네트워크·사설
IP 판정·CORS가 없다** — 그래서 그 13건은 #169류(사설 IP를 가리키는 서명 URL이 인터넷에서 도달
불가한 문제)를 **원리적으로** 잡을 수 없다. 오늘 신설한 `media-reachability.mjs`가 GitHub
Actions 러너 위에서, 컨테이너·LAN 안이 아니라 **인터넷 경로로** 돌도록 명시적으로 배치된 이유가
정확히 이것이다(위 "공개 미디어 도달성 검사 축 신설" 절 참조). ROOT-CAUSE의 핵심 주장
(*"검사가 닿지 않는 축에만 결함이 산다"*)이 이틀 연속(어제 #207/#208/#209의 grep 방법론 사각 →
오늘 jsdom의 네트워크 부재 사각) 실증됐다.

## ③ 막힌 것

- **대장 #169 — 업로드는 여전히 막혀 있다.** 재생 경로는 오늘 풀렸지만(위 ② 참조), 원본 촬영본
  다수가 Cloudflare 100MB 한도(#211)를 넘어 업로드 자체가 안 된다. #211 처방이 없으면 #169는
  끝까지 해소로 옮길 수 없다.
- **대장 #211(Cloudflare 100MB 한도)** — 처방 방식 무전제(규율 17), QUEUE 미편입, 조율자 판단
  대기.
- **대장 #212(업로드 재시도 영구교착)** — **사용자의 실제 콘텐츠 1건이 지금도 `uploading`에
  갇혀 있다.** 처방 후보만 제시된 상태, QUEUE 미편입.
- **대장 #213(4xx 무로그)** — 처방 무전제, QUEUE 미편입. ⚠️ 오늘의 롤백 사고(위 ②)는 4xx가
  아니라 `bad-sha`(배포 신선도) 판정이었다 — #213과 직접 연관은 없다, 혼동하지 않는다.
- **대장 #215(신규 — 제온 수동 배포 `IMAGE_TAG` 누락)** — 오늘의 롤백 사고 자체가 대장에
  채번됐다(위 ② 참조). 처방 방식 무전제, QUEUE 미편입, 조율자 판단 대기.
- **대장 #214 — 코드는 완료됐지만 대장 상태 칸은 "미수신"으로 남아 있다.** 커밋 `48cfa70`이
  origin에 아직 없다(PR 미제출). 다음 세션이 PR 제출·머지·조율자 확인을 거쳐야 대장 행이
  갱신된다.
- **QUEUE 2단계 대장 #177(전이맵 `to`-상태 grep 테스트)** — 아직 미착수. 2단계 완료(→ D-2 카카오
  실 송출 범위 결정 트리거)가 이 1건에 걸려 있다.
- **대장 #207 — 07 §3-10·§4·§6 법률 문언의 법률적 정확성 판정** — 07 §6 14항목 법률자문 계약
  완료 후로 이월, 여전히 미착수(HANDOFF "열린 항목" 변경 없음).
- **대장 #208·#209 — QUEUE 편입·처방 판정** — 변경 없음, 여전히 조율자 판단 대기.
- **`05-monetization.md:583` 다운스트림 인용 편입 여부** — 이 세션에서 변경을 확인하지 못했다
  (그 라인을 다시 열람하지 않았다) — **확인 불가**로 남긴다.
- **monitor.yml 최신 스케줄 틱이 `failure`로 남아 있다** — 위 사고의 기록일 뿐 현재 장애는
  아니다(복구는 방금 curl로 확인됨, 위 ②). 다음 스케줄 틱(간격 실측 평균 3.70h, 아래 ④)이
  `success`로 돌아오는지는 이 보고 작성 시점까지 확인되지 않았다 — 다음 세션이 재확인할 것.

## ④ 지금 상태 【자동 산출 — 사람의 판단 없음】

| 항목 | 값 | 재현 |
|---|---|---|
| 이 작업 디렉터리 HEAD | `48cfa70`(브랜치 `fix/media-reachability`, origin/main보다 1커밋 앞 — 미푸시) | `git rev-parse --short HEAD` |
| origin/main | `2035cf2`(PR #100 병합커밋) | `git fetch -q origin main && git rev-parse --short origin/main` |
| 전체 열린 PR | **0**건 | `gh pr list --state open --json number --jq 'length'` |
| 배포 서빙 중 — api | `2035cf2d0618d06e1b9a9756f7d40c1f80d231cd` = origin/main과 일치(사이 이탈·복구 있었음 — 위 ② "조율자 실수" 참조) | `curl -s https://api.bapfull.com/health/version` |
| 배포 승인 대기(`waiting`·`action_required`) | **0**건 | `gh api "repos/HomeDCP/gachinol/actions/runs?status=waiting" --jq '.total_count'`(+`?status=action_required`) |
| 미디어 도달성(대장 #169 ②) | thumbnailUrl 호스트 `media.bapfull.com` · 썸네일 200/image/jpeg/24124B/640×360 · 영상 Range 206/video/mp4/h264 1280×720 | `curl -s "https://api.bapfull.com/v1/feed?limit=1"` 후 thumbnailUrl·playback.hlsUrl GET(본문 ② 인용) |
| monitor.yml schedule 간격(최근 10구간·표본 11건, 09-10~12 KST) | 최소 **2.07h**·최대 **5.42h**·평균 **3.70h**·7h 허용치 여유 **1.58h**. 최신 틱(`14:13:48Z`)은 위 사고로 **failure** | `gh run list --workflow monitor.yml --limit 20 --json createdAt,event,conclusion --jq '.[]\|select(.event=="schedule" or .event=="workflow_dispatch")\|[.createdAt,.event,.conclusion]\|@tsv'\|sort` |
| 대장 재현 | 대상 **66**행(舊 63 — #211~214 신규 4 · #178 해소 제거 -1) · 일치 27 · 어긋남 **1**(#173, carry) · 수동 49 · 오류 0 · 표 무결성 **0**행 | `node infra/scripts/daejang-recheck.mjs` |
| 로컬 게이트(infra/scripts) | 11스위트 **481/481** pass, exit 0(舊 09-11 시점 9스위트/365 → PR #100이 10스위트/410로, 이 세션 대상 커밋 `48cfa70`이 11스위트/481로 늘렸다) | `pnpm run test:scripts` |
| reporter 앱 테스트 | 31스위트 **323/323** pass(불변) | `pnpm --filter @gachinol/reporter test` |
| `.web.*` 플랫폼 축 테스트(대장 #178) | reporter 1스위트 3/3 · subscriber 3스위트 10/10 = **13/13**(불변) | `pnpm --filter @gachinol/reporter test:web && pnpm --filter @gachinol/subscriber test:web` |
| 미커밋 변경 | `docs/**` 4개(PIVOT-PLAN·QUEUE·HANDOFF 수정 + 본 파일 신규) — 코드는 이미 `48cfa70` 커밋, PR 미제출 | `git status --short` |

## ⑤ 다음 세션 예정

1. **커밋 `48cfa70`(미디어 도달성 검사) PR 제출·머지** — 지금 로컬에만 있다. 머지돼야
   `build-images.yml`의 `--media-base-url` 배선이 다음 실배포부터 실제로 작동한다.
2. **대장 #214 상태 칸 갱신** — 위 1이 끝나고 조율자가 확인하면 scribe가 "미수신"→해소로 판정.
3. **HANDOFF.md "⭐⭐⭐ 지금의 다음 1건" 절에 오늘 사고(탐지→원인→복구)를 반영** — 지금 그 절은
   "원인 불명, 조율자가 확인할 것"에서 멈춰 있다. 이 일간보고가 사실을 채웠으니 다음 scribe
   세션이 HANDOFF·필요하면 PIVOT-PLAN에 옮겨 적을 것(오늘은 위임 범위가 일간보고 1건뿐이라
   두 문서를 손대지 않았다).
4. **대장 #211(100MB 한도) 처방·QUEUE 편입 판정**(조율자) — #169가 완전히 풀리려면 이게
   필요하다.
5. **대장 #212(영구교착 콘텐츠 1건) 처방**(조율자) — 실사용자 데이터 문제라 방치 시간이
   늘수록 운영 부담이 커진다.
6. **대장 #213(4xx 무로그) 처방**(조율자).
7. **QUEUE 2단계 대장 #177(전이맵 grep 테스트) 착수 여부**(조율자) — 착수하면 2단계 완료 →
   D-2(카카오 실 송출 범위) 트리거 도달.
8. **2주 report-only 관측 지속**(기산일 2026-09-11T07:52:33Z, 종료 2026-09-25) — 오늘의 사고
   1건이 그 관측 창 안에서 실제로 발생한 첫 유의미한 이탈 사례다. 종료 후 임계(1h/6h) 재조정
   논의에 이 건도 참고 자료로 포함할 것.
9. **다음 monitor.yml 스케줄 틱이 `success`로 복귀하는지 확인** — 이 보고 작성 시점까지는
   아직 다음 틱이 돌지 않았다(가장 최근 틱 = 사고 당시 `failure`).
10. (이월, 변경 없음) 대장 #207 법률 문언 판정 · 대장 #208·#209 QUEUE 편입 판정 ·
    `05-monetization.md:583` 편입 판정.
11. **대장 #215(제온 수동 배포 `IMAGE_TAG` 누락) 처방·QUEUE 편입 판정**(조율자) — 처방 후보
    3가지(주석 보강·절차 명문화·배포 스크립트화) 중 택일 또는 조합 필요. `monitor-freshness.mjs`의
    `bad-sha` 판정 코드를 `api-stale`류로 세분할지도 이 판단에 곁들일 것(대장 #215 비고 참조).

## ⑥ 지난 보고서(2026-09-11) 예정 대비

09-11 보고서 ⑤가 예정한 7항목(원문 요지: *"1. PR #99 머지 승인 2. 대장 #207 법률 문언 판정
3. `05-monetization.md:583` 다운스트림 인용 편입 여부 판정 4. 대장 #208·#209 QUEUE 편입 여부
판정 5. 대장 #207(L601) 표 무결성 경고 재발 여부 관찰 6. 2주 report-only 관측 지속 7. 관측
종료 후 2단계 2-2(`.web.*`) 착수"*)을 오늘 자료와 대조한다:

1. **PR #99 머지 승인** → ✅ **됐다** — 같은 날(09-11) 08:22:05Z에 머지(`9d0ed53`), 09-11
   보고서 자체의 ⑦절이 이미 기록.
2. **대장 #207 법률 문언 판정** → **안 됐다** — HANDOFF "열린 항목"에 변경 없이 그대로 남아
   있다(07 §6 14항목 법률자문 계약 완료 후 대기).
3. **`05-monetization.md:583` 편입 판정** → 이 세션에서 변경을 확인하지 못했다 — **확인
   불가**로 남긴다(위 ③ 참조).
4. **대장 #208·#209 QUEUE 편입 판정** → **안 됐다** — HANDOFF "열린 항목"에 변경 없이 그대로.
5. **대장 #207(L601) 표 무결성 재발 관찰** → **재확인했다, 여전히 0행**(오늘 `node
   infra/scripts/daejang-recheck.mjs` 재실행 결과 "표 무결성 0행" — 재발하지 않았다, 위 ④).
6. **2주 report-only 관측 지속** → 계속 진행 중. 오늘 그 관측 창 안에서 처음으로 **실제 이탈
   사례**(위 ② "조율자 실수" 사고)가 발생했고 monitor.yml이 그것을 잡았다 — 관측이 유의미하게
   작동한 첫 사례다.
7. **"관측 종료 후 2단계 2-2(`.web.*`) 착수"** → **예정과 다르게 진행됐다.** HANDOFF.md 원문을
   scribe가 재확인하니, 같은 날(09-11) 안에서 조율자가 이 문구 자체를 이미 정정해 두었다
   (원문 인용): *"위 '舊' 절의 '관측 종료 후 착수'는 부정확했다 … 2주 report-only 관측은 개발
   착수를 막지 않는다 … report-only는 '감시가 배포를 막지 않는다'는 뜻이지 개발 정지가
   아니다."* 그래서 대장 #178(2-1)은 관측 종료(2026-09-25)를 기다리지 않고 다음날(오늘,
   09-12) 바로 진행돼 PR #100으로 완료됐다. **09-11 보고서 자체가 같은 날 안에서 낡아진
   사례**다 — 이 문서가 스스로 경계하는 "박제된 보고서" 현상이 09-11에도 일어났다는 뜻이며,
   scribe는 이를 그대로 정직하게 기록한다.
